# Protocole — forme (b) : GEMV de décodage lisant la disposition Marlin (Laurine 5f9c4cc, `nvfp4_gemv_marlin[_gateup]`), un bloc après P2

instrument : `scratchpad/forme-b-18-09/chaine.sh` — attend la fin de P2 hors moteur (FIN bf16) puis fusionne laurine 5f9c4cc dans laure ; (1) compilation de l'extension acvram (`get_extension()`) SEULE sous carte.sh (JIT hors banc, REGLES §6) ; (2) `pytest tests/test_gemv_marlin.py -q` sur carte (13 tests, arrêt si échec) ; (3) `outils/banc-marlin-decode-18-09.py` (b=12 et b=1, sous graphe, 20 routages tirés, juge fp32 ; version 5f9c4cc — si le témoin GEMV y lit encore les échelles fp8, copie locale avec `.view(torch.uint8)` comme au verdict précédent, dit tel quel).
scellé (Sage) : forme (b) ≤ 0,97 × GEMV actuel à b=12 ET b=1 → disposition unique, P1 continue ; sinon P1 fermé VRAM. Prédiction Laurine (corrigée) : b=12 5,0-6,5 ms/pas, b=1 2,2-2,9 ms. Témoins du verdict précédent : GEMV 6,92 (b=12) / 0,82 (b=1) ; Marlin GEMM 6,81 / 1,41. Remarque écrite avant : la prédiction b=1 (2,2-2,9) est au-dessus du GEMV (0,82) et du seuil (0,80) — si elle se réalise, le scellé b=1 est faux par construction.
