# Pièce 61, étape 1 — GEMV Marlin par créneau d'expert : AU BIT (fantômes, experts à 1 et 12 jetons compris) mais **0 % de gain** (TPB 4 : − 0,3 %, TPB 8 : − 7,4 %) ; la sonde le dit pourquoi : à 96 paires par couche le noyau par paire a un PLANCHER DE CALCUL de 66 µs (tout en L2), les rechargements de tuiles ne coûtaient rien — prédiction RÉFUTÉE — 23/09 (Gaelle)

* instrument : `acvram/kernels/acvram_kernels.cu` (+ 180 l., commit 3d589067 puis attribut corrigé) : `nvfp4_gemv_marlin_slots_kernel<XT, NW, TPB>` (bloc = tuile N × créneau ; créneau = un expert et ≤ TPB paires ; x des TPB lignes en partagée ; mêmes tuiles, mêmes fma par paire, même réduction, même épilogue que le noyau par paire), `moe_slots` (tri par comptage, grille statique G créneaux, fantômes = créneau − 1 à zéros, vides = − 2), lanceurs `nvfp4_gemv_marlin_slots` / `_gateup_slots` ; `outils/gpu/mesure/banc-gemv-creneaux.py` (piles synthétiques Marlin E 128, gate·up [2048 → 768], down [768 → 2048], godet 16 = 12 jetons + 4 fantômes, 8 copies en rotation = L2 froid, 200 répétitions, égalité `torch.equal`) ; `scratchpad/gaelle-p61-23-09/sonde-reutilisation.py`
* commit : 3d589067 + correctif (cudaFuncSetAttribute à la valeur exacte, erreur vérifiée — la première valeur, 393 Ko, dépassait 227 Ko et la première prise est morte « invalid argument », nommée) ; extension recompilée deux fois (`get_extension()`, symboles vérifiés, `build_info` sm_120)
* régime : 4 prises de 0 + 3 + 4 + 4 s, eco 2700, compute-apps début = fin ; ptxas : `cuobjdump -res-usage` ne rend pas les noyaux de l'extension (fatbin), registres non lus — écart à la règle § 3, nommé
* scellé (gaelle.md 1f6517f3, avant) : 3,92 → 2,9-3,3 ms/pas équivalent ; réfuté si > 3,5
* mesuré (µs par couche, gate·up + down, L2 froid, routage synthétique 41-56 experts distincts) :

| bras | µs / couche | ms/pas équiv. (× 48) | au bit | fantômes à zéro |
|---|---|---|---|---|
| par paire (servi) | **103,5** | 4,97 | — | — |
| créneaux TPB 4 (+ moe_slots 6,1 µs) | 103,9 | 4,99 | **oui** (écart 0) | oui |
| créneaux TPB 8 (+ 6,2 µs) | 111,2 | 5,34 | **oui** | oui |

  Sonde, noyau par paire, 96 paires : **1 seul expert (tout en L2) 66,2 µs · 48 experts distincts 110,9 · 96 distincts (tout HBM) 168,5**. Donc à routage réel (33 distincts, réutilisation 2,9), le temps se partage en un **plancher de calcul ≈ 66 µs par couche (64 %)** — décodage e2m1 + fma scalaires sur cœurs CUDA pour 96 paires × K × N, indépendant des octets — et une part HBM proportionnelle aux experts distincts ; les rechargements d'une tuile déjà en L2 sont gratuits, c'est exactement ce que les créneaux suppriment. TPB 8 est pire (partagée 98 Ko, un bloc par SM).
* verdict : **étape 1 RÉFUTÉE** (gain − 0,3 %, seuil − 15 % ; > 3,5 ms) — le noyau par créneau est juste (au bit dans tous les cas demandés) et inutile : **le mur du GEMV Marlin à b = 12 n'est pas le trafic L2 des tuiles, c'est le calcul scalaire de 96 paires**. La pièce 60 avait raison sur les octets (4,29 Go, 71 % du plancher HBM) et tort sur la cause : notre kernel n'atteint pas la HBM parce qu'il est borné par ses cœurs CUDA, pas par ses rechargements. vLLM (26,6 µs par lancement) fait le même travail sur les **tensor cores** (Marlin = mma.sync m16n8k16, poids e2m1 → bf16 en registres, 16 lignes de jetons par tuile — le godet 16, exactement) : son plancher de calcul est ≈ 10 × plus bas, la HBM redevient le mur, 1,68 To/s. Je n'intègre pas les créneaux (rien à gagner) ; les tests (2) et la capture (3) ne se jouent pas sur un noyau sans gain.
* durée : 11 s de carte, ≈ 1 h 30 à sec (conception, noyau, banc, deux compilations) ; carte LIBRE

## Ce qui suit (à Jerome) : la vraie pièce est un GEMM W4A16 tensor-core par créneau
Même découpe (créneau = expert × ≤ 16 lignes, grille statique, `moe_slots` réutilisable tel quel), mais le corps devient `mma.sync.m16n8k16` bf16 : tuile de poids e2m1 décodée en fragments bf16 (registres, une fois par créneau), x bf16 [16, K] en partagée, accumulation fp32 par mma. **Pas au bit** (ordre de sommation des tensor cores ≠ fma scalaires) : ± quelques ulp → règle « rapide ± 1 ulp » (opt-in, PPL à 2 SE, KL contre le chemin exact) ou décision de l'utilisateur. Prédiction : plancher de calcul 66 → ≤ 10 µs par couche, mur = HBM 4,29 Go/pas → **3,92 → 2,8-3,1 ms/pas (− 0,8 à − 1,1), 1 634 → 1 830-1 920** ; réfuté si le noyau mma reste > 3,5 ms au banc (décodage e2m1 → bf16 des fragments trop coûteux, ou occupation). Coût : 1-2 jours (fragments Marlin, épilogue act·up, test ± ulp, banc), la pièce 47 (échelles AWQ) à porter aussi.
