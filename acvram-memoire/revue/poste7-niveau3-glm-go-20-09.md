# poste7 — niveau 3 GLM b=1 (≤ 700 nœuds) : ordre des pièces par nœuds gagnés à l'heure, un scellé par pièce, chaque pièce livrable seule ; le préalable « niveau 2 » devient une pièce conditionnelle jugée par ligne contre fp64, plus jamais par PPL (20/09, 08 h 48, horloge machine)

Source : `chantier-c15-niveau3-20-09` (attribution : 2 163 nœuds = 33 couches Marlin × 44 + 13 xreg × 51 + 40 hors couches ; 1,06 µs par nœud ; scellé ≤ 700 nœuds, espaces ≤ 0,7 ms, pas b=1 ≤ 7,0 ms) ; `poste7-glm-b1-noeuds-c15-niveau3-20-09` § 2 ; β et 36 tranches (`verdict-beta-ordre-de-somme-19-09`, `verdict-regime-2048-cles-19-09`) : une PPL GLM ne rend jamais « équivalent » ; REGLES § 7 (juge fp64 par ligne) ; C14-b en cours (poste1-c14b, scellé par poste du 07 h 50). Utilisateur 08 h 5x : « continue les travaux avec le groupe le plus rapidement possible » — d'où : des pièces courtes, servies une à une, pas un chantier bloqué sur son préalable.

## 1. Ce qui change depuis la fiche
* Le préalable « niveau 2 en état de marche » n'a plus de juge PPL possible (± 1,31 % à 9, ± 0,66 % à 36 : un +1,7 % est un tirage). Il a un juge : **par ligne contre fp64** sur entrées réelles b=1 (d(=2) ≤ d(=1) + 6 ulp, médiane, max ; 47 couches × 8 pas), une prise ≤ 5 min. Tenu → `=2` au défaut à b=1 et −8 nœuds/couche ; faux → la pièce MLA-prep se fait sans `=2` (q_a_norm dans le prologue de q_b, RoPE + cat en un noyau : 12 → 6, −6/couche).
* Aucune pièce n'attend une autre : chacune a son compteur de nœuds **à sec** (le crochet TorchDispatchMode de `test_mla_glue_c15`, par couche et par famille : le compte prédit est un test qui casse), son juge d'équivalence, sa prise carte ≤ 30 min, son défaut.
* Le scellé global reste (≤ 700 / ≤ 0,7 / ≤ 7,0) et se lit à la fin ; **ma prédiction, écrite ici : il sera FAUX de 5-10 %** — 900-1 100 nœuds, espaces 1,0-1,2 ms, pas 7,3-7,8 ms (128-137 t/s contre 122,5) — parce que la tête et le bloc gardent ≈ 25 + 2 × 47 nœuds incompressibles et que `=2` a une chance sur deux au juge fp64. Si le pas ne suit pas les nœuds (pièce à −N nœuds et pas −< 0,5 × N × 1,06 µs, deux fois de suite) → la latence est la profondeur de la chaîne sérielle, pas le nombre : on arrête et on mesure la profondeur critique avant d'insister (clause de la fiche, gardée).

## 2. Pièces (par nœuds gagnés par heure de code ; chiffres de la fiche, 1,06 µs/nœud)
| # | pièce | nœuds | code | juge d'équivalence | scellé de la prise (≤ 30 min) | défaut |
|---|---|---|---|---|---|---|
| 1 | **C10 (b) au défaut** (`MARLIN_DISTINCT`, déjà écrit, opt-in) : les 13 xreg prennent le chemin Marlin | −91 (51 → 44 × 13) | 0 (chaîne seule) | juge fp32 par ligne existant (`test_gemv_marlin`) + jetons au bit sur 32 pas ou « B ≤ témoin ON/OFF » | nœuds réels −91 ± 10 (nsys graph-trace), pas b=1 −0,10 ± 0,05 ms, capture {1,2,8,16} 4/4, PPL 9 tranches b=1 « non établi pire » | oui si tenu |
| 2 | **MLA-prep** : (a) juge fp64 de `=2` à b=1 (une prise) ; tenu → `MLA_GLUE=2` défaut b=1 ; faux → (b) q_a_norm en prologue + RoPE/cat en un noyau | (a) −376 (12 → 4 × 47) ; (b) −280 | (a) 0 ; (b) 1 jour (.cu) | (a) d(=2) ≤ d(=1) + 6 ulp, médiane, max, par ligne, 47 × 8 pas ; (b) ≤ 1 ulp bf16 contre le chemin actuel | nœuds −N ± 10 %, pas −N × 1,06 µs ± 30 %, capture 4/4, PPL filet | oui si tenu |
| 3 | **routeur en un nœud** (`_route_fusee_kernel` prend x bf16, produit fp32 dans le noyau, softmax/top-k dedans) | −235 (6 → 1 × 47) | ½ jour (Triton) | logits ≤ ref + 6 ulp contre fp64 ET **top-k identique** sur ≥ 4 096 jetons réels (un changement d'expert n'est pas un ulp) | idem | oui si tenu |
| 4 | **experts : division AWQ dans le chargement de x (gate·up, down), `y + shared` dans l'épilogue de `moe_reduce`, expert partagé en un gemv à deux sorties** | −235 (8-9 → 3-4 × 47) | 1 jour (.cu) | au bit (même arrondi bf16 du quotient, même ordre) — sinon la pièce est fausse | idem + J_B ≤ J_A (lectures en moins) | oui si tenu |
| 5 | **C14-b** (en cours, scellé par poste du 07 h 50) | −118 (5 → 2-3 × 47) | fait | ≤ 1 ulp + juge fp64 | déjà écrit | déjà écrit |
| 6 | **hors couches** : norme finale fusionnée, échantillonnage en un noyau | −15 | ½ jour | au bit / argmax | idem | oui |
Somme si tout tient : 2 163 − 91 − 376 − 235 − 235 − 118 − 15 = **1 093** ; sans (2a) : 1 189. Le ≤ 700 de la fiche demandait Marlin 44 → 13 : je ne vois pas les 8-10 nœuds/couche manquants dans la table — **poste1 les nomme (fichier:ligne) ou le scellé global passe à ≤ 1 100** au premier verdict de pièce.

## 3. Chaîne d'une pièce (poste2, une prise ≤ 30 min, sortie au premier faux)
(0) à sec, dans le commit : test de compte de nœuds par couche (prédit → casse) et juge d'équivalence sur formes réelles ; (1) capture {1,2,8,16} + nsys graph-trace : nœuds réels, espaces, pas b=1 (5 min) ; (2) juge sur carte (ulp/bit/top-k, 5 min) ; (3) `certifie` b=1 ABAB avec énergie (5 min) ; (4) `ppl-decode-kv` 9 tranches b=1, outil 3.2 (5 min). Six lignes + durée + prédiction en tête.

## Ordre
* **poste1** — après C14-b : pièce 1 (chaîne seule, aujourd'hui), puis 2a (juge fp64 de `=2` à b=1 : script + prise), puis 3, 4, 6 ; 2b seulement si 2a tombe ; chaque pièce = branche, commit, pointeur à poste2 ; les nœuds manquants pour 700 nommés ou le scellé global réécrit à ≤ 1 100 dans sa fiche.
* **poste2** — après déterminisme ON/OFF et S2 : C14-b P1-P3, puis les pièces dans l'ordre des pointeurs, une prise ≤ 30 min chacune.
* **chef** — un .deb par pièce tenue (bras éco), ETAT : « niveau 3 = pièces, prédiction poste7 ≤ 7,0 faux de 5-10 % » ; me solliciter à la première pièce dont le pas ne suit pas les nœuds.
