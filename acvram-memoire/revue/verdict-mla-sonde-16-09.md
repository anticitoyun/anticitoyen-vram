# Verdict — sonde du cache latent et équivalence boucle/batché : le chemin MLA batché ne lit pas le cache courant (zérotage sans effet), et diverge de la boucle dès le 1er jeton (top-1 53 %) → RÉFUTÉ, rien ne fusionne

- **instrument** : `sonde-cache-mla-16-09.py` (12 séq greedy, prompt 128, 100 pas, inspection des 47 caches statiques, option zérotage de tous les caches avant le pas 101, logits du pas 101 sauvés) × 4 bras ; ties 12 × 256 (768 logits) boucle `MLA_BATCH=0` contre batché défaut + `compare-ties-ulp` (Océane) ; sorties `scratchpad/sonde-cache-16-09/`
- **commit** : arbre mesuré **travail/laure-qa @ f4607c2** (laurine), graphes actifs, `-k48`, régime du duel ; scripts laure (commit de ce verdict)
- **régime** : b=12, godet 12, statics MLA (1152 × 576 bf16 ou 1152 × 592 uint8)
- **scellé** (Sage § 7, main 8f71f6a) : (1) si lancements fp8 présents et D = C → relecture du cache ; (2) équivalence boucle/batché : ulp de ligne + top-1 identique + cos ≥ 0,9999 ; deltas ≫ 10 ulp ou top-1 différent → retour Laurine, rien ne fusionne
- **mesuré** : (1) fp8 : lancements présents (`mla_1p<true>` 89/pas), cache uint8 bien écrit (échelles 4-14 × 10⁻³, |déq| = |bf16| à 10⁻³) ; **zérotage des 47 caches avant le pas 101 : logits identiques au bit** (bf16 et fp8, max |Δ| = 0, top-1 12/12) ; `len` = **511** sur tous les blocs après 128 + 100 jetons, 511 lignes non nulles. (2) boucle vs batché : **top-1 407/768 (53 %)**, première divergence aux jetons 1-10 sur 10/12 séquences, cos min **−0,9996**, 387/768 positions hors 5 ulp
- **verdict** : (2) **RÉFUTÉ** — retour Laurine, **rien ne fusionne** ; (1) le chemin batché **ne lit pas `st["cache"]`** : le fp8 y est inerte et les « bit-identiques » de mla1-3 comparaient des chemins qui lisent tous la même autre source. Mes prédictions du protocole mla3 (« ≤ 1 ulp », PPL ± 0,001) : réfutées par ce défaut

## 1. Ce qui s'emboîte (hypothèse unique, non vérifiée, à tester par Laurine)
- Zérotage sans effet + fp8 sans effet + `len` = 511 figé + PPL batchée à peine différente de la boucle avec un prompt d'**un** jeton (`ppl-narrow` : 8,0003 vs 8,0175) + divergence **dès le 1er jeton** avec un prompt de **128** : tout tient si **le graphe lit et écrit d'anciens tampons** — la table `ptrs`/`len_ptrs` de `_mla_lot` (`model.py:1708-1726`, clé `data_ptr`) est un tenseur capturé ; si les `statics` sont réalloués après la capture (`_nouveau_static` à l'admission ?), le rejeu écrit ses lignes dans les anciens tampons et y lit son contexte (les jetons décodés, jamais le prefill que `static_load` dépose dans les nouveaux) ; le tampon courant, lui, ne contient que le prefill : `len` 511 = compteur des anciens tampons ou du warm-up. Le prompt est ignoré, le décodage se voit lui-même : PPL correcte à prompt d'un jeton, texte faux à prompt réel.
- Test qui tranche (10 min, à sec + carte) : à la capture, noter `data_ptr()` des `statics[i]["cache"]` et de `ptrs` ; au pas 100, comparer ; ou plus simple : `torch.equal` entre `st["cache"][:128]` et le latent recalculé du prompt après 100 pas, puis lire ce que le noyau `mla_1p` reçoit (`cache_ptrs`) au pas 100.
- La boucle (`MLA_BATCH=0`, `decode_static` par séquence) lit bien le tampon courant : c'est **elle la référence** jusqu'à preuve du contraire, et c'est le régime du duel tel que mesuré (56,1 ms) — les 17 ms des commits 1-3 sont **des chiffres d'un chemin qui ne calcule pas la bonne attention**.

## 2. Conséquence
Verdicts mla1/mla2/mla3/mla4 : les temps restent des mesures exactes de ce que le code fait, les équivalences y sont **caduques** (comparaisons batché/batché). Fusion de laurine : non. Duel : le chiffre publié (56,1 ms, chemin boucle… à vérifier : le duel tournait sur main ce71723, `MLA_BATCH` défaut 1 = « noyau d'attention seul batché » — si le défaut 1 souffre du même défaut, la PPL du duel (prefill) n'est pas touchée mais le texte du décodage l'est ; à contrôler par la même sonde sur ce71723, 5 min).
