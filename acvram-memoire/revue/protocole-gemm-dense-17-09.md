# Protocole — GEMM dense NVFP4 de poste4 (b ≥ 2, remplace la GEMV O(b) : profil FLA 86 ms/pas à b=12) : scellés en situ, ≤ 30 min (poste7 via chef, 17/09 14h45)

objet : le commit d'intégration de poste4 (après son micro-banc), fusionné sur main ; Qwen3.8-27B calibA sous FLA (défaut) ; bonus Coder-30B (`_etroit` dense int8 : 1,09 ms/pas → ≤ 0,6 attendu si le noyau couvre aussi int8).
instrument : `certifie-b12` b=12 ×2 et b=1 ×1 (régime classé), `ppl-acvram-17-09.py` 3 tranches × bf16 6,5344, `ppl-decode-kv-17-09.py` 256+1024, `profil-gdn-17-09.py b12` (part GEMV/GEMM et ms) ; test d'équivalence logits GEMM vs boucle GEMV dans le commit de poste4, avec bras cassant (échelle de bloc décalée d'un rang → rouge) ; Coder : `profil-pas-coder-17-09.py b12` ; chaîne `scratchpad/gemm-dense-17-09/chaine.sh`.
scellé (poste7) : Qwen3.8 b=12 128 → ≥ 500 t/s (≈ 800 à 73 % de bande ; faux < 300) ; J/jeton 3,12 → ≤ 1,0 ; b=1 ± 3 % de 69,9 ; PPL 3 tranches et ppl-decode-kv ± 0,0005 de fla (1,0255 / 4,0882) ; test d'équivalence vert et bras cassant rouge dans le même commit ; bonus Coder `_etroit` ≤ 0,6 ms.
falsification : b=12 < 300 ⇒ le noyau ne lit pas les poids une fois (profil : ms de GEMM vs 11 ms plancher) ; PPL hors ± 0,0005 ⇒ arithmétique ≠ GEMV, refus du défaut ; b=1 hors ± 3 % ⇒ le seuil de bascule GEMV/GEMM est mal placé.

addendum 14h55 — noyau livré à sec (poste4 a869a46) : porte = `outils/banc-gemm-dense-etroit-17-09.py` (formes Qwen3.8 q/kv/o, gdn_qkv, gate_up, down ; M 2/12/32 ; 8 configs Triton ; témoins gemv_boucle et narrow_gemm ; rejeu de graphe) ; scellé porte : min sur les formes à M = 12 ≥ 1,3 To/s OUVRE, < 0,9 FAUX ; prédiction poste4 : q/k/v/o 1,0-1,4, gate_up/down ≥ 1,3, gemv 0,2-0,3, narrow 0,5-0,8. Si ouvert : `ACVRAM_DENSE_NVFP4=triton` en situ (scellés ci-dessus).

addendum 15h05 (poste7-gemm-dense-porte-fermee-palier) — palier Triton en situ maintenant : variable livrée `ACVRAM_DENSE_NVFP4=gemv|triton` (kernels/__init__.py:597, bascule 2 ≤ M ≤ 32 — poste7 demande M ≥ 4 : à ajuster par poste4 si le b=2 y perd ; M = 1 garde la GEMV) ; scellé : b=12 128 → ≥ 300 (faux < 250, surcoût lu au profil), ppl-decode-kv ± 0,0005 de 4,0882, b=1 ± 3 % de 69,9, J/jeton ≤ 1,3 ; tenu ⇒ défaut pour M ≥ 4, cellule aux menus. Chaîne `scratchpad/gemm-dense-17-09/situ.sh` (test d'équivalence sur carte, b=12 ×2, b=1, ppl-decode-kv, profil b=12).

addendum 15h20 — palier 2 Triton (poste4 c356e78 : MultiProjection q/k/v empilés, tranches K, réduction fusionnée) : banc rejoué (même commande) sur les formes réelles du pas (qkv_multi, gdn_multi, o, gdn_out, gate_up, down × 17/48/65 couches), témoin `separees_triton` ; scellé : taux pondéré par les octets du pas ≥ 1,0 To/s OUVRE / < 0,85 FAUX ; prédiction poste4 : qkv_multi 0,95-1,05, gdn_multi 1,0-1,1, o/gdn_out 0,90-1,0, gate_up 0,95-1,05, down 1,05-1,15, pondéré 0,95-1,05 ; si ouvre : en situ, GEMM du pas 16-19 ms ⇒ 500-600 t/s. Arbre gelé (a869a46) jusqu'à la fin de la chaîne palier 1.

addendum 15h35 — tête `lm_head` par `gemm_dense_etroit` sortie fp32 (poste4 7c09b53, `DENSE_NVFP4=triton` défaut, bascule MIN_M=4, test logits + argmax) : scellé poste7 b=12 349 → ≥ 400 (faux < 380), ppl-decode-kv ± 0,0005 de 4,0882, b=1 ± 3 % de 69,9 ; palier 2 multi-projection = témoin `ACVRAM_MULTI_PROJ=1`, non ouvert. Chaîne `scratchpad/gemm-dense-17-09/tete.sh` (défauts, sans variable).

addendum 15h50 — réduction split-K fusionnée (poste4 3b61caa, épilogue du dernier bloc, un lancement) : scellé deux bandes — juge = tps NU du profil (`profil-gdn-17-09.py b12`) ≥ 455 t/s tenu / < 435 faux ; J/jeton de la ronde ≤ 0,95 × 1,02 = 0,97 ; prédiction poste4 ≈ 450-460 nu. Repères : tête 415 nu (profil), 392 en ronde bridée. Chaîne `scratchpad/gemm-dense-17-09/fusion.sh` (test, b=12 ×2, ppl-decode-kv, profil).
