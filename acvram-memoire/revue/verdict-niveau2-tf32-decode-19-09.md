# Verdict — niveau 2 (`ACVRAM_MLA_CORE_DECODE=tf32`, TF32 sur les 4 sites einsum du décodage GLM) : **FAUX** — cœur fp32 du pas b=12 **1,58 → 1,48 ms** (scellé ≤ 0,6) : un seul des deux GEMM passe en `tensorop_s1688` (0,43 ms), l'autre reste `gemmSN_TN` fp32 (0,53) et le `reduce` fp32 (0,52) ne bouge pas ; pas total 13,88 → 13,87 ms (rien) ; PPL b=1 identique au bit **parce que b=1 n'emprunte pas les einsum** (cœur = `mla_1p`) ; en lot de 3 la PPL varie de ± 1-3 % par tranche (arithmétique du lot) — le ± 0,001 n'est pas mesurable ainsi ; capture 5/5

instrument : `scratchpad/niveau2-19-09/{chaine.sh,chaine-ppl.sh,chaine-ppl-3seq.sh}`, arbre `01749c0`/`2392408` (main `51f55188`+), prises `carte.sh` 21:15:04-21:18:05, 21:20-21:21, 21:38-21:41 ; (a) `decode-nsys.py` b=12 50 pas, `nsys --cuda-graph-trace=node`, A défaut / B `MLA_CORE_DECODE=tf32` (`[régime] … ACVRAM_MLA_CORE_DECODE=tf32 … eco=2700(2670)`) ; (b) `ppl-decode-kv-17-09.py` b=1 (corrigé : MLA sans cache paginé → champs KV None) et `ppl-decode-kv-3seq-19-09.py` (lot de 3, godet 4), corpus préfixé `corpus-prefixe-glm/prive-tr{0,1,2}`, 8 192 + 512 notés ; (c) `capture-godets.py` {1,2,8,12,16} sous B ; en-tête : `cpu [leann 396 ; cicc 50 ; python 23]` (indexeur, hors carte, sans effet sur nsys/PPL/capture), horloge éco 2 700 posée par le moteur
scellé (poste7, poste7-c13a-defaut § 2) : sgemm fp32 b=12 1,5 → ≤ 0,6 ms ; ppl-decode-kv 3 tranches géo ± 0,001 ; capture 5/5 ; pas 13,2 → 12,2 (certifie ABAB, non joué : le reste est faux avant) ; prédiction poste2 : 0,6-0,75 ms (borderline), PPL non résoluble à ± 0,001
mesuré : (a) A **1,581 ms/pas** = `gemmSN_TN<float>` 1,066 + `reduce_kernel<128,4>` 0,514 ; B **1,477** = `gemmSN_TN` 0,533 + `reduce` 0,516 + `cutlass tensorop_s1688gemm` 0,427 ; noyaux/pas 13,88 / 13,87 ; (b) b=1 : A **15,4069 / 17,2714 / 15,2397**, B **identiques aux 4 décimales** ; lot de 3 : A **15,4441 / 17,3534 / 14,9863**, B **15,6544 / 16,8467 / 15,0571** (+1,4 % / −2,9 % / +0,5 %, géo −0,4 %) ; (c) capture B **5/5** (9,12 / 10,39 / 13,42 / 14,54 / 15,96 ms/pas, replis [])
verdict : **FAUX** — le décodage à M=12 n'est pas le prefill : les einsum y sont des GEMM « minces » que cuBLAS sert par `gemmSN_TN` (SIMT, fp32 quel que soit le drapeau) ; seul le site qui tombe dans une forme cuTLASS bascule en TF32 (−0,64 ms sur un GEMM… mais 0,43 s1688 apparaît : −0,1 ms net), et le `reduce` fp32 est hors portée ; **niveau 2 ne rend rien (−0,7 % de noyaux), reste fp32** ; sur la qualité : la PPL b=1 « identique » ne juge rien (les sites TF32 ne sont pas empruntés à b=1 : un contrôle qui ne peut rendre faux), et la PPL en lot de 3 est dominée par l'arithmétique du lot (± 3 %, C4) — pour juger la PPL du niveau 2 il faudrait le teacher forcing à b=12 avec 12 notées, instrument à écrire
suite : poste1 : rien pour le niveau 2 (les 1,5 ms du cœur b=12 = C13-b bf16 sur le décodage, pas TF32) ; poste7 : niveau 2 clos, `MLA_CORE_DECODE` reste fp32 ; chef : indexer ; ma file : C14 étape 5 → concurrents 2 700 → p2 GLM propres (indexeur) → C9

## Rejouable
`ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/niveau2-19-09/chaine.sh` (3 min) + `chaine-ppl.sh` + `chaine-ppl-3seq.sh`.
