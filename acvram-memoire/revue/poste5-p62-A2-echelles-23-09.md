# Pièce 62, jalon A2 — échelles : la conversion du port est EXACTE sur l'alias officiel (0 échelle non nulle mise à zéro sur 113 M, échelle globale gardée en fp32) et les piles servies SONT déjà le format du port (`_construire_marlin` = `marlin_port.preparer_pile`, disposition unique depuis le 18/09) : aucun alias distinct ; le biais de 1-2·10⁻⁴ du banc est l'arithmétique du noyau, sous le critère — la KL Coder se joue au jalon A4 (chemin moteur) — 23/09 (poste5, à sec)

* instrument : lecture `acvram/kernels/marlin_port/__init__.py:108-158` (`facteur_nvfp4`, `traiter_echelles_nvfp4` : e4m3 → demi-précision × facteur (puissance de 2) × 2⁷, valeurs < 2 mises à 0, décalage d'un bit ; `traiter_echelle_globale` : fp32 × 2¹¹⁹ ÷ facteur, jamais bf16) ; comptage à sec sur `Qwen3-Coder-30B-A3B-nvfp4-qkvo-i8c` (`scratchpad/poste5-p62-23-09/a2-echelles-flush.json`, safetensors lus directement) ; `acvram/engine/moe.py:422-490` (`_construire_marlin` appelle `MP.preparer_pile` sur la pile NVFP4 et libère la pile naturelle) ; biais et exactitude : banc A1 (fp32 témoin, 2⁻⁷ par ligne)
* commit : 59e84279 ; 0 min de carte
* régime : à sec ; échantillon : couches 0, 5, 11, 17, 23, 29, 35, 41, 47 (gate, up, down), puis 0, 23, 47 pour les zéros et l'échelle globale
* scellé : « alias distinct, ulp contre témoins + KL Coder ≤ 0,74 » (chef) — réécrit ici sur les faits : la conversion est-elle lossless ? (rend faux si une échelle non nulle est mise à zéro ou si l'échelle globale est arrondie)
* mesuré : facteur = 2 sur toutes les piles ; **échelles de bloc mises à zéro : 969 367 sur 113 M (0,86 %), TOUTES déjà nulles dans l'alias (blocs entièrement nuls) ; non nulles mises à zéro : 0** ; subnormales e4m3 : 0 ; échelle globale : fp32 conservée (× 2¹¹⁹ ÷ 2, exact), son arrondi bf16 ferait ± 3,7·10⁻³ — il n'a pas lieu. Le banc A1 : Marlin 0 ligne hors 2⁻⁷ (sur 196 608), biais de gain − 1 à − 2·10⁻⁴, signé ≈ + 1·10⁻⁵ ; GEMV servi : gain − 1 ≈ 1·10⁻⁵.
* verdict : **la conversion des échelles est exacte et les piles servies sont déjà celles du port** — `experts_layout=marlin` signifie « disposition du port », lue par le GEMV (forme (b)) au décodage et par `gemm_moe` du port au PRÉFILL (`_forward_prefill_grouped`, moe.py:897-905, depuis le 18/09) : **pas d'alias distinct, pas de reconversion, zéro octet de plus en VRAM** ; ma crainte « e5m3 maison → e4m3 » (61/0) était fausse : le S0E5M3 est le format du port, pas le nôtre. Le biais résiduel de 1-2·10⁻⁴ est celui de l'arithmétique Marlin (déquantification e2m1 → demi-précision, produits sur tensor cores, réduction fp32) : 40 × sous le 2⁻⁷ par ligne du banc, invisible à une KL (≈ 10⁻⁸ nat) ; la **KL Coder ≤ 0,74 se mesure au jalon A4** sur le chemin moteur réel (mêmes 5 dumps HF que 22/09), pas sur un banc.
* durée : 0 min de carte, 20 min à sec

## Ce que A2 change pour A3/A4
A3 (AWQ d'activation, pièce 47) : le préfill du port applique déjà `x / awq_g[e_sorted]` côté activation avant la GEMM (moe.py:1076-1081) ; l'alias officiel n'a **aucune table d'experts** (18 432 sans statistiques, tables d'unité écartées) → A3 est un no-op pour la cellule de poste2, à porter sur le chemin de décodage seulement pour les alias calibrés (glue torch, 0,47 ms/pas, pièce 47 : à fusionner plus tard dans le chargement de A du noyau). A4 : au décodage, godet ≥ 2, `ACVRAM_MOE_TENSOR=1` → `aligner_blocs_capturable` + `gemm_moe` (gate, up) + act + `gemm_moe` (down) + `moe_reduce` existant, tampons fixes pour la capture ; godet 1 reste au GEMV (Marlin 1,40 contre 0,82 ms/pas).
