# Chantier C7 — GLM MLA en FP8 au préfill (`_scaled_mm`, q_b / kv_a / o) — 19/09, branche `poste1-c7-mla-fp8`, à sec

**Objectif** (poste7-poursuite-chantiers-19-09 § 2, ligne C7) : calculer les trois projections MLA que REGLES § 9 tient hors du NVFP4 en GEMM FP8 E4M3 (`torch._scaled_mm`, accumulation fp32, sortie bf16) au préfill, derrière `ACVRAM_MLA_FP8=1` (défaut 0 = inchangé). La porte qualité `ACVRAM_MLA_A8=e4m3` (832d4ae) arrondit l'entrée ; C7 ajoute le poids FP8 et le vrai GEMM.

**Prédiction scellée et seuil (copiés de la commande, AVANT toute mesure)** : prefill GLM ≥ 8 500 j/s (5 502 au défaut aujourd'hui, `verdict-p1-situ-complet-18-09` : calibA, P1) tenu, sinon faux ; ratio PPL ≤ 1,005 contre le défaut (bf16 MLA). Valeur d'alarme : une ligne `[régime]` sans `ACVRAM_MLA_FP8=1` ou `CHEMINS_FP8["scaled_mm"] = 0` après un préfill = le bras défaut mesuré deux fois.

## Ce qui existe déjà (fichier:ligne, lus dans ce worktree)
- Les projections MLA sont des `QuantLinear` (`engine/layers.py:425`), poids `qweight` = `PlainTensor` bf16 (`quant/formats.py:34`) ou `INT8Tensor` (`:187`) ; `forward` → `kernels.matmul` → référence `F.linear` pour bf16 (`kernels/__init__.py:1079-1092`). **Prémisse à corriger** : sur les convertis du disque elles ne sont PAS bf16 — manifeste calibA (celui des 5 502 j/s) : q_a/q_b/kv_a/o **int8 g128** (143/144, couche 0 o et q_a nvfp4) ; `-qkvo-i8c` : int8 par canal (`group_size` = K). Au préfill (n > `ACVRAM_INT8_GEMV_MAX` = 80, `kernels/__init__.py:716`) et `ACVRAM_PREFILL_INT8=bf16` (défaut, `:727`), l'int8 g128 passe par le repli « déquant par tranches + `F.linear` bf16 » (`:960-976`) : le GEMM que C7 remplace est bf16 cuBLAS, précédé d'une déquant int8 → bf16.
- Porte A8 : `engine/mla.py:44-50` (`_a8`), appels `_o/_kv_a/_q_b` ; `loader.py:779-804` construit `MLAttention` puis `fuse_projections()` → `stack_int8_linears` (`layers.py:1207`) empile q_a + kv_a en `qa_kv` quand les deux sont int8 de même K et groupe (calibA : oui) → `_proj_entree` (`mla.py:227`) servait la pile et **kv_a ne passait pas par `_kv_a`, donc pas par la porte A8** sur calibA (q_b et o seulement) — à porter sur la porte si G1 a mesuré calibA (une ligne : même contournement que C7 sous `_MLA_A8 != off`).
- Formes réelles (`config.json` de GLM-4.7-Flash, lu) : hidden 2048, q_lora 768, kv_lora 512, 20 têtes, nope 192, rope 64, v 256, 47 couches → q_b [5120, 768], kv_a [576, 2048], o [2048, 5120], pile qa_kv [1344, 2048] : K et N tous multiples de 16.

## Fait ce soir (commit de cette branche)
- `kernels/gemm_fp8.py` : `quantifier_lignes_e4m3` (E4M3 par ligne, s = amax/448 fp32, ligne nulle → 1), `poids_fp8` / `poids_fp8_du_module` (poids E4M3 + échelle fp32 **par canal de sortie** [1, N], convertis UNE fois et gardés sur le module — `module.__dict__["_fp8"]`, précédent `_i8c_poids` ; PlainTensor tel quel, INT8Tensor déquantifié torch pur une fois, nvfp4 par le backend, module exilé → inéligible), `contraintes` (K, N multiples de 16), `disponible` (CUDA, capacité ≥ 8.9 : `scaled_mm_arch_allowed`), `gemm_fp8_scaled_mm` (`_scaled_mm(a8, w8.t(), s_x [M,1], s_w [1,N], out_dtype)` mode rowwise), `gemm_fp8_torch` (repli : produit fp32 des codes puis échelles), `lineaire_fp8`, compteur `CHEMINS_FP8`. Par canal plutôt que par tenseur : le mode rowwise est le seul qui admette échelle par jeton ET échelle du poids ensemble, et il multiplie l'épilogue par un vecteur [1, N] de toute façon — une échelle par tenseur s'y écrirait comme un vecteur constant, même coût, pas de quantification d'une ligne au pas d'une autre.
- `engine/mla.py:59-91` : `_MLA_FP8`, `_verifier_portes` (FP8 et A8 ensemble = ValueError, un seul arrondi), `_regime_fp8` (n > `kernels._INT8_GEMV_MAX` : préfill seulement, le décodage garde GEMV/narrow/graphes), `_proj` ; `_o/_kv_a/_q_b` (`:208-215`) passent par `_a8` puis `_proj` ; `_proj_entree` (`:227-233`) contourne la pile au préfill sous FP8 (kv_a en FP8, q_a garde son chemin).
- `regime.py:136` (`MLA_FP8`, défaut 0, torch 0, lu à l'import) — `regime_ligne()` la porte ; `cli.py:196` liste de garde.

## Preuve à sec (`tests/test_mla_fp8_c7.py`, 9 tests ; rejouer : `CUDA_VISIBLE_DEVICES= ACVRAM_TESTS_PENDANT_MESURE=1 python -m pytest tests/test_mla_fp8_c7.py tests/test_mla_a8_porte.py tests/test_regime_noyaux.py tests/test_cadrage_perplexite.py -q -p no:cacheprovider` → 31 passed)
- Repli torch = `x @ W.T` bf16 à l'erreur E4M3 près : erreur relative (Frobenius) bornée à 0,08 ET plancher 0,01 (le bras doit différer) ; mesurée **0,037** sur les trois formes GLM (gaussien, M=256), contre 0,0062 pour l'int8 g128 des mêmes projections (×6).
- `_scaled_mm` CPU (torch 2.14.0+cu130, mode rowwise) sur les mêmes codes = repli à **1 ulp bf16** près (écart max 2⁻⁵ pour |y| < 8) — même arithmétique, ordre des sommes à part.
- Poids converti une fois (même objet sur deux appels), depuis bf16 et depuis INT8Tensor g128 ; reconstruit à 2⁻⁴ près ; exilé → None. Outlier 1e4 dans une ligne : code max = 448, aucun inf/nan, ligne juste (< 0,08) ; ligne nulle → 0. Témoin cassant : `s_w × 2` → erreur ≈ 1,0 > 0,08 (la borne rend faux), remise → tient. Contraintes sur les formes réelles (config.json relu quand présent) : aucune violation ; K=760 nommé et `lineaire_fp8` décline (None) au lieu de planter ; `w8.t()` colonne-majeur (strides (1, K)).
- Dispatcher : n=96 → FP8 (compteur +1, sortie ≠ module, < 0,08) ; n=8 → module au bit ; pile qa_kv contournée à n=96, prise à n=8 ; `forward` complet d'un MLAttention CPU sous la porte : fini, cache latent < 0,08 du défaut ; défaut = identité au bit ; table de régime + liste de garde (les deux tests de garde verts).

## Arithmétique (préfill 2 047, 47 couches, formes ci-dessus — pas de mesure)
- 3 projections : 15,6 M poids/couche → poids lus par préfill **bf16 1,466 Go / int8 0,733 / fp8 0,733** ; chemin calibA au défaut (int8 lu + bf16 écrit + bf16 relu) **3,67 Go** ; activations des 3 GEMM (entrées+sorties bf16) 3,02 Go, + quantification E4M3 des entrées 2,29 Go. Copie FP8 gardée à côté des poids de décodage : **+0,68 Gio de VRAM**.
- FLOP des 3 GEMM : **3,0 TFLOP** → ≥ 14 ms à 210 TFLOPS bf16 dense, 30 ms à 100 ; 3,6 ms à 840 TFLOPS FP8. Le pas mesuré vaut 372 ms (2 047/5 502) ; 8 500 j/s exige 241 ms (**−131 ms**). Le cœur d'attention du préfill (`mla.py` `forward`, einsum fp32 `C32`/`V32`, pas de TF32 dans le dépôt) pèse 8,6 TFLOP fp32 ≈ 86 ms à 100 TFLOPS, 171 à 50 : **le poste dominant du MLA au préfill est là, pas dans les projections** ; C7 ne peut rendre que ce que les 3 GEMM coûtent (≤ 30-40 ms) → 5 502 → ≈ 6 000 j/s. La prédiction « ≥ 8 500 » est donc à risque par l'arithmétique seule ; le profil de la fenêtre (part des 3 GEMM dans le pas) tranche.

## État
- Fait : dispatcher, repli, table de régime, 9 tests verts (31 avec les gardes), aucune carte touchée, rien compilé.
- Non vérifié : `_scaled_mm` rowwise sur sm_120 (cuBLASLt CUDA 13.0) — `disponible()` dit oui sur la 5090, et l'appel n'est PAS enveloppé d'un try : s'il refuse, la fenêtre échoue bruyamment plutôt que de mesurer le repli fp32 ; vitesse cuBLASLt FP8 réelle ; coût de la quantification par jeton (torch, 3 lancements par projection : amax, division, conversion) ; PPL.
- Reste : porter le contournement de la pile sur la porte A8 (décision poste1) ; si la fenêtre tient, fusionner la quantification E4M3 de l'entrée dans un noyau (Triton) ; le cœur d'attention fp32 (SDPA bf16 / noyau) est le chantier que l'arithmétique désigne.

## Première fenêtre carte (≤ 30 min, hors trou de la file, sous verrou)
1. Prouver que la configuration a pris : ligne `[régime]` avec `ACVRAM_MLA_FP8=1`, `ACVRAM_MLA_A8=off`, et après le premier préfill `gemm_fp8.CHEMINS_FP8` = {"scaled_mm": 141} (3 × 47) — imprimé par l'instrument.
2. ABAB prefill GLM calibA 2 047 : `prefill-glm-acvram` 7 rép., bras A = défaut, B = `ACVRAM_MLA_FP8=1`, alternés A B A B, σ par bras ; verdict ≥ 8 500 tenu / < faux ; profil (nsys ou minuterie CUDA) : part des 3 GEMM + quantification dans le pas.
3. PPL `ppl-acvram` 3 tranches (privé + `[gMASK]<sop>`) sur les deux bras, ratio géo B/A ≤ 1,005 tenu / > faux (rappel : la porte G1 bloc 16 ne couvre ni l'échelle par jeton ni le poids E4M3 ; l'erreur mesurée à sec est ×6 celle de l'int8 g128).
4. Décodage : rien ne change (n ≤ 80, prouvé au bit à sec) — pas de capture à refaire ; `ppl-decode-kv` un bras suffit comme témoin.
