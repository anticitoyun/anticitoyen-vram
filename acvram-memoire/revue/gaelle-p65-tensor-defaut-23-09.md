# Pièce 65 — le chemin tensor-core servi par défaut, aux godets ≥ 8 (mesuré, pas ≥ 2 comme ordonné) : b=12 6 197 µs/pas contre 6 919 en GEMV (−10,4 %), b=8 −4,3 %, mais b=4 +9,5 % et b=2 +12,7 % en tensor — seuil `ACVRAM_MOE_TENSOR_MIN_T=8`, glue fusionnée obligatoire, repli statique nommé sur la ligne de régime, contrôle à sec des 40 alias MoE du parc, 0.6.36

* instrument : `frontiere-pas.py` (200 pas, graphes) Coder `qkvo-i8c` à b = 2, 4, 8, 12, défaut contre `ACVRAM_MOE_TENSOR=0` (`chaine-p65.sh`, `chaine-p65b.sh`, `chaine-p65c.sh`) ; GLM `k48-calibA` b = 2 (40 pas) pour le repli nommé ; `outils/controle-moe-tensor-alias.py` à sec sur le parc ; tests `test_moe_tensor_defaut.py` (défauts nus à sec, règle de forme, parc, reproductibilité 20× sur carte), `test_moe_tensor_decodage.py` (godets 2/5/12/16), `test_moe_tensor_glue_fusee.py` (G = 8 à 128), `test_aligneur_cuda_marlin_port.py`, `test_defaut_servi.py` (ligne gelée 0.6.36)
* commit : arbre de a756f06b + ce commit (0.6.36) ; alias Coder `Qwen3-Coder-30B-A3B-nvfp4-qkvo-i8c`, GLM `GLM-4.7-Flash-srcbf16-nvfp4-k48-calibA`
* régime : b pleins, graphes on, repli_eager=0 ; ligne `chemin_moe=…+tensor(b≥8)` (défaut), `+tensor(off)` (=0), `+tensor(b≥8,repli:<raison>)` (forme refusée), `-glue-a4` (témoin non reproductible) ; carte ≈ 3 min en 12 prises ≤ 40 s, entre les prises de Manon (pièce 64) ; llama-server 4627 étranger, inactif
* scellé : « chemin tensor par défaut aux godets ≥ 2, GEMV au godet 1 ; repli nommé ; contrôle à sec des alias ; ligne gelée » et « défaut = chemin fusionné, reproductible ; test qui casse si l aligneur A4 est rebranché » (Jerome) ; prédictions au carnet (04 h 0x, avant) : (1) b=12 défaut 6 170-6 230, =0 6 820-6 890 ; (2) b=2 tensor sans régression > 5 % ; (3) GLM calibA repli nommé, pas identique ±2 % ; puis (seuil, avant) b=4 tensor +0,15/+0,35, b=8 −0,15/−0,40 ; contrôle du seuil b=4 4 740-4 790, b=8 5 350-5 410
* mesuré : (1) **6 197 / 6 919** (tenu ; le témoin GEMV 29 µs au-dessus de sa fourchette) ; (2) **b=2 tensor 4 931 contre GEMV 4 375 : +12,7 % — RÉFUTÉ** ; seuil : **b=4 5 215 / 4 764 (+9,5 %), b=8 5 381 / 5 623 (−4,3 %)** (sens tenu, b=4 0,1 ms au-delà de la fourchette) ; sous le seuil 8 : b=4 défaut 4 756, b=8 5 383 (tenus, GEMV et tensor respectivement) ; (3) GLM calibA `+tensor(repli:pas de piles Marlin (gate/up à entrées distinctes (tables AWQ séparées)…))`, 6 947 / 6 951 (tenu) ; parc à sec : 40 alias MoE, **19 acceptés** (Coder ×3, Nemotron ×3, Kimi, Ornith, Qwen3.5, Jan-VL, LFM2.5, DeepSeek-V2-Lite, GLM ×5 sans tables, gemma-4-26B, coder-next-80b), 1 partiel (GLM sansawq : 1 couche int4_awq), **20 refusés** (17 : tables AWQ non unité ; 1 : experts bf16 ; 1 : hadamard512 sur down ; 1 : int4_awq) ; tests 21 GPU + 7 à sec verts ; suite à sec complète : voir commit
* verdict : **tenu avec correction de l ordre** — le seuil « godets ≥ 2 » est faux (le GEMV par paire gagne tant que les experts distincts sont peu nombreux : bande passante ; la GEMM groupée gagne à partir de 8 jetons), le défaut est `MOE_TENSOR_MIN_T=8` (godets 8, 12, 16 en tensor ; 1, 2, 4 en GEMV, comme MOE_DECODE_MMA_MIN_T=5 avant lui) ; la glue A4 n est jamais servie (`_MOE_TENSOR_FUSION` défaut 1, `test_chemin_servi_reproductible_au_bit` 20× sur le cas à deux blocs) ; le repli est nommé par la même fonction (`forme_tensor_refus`) dans le moteur et dans le contrôle à sec ; le témoin `test_glue_fusionnee_au_bit` a été réécrit une seconde fois (une ligne d A4 égale entre deux appels n est pas forcément celle de la fusion : critère = fusion reproductible + 2⁻⁷·max contre A4)
* durée : 55 min (dont ≈ 3 min de carte) ; gênant nommé et arrivé : (2) — dit à Jerome avec la mesure, seuil pris sur mesure et non sur l ordre

## Ce que la 65 laisse
Les alias à tables AWQ non unité (GLM k48/calibA/B, Nemotron calibA, Coder qkv-alpha, VL abl) restent en GEMV : le chemin tensor ne divise pas x par la table (A3 du 62, no-op sur l officiel seulement) — pièce à part si l un d eux est servi. Hadamard sur down : idem. Godet 4 : +0,45 ms de tensor à rattraper si un jour la glue (0,22 ms) et le lancement des 3 GEMM baissent.
