# Verdict — palier 2 plein, Nemotron-3.5-Lightning-30B-A3B (`nemotron_h`, hybride Mamba + MoE) : **vLLM NVFP4 officiel 0,987 × bf16 → classé** (b=1 401 t/s, b=12 1 799 t/s 0,221 J, prefill 21 967 j/s, décodages bridés en puissance) ; **acvram srcexl3-nvfp4 1,0795 → non classé** (b=1 266,1 t/s 1,171 J, b=12 457 t/s 0,760 J, prefill refusé par exil) ; référence bf16 HF géo 13,416

instrument : `chaine-famille.sh nemotron-3.5-30b-a3b` + `reprise-nemotron.sh` (vLLM décodage avec vocabulaire lu du config, bf16 `PPL_HF_GPU=18GiB`) ; JSON et journaux `scratchpad/palier2-17-09/nemotron-3.5-30b-a3b/`
commit : arbre poste3 6d57cba → f2da860 (acvram/ = main) ; acvram `Nemotron-3.5-Lightning-30B-A3B-srcexl3_6bpw-nvfp4` (source EXL3 6 bpw : double quantification) ; vLLM `Nemotron-3.5-Lightning-30B-A3B-NVFP4` (officiel, KV FP8 → `attention_config TRITON_ATTN`) ; bf16 `models/Nemotron-3.5-Lightning-30B-A3B-bf16` (62 Go, HF eager device_map=auto 18 GiB + hôte)
régime : acvram classé, graphes on, 52 couches Mamba/attention en voie torch pour Mamba ; vLLM 0.29 `max_num_seqs` défaut, noyau Mamba natif ; corpus tranches-glm sans préfixe, ids fenêtre 0 = [1035, 3094, 1332, 1032] identiques dans les trois JSON
scellé (`protocole-palier2-familles-2-5-17-09`) : acvram 1,02-1,04, b=1 ≤ 80, prefill ≤ 800 ; vLLM ≤ 1,02 classé, b=1 ≥ 300, b=12 ≥ 1 200, prefill ≥ 10 000
mesuré : PPL par tranche — bf16 15,367 / 13,315 / 11,801 (géo 13,416) ; vLLM 14,833 / 13,254 / 11,810 → **0,9652 / 0,9954 / 1,0007, géo 0,987** (méd 0,993) ; acvram 16,509 / 14,582 / 12,617 → **1,0743 / 1,0952 / 1,0691, géo 1,0795** (méd 1,054) ; 0 fenêtre explosée partout ; acvram rondes b=1 pas 3,759 ms **266,1 t/s 1,171 J 312 W**, b=12 pas 26,26 ms **456,9 / 457,7 t/s 0,760 J 348 W**, aucune invalidation ; acvram prefill 2048 : refusé (« 3/52 couches exilées ; 384/2944 experts exilés » au chargement de l'instrument) ; vLLM b=1 **401,1 t/s 0,959 J brut / 0,771 net**, b=12 **1 798,9 t/s 0,221 / 0,181 J**, bridage puissance aux deux ; prefill 2048 **21 967 j/s** (σ 1 269, 93 ms)
verdict : **vLLM classé (0,987 ≤ 1,02) ; acvram non classé (1,0795) — trois prédictions tenues (vLLM classé, b=1, b=12, prefill), deux réfutées : acvram est au-dessus de 1,04 (double quantification EXL3 → NVFP4 non calibré sur un MoE : + 8 %, pas + 3) et son b=1 rondes est de 266, pas ≤ 80 (le décodage Mamba en torch ne coûte que 33 % de moins que vLLM natif, c'est le PRÉFILL torch qui s'effondre : 574 j/s à la fiche, refusé ici par exil).** Deux faits à examiner avant publication : (1) vLLM sous sa référence bf16 sur la tranche 0 (0,965) et à 1,00 sur les deux autres — « un converti sous sa source est un symptôme » : c'est la référence HF (nemotron_h eager, poids partiellement en hôte) qui est suspecte sur cette tranche, pas le NVFP4 ; à contrôler par une seconde référence (vLLM bf16 sur le même checkpoint source, 62 Go : exil vLLM impossible → hors carte, ou la référence HF sur GPU seul avec les 3 tranches en 2 048 réduits) — je publie 0,987 avec cette réserve ; (2) l'instrument de préfill acvram exile sur les MoE de ~20 Go (Kimi, Nemotron) là que la fiche du palier 1 avait mesuré un préfill de 512 : le budget de `prefill-glm-acvram-15-09.py` (2 048 × piles) dépasse ce que le plan laisse — cellule « refusé », instrument à revoir pour les MoE, pas le moteur. Comparatif : sur cette famille, vLLM bat acvram de 1,5 × au b=1, 3,9 × au b=12 et 2,3 × en qualité de bits ; la voie « hybride en torch » n'est pas un chemin de classement.
