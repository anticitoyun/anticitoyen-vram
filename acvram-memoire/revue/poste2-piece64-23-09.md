# Verdict — pièce 64 : acvram (tensor) contre vLLM, même client HTTP, même régime d'horloge — vLLM devant de +9,13 % t/s et -7,14 % J/jeton, sans ambiguïté cette fois

* instrument : `scratchpad/poste2-p64-23-09/run.sh` sous `outils/carte.sh` : ABBA X1,Y1,Y2,X2 b=12, MÊME client `banc-llamacpp-16-09.py` (BANC_MOTEUR=acvram, endpoint OpenAI `/v1/completions`) pour les deux moteurs — X=`acvram serve` ACVRAM_MOE_TENSOR=1, Y=`vllm serve` (--attention-backend TRITON_ATTN --max-num-seqs 12) ; **1ʳᵉ tentative rejetée avant verdict** (Y à horloge libre 2927-2940 MHz car `vllm serve` ne pose pas de `-lgc`, contrairement au service acvram qui le fait lui-même au chargement et le relâche à l'arrêt — cause trouvée avant publication, cf. message à chef 04:4x) ; 2e tentative : `sudo nvidia-smi -lgc 2700,2700` explicite autour de chaque bras Y, `-rgc` après
* commit : arbre poste2 90699d63 (= main, pièce 63 fusionnée, glue tensor reproductible) ; alias `Qwen3-Coder-30B-A3B-nvfp4-qkvo-i8c` (acvram) / `Qwen3-Coder-30B-A3B-Instruct-FP4-a16` (vLLM Marlin W4A16)
* régime : compute-apps avant/après = llama-server du pair (4627) seul ; verrou tenu 04:52:31→04:56:52 (261 s) ; **les deux moteurs dans la même bande d'horloge** (X 2649/2665, Y 2665/2668 MHz), bridage `puissance` sur les 4 bras — comparaison valide cette fois
* scellé (avant mesure) : pas de direction prédite, écart significatif seulement si |Δ%| > 2 % (sinon égalité) ; issue gênante nommée d'avance : vLLM devant dans la même séance publié tel quel
* mesuré : X (n=2) 1829,5 / 1833,8 → moy **1831,7** ; Y (n=2) 2027,9 / 1970,0 → moy **1999,0**. **Gain vLLM = +9,13 %**, au-delà du seuil de 2 % — pas une égalité. Énergie : X 0,1407 J/jeton net, Y 0,1306 → **vLLM −7,14 %**, aussi devant. Question instrument (chef) tranchée : les cellules publiées 1634 (acvram) et 1782 (vLLM) du 22/09 ne sont PAS le même instrument — 1634 vient de `banc-llamacpp-16-09.py` HTTP contre `acvram serve`, 1782 (et le b=1 290,6 du T1 20/09) viennent de `decode-vllm-17-09.py`, API offline `LLM().generate()` en processus, jamais de `vllm serve` — mésappariement systémique sur toute la colonne vLLM du comparatif, pas un cas isolé
* verdict : **dans le régime servi, à horloge et bridage comparables, vLLM reste devant acvram à b=12** (+9,13 % t/s, −7,14 % J/jeton) — le gain du chemin tensor-core (A4/A5, +12,2 % sur l'ancienne référence acvram) réduit l'écart sans le combler. Publiable tel quel avec la ligne de régime (`-lgc 2700` explicite, bridage puissance nommé des deux côtés). Recommandation : requalifier toute la colonne vLLM du README/comparatif mesurée par `decode-vllm-17-09.py` (b=1 290,6, b=12 1596,1/1782) comme instrument différent, ou la remesurer par `vllm serve` + banc commun avant toute comparaison publiée
* durée : 261 s de carte (4 bras propres), verrou rendu 04:56:52, carte cédée à poste5 (pièce 67 puis vLLM b=1)
