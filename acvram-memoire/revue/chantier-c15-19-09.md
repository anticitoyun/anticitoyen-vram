# Chantier C15 — glue du pas de décodage GLM b=1 : attribution des 2 622 lancements, 6 (MLA) + 3 à 6 (MoE) retirés par couche au bit (`ACVRAM_MLA_GLUE=1`, −460/pas), 15 de plus par le noyau de préparation (`=2`, numérique du lot) ; le seuil ≤ 18 élémentaires n'est atteint qu'avec le niveau 2 (Océane, 19/09, à sec ; second passage 20 h 40 : compte à sec par couche, glue MoE, chaîne carte)

**État (second passage, branche fusionnée `origin/main` 435a9f4c : éco 2700, C13 MLA_CORE*, C14, C17)** : `ACVRAM_MLA_GLUE=1` retire maintenant, au bit, 6 lancements par couche MLA **et** 3 (Marlin) / 6 (tables AWQ distinctes) par couche MoE ; prédiction carte `=1` : 2 622 → **2 162 lancements/pas** (−460), `=2` : ≤ 1 460. Prouvé à sec : comptes exacts par couche et par niveau (§ « Compte à sec par couche »), jetons/logits/caches égaux au bit `=0`/`=1`/`=2` (à sec `=2` retombe sur `=1`), MoE `_forward_grouped` isolé égal au bit avec ses ops retirées nommées, témoins qui cassent. Chaîne carte prête, non exécutée : `scratchpad/c15-carte-19-09/chaine.sh`.

**Objectif** (sage-glm-decode-budget-c14-c15-19-09 § 2, C15) : les ~1 700 petits noyaux du pas b=1 (2,0 ms, 26 %) — les attribuer ligne par ligne, retirer ce qui ne calcule rien sans changer un bit, nommer ce qui exige un noyau. Aucun GPU ce soir ; le `.cu` n'est pas recompilé.

**Prédiction scellée (Sage, recopiée)** : « glue MLA/MoE par couche : ≤ **18 noyaux/couche** (36 aujourd'hui) ; jetons identiques ; b=1 −0,8 ms au moins ; prédiction −1,0 ms ; réfutation : < 0,5 ms gagnée pour 18 noyaux : le coût n'est pas le lancement, arrêter ». **Comment je compte** : lancements/pas = `Instances / 50` du CSV `glm-decode-b1_cuda_gpu_kern_sum.csv` (Manon, 53acea4) — 2 622 noyaux/pas au total (7,73 ms), dont « élémentaires » = tout sauf GEMV/attention/experts/routeur/reduce/swiglu ; par couche = sur une couche MoE Marlin (33 des 46), le total de la couche est 53, dont 37 élémentaires (le « 36 » de Sage : 1 700 / 47). **Ma prédiction, avant toute mesure** : `MLA_GLUE=1` : 2 622 → ≤ 2 340 lancements/pas (−6 × 47 − 1), jetons identiques au bit sur 256 pas, −0,3 à −0,5 ms/pas ; `=2` : ≤ 1 650 lancements/pas, élémentaires 18/couche, −1,0 ± 0,3 ms, PPL décodage ± 0,001 (jetons non garantis : q_abs ≤ 1 ulp). Réfutation : jetons différents à `=1` = bogue (retirer) ; `=2` sous −0,5 ms → Sage a raison, le coût n'est pas le lancement.

## Attribution (b=1, 47 couches = 1 dense + 46 MoE : 33 Marlin, 13 « distinctes » xreg ; chemin = `_la_decode` model.py:2501 → `decode_static` mla.py:610)

| noyau nsys | /pas | /couche | fichier:ligne | pourquoi |
|---|---|---|---|---|
| `int8_gemv<bf16,bf16>` | 233 | 3 + 2 MoE | mla.py:233 (`qa_kv`), :264 (`q_b`), :523 (`_o`) ; model.py:2001 expert partagé (gate_up, down) | 47×3 + 46×2 |
| `rmsnorm_bf16` | 189 | 4 (+1 finale) | model.py:2439 entrée, mla.py:264 q_a_norm, :509 kv_a_norm, model.py:2551 post ; :3007 `norm` | déjà 1 lancement (layers.py:578) |
| `add<bf16>` | 234 | 5 | model.py:2441 et :2554 résidus (2) ; mla.py:300-301 RoPE (sub = add α=−1, add) (2) ; :1882 `y + shared` (1 MoE) | 47×4 + 46 |
| `Mul<bf16>` non vectorisé | 188 | 4 | mla.py:300-301 `x0*c, x1*s, x0*s, x1*c` (vues à pas 2) | RoPE « norm » |
| `direct_copy` → fp32 | 212 | 4,5 | mla.py:619 `q_eff.to(f32)` (47), :521 `v_b.to(f32)` (47) ; model.py:1812 `x.to(f32)` routeur (46) ; :1770 `.to(act.dtype)` après division AWQ (33+13) ; :1762-1764 `x_g/x_u.to(f32)` (13×2) | conversions bf16→fp32 (cast dynamique, 2,8 µs, 0,60 ms : le plus cher) |
| `bfloat16_copy` (fp32→bf16) | 93 | 2 | mla.py:523 `.to(x.dtype)` (47) ; model.py:1770 `act.to(bf16)` (33 Marlin : la gate·up sort en fp32 ; 13 xreg) | |
| `direct_copy` → int64 | 164 | 3-5 | model.py:1684 `tok.long()` (33+26), :1686 `eid.long()` (33+26), :1770 `eid.long()` (46) | int32→int64 pour l'indexation |
| `vectorized_gather` | 164 | 3-5 | model.py:1684 `x[tok]`, :1686 `table[eid]`, :1770 `awq_down[eid]` | 33×3 + 13×5 |
| `Div<bf16>` | 108 | 2-3 | model.py:1686 (33 + 13×2), :1770 (46) = 105 ; 3 hors couche non attribués | échelles AWQ par expert |
| `index_kernel` | 94 | 2 | layers.py:757 `_cos[positions]`, `_sin[positions]` via mla.py:284 | tables RoPE bf16 |
| `Cat` 2-D vect. | 94 | 2 | mla.py:507 `cat([c, k_pe])` redécoupée :508, :512 `k_new` | |
| `Cat` 3-D (2 variantes) + `alignedK` 4-D | 47+47+47 | 3 | mla.py:306 `cat([q_pe, k_pe])`, :511 `cat([q_abs, q_pe])`, :302 `torch.stack((y0,y1))` | |
| `index_copy`, `add_<long>` | 47, 47 | 2 | mla.py:489 `_ecrit_ligne`, :522 `len.add_(1)` | écriture du latent, avance |
| `gemv2T` bf16, `gemvx` f32 (cuBLAS) | 47, 47 | 2 | mla.py:510 einsum k_b, :521 einsum v_b | |
| `dot`+`reduce_1Block` f32, `_route_fusee`, `arange` | 46 ×3 | 3 | model.py:1812 `F.linear` fp32 (M=1 → 2 noyaux cuBLAS), :1868, :1690 `tok_g` | routeur |
| `mla_1p` + `combine`, `moe_reduce`, `swiglu`, Marlin ×2, xreg ×3, `silu`+`Mul<f32>` ×13 | | | mla.py:138 ; model.py:1786, :2002 ; :1706/:1774 ; :1763-1776 ; :1765 | calcul, pas glue |

Total par couche Marlin : 4 (bloc) + 27 (MLA) + 22 (MoE) = 53, dont 37 élémentaires ; xreg : 63.

## Ce qui existe déjà, et pourquoi pas sur MLA
`add_norm` layers.py:585 → `rmsnorm_bf16` avec résidu (.cu:5738, :5751 `bf16(fp32(res)+fp32(y))` = l'addition bf16 de torch, au bit) ; pris seulement par `ACVRamModel._res_differe` model.py:3054, qui exige `type(l) is DecoderLayer` — GLM est en `DecoderLayerGDN` (MLA dans `linear_attn`), donc `decode_fixed` :2439-2441 + `_mlp` :2551-2554 = 2 rmsnorm + 2 add séparés. `int8_matmul_norme` kernels/__init__.py:877 (`Attention.decode_fixed_norme` model.py:384) : réfuté 6dbb1bb (+0,31 ms, norme recalculée par bloc), pas transposé. `mla_prep_batch` .cu:5600 (RoPE + einsum k_b + norme kv_a + cat + fp32 en 1 lancement) et `mla_ecrit_latent` .cu:5285 : chemin `decode_static_batch_complet` mla.py:556, pris à b > 1 seulement (`_MLA_BATCH=2`, model.py:2486) ; à b=1 `decode_static` refait tout en torch. `_v_b32` mla.py:548 : utilisé par le chemin complet, pas par `_sortie_decode`.

## Fait ce soir — `ACVRAM_MLA_GLUE` (mla.py:102, regime.py, garde cli.py), défaut 0 = inchangé
`=1`, mêmes opérations et arrondis : (a) `_v_b32` dans `_sortie_decode` :519 et le repli :632 (−1 copie fp32) ; (b) cat kvp :507 supprimée (−1 cat) ; (c) RoPE écrite en place :297-298 (`torch.sub/add(out=)`, −1 stack) ; (d) demi-tables cos/sin empilées `tables_demi` layers.py:710, une indexation :279 (−1 index) ; (e) `DecoderLayerGDN.decode_fixed_res` model.py:2446 + `_res_differe` :3054 étendu aux couches MLA (−2 add, par `add_norm`, + −1 sur la norme finale). **−6 lancements/couche** : 53 → 47, élémentaires 37 → 31. `=2` : b=1 par `decode_static_batch_complet` (model.py:2492) — −15 lancements MLA (27 → 12) : 53 → 32, élémentaires 31 → **18** = le seuil de Sage, atteint seulement ainsi.

**Preuve à sec** : `tests/test_mla_glue_c15.py` — jouet GLM (2 couches MLA bas rang + RoPE « norm » + MLP, bf16, processeur), 32 pas greedy : jetons, logits et caches latents **égaux au bit** entre `=0` et `=1` ; compteur d'opérations aten hors vues (`TorchDispatchMode`) : **154 → 146 ops/pas, −8 = 4/couche exactement** (`to`, `cat`, `stack`, `index` : ×2) ; témoins qui cassent : cos/sin permutés → diverge ; delta doublé dans `decode_fixed_res` → diverge ; à `=0` ni `tables_demi` ni `decode_fixed_res` ne sont appelées. Rejouer : `CUDA_VISIBLE_DEVICES= ACVRAM_TESTS_PENDANT_MESURE=1 python -m pytest tests/test_mla_glue_c15.py tests/test_regime_noyaux.py tests/test_cadrage_perplexite.py tests/test_mla_norme.py tests/test_mla_decode_batch.py -q -p no:cacheprovider` → 41 passed, 13 skipped (CUDA) au premier passage ; second passage (fusion main, 6 tests ajoutés) : `tests/test_mla_glue_c15.py tests/test_mla_decode_batch.py tests/test_regime_noyaux.py tests/test_mla_tf32_c13.py tests/test_moe_awq_pile.py tests/test_route_prep.py tests/test_marlin_refus_distinct.py tests/test_c10_marlin_distinct_glm.py tests/test_moe_hadamard_pile.py tests/test_quant_act_echelle.py tests/test_cadrage_perplexite.py tests/test_mla_norme.py` → **69 passed, 45 skipped** (CUDA).

## Compte à sec par couche (second passage, `test_compte_a_sec_par_couche_par_niveau`, `test_moe_glue_tok_eid_int64_au_bit`)
Jouet GLM, dernier des 32 pas, ops aten hors vues (`_VUES`) par appel de `decode_fixed`/`decode_fixed_res` de chaque couche ; « hors couches » = plongement, norme finale (repli torch : 6 ops), tête. Les comptes sont **asserts exacts** ; les ops retirées sont nommées et assertées par couche ; jetons, logits et caches égaux au bit entre les trois niveaux (à sec `=2` retombe sur `=1` : `_la_decode` model.py:2569 exige `mla_decode_batch`).

| niveau | couche 0 | couche 1 | hors couches | total/pas | retiré par couche (nommé) |
|---|---|---|---|---|---|
| `=0` | 72 | 72 | 10 | 154 | — |
| `=1` | 67 | 68 | 11 | 146 | c0 : `to`, `cat`, `stack`, `index`, `add` ; c1 : `to`, `cat`, `stack`, `index` (l'`add` de la couche 0 est différé dans la couche 1, le dernier delta passe hors couche : le résidu différé déplace, à sec, sans fusionner — la fusion est le .cu) |
| `=2` | 67 | 68 | 11 | 146 | rien à sec (même chemin que `=1`) |

Glue MoE, `_forward_grouped` isolé (model.py:1700 ; GEMV groupé remplacé par une référence torch NON comptée ; T=1, k=2, tables AWQ) : ops comptées hors GEMV, `=0` → `=1`, sortie égale au bit dans les quatre formes :

| forme | `=0` | `=1` | retiré (nommé) | à la carte |
|---|---|---|---|---|
| eid servi (route_fusee), tables gate = up (Marlin, 33 couches) | 17 | 14 | `to` ×2 (tok int64, eid int64 une fois), `arange` (tok_g = seq) | −3/couche = −99 |
| eid servi, tables distinctes (xreg, 13 couches) | 23 | 17 | `to` ×4 (tok ×2, eid ×2), `index` (x[tok] une fois), `arange` | −6/couche = −78 |
| eid non servi (chemin `_route` sans route_prep), Marlin | 20 | 19 | `to` (eid une fois) | — (b=1 GLM passe par route_fusee) |
| eid non servi, distinctes | 26 | 22 | `to` ×3, `index` | — |

Témoin qui casse : `index_jetons_long` décalé d'un jeton (T=2) → sortie différente ; à `=0` le chemin `_ligne` d'avant est intact (model.py:1754). **Ce que le compte à sec ne dit pas** : que `x[tok64]` et `table[eid64]` lancent à la carte le même `vectorized_gather` qu'avant (des index, même noyau, même dtype : c'est le cas par construction, mais le nsys le confirme), et le coût en µs.

## Fait au second passage — glue MoE (`ACVRAM_MLA_GLUE=1`, model.py:1730-1752, :1841-1843 ; kernels/route_prep.py:120 `index_jetons_long`)
`tok` int64 réservé une fois par (T, k, appareil) à côté de `index_jetons` ; `eid64 = eid.long()` une fois par couche (au lieu de :1736 ×1-2 et :1820) ; `xe = x[tok64]` une fois pour gate ET up (les deux `_ligne` refaisaient le gather) ; `tok_g = _seq` (même `arange(T·k)` int32 que `seq`, model.py:1740 le refaisait). Rien de numérique : des index. Attendus carte `=1`, par pas : `direct_copy`→int64 164 → **46**, `vectorized_gather` 164 → **151**, `arange` 46 → **0**.

## Reste (fichier:ligne, gain attendu par pas) — noyau CUDA/Triton exigé, NON écrit
* **Division AWQ dans les GEMV experts** (model.py:1745-1748 `xe / tg[eid64]`, :1843 `act.to(bf16) / awq_down[eid64]` puis `.to(f32)`) : les 105 `Div<bf16>` + 93 `bfloat16_copy` + 46 `direct_copy`→fp32 de :1843 ; il faut l'arrondi bf16 du quotient reproduit DANS `nvfp4_gemv_marlin_kernel` / `xreg` (acvram_kernels.cu) : −5 (Marlin) / −8 (xreg) par couche, ≈ −270/pas. Torch ne peut pas : la division bf16 séparée est ce qui fixe les bits.
* **`q_eff` fp32 direct** (mla.py:653 `q_eff.to(torch.float32)`, 47/pas) : `cat(out=fp32)` n'existe pas au même arrondi sur CUDA (cat de dtypes mêlés = une copie par entrée) ; c'est `mla_prep_batch` (niveau 2) ou rien.
* **Logits du routeur** (model.py:1885 `F.linear(x.to(f32), w)` : `direct_copy` 46 + `dot` 46 + `reduce_1Block` 46) dans `_route_fusee_kernel` (Triton, route_prep.py:54) : −3/couche = −138 ; l'ordre d'accumulation fp32 change → à juger par jetons + PPL, pas au bit.
* **`y + shared`** (model.py:1955, 46 `add<bf16>`) dans `moe_reduce_kernel` : −46 ; **`o_proj` lisant fp32** (mla.py:557 `.to(x.dtype)`, 47 `bfloat16_copy`) : `int8_gemv<float,bf16>` non instancié : −47 ; **`eid` int64 écrit par `_route_fusee_kernel`** (`tl.int64` au store, route_prep.py:94) : −46 (les 46 `direct_copy`→int64 qui restent) — ou indexation int32 native de torch (`IndexingUtils.h:78` accepte `kInt`, mais si le noyau `index` convertit en interne le lancement reste : à lire au nsys, pas à supposer).
* **Reste torch au bit, non fait** : rien d'autre trouvé au niveau 1 — `k_b.to(x.dtype)` (mla.py:540) est inerte (bf16 déjà), `.to(bf16)`/`.to(x.dtype)` de :1745-1748 sont inertes au décodage (x bf16), les `cat` :544-545 et :336 écrivent des tampons que les noyaux lisent contigus.
* **Non vérifié à sec** : l'égalité au bit de `add_norm` (le .cu, pas le repli torch) ; le niveau 2 (`mla_prep_batch`, B=1 ; `test_mla_decode_batch.py::test_module_batch_complet` saute sans carte) ; que `_dt_coeur(decode=True)` sous `MLA_CORE_DECODE=bf16` avec `_v_b32` fasse une conversion fp32→bf16 de plus par couche (main :554 la faisait de bf16, inerte) — régime opt-in, hors défaut, à dire si C13-déc passe bf16 ; toute durée.

## Chaîne carte (prête, non exécutée) — `scratchpad/c15-carte-19-09/chaine.sh` (+ `lancements.py`, `jetons.py`)
Arbre = celui qui contient la chaîne (cette branche fusionnée main) ; `outils/carte.sh` autour de chaque bras, `ACVRAM_*` hérités retirés, `ACVRAM_CUDA_HOME=/usr/local/cuda-13.4`, `ACVRAM_ECO=off`, `nvidia-smi --query-compute-apps` au début, avant chaque bras, à la fin ; étape 0 = extension de l'arbre nommée (chemin, sha ; compile sous verrou `etat` si le .so manque — ~9 min, dit) ; étape 1 = les tests à sec ci-dessus. `PARTIES=abc` (défaut), `TRANCHES=1` (3 pour le scellé PPL).
1. **(a) nsys b=1**, invite 256, 50 pas, `decode-nsys.py` de Manon, sous `ACVRAM_MLA_GLUE=0 | 1 | 2` → `lancements.py` : lancements/pas par nom court (`Instances/50`) contre les **attendus écrits avant** (dans le script) : `=0` = les chiffres de 53acea4 (2 622 ; un FAUX ici = le régime a bougé avec main, à dire) ; `=1` : `direct_copy`→fp32 212 → 165, →int64 164 → 46, `Cat` vect. 141 → 94, `alignedK` 47 → 0, `index_kernel` 94 → 47, `add<bf16>` 234 → 140, `gather` 164 → 151, `arange` 46 → 0, `rmsnorm_bf16` 189 inchangé, **total ≤ 2 182** (prédit 2 162) ; `=2` : plus de `Mul<bf16>`, `gemv2T`, `Cat`, `index_kernel`, `index_copy`, `add_<long>` ; `mla_prep_batch` 47, `mla_ecrit_latent` 47, **total ≤ 1 500** (prédit ≤ 1 460). Avec `--ref` la liste des noyaux dont le compte a changé est imprimée (comparer les listes, REGLES § 5). Par couche Marlin : 53 → 44 (`=1`) → 29 (`=2`) ; élémentaires 37 → 28 → 15 ; xreg : 63 → 51 → 36.
2. **(b) 256 pas greedy**, invite = 256 premiers jetons de `corpus-prefixe-glm/prive-tr0.txt`, graphes ON, `jetons-glue{0,1,2}.json` : **`=1` identiques à `=0` exigés** (divergence = bogue, retirer) ; `=2` informatif (q_abs ≤ 1 ulp : des jetons peuvent bouger, c'est la PPL qui juge).
3. **(c) PPL de décodage** `ppl-decode-kv-17-09.py` GLM corpus préfixé, 8 192 + 512 notés, `=0` contre `=2` : **± 0,001 (géo sur 3 tranches)** — à 1 tranche c'est indicatif (résolution ~± 0,005), dit par le script.
4. ms/pas (`RESULTAT ms_par_pas` de decode-nsys, sous nsys : ordre de grandeur seulement ; le scellé −0,8 ms se prend hors nsys, ABAB, chaîne harnais à écrire si (a) tient). Réfutation Sage : < 0,5 ms gagnée pour 18 noyaux → le coût n'est pas le lancement, arrêter.


## Juges par niveau (Sage `sage-c15-niveaux-20-09`) — chaîne corrigée (ce commit)
* `=1` (au bit à sec) : **jetons identiques 256 pas** contre `=0`, compte nsys ≤ 2 200, capture 5/5, pas b=1 GLM ≤ défaut − 0,3 ms, Coder b=12 ≤ défaut − 0,2 ms → défaut à la fusion.
* `=2` (noyaux fusionnés, ordre de somme différent) : **pas les jetons** — `logits.py` : teacher forcing sur la suite de `=1` (invite 256 + ses 256 jetons, `jetons-glue1.json`), logits bf16 de chaque pas écrits (`logits-glue<n>.pt`), écart max par pas en ulp bf16 de la référence, **scellé ≤ 1 ulp bf16 par pas** (TENU/FAUX imprimé, `logits-glue2-vs-glue1.json`) ; + `ppl-decode-kv` `=0` contre `=2` ± 0,002 (partie (c)) ; capture 5/5 ; pas b=1 GLM ≤ `=1` − 0,5 ms → défaut.

## Niveau 2 : jumeaux torch par noyau (Sage 04 h 20 : « jumeaux torch par noyau à sec, déviant nommé ») — `tests/test_mla_niveau2_jumeaux.py`
Le chemin `=2` (b=1 par `decode_static_batch_complet`) compose trois noyaux CUDA : `mla_prep_batch`, `mla_ecrit_latent`, `mla_decode_batch`. Chacun a son jumeau torch (le contrat du binding, rien d'autre), branché par une fausse extension à registre d'adresses (`FauxExt` : {data_ptr → tenseur}).
* **À sec, prouvé** : B=1, 32 pas, formes GLM (nh 20, nope 128, rope 64, rank 512, dv 128, q_a/q_b, RoPE), `decode_static_batch_complet` (prep torch + jumeaux écrit/décode) = `decode_static` **au bit** (sorties, caches, len) à chaque pas ; témoin cassant (un jumeau qui masque une clé de moins → diverge). **La glue et les contrats sont innocentés : le déviant du +3,3 % est un noyau ou l'aiguillage `=2` du moteur sous graphes**, pas la composition.
* **Carte (1 min, saute sans CUDA)** : `test_chaque_noyau_contre_son_jumeau_sur_carte` — (1) `mla_prep_batch` réel vs jumeau sur mêmes q/kvp/lens (q_eff, k_new ≤ 1 ulp bf16), (2) `mla_ecrit_latent` réel vs jumeau (cache et len égaux), (3) `mla_decode_batch` réel vs jumeau sur mêmes q_eff/caches (≤ 1 ulp bf16), (4) le chemin complet réel 32 pas vs `decode_static` — **le premier écart nomme le déviant** (message `DÉVIANT : <noyau>`). Correctif après le nom, pas avant.
* **Carte (Manon, 20/09 ~03 h)** : les trois noyaux tiennent (`mla_prep_batch` q_eff 1,00 / k_new 0,00 ulp bf16, `mla_ecrit_latent` au bit, `mla_decode_batch` 0,07 ulp) ; chemin complet réel : **2 ulp bf16 au pas 15** = propagation du 1 ulp de `mla_prep_batch` (RoPE tables fp32), bornée, attendue — pas +3,3 % de PPL. Déviant restant, par le protocole : l'aiguillage `=2` du moteur (`model.py:2592-2599`, `_la_decode` q_len == 1 → `_mla_lot(1)` + `decode_static_batch_complet`), dont la seule différence de nature avec `=1` est d'être capturé dans un graphe (`len` avancé sur la carte par `len_ptrs`, table créée à l'échauffement). Test qui tranche (Manon, 3 min) : `=2` graphes OFF contre `=1` graphes OFF, 64 pas, logits ≤ 2 ulp → défaut de capture ; sinon `_la_decode` =2 à la ligne. Aucune correction avant le nom.
* **Remède REGLES § 6, prêt (ce commit, à activer si le test « graphes off » de Manon rend ≤ 1 ulp)** : `RotaryEmbedding.reserver()` matérialise `tables32` et `tables_demi` **sans condition** (`layers.py` : avant, seulement si elles existaient déjà — au niveau 2 à b=1, `tables32` n'est lue que par le chemin de lot et n'existait pas avant la capture : sa première allocation tombait sous capture, dans le bassin du graphe) ; `tables32` refuse **toute** allocation en capture (première comprise) ; `MLAttention.chauffer(device, max_pos)` matérialise k_b contigu, v_b fp32, tables (appelé par `graphs.py` avec `reserver` avant toute capture) ; `_refuser_en_capture` sur `_k_b_c`, `_v_b32`, `_mla_lot` : une allocation sous capture lève au lieu de corrompre en silence. Test à sec `test_remede_regles_6_caches_paresseux_refuses_en_capture` (capture simulée : refus ; après `chauffer`, mêmes objets ; `reserver` crée `_cos32` sur un module neuf). 22 passed. Cause nommée sous réserve du test : `layers.py:reserver` conditionnel + première allocation de `tables32` sous capture ; si le test rend « ≠ hors graphes », l'aiguillage est ailleurs et le remède reste une hygiène.
* **Test qui tranche (Manon, verdict-c15 addendum 23 h 51, logits 64 pas teacher-forcés, GLM, ed37fe01)** : (a) `=2` graphes OFF contre `=1` OFF : **0,00 ulp bf16, argmax identiques** — l'aiguillage `_la_decode` =2 est sain ; (b) `=2` ON contre OFF : **2 019 ulp au pas 2**, 2/64 argmax différents — capture/rejeu ; (c) témoin `=1` ON contre OFF : 0,00. **Cause confirmée : classe REGLES § 6** (première allocation de `tables32` sous capture, `reserver` conditionnel) ; correctif e095f227 à rejouer par (b) + PPL niveau 2.
* **Rejeu sur f8a495a3 (Manon b5b005b3)** : déviant inchangé (2 019 ulp au pas 2, argmax 2/64), **aucune levée de `_refuser_en_capture`** → l'hypothèse « allocation en capture » est **écartée** par ses propres gardes (le remède reste une hygiène). Relu à sec sous l'angle « entier Python figé dans le graphe » : `lens` lu sur la carte par les trois noyaux, `bucket` constant par godet, `len` restauré en place — rien de figé trouvé. Ce qui reste : l'un des trois noyaux SOUS REJEU. Trois sondes d'1 min (une variable à la fois, `=2` ON contre OFF) : (i) `ACVRAM_MLA_PREP_NOYAU=0` (prep torch) ; (ii) `ACVRAM_MLA_UNE_PASSE=0` (`mla_decode_batch` au lieu de `mla_decode_1p` C14 par table d'adresses) ; (iii) **`ACVRAM_MLA_ECRIT_TORCH=1`** (nouvelle sonde, ce commit : écriture du latent par `_ecrit_ligne` torch). Prédiction : (ii) rend 0 ulp — `mla_decode_1p` sur table d'adresses sous rejeu.
* **Réponse à « un scalaire hôte figé à la capture » (Sage, addendum 00 h 15)** : les arguments des trois noyaux du chemin `=2`, un par un — `mla_prep_batch(q, kvp, lens, cos32, sin32, k_b_c, kv_a_norm, nope, rope, rank, eps)` (`mla.py`, `decode_static_batch_complet`) : scalaires `nope, rope, rank, eps` = constantes du module ; `mla_ecrit_latent(k_new, cache_ptrs, len_ptrs, fp8)` : `fp8` constant ; `mla_decode_1p/_batch(q_eff, cache_ptrs, lens, scores, bucket, rank, scale, fp8)` : `bucket` = godet (constant par capture, légitime), `rank`, `scale`, `fp8` constants ; le compteur par pas est `lens` (tenseur, `torch.stack([st["len"]])` recalculé dans le graphe) et `len` avance sur la carte (`len_ptrs`). **Aucun scalaire par pas** — prouvé par `test_rejeu_simule_avec_un_pas_different_change_la_sortie` (REGLES § 6, forme demandée : les deux noyaux rejoués avec les scalaires capturés au pas 0 après avancement de l'état rendent le pas 1, jamais le pas 0). La cause n'est donc pas dans le contrat des appels ; elle est **dans un noyau sous rejeu** (état interne, espace de travail, dépendance de flux) — les sondes (i)-(iii) le nomment, le correctif suit le nom.

## 3.2 : instrument multi-tranches — `outils/gpu/mesure/ppl-decode-kv.py` (Océane, 20/09 07 h 34, à sec ; ordre Sage `sage-tests-30min-20-09` § 3.2)
Un chargement par bras au lieu de 9 tranches × 2 bras = 18 chargements × 21 s : même arithmétique que `scratchpad/ppl-decode-kv-17-09.py` (mêmes ids, `PPL_PREFIXE` en tête compté dans P, même teacher forcing par `_sample_only`), le modèle et le moteur chargés UNE fois, chaque tranche une requête neuve, chaque résultat écrit dès qu'elle finit (ligne `RESULTAT {json}` + `<sortie>/ppl-<bras>-t<k>.json`, schéma de l'ancien + `t_charge`, `t_prefill`, `t_decode` en s après `torch.cuda.synchronize()`, ligne `TOTAL` finale avec `t_total`). Commande, par bras (répliquer les variables d'environnement de la chaîne : `ACVRAM_KV_FORMAT`, `ACVRAM_MLA_GLUE`, `PPL_PREFIXE`, `ACVRAM_MODELE_MESURE`) :
`ACVRAM_MODELE_MESURE=<GLM> PPL_PREFIXE='[gMASK]<sop>' ACVRAM_MLA_GLUE=<n> python outils/gpu/mesure/ppl-decode-kv.py --tranches t1.txt … t9.txt --bras glue<n> --sortie <dossier> --prefixe 8192 --notes 512` (ou `--dossier D --motif 'tranche-*.txt'` ; reprise `--depuis k`, ou `--reprendre` = sauter les JSON présents). **Contrôle (Manon, avant de croire la chaîne)** : la PPL de la tranche k rendue par le multi-tranches doit être **identique à 10⁻⁴** à la prise isolée `ppl-decode-kv-17-09.py` sur la même tranche, même bras — sinon un état survit entre tranches et l'instrument ment ; la tranche 1 paie la capture des graphes dans son `t_decode`, les suivantes disent le régime établi. Ce qui survit entre requêtes (lu dans `runner.py`) : blocs KV rendus à `_finish` (:985) mais publiés au cache de préfixe juste avant (:984) → cache de préfixe DÉSACTIVÉ (`enable_prefix_cache=False`, :383) ; états des tampons fixes retirés du magasin (:994-995) et remis à zéro au premier forward de la requête suivante (`static_load(st, None)` : gdn.py:212-214, mla.py:613-614 — `len` à 0, lignes au-delà masquées mla.py:818), l'id de séquence étant un compteur jamais réutilisé (:37, model.py:2619). Prédiction de Sage à réfuter par `REGIME`/`TOTAL` : `t_charge` ≈ 15-18 s des 21 s ; si `t_charge` < 3 s, le gain est nul et le poste de 10 s est à nommer (`t_moteur`, `t_prefill`, `t_decode` de la tranche 1 le séparent). Tests à sec `tests/test_ppl_decode_kv_multi.py` (7) : découpe = ancien script sur tokenizer jouet, `--depuis`/`--reprendre`, RESULTAT écrit avant la tranche suivante (moteur factice), témoin deux tranches → deux PPL, cache de préfixe à faux.
