# Verdict — passe ncu bornée (lecture seule) GEMV groupée des experts, rpw=4 contre témoin rpw=1, Coder b=12, 1 pas profilé (96 lancements) ; tableau complet : `scratchpad/ncu-rpw-18-09/tableau.txt`

instrument : `outils/ncu_gemv_experts_rpw_18-09.sh` (Laurine 3cc0e9f, pris sur origin/laurine) sur `banc_decodage_moe.py gemv 12`, `--launch-skip 192 --launch-count 96`, régime classé posé dans un wrapper `PY` (`scratchpad/ncu-rpw-18-09/py-rpw{4,1}.sh`), CSV relu par `tableau.py` (locale fr) ; horloges verrouillées par ncu (absolus non comparables aux ms/pas nus)
commit : a2f7d4a (main 4b83a2f), 07:07-07:37, un processus par valeur
régime : classé (`fg` = 288 = 6 pas × 48, `mma` = 0), graphes off (profileur), b=12, BANC_JETONS=8
scellé : aucun (lecture seule demandée par Sage ; Laurine lit avant d'écrire)
mesuré (médianes, gateup | down) : durée rpw4 **87,9 | 65,2 µs** (×48 = 4,22 + 3,13 = 7,35 ms/pas) contre rpw1 **95,5 | 81,0 µs** (4,58 + 3,89 = 8,47) → −8 % | −20 % ; grille rpw4 (24,96) | (64,96) contre (96,96) | (256,96) ; vagues/SM 2,26 | 6,02 contre 9,04 | 24,09 ; warps actifs 86,8 | 91,9 % contre 94,1 | 93,0 % ; DRAM 40,4 | 27,3 % contre 36,8 | 21,8 % ; L2 hit 65,8 | 71,5 % contre 67,2 | 76,7 % ; requêtes L1 ld 1,106 M | 0,885 M contre 1,769 M | 1,769 M (−37 % | −50 %) ; secteurs/requête 5,67 | 4,39 contre 4,17 | 3,44 ; issue actif 56,0 | 64,8 % contre 55,7 | 59,7 % ; décrochages par issue rpw4 : long_scoreboard 4,38 | 4,09 (rpw1 6,53 | 6,28), mio_throttle 4,58 | 3,55 (3,90 | 3,54), short_scoreboard 4,49 | 1,85 (3,91 | 1,19), barrier 0,42 | 0,96 (0,79 | 0,86), lg_throttle 0 ; 40 registres/thread, limite d'occupation 6 blocs (registres) | 10-15 (smem) dans les deux cas
verdict : lecture seule — le gain de rpw=4 est celui prédit par Laurine (moins de requêtes de chargement : −37 % gate/up, −50 % down ; latence mémoire long_scoreboard 6,5 → 4,4) ; ce qui reste : le premier décrochage devient mio_throttle + short_scoreboard (files de la mémoire partagée / de l'étage d'activation), la coalescence du gate/up se dégrade (4,17 → 5,67 secteurs/requête) et le gate/up n'a plus que 2,26 vagues par SM (grille 24×96) : la piste suivante est là, pas dans la DRAM (40 % au mieux)

## Défauts d'instrument rencontrés (corrigés côté chaîne, à porter dans le script par Laurine)
1. `sudo -n ncu` remet l'environnement à zéro : `ACVRAM_*`, `BANC_JETONS`, `ACVRAM_GROUPED_RPW` n'atteignaient pas le processus cible (essais 1-2 : `chemin_moe=mma`, `fg=0`, « No kernels were profiled ») → régime posé dans le wrapper `PY` ; conséquence : le processus cible tourne en root (fichiers de cache créés root).
2. Parseur : `r[14].replace(",", "")` sur une sortie ncu en locale fr (`91 040`, `45,56`) → `ValueError` silencieux puis `median` vide ; et `45,56` → 4556 si l'espace manque. `tableau.py` : retirer espaces (y compris insécables), virgule → point.
3. `dram__bytes_read.sum` rend `n/a` sur cette carte/ncu 2026.3 (les 17 autres métriques répondent) ; la bande se lit par `dram__throughput` %.
