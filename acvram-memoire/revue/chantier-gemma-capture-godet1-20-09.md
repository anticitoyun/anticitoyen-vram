# Chantier — gemma-4-26B-A4B : capture du godet 1 impossible (OOM), service en eager 2,9 × plus lent sans le dire (verdict-capture-parc-19-09 ; sage-cloture-nuit-0540-20-09 rang 3) — fait le 20/09 06 h 10 : le repli est ANNONCÉ (ligne de régime, journal, /metrics) ; 06 h 30 : cause lue (H3, cache de l'allocateur non rendu après les piles Marlin), instrument + correctif à sec sur `oceane-gemma-capture`, preuve carte pour Manon (§ « Fait le 20/09 »)

## Fait à sec (oceane-11, ce commit)
* `GraphRunner._capture` en échec appelait `enabled = False` sans compter un repli : `repli_eager=0`, `replis_eager_raisons=[]`, ligne `graphes=off` sans cause, `/metrics` muet — un service entièrement en eager passait pour un service sans repli (capture-godets.py le voyait seulement par `graphes=on` absent). Désormais l'échec est un repli comme les autres : `_eager("capture impossible (godet b=1, ql=1) : AcceleratorError: CUDA error: out of memory")` — compté (`repli_eager`), nommé une fois au journal `[graphe] repli eager — …`, porté par `replis_eager_raisons` (donc `/metrics.replis_eager_raisons`).
* Ligne de régime (`Engine.regime_ligne`) : graphes demandés mais retombés → **`graphes=off(repli eager: AcceleratorError: CUDA error: out of memory)`** ; off demandé → `graphes=off` sans cause. Tests : `tests/test_regime_graphes_vivants.py` (cause sur la ligne ; off demandé sans cause ; repli compté et nommé une fois).

## Ce qui reste (reprise, > 30 min à sec) — le correctif de mémoire
Lecture du journal de Manon (`scratchpad/capture-parc-19-09/gemma*.log`) : chaque godet est un moteur séparé (`capture-godets.py`), tous avec `kv_budget=15328/16` ; **seul le moteur `max_batch_size=1` échoue, à sa première capture**. Donc ce n'est pas « la première capture après le chargement » en général : c'est le plan à b=1 qui laisse moins de VRAM libre au bassin du graphe (la fraction KV remplit le reste à un lot plus petit, ou le bassin du godet 1 de gemma — 30 couches, 3 840 experts, hybride ≤ 4 — est plus grand que celui des godets suivants). Correctif nommé : (1) instrument d'abord — `torch.cuda.memory_summary()` et `mem_get_info()` juste avant la première capture, dans le journal (une ligne : libre / réservé / alloué) ; (2) le plan réserve la marge du bassin de graphe AVANT le budget KV (`auto_plan` : `kv_vram_fraction` moins une marge = taille du bassin mesurée au godet le plus grand, ou `ACVRAM_GRAPH_POOL_MARGE`) ; (3) test cassant « godet 1 capturé sur gemma » (chaîne carte `capture-godets.py <gemma> 1`, `ok=true`) et test à sec du plan (budget KV ≤ libre − marge). Ornith-1.0-35B (OOM Triton avant tout godet, 20 Go + 10 240 experts) : pile Triton sous `ACVRAM_TRACEBACK=1` et taille demandée — hors de ce chantier.

## Fait le 20/09 (06 h 05 – 06 h 30, à sec, branche `oceane-gemma-capture` depuis oceane-11 ca0108de) — hypothèse retenue : H3, un cache rendu trop tard ; correctif `empty_cache` après chaque pile ; preuve carte à faire (Manon)
### Ce que la lecture chiffre (fichier:ligne, arbre de la branche)
* **H1 (plan ou réservation différents à b=1) : FAUX.** `outils/gpu/mesure/capture-godets.py:17` charge UNE fois avec `max_concurrent_seqs=max(GODETS)=16` ; le plan est arrêté et les caches KV alloués dans `load_model` (`loader.py:337` `_borner_kv_avec_exil`, `loader.py:929` `PagedKVCache`), pas dans `Engine` : les quatre moteurs partagent le même cache — `kv_budget=15328/16` sur les quatre lignes de régime = 15 328 × 123 840 o (`kv_bytes_per_token` du manifeste) = **1,77 Gio**, quel que soit b. La réserve de préfill (`loader.py:1692` `_reserve_prefill` : `activations_prefill_bytes(2048)` + `_DENSE_SLOTS × couche max`) est une entrée du plan, identique aussi. Rien dans le plan ne dépend de b.
* **H2 (bassin du godet 1 plus gros) : FAUX.** Les tampons statiques d'un graphe sont en `b·ql` (`graphs.py:770` `_capture` : x [b·ql, 2816], positions, slots, tables [b, nblk]) ; le bassin privé (`graphs.py:853` `graph.pool()`) est partagé par les godets d'un même moteur. Le godet 1 est le plus petit ; « hybrides≤4 » est un plafond (`plafond_hybride`), gemma n'a pas de couche hybride.
* **H3 (réserve rendue trop tard) : RETENUE.** Ce qui distingue le moteur b=1, c'est d'être le PREMIER : `GraphRunner.__init__` (`graphs.py:236`) → `_eligible` (`graphs.py:310`) construit paresseusement les piles Marlin de chaque `MoEBlock` (`model.py:828` `_try_build_stacks`) : par couche, empilement des 128 experts (copie), repack Marlin (seconde copie), puis sources et pile naturelle **rendues à l'allocateur PyTorch — qui les garde en cache** (`model.py:1022` `_liberer_pile_naturelle`). Sur gemma, les sources sont 3 840 experts × 3 projections de **991 Kio** (704 × 2 816 / 2) : sous 1 Mio, donc dans le **bassin des petits blocs** de l'allocateur (segments de 2 Mio) — ~10,9 Gio que rien d'autre ne peut réemployer. Pendant une capture, l'allocateur **ne rend jamais son cache** (garde `captures_underway` dans `DeviceCachingAllocator::malloc`, c10/cuda/CUDACachingAllocator.cpp, torch 2.14 — cudaFree interdit sous capture) et le bassin privé du graphe exige des segments neufs : `cudaMalloc` échoue → `AcceleratorError: out of memory`. Les moteurs 2/8/16 passent parce que `capture-godets.py:46` fait `del eng; torch.cuda.empty_cache()` entre deux moteurs. **Le chiffre vient d'Ornith** (`capture-parc-19-09/Ornith-1.0-35B-kimi-nvfp4.log`, ligne 2, pendant la construction des piles) : `free: 394 264 576` (0,37 Gio) sur 31,36 Gio pour ~19 Gio de poids + KV selon le manifeste (17,35 Gio MoE + 0,40 attn + tête + KV 32 768 × 41 280 o = 1,26 Gio) — **≥ 11 Gio en cache non rendu** ; sur gemma le journal du 19/09 n'a pas la ligne (instrument absent : commit 1).
### Commits (branche `oceane-gemma-capture`)
* **9c8ee22b** instrument : `GraphRunner._avant_premiere_capture` / `_apres_echec_capture` (`graphs.py`) — `[graphe] mémoire avant capture : libre X Gio, réservé Y, alloué Z (cache non rendu Y−Z)` une fois par moteur, même ligne après un échec AVANT le `empty_cache` du repli ; `regime()['graphes_memoire_avant_capture']` et `['graphes_memoire_apres_echec']` (`runner.py`, octets, None à sec). Test `tests/test_graphes_memoire_avant_capture.py` (4 tests : photo prise une fois avec les valeurs lues, ligne exacte, après échec, témoin sans CUDA = None et rien d'imprimé).
* **bdcb4bee** correctif : `MoEBlock._rendre_le_cache_apres_la_pile` (`model.py:984`), `torch.cuda.empty_cache()` à la fin de chaque `_try_build_stacks` réussi (les deux appelants : `graphs.py:310` et `model.py:2080`) — une synchronisation par couche au chargement, rien en service. Témoin `ACVRAM_PILE_SANS_RENDU=1` (bras A, nommé une fois au journal). Test `tests/test_pile_rend_le_cache.py` (4 tests : une pile construite rend une fois ; une pile refusée ne passe pas par ce chemin ; sans CUDA rien ; témoin nommé et muet sur le rendu). Pas de marge de plan : le budget KV (1,77 Gio) n'est pas en cause, une marge aurait masqué la cause.
* commit 3 : `capture-godets.py` porte les deux photos dans la ligne `GODET` et, sous `ACVRAM_TRACEBACK=1`, la pile entière au journal ; chaîne `scratchpad/gemma-capture-20-09/chaine.sh` (scellé en tête).
### Chaîne carte (Manon) — `bash scratchpad/gemma-capture-20-09/chaine.sh` (~15 min, arbre sur `oceane-gemma-capture`, `ACVRAM_ARBRE` = son arbre)
Scellé : A (`ACVRAM_PILE_SANS_RENDU=1`) gemma godet 1 `ok=false`, cache non rendu ≥ 5 Gio et libre < 1 Gio sur la ligne mémoire ; B gemma **godet 1 `ok=true`, ms/pas b=1 ≤ 1,3 × b=2**, cache < 1 Gio ; Ornith (B, `ACVRAM_TRACEBACK=1`) godet 1 ok ou pile entière ; les 9 autres MoE 4/4. Si A tient et B tombe : H3 est une cause mais pas la seule — lire les deux photos avant tout autre correctif. Si A ne montre pas ≥ 5 Gio de cache : H3 est fausse, revenir aux photos.
### Ornith-1.0-35B (kimi : 40 couches, 256 experts de 512 × 2 048, 30 couches KDA + 10 attention ; fiche-service ligne 87 : `charge=True`, 22,1 Gio chargés, `decode_tok_s` vide)
* Le noyau qui tombe (json de Manon, champ `trace`) : `triton/runtime/autotuner.py:155 kernel_call` → `do_bench` → `driver.py:331 launch` — **un autotune de fla**, au PRÉFILL d'échauffement de `warm_graphs` (126 jetons, `runner.py:1728`) : `kda.py:154` → `chunk_kda` (`fla/ops/kda/chunk_fwd.py`) → premier noyau autotuné `chunk_kda_fwd_intra` (`chunk_intra.py:35`, 6 configs BK∈{32,64} × warps∈{1,2,4}, puis 12 configs warps∈{1,2,4,8} × stages∈{2,3,4}), puis `wy_fast.py`, `gate.py`. Aucun `@triton.autotune` dans acvram (grep). Ce n'est pas une taille demandée par un `torch.empty` (le tampon de `do_bench`, 256 Mio, passe par l'allocateur PyTorch) : `cuLaunchKernel` rend `CUDA_ERROR_OUT_OF_MEMORY` quand le pilote ne peut plus allouer la mémoire locale des fils (registres débordés d'une config large) — hors de l'allocateur PyTorch, donc hors de son cache : 0,37 Gio libres au pilote (ligne 2 du journal) pendant que ≥ 11 Gio dormaient en cache. Kimi-Linear-35B (même KDA, mêmes noyaux, 4/4 le 19/09) prouve que le noyau n'est pas trop gros par nature : c'est la VRAM libre.
* Correctif proposé : **le même** (bdcb4bee) — les 10 240 experts de 0,5 Mio (512 × 2 048 / 2) vivent aussi dans le bassin des petits blocs (~15 Gio). Chaîne B-ornith sous `ACVRAM_TRACEBACK=1`. Si elle tombe encore : refus nommé dans `fiche-service.tsv` — « non servi au décodage : OOM Triton (fla chunk_kda, autotune) au premier préfill, 22,1 Gio de poids + piles » — et témoin `ACVRAM_KDA_CHUNK=0` (chemin torch, `kda.py:36`) pour départager Triton de la VRAM.
### Ce qui reste
Preuve carte (Manon) ; si B tient, `verdict-capture-parc-19-09` passe à 11/11 ou nomme Ornith seul ; Jérôme : `fiche-service.tsv` Ornith selon B-ornith ; fusion dans oceane-11 après la preuve.
