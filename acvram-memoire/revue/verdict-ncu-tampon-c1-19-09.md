# Verdict — ncu du tampon C1 (route (i), une couche de prefill 2 047 jetons réels, repli torch `w4a8`) : **scellé FAUX à la lettre (W_déquant + R_GEMM = 22,5 Gio = 39,8 × tampon)** parce que le repli torch écrit 40 fois le tampon en intermédiaires fp32 ; **la question de fond est tranchée : le L2 sert la relecture du GEMM** — R_GEMM = **0,007 Gio (0,01 × tampon)** avec cache, **0,604 Gio (1,07 ×)** purgé (le contrôle rend bien faux quand on lui retire le L2)

instrument : `scratchpad/ncu-tampon-c1-19-09/{chaine.sh,cible.py,agrege.py}` (commit `f14578b`), arbre C1 `0512a00` (worktree `manon-c1`) ; cible = UN `_forward_prefill_grouped` de la couche 5 (MoEBlock n° 5) sur `x (2047, 2048) bf16`, `topi (2047, 8)` capturés pendant un prefill d'échauffement réel (tranche 1 privée, routage réel), rejoué sous `torch.inference_mode()` dans une plage NVTX « mesure » ; `sudo -n ncu --csv --target-processes all --clock-control none --cache-control none|all --launch-count 6000 --nvtx --nvtx-include "mesure/" --metrics gpu__time_duration.sum,dram__bytes_op_read.sum,dram__bytes_op_write.sum` (`dram__bytes_read.sum` rend `n/a` sur sm_120 : le premier essai sommait des zéros et mon agrégateur disait « tenu » — corrigé : vide = NON MESURABLE) ; HOME root `home-ncu` avec `kernels-cebf7bde0b48` + `marlin_port` copiés (`ACVRAM_MARLIN_CACHE`), variables `ACVRAM_PREFILL_GROUPED=w4a8 ACVRAM_W4A8_GEMM=torch` seules ; prise `carte.sh` 20:22:22-20:24:39 (2 × ~1 min), 2 949 noyaux profilés par passe ; tampon = 16 groupes × (8 × 3 × 768 × 2 048 o + échelles) = 0,565 Gio
scellé (fiche C1 / Sage, avant) : W_déquant(écrits) + R_GEMM(lus) ≤ 1,2 × 0,565 Gio → tenu ; > 1,2 → groupes de 4 puis 2 ; contrôle : `--cache-control all` doit donner R_GEMM ≈ tampon ; prédiction Océane none 0,9-1,1 ×, all ≈ 2,0 × ; Manon none : R_GEMM 0,25-0,45 ×, all ≈ 1,0 ×
mesuré (par couche, une passe) — **none** : déquant/requant (torch : 2 226 `elementwise`, 1 356 `direct_copy`, 1 062 copies bf16, 981 `Fill`, 144 `_depaqueter` Triton, 144 `Abs`+`reduce`…) **lus 7,21 Gio, écrits 22,50 Gio**, 25,6 s sous rejeu ncu ; **GEMM** (`cutlass_80_tensorop_i16832gemm_s8` 64×64 ×1 014 + 128× ×48 = 1 062 lancements = 16 groupes × 3 × 8 experts… ) **lus 0,007 Gio (0,01 × tampon), écrits 0,210 Gio** (sorties s32/bf16), 2,08 s ; autres (tri, scan) 0,05 / 0,07 Gio ; **all** (purge avant chaque noyau) : déquant lus 34,5 / écrits 1,96 Gio ; **GEMM lus 0,604 Gio (1,07 × tampon)**, écrits 0 ; autres 0,14
verdict : **à la lettre FAUX (39,8 ×) et sans objet : les 22,5 Gio écrits sont les intermédiaires fp32 du repli torch** (`requant_w8_depuis_bf16` matérialise poids déquantifiés, |w|, max, arrondi… 4-8 fois la taille du tampon) — ce n'est pas ce qu'un noyau de déquant écrirait (une fois : 0,565 Gio). **Ce que le protocole voulait savoir est établi et positif : sous cache, le GEMM relit le tampon depuis le L2 (0,007 Gio de DRAM pour 0,565 de tampon, ×80 de moins), et le contrôle purgé rend 1,07 × — la métrique voit le L2.** Avec un noyau qui écrit le tampon une fois : W + R ≈ 0,565 + 0,007 = 1,01 × → tenu ; les groupes de 8 (35 Mo) tiennent dans le L2 de la 5090 ; pas besoin de 4 ni 2. Réserve : les écritures du GEMM (0,21 Gio) et les activations (46 Mo) ne sont pas dans le scellé tel qu'écrit ; le sont-elles dans le noyau : +0,4 × au pire → 1,4 × si l'on compte tout, à Sage de dire ce qui compte
suite : Océane (C1 route (i)) : continuer vers 24-28 ms, groupes de 8, noyau de déquant à écriture unique ; Sage : scellé à réécrire « R_GEMM ≤ 0,2 × tampon sous cache ET ≈ 1 × purgé » (c'est ce qui se mesure), le W_déquant se juge sur le noyau, pas sur torch ; ma file : six cellules éco 2700 (décision utilisateur 20 h 22) → statics → NARROW_GEMM → P2+éco (absorbé par les cellules 2700)

## Rejouable
`ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/ncu-tampon-c1-19-09/chaine.sh` (2 min 20) ; CSV `ncu-none.csv`, `ncu-all.csv` (8 847 lignes chacun). Chaîne équivalente d'Océane : `acvram-memoire/revue/donnees-c1-routes-19-09/chaine.sh` (dd047634, non jouée).
