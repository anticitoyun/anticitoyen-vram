# Verdict — C4 (godets sur b, sentinelles de rembourrage, oceane-c4-godets-b fffd17c) sur Qwen3.8-27B (hybride GDN) : capture **5/5 vraie** ; contrôle d'instrument **exact** (mono défaut = mono `GODETS_B=0` = 8,8181) ; **LA mesure FAUSSE** : la séquence notée décodée dans un lot de 3 (godet 4) rend **8,7980 (−0,0201, −0,23 %)** au lieu de ± 0,001 — et **sous `GDN=torch` aussi (+0,0116)** : ce n'est pas fla, c'est l'arithmétique du lot ; drain 12 → 1 à rejouer (plafond hybride 4 dans ma chaîne : 287 pas eager sur 415, comparaison aveugle)

instrument : `scratchpad/c4-godets-19-09/chaine.sh`, arbre C4 `fffd17c` (worktree `manon-c4`, `arbre exécuté` imprimé), instruments `118b68f` : (1) `outils/gpu/mesure/capture-godets.py` {1,2,4,8,16} avec `ACVRAM_HYBRID_SLOTS=16` posé (cet arbre n'a pas 24b6565) ; (2) `ppl-decode-kv-17-09.py` (préfixe 8 192 + 512 notés, b=1, tranche 1 Coder) défaut vs `ACVRAM_GODETS_B=0` ; (3) **`ppl-decode-kv-3seq-19-09.py`** (nouveau, variante à 3 séquences : la notée + 2 remplissages de 1 024 jetons en glouton libre, `max_batch_size=4`, hook `_sample_only` par ligne de lot ; `lots_vus {3: 511, 2: 96, 1: 3}` = la notée a décodé 511 de ses 512 pas dans un lot de 3) ; rejeu (3) et (2) sous `ACVRAM_GDN=torch` ; (4) `drain-12-1-19-09.py` (12 invites, longueurs 64..416) défaut vs `GODETS_B=0` ; prise `carte.sh` 19:44:30-19:50:17, carte 0 vide
scellé (Sage, avant) : (3) PPL notée = mono ± 0,001 ; fiche C4 : ± 0,002, et si faux rejouer sous `GDN=torch` pour séparer fla du rembourrage ; drain publié non scellé ; prédiction Manon : (2) identique à 1e-4, (3) ± 0,0005
mesuré : capture **13,92 / 17,27 / 23,71 / 25,42 / 28,36 ms/pas** à b=1/2/4/8/16, `replis_eager` [] partout (5/5) ; (2) mono défaut **8,8181**, mono `GODETS_B=0` **8,8181** (Δ 0) ; (3) 3seq défaut **8,7980** (Δ **−0,0201**) ; sous `GDN=torch` : mono **8,8192**, 3seq **8,8308** (Δ **+0,0116**) ; (4) défaut : 415 pas, 16,67 s, 40,2 ms/pas, captures 5 → 8, voies graphe 133 / eager 287, replis « lot hybride 10|11|12 au-delà du plafond `ACVRAM_HYBRID_SLOTS=4` » ; `GODETS_B=0` : 415 pas, 16,68 s, captures 5 → 9, 133 / 287 — identiques : **le drain n'a pas mesuré le godet, il a mesuré l'eager**
verdict : **(3) FAUX aux deux régimes** — la notée dans un lot de 3 ne rend pas la PPL de la notée seule : −0,23 % sous fla, +0,13 % sous torch ; ce n'est donc pas le noyau fla ni le seul rembourrage : **un lot de 3 (godet 4) n'accumule pas comme un lot de 1** (une GEMM sur 4 lignes contre 1 : réductions et tuiles différentes en bf16) — le ± 0,001 du scellé supposait une exactitude que l'arithmétique par lot n'a pas ; la variante 3 séquences fait bien ce pour quoi elle est là (elle rend faux), et ce qu'elle voit n'est pas C4 mais le bruit de forme du lot ; la sentinelle distincte (C4) n'est ni confirmée ni infirmée par cette mesure ; **(2) exact** : `GODETS_B=0` à b=1 ne change rien, comme prévu (contrôle de l'instrument) ; **(4) invalide par ma chaîne** (`HYBRID_SLOTS=16` posé sur la capture seule, pas sur le drain) — rejeu avec 12 dans la fenêtre suivante
suite : Sage : le scellé (3) doit dire ce qu'il compare — la PPL d'une séquence dans un lot contre elle-même seule diffère de ±0,2 % par construction (bf16) ; pour juger la sentinelle il faut un bras qui doit différer (rembourrage à −1 unique, l'ancien) contre les sentinelles distinctes, même lot ; Océane : le bras « ancien rembourrage » existe-t-il par variable (`GODETS_B=0` n'agit qu'au-delà de b=1 sur le godet, pas sur la sentinelle) ; ma file : drain rejoué (2 min) → bras F → Mesure 2 (si W_mma2 ≤ 350) → porte A8+W8r → statics → NARROW_GEMM → P2+éco

## Rejouable
`ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/c4-godets-19-09/chaine.sh` (6 min) ; drain corrigé : `chaine-drain.sh`.

## Addendum 19 h 58 — drain 12 → 1 rejoué sur l'arbre main `b16313e` + fffd17c (`281b522`, plafond hybride = `--max-batch`, aucun `HYBRID_SLOTS` posé) : **godets −11,5 % de temps, 5 captures de moins, 0 pas eager**
instrument : `chaine-drain.sh` (12 invites de 256, longueurs 64..416, un moteur), prise `carte.sh` 19:57:33-19:58:16 ; mesuré : **défaut (godets)** 415 pas, **12,08 s, 29,1 ms/pas, captures 5 → 11, voies graphe 420 / eager 0** ; **`GODETS_B=0` (lot exact)** 415 pas, **13,65 s, 32,9 ms/pas, captures 5 → 16, graphe 356 / eager 64** ; lots vus 12 → 0 dans les deux ; verdict : publié non scellé — le godet sur b épargne 5 captures et 64 pas eager sur un drain de 12 à 1, −1,6 s sur 13,7 (−11,5 %) ; la première passe (plafond 4) ne voyait rien parce que 287 pas sur 415 étaient eager dans les deux bras

## Addendum 20 h 05 — A/B § 2 (sage-c4-lot-arithmetique-19-09) dans le MÊME lot : **issue (b) — ancien (47812d9) = nouveau (fffd17c) AU BIT** (s0 8,7980 ; témoins f1 8,1178, f2 3,7002 ; 511 pas sur 512 dans un lot de 3) ; nouveau = lot-de-3 de 19 h 48 au bit et reproductible au bit (nouveau-bis) → sur carte le fantôme n'écrit jamais dans un bloc réel ; **C4 fusionne comme hygiène, sans cellule ni revendication**
instrument : `chaine-ab.sh` + `ppl-decode-kv-3seq-19-09.py` (témoins désormais notés : forcés sur la suite du corpus, 608 jetons chacun ; s0 ajoutée en dernier = dernière réelle, `store[-1]`) ; rembourrage du fantôme x=0 (le `_fill` du runner, Océane : c'est celui du test au bit) ; bras ANCIEN = worktree `manon-c4-ancien` à `47812d9e` (fffd17c^), NOUVEAU = `manon-c4` `281b522` (main b16313e + fffd17c), `arbre exécuté` imprimé pour chaque bras ; prise `carte.sh` 20:01:01-20:04:43 (l'ancien arbre a autotuné 2 min 30)
scellé (Sage, avant) : (a) ancien ≥ +1 % ET nouveau au bit → C4 prouvé ; (b) ancien = nouveau au bit → hygiène ; (c) nouveau ≠ 8,7980 → arrêt ; prédiction Manon : (b)
mesuré : nouveau **8,7980** / f1 8,1178 / f2 3,7002 ; ancien **8,7980** / 8,1178 / 3,7002 ; nouveau-bis **8,7980** / 8,1178 / 3,7002 ; lots vus {3: 511, 2: 96, 1: 3} dans les trois
verdict : **(b)** — l'ancienne liaison (état du fantôme exporté sous `store[-1]` puis rechargé) ne corrompt pas la dernière réelle sur carte avec le rembourrage servi (x=0, positions 0) : l'état écrit est celui d'un fantôme à zéro et la ligne réelle ne le relit pas, ou le relit identique — le test à sec (`test_temoin_ancienne_liaison_sur_sids_corrompt_40`) exhibe la corruption avec un rembourrage qui diffère, le service ne la produit pas ; fusion de fffd17c comme hygiène (la garde et le test restent), aucune cellule
suite : Jérôme : fusion fffd17c → main (hygiène), ETAT une ligne ; Océane : rien ; ma file : porte A8+W8r → statics → NARROW_GEMM → P2+éco (trou Océane 7 min entre deux)
