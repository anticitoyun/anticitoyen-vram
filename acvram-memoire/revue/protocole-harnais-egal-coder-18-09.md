# Protocole — Coder acvram b=1 et b=12 sous le harnais des concurrents (Sage, sage-profil-verdict-18-09 §4 ; REGLES §3 : une cellule du comparatif se mesure avec le harnais des concurrents, certifie = cellules moteur)

instrument : `scratchpad/banc-llamacpp-16-09.py` PARAMÉTRÉ, pas copié (diff : `BANC_URL` → serveur externe, rien lancé ; `BANC_MOTEUR=acvram` → `/v1/completions` avec la MÊME invite en ids, flux SSE, `usage.completion_tokens` final ; le chemin llama.cpp `/completion` est inchangé au caractère près) ; mêmes fenêtres ≥ 20 s, BANC_SLOTS=1,12, BANC_JETONS=1024, invite 256, repos 8 s, 7 passes courtes, energie.py ; `scratchpad/harnais-egal-18-09/`
contrôle du harnais AVANT la cellule : llama-server Coder Q4_K_M b=1, 20 s, script paramétré (chemin llamacpp) → 341,4 ± 3 % (331-352) ; hors bande = le script a changé, on s'arrête.
acvram : `acvram.cli serve Qwen3-Coder-30B-A3B-nvfp4 --port 8094 --max-batch 12 --max-model-len 2304` (= ctx 2304/slot de llama.cpp), régime classé, défauts du jour (rpw=4, xreg=down), HYBRID_SLOTS=12 ; b=1 puis b=12 dans la même session de serveur.
scellé (Sage) : b=1 275-295 t/s, b=12 1 150-1 200 t/s (bridé 400 W) ; b=1 < 270 ⇒ le chemin HTTP d'acvram serve coûte plus que celui des concurrents : poste moteur à ouvrir. Écarts connus et notés : llama.cpp force `ignore_eos` (pas d'équivalent HTTP côté acvram : les lots peuvent finir sur EOS avant 1 024 jetons — `n_jetons_decodes`/`lots` publiés) ; `/completion` natif contre `/v1/completions` OpenAI (mêmes ids). Dans le comparatif : nouveaux chiffres avec note datée ; 287,1 et 1 198 restent étiquetés « certifie, en processus » ; rien d'édité avant la mesure.
addendum 13h10 : (1) première passe = contrôle FAUX par charge machine (verdict-harnais-egal-coder-18-09), rejeu seulement si load1 < nproc/2 (garde en tête de chaîne, exit 3 sinon) ; (2) Océane f6a2680 (oceane-11, fusionné dans laure) : `ignore_eos` sur `/v1/completions` — le bras acvram le pose comme llama.cpp, b=12 n'est plus indécidable ; validité b=12 : `n_jetons_decodes = lots × 12 × 1 024` exactement, sinon indécidable ; `regime_ligne()` du serveur doit porter `ignore_eos=N` (N > 0) dans le verdict.
