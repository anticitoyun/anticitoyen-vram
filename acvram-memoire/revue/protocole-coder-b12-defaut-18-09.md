# Protocole — cellule officielle Coder b=12 au DÉFAUT (poste7, poste7-xreg-down-defaut-18-09) : aucune variable `ACVRAM_GROUPED_*` posée à la main ; la ligne [régime] doit montrer `GROUPED_XREG=down` et `GROUPED_RPW=4` d'eux-mêmes

instrument : `scratchpad/coder-defaut-18-09/chaine.sh` = `certifie-b12-15-09.py` ×2 (bridé 400 W : pas, t/s, J/jeton, `regime_ligne` + `regime.variables` dans le JSON) puis `profil-pas-coder-17-09.py … b12` ×2 (ms/pas nu) ; environnement = régime classé seul (`MOE_MMA=0 DECODE_MMA=0 DECODE_MMA_MIN_T=5 NARROW_GEMM=1 HYBRID_SLOTS=12`), rien de `GROUPED_*` ; la chaîne refuse de partir si une variable `ACVRAM_GROUPED_*` est présente dans son environnement ; preuve du défaut : `regime.variables["ACVRAM_GROUPED_XREG"] == "down"` et `["ACVRAM_GROUPED_RPW"] == "4"` dans les JSON ET absence des deux de `hors_defaut` (la ligne [régime] ne nomme que les écarts au défaut : c'est cette absence qui prouve « tous seuls »).
prérequis : arbre = main avec le défaut `down` de poste4 (regime.py + .cu) — au 35dff8d main est encore à RPW=1/XREG=0 ; attendre la fusion.
scellé (poste7) : pas ≤ 9,30 ms tenu (moyenne des deux nus), > 9,30 faux. Prédiction : 9,15-9,25 (ABAB : 9,165 / 9,193). Ce verdict devient la source de la cellule (nu / bridé / J).
