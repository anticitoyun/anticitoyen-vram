# Verdict — F3b (`int8_gemv_norme`, `ACVRAM_NORME_FUSEE=1`) : compile et passe sur sm_120 (`test_norme_fusee` vert, x_out au bit), capture ok, bit à bit b=1/b=12, **− 47 lancements** (tenu) mais **+ 0,31 ms par pas** (réfuté : − 0,10 à − 0,12 prédit) — `int8_gemv` passe de 0,747 à 1,166 ms, la norme est recalculée par chaque bloc ; b=8 : 7,5 → 12,0 ms/pas

instrument : première compilation du .cu sur la carte (hors verrou, 95 s, `int8_gemv_norme` présent) ; `tests/test_norme_fusee.py` sous `carte.sh` ; `capture-godets-17-09.py 1,2,8,16` ; `equiv-route-prep-17-09.py` 512 pas b=1 et b=12 (défaut contre défaut + F3b) ; `profil-pas-coder-17-09.py b1` — `scratchpad/f3-17-09/` ; base de comparaison : défaut 0.6.9 + F2 (`ROUTE_PREP=2` est défaut depuis poste7), F3a inactif (`rope_inplace` présent)
commit : arbre poste3 0ab28f3 (= main : F3b a0b472a, 6/6 CPU) ; régime classé + défauts ; `ACVRAM_NORME_FUSEE=1` opt-in
scellé (poste4) : − 48 lancements/pas, − 0,10 à − 0,12 ms ; test : x_out au bit, y ± 2⁻⁸ × Σ|h·w|, N = 1/2/8, eps × 10⁶ change y
mesuré : test cuda **vert** (toutes les paramétrisations ; x_out au bit) · capture godets 1 / 2 / 8 / 16 : ok, **3,47 / 5,63 / 12,0 / 12,07 ms/pas** (F1 : 3,20 / 4,96 / 7,53 / 11,98 — à b=8, dernier godet éligible, + 60 %) · bit à bit : b=1 0/514, b=12 0/6 168 (F3b inactif à b > 8) · profil b=1 : **652 lancements** (base F2 seul ≈ 699 : − 47 ; `rmsnorm` 97 → 50 lancements, 0,235 → 0,122 ms) mais **`int8_gemv_kernel` 0,747 → 1,166 ms** (+ 0,42), GPU **3,08 (F2 seul, estimé de d462c24) → 3,394 ms**, pas nu 3,462, 288,9 t/s pur (F2 + F3a : 327,6)
verdict : **F3b est exact et supprime bien un lancement par couche (− 47), mais il coûte 0,31 ms au lieu d'en rendre 0,10 : le prologue de norme est recalculé dans chaque bloc du GEMV (la ligne de K = 2 048 est normalisée en mémoire partagée par tous les blocs de la grille, au lieu d'une fois), et à b=8 le surcoût est ×1,6 sur le pas. Ne pas fusionner en défaut ; la forme qui tient est soit un seul bloc qui normalise et diffuse (grille à deux étages), soit la norme fusionnée en épilogue du GEMV précédent (o_proj / down_proj écrivent déjà la résiduelle), soit renoncer : le gain visé (0,12 ms, 4 % du pas) ne vaut pas un GEMV deux fois plus lent. Bilan F à ce point (b=1, pur) : F1 313 → F2 + F3a 327,6 t/s ; F3a bloqué sur `_rint` (sonde : r = p − floor(p) inexact sur la carte, `libdevice.rint` juste) ; F3b réfuté en vitesse ; le scellé b=1 ≥ 300 t/s en rondes n'est pas encore mesuré (257,8 avant F).**
