# Verdict — budget nsys du prefill Coder L=2 047 : défaut (classé, Marlin) **112,5 ms de noyaux** = experts Marlin **49,5 (44 %)** + projections q/k/v/o **31,3 (28 % : déquant int8 11,4 + GEMM bf16 19,9 — le classé n'est PAS éligible au cublas)** + flash 12,5 + glue MoE 8,0 + norm/élém 6,2 ; P2 (i8c, cublas) **96,8 ms** = experts **49,4 (51 %)** + projections **12,0** + flash 12,7 + glue 8,2 + norm/élém 9,1 — scellé C1 : **T_experts(C1) ≤ 0,55 × 49,5 = 27,2 ms**

instrument : `scratchpad/nsys-budget-19-09/prefill-nsys.py` (Engine direct, sans graphes, sans cache de préfixe, `max_model_len 4096`, 2 passes d'échauffement hors capture, 1 passe de 2 047 jetons entre `torch.cuda.profiler.start/stop`) sous `nsys profile -t cuda --capture-range=cudaProfilerApi`, `nsys stats cuda_gpu_kern_sum`, `classer.py` (regex par nom, table reclassée à la main depuis le CSV) ; `chaine.sh`, arbre `cce062b` (main `16528d1` + verdicts : P2 au défaut, `prefill_int8=cublas`), prise `carte.sh` 19:06:50-19:07:47, aucune variable `ACVRAM_*` (le bras P2 = converti `…-qkvo-i8c` au même défaut), carte 0 vide, charge 1,0 ; `NSYS_TMPDIR` hors `/tmp/nvidia` (à root depuis le ncu de 18:52). Temps mural sous nsys : 139,7 / 124,5 ms (14 650 / 16 444 j/s) — gonflé de ~25 % par le traçage, ne compte que la somme des noyaux ; sans nsys : 15 987 (128 ms) / 18 850 j/s (108,6 ms) publiés
scellé : aucun (budget publié, non scellé) ; ce budget devient le scellé de C1 ; prédiction Sage : experts 60-70 ms, projections 10-12 sous cublas, attention 8-12, glue 12-18, reste 5-8 ; Manon : experts 55-65, glue 10-15, projections défaut 8-12 / cublas 6-9, attention 6-10
mesuré (ms, × lancements) — **défaut** : `marlin_moe_wna16::Marlin` **49,5** ×144 (gate·up + down, 48 couches × 3) · `flash_fwd_kernel<128,128,64,4>` **12,5** ×48 · `int8_dequant_kernel` **11,4** ×192 + `cutlass tensorop_bf16 128x64` 11,2 ×144 + `64x256` 8,4 ×48 (= q/k/v/o déquantifiés puis GEMM bf16 : **31,3 ms**, `CHEMINS_INT8 {'dequant': 576}`) · rope 2,1 ×48 · rmsnorm 2,1 ×97 · `moe_act` 2,0 + radix sort 1,7 + `moe_reduce_trie` 1,6 + route 0,9 + histogramme 0,6 (glue MoE **8,0**, 1 298 lancements) · élémentaires 4,1 ; **P2 (i8c)** : Marlin **49,4** ×144 · flash 12,7 · `cutlass tensorop_i16832gemm_s8 128x128` 6,0 ×96 + `128x64` 1,1 (= `_int_mm`, `{'cublas': 576}`) + `_quant_a8` 1,0 + élémentaire fp32 3,8 ×384 (échelles) = projections **12,0** · glue 8,2 · norm/élém 9,1 (copies 2,1 ×432 en plus) · rope 2,2
verdict : **les experts Marlin = 49,5 ms = 44-51 % du prefill, en dessous des 60-70 prédits ; le second poste du défaut est la projection q/k/v/o à 31 ms (28 %) parce que le classé (int8 g128 affine, promus par plancher SNR) est inéligible au cublas de P2 (groupe = K, zéros 128) et passe par `int8_dequant` + GEMM bf16 — c'est exactement les 16 ms que P2 rend (112,5 → 96,8 : −14 %, cohérent avec 15 987 → 18 850 j/s = +18 % mural)** ; attention 14,7 (flash + rope, un peu au-dessus des 8-12), glue 8-10 (sous les 12-18), norm/élém 6-9. **Scellé C1 posé par ce budget : T_experts ≤ 0,55 × 49,5 = 27,2 ms sur le même instrument** (mêmes 144 lancements ou leur équivalent groupé) ; le reste du pas (47 ms sous P2) borne le gain total : même à 27 ms d'experts, le prefill P2 irait à ~75 ms de noyaux (≈ 24 000 j/s au mieux, pas 36 000)
suite : Océane (C1) : viser 27,2 ms d'experts, budget des 47 ms restants inchangé ; Sage : le classé au défaut paie 31 ms de déquant q/k/v/o → soit convertir i8c par défaut (P2 est au défaut mais ne s'applique qu'aux convertis `attn_int8: canal`), soit rendre le g128 éligible (C11 l'a fait pour le décodage étroit, pas pour M > 16) ; Jérôme : indexer ; ma file : nsys GLM (fait, verdict séparé) → C13-a (en cours) → C2 → C5 → C4 → porte A8+W8r → statics → NARROW_GEMM → P2+éco

## Rejouable
`ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/nsys-budget-19-09/chaine.sh` (57 s) ; CSV `defaut_cuda_gpu_kern_sum.csv`, `p2-cublas_cuda_gpu_kern_sum.csv`, journal `journal-coder.log`.
