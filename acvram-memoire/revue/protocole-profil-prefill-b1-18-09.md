# Protocole — poste7-reprise-ordre-18-09 §4 (reçu en clair de chef, note a0a8596 non poussée) : (a) profil prefill Coder 2048 aux défauts (B0) ; (b) événements hôte par pas sous rondes b=1

instrument (a) : `scratchpad/profil-pas-coder-17-09.py … prefill` (torch.profiler noyaux CUDA seuls, eager L=2048, 3 nues + 3 profilées, `regime_ligne()` en tête du JSON) ; défauts du jour, régime classé ; `scratchpad/profil-prefill-b1-18-09/profil-prefill.json`.
scellé (a) (poste7) : experts (GEMM groupée + nvfp4_dequant) ≥ 55 % du GPU ; « préparation par appel » (nvfp4_dequant + int8_dequant) ≥ 25 % ; hors-experts ≥ 60 ms ; ce qui gênerait : nvfp4_dequant < 30 ms. Décisions écrites avant : (i) nvfp4_dequant ≥ 40 ms → P1 (GEMM W4A16 classe Marlin, porte micro-banc à sec ≥ 110 TFLOPS, EN SUSPENS, sans carte) ; (ii) int8_dequant + colle MoE + elementwise ≥ 25 ms → P0 à sec (poste4 ≤ 1 j, `ACVRAM_PREFILL_INT8=a8`, porte PPL privé ≤ 1,020, scellé ≥ 11 000 j/s). Repère antérieur : 193 ms GPU (gemm_groupe 79,9 + nvfp4_dequant 50,8 + int8 11,0 + cutlass 19,2 + flash 11,5 + colle 8,2 + elementwise 6,6).
instrument (b) : `scratchpad/profil-hote-b1-18-09.py` — rondes b=1 identiques à certifie (invite 256, 1 788 jetons max, `smi` tous les 200 pas) : série mur par pas d'une ronde entière par tranche de contexte ; trois fenêtres torch.profiler (CPU+CUDA) à contexte 300 / 1 000 / 1 800 : GPU/pas, mur/pas, hôte = mur − GPU, CPU propre par poste (copie DtoH + synchro, lancement de graphe, ops d'échantillonnage, autre) ; cProfile d'une ronde (planificateur, échantillonnage/_emit, tolist, détokenisation) ; coût des appels `smi` et `add_request`. Pas de HTTP (rondes en processus, comme certifie).
scellé (b) (poste7) : ≥ 60 % des ~0,4 ms hors GPU dans un poste nommé (échantillonnage + copie) → chantier M (poste1) ; < 40 % partout → dispersé, b=1 reste derrière. Prédiction (mienne, écrite avant) : l'écart pas pur (3,05-3,14) / rondes (3,48) n'est PAS hôte pour l'essentiel — le pas pur est mesuré à contexte ~300 et les rondes moyennent 256 → 2 044 : l'attention paginée croît avec le contexte (GPU), et `smi` (subprocess ~30 ms / 200 pas ≈ 0,15 ms/pas) pèse dans les rondes ; hôte vrai par pas ≈ 0,15-0,25 ms, concentré dans échantillonnage + copie DtoH (`.tolist()`) — donc ≥ 60 % de l'hôte vrai, mais l'hôte vrai < 0,4 ms.
