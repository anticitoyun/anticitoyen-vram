# Verdict — palier 1 du GEMM dense NVFP4 Triton en situ (`ACVRAM_DENSE_NVFP4=triton`, a869a46, Qwen3.8 calibA, FLA) : **b=12 348,4 / 349,8 t/s, 1,145 J/j** (GEMV : 128,0 / 3,12 J → × 2,7) ; **b=1 69,9 t/s** (= GEMV 69,9) ; **ppl-decode-kv 4,0882** (= GEMV 4,0882) ; tests 13/13 dont le bras cassant → **scellé tenu (≥ 300, ≤ 1,3 J, ± 3 %, ± 0,0005) : défaut pour M ≥ 4 à porter, cellule aux menus** ; profil : `_dense_etroit_kernel` 17,6 ms/pas (56 %, ≈ 1,14 To/s effectifs), et la tête `lm_head` reste en GEMV fp32 (5,6 ms, 17 %) — prochain gras

instrument : `scratchpad/gemm-dense-17-09/situ.sh` (`pytest tests/test_gemm_dense_etroit.py` sur carte ; `certifie-b12` b=12 ×2, b=1 ×1 ; `ppl-decode-kv-17-09.py` 256+1024 ; `profil-gdn-17-09.py b12`) ; JSON et journaux dans le dossier ; arbre laure gelé sur a869a46 pendant la chaîne
commit : acvram/ = main a869a46 (Laurine : `kernels/gemm_dense_etroit.py`, bascule `2 ≤ M ≤ 32`, `ACVRAM_DENSE_NVFP4` dans `regime_ligne`) ; Sage demande la bascule à M ≥ 4 (à M = 2 la GEMV gagne au banc : 1,50 contre 1,12 To/s)
régime : classé, FLA (défaut), graphes on, `DENSE_NVFP4=triton` dans les JSON ; bridage puissance sur les trois rondes (398-400 W)
scellé (Sage) : b=12 128 → ≥ 300 (faux < 250) ; J/jeton ≤ 1,3 ; b=1 ± 3 % de 69,9 ; ppl-decode-kv ± 0,0005 de 4,0882 ; test d'équivalence + bras cassant verts dans le commit
mesuré : b=12 pas 34,45 / 34,31 ms → **348,4 / 349,8 t/s, 1,146 / 1,143 J/j, 399 W** ; b=1 pas 14,30 ms **69,9 t/s, 5,70 J** ; ppl-decode-kv **4,0882** ; tests **13 passed** (bras cassant `test_bras_cassant_echelle_de_bloc_decalee_d_un_rang` inclus) ; profil b=12 : 31,4 ms GPU / 32,5 nu (97 %), 3 768 lancements ; `_dense_etroit_kernel` **17,58 ms (56 %, 495 appels)**, `nvfp4_gemv_kernel<4,8,float,float>` 4,33 ms + `<4,4,float,float>` 1,31 ms (tête de sortie fp32, 1 appel chacun : **5,6 ms, 17,8 %**), `fused_recurrent_gated_delta_rule` 1,49 ms (4,8 %), reduce 1,23 + elementwise 1,22 ms (colle 14,6 %, 4,6 ms), rmsnorm 0,5, paged_attn 0,4
verdict : **tenu sur les quatre scellés — le palier Triton multiplie le b=12 par 2,7 sans toucher la sortie ni le b=1, et divise le joule par 2,7 ; cellule Qwen3.8 acvram b=12 : 349 t/s (bridé), 1,14 J.** Le profil dit où est la suite : (1) la projection de sortie (`lm_head`, 248 320 × 5 120, sortie fp32) n'entre pas dans la voie Triton (`sortie_fp32` / dtype) et reste une GEMV O(b) de 5,6 ms — 18 % du pas pour 1,3 Go : à couvrir dans le palier 2 ou par une garde `sortie_fp32` (gain attendu ≈ 4,5 ms → ~400 t/s) ; (2) le GEMM dense lui-même : 17,6 ms pour ≈ 20 Go = 1,14 To/s en situ (mieux que le minimum du banc 0,47 : les grandes formes dominent), plancher 11 ms ; (3) colle 4,6 ms (reduce des tranches K + elementwise, 990 lancements). vLLM à 621 t/s reste à × 1,8.
