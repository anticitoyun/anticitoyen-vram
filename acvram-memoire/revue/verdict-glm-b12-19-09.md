# Verdict — G1 GLM-4.7-Flash b=12 au défaut : **NON MESURABLE — DÉFAUT du moteur** : sous `GEMV_LAYOUT=marlin` (défaut P1) le décodage GLM appelle `nvfp4_gemv_grouped` sur la pile naturelle déjà rendue (`None`), capture des graphes impossible, serveur mort avant la première réponse

instrument : `scratchpad/glm-b12-19-09/chaine.sh`, arbre `1b3c0ac` (main `b336c15` + verdicts), aucune variable `ACVRAM_*` de régime, prise `carte.sh` 17:31:26-17:39:32 (coupée par moi à 8 min : serveur absent de `nvidia-smi`, banc en attente HTTP) ; `acvram serve GLM-4.7-Flash-srcbf16-nvfp4-k48-calibA --max-batch 12 --max-model-len 2304` puis harnais b=1/b=12 ; carte 0 : rien d'autre (llama-server 8081 sur la 3080 Ti)
scellé (avant) : b=12 t/s ≥ 858 tenu / < 858 faux ; J/jeton ≤ 0,397 tenu / > faux ; prefill défaut bf16 publié (réf. 5 502) ; prédiction Sage 900-1 100 t/s, J 0,30-0,38
mesuré : aucune cellule — chargement OK (`régime NOMINAL — graphes=on couches_exilées=0/47 experts_exilés=0/2944 piles_ok=True chemin_moe=mma experts_layout=marlin`, un avertissement OOM de l'allocateur à 300 Mo pendant le chargement), puis au premier pas de décodage : `graphes CUDA desactives, decodage en eager — capture impossible : TypeError: nvfp4_gemv_grouped(): incompatible function arguments … Invoked with: None, None, tensor([...échelles globales...]), tensor([54, 9, 44, 37]) (expert_ids), tensor([0,1,2,3]) (token_ids) …, 2048` — site : `acvram/engine/model.py:1070` (`MoEBlock._grouped`, branche `pile[0] == "nvfp4"`, `qw`/`bs` = `None`) ; le processus serveur a disparu ensuite (plus de PID sur la carte, banc muet) ; **Coder n'a pas ce défaut** (mêmes variables, 1 361 t/s à b=12, capture 4/4)
verdict : **NON MESURABLE, défaut nommé** — la disposition unique Marlin (P1, adoptée le 18/09) rend la pile NVFP4 naturelle après le repack (`regime.py:75`, « la pile NVFP4 est rendue après le repack »), mais sur GLM le décodage MoE emprunte encore `MoEBlock._grouped` (v1, `nvfp4_gemv_grouped`) et non le GEMV Marlin : 64 experts top-4 (vs 128 top-8 Coder), expert partagé, ou seuil `_MOE_GEMM_MAX`/`tri` différents — à trancher dans le code, pas ici. L'in situ P1 du 18/09 n'avait mesuré GLM qu'en prefill (5 502 j/s) : le décodage GLM sous P1 n'avait jamais tourné. Régime 6 : la cellule reste **vide**, pas remplie par un chiffre en eager ou sous un autre régime
suite : Océane (moteur, à sec) : sur GLM, forcer le chemin Marlin dans `_grouped`/l'aiguillage de `MoEBlock` quand `experts_layout=marlin`, ou garder la pile naturelle si un chemin en a encore besoin (le refus nommé de `_construire_marlin` ne couvre pas ce cas) ; test qui casse : `nvfp4_gemv_grouped` reçoit `None` sous `GEMV_LAYOUT=marlin` sur un jouet à 64 experts top-4 avec expert partagé ; puis 45 min de carte pour G1 (chaîne prête, rejouable) ; en attendant, la carte passe à P2 ligne 2 (93d3cfb) puis porte A8

## Addendum 18 h 22 — rejeu sur d3bf9d2 (arbre `f78dbcd`) : **FAUX aux deux régimes** — servi au défaut **155 t/s** (eager : plafond hybride 4 < 12), certifie 12 créneaux **711 t/s · 0,521 J** (< 858 / 0,397) ; b=1 **113 t/s** (8,4 ms/pas) ; prefill **5 739 j/s** ; et l'instrument de capture était aveugle sur MLA

instrument : même `chaine.sh` + capture godets {1,2,8,16} en tête et attente HTTP bornée, arbre `f78dbcd` (main `9f26291` + verdicts, d3bf9d2 inclus), prise `carte.sh` 18:11:58-18:22:10, aucune variable `ACVRAM_*` (`[régime] défaut ACVRAM_GEMV_LAYOUT=marlin`), charge 7,2 → 11,5 (nvcc des pairs, hors carte) ; harnais égal 2 passes (`acvram serve --max-batch 12 --max-model-len 2304`, b=1 puis b=12, 1 024 jetons/séquence, 20 s, J `energie.py`) ; `certifie-b12-15-09.py` b=12 (pose `ACVRAM_HYBRID_SLOTS=12` avant l'import) ; `prefill-abab-19-09.py` L=2048 ×7
scellé (inchangé) : b=12 t/s ≥ 858 ET J ≤ 0,397 → tenu ; prédiction Sage 900-1 100 / 0,30-0,38
mesuré : moteur `NOMINAL graphes=on 0/47 exilées 0/2944 experts piles_ok chemin_moe=mma experts_layout=marlin` (plus de crash : d3bf9d2 tient) ; **harnais égal** p1/p2 : b=1 **113,2 / 114,0 t/s · 2,036 / 2,041 brut · 1,371 / 1,370 net** ; b=12 **155,1 / 155,3 · 1,059 / 1,058 · 0,560 / 0,561** (12 288 jetons, 1 lot) — log serveur : `[graphe] repli eager — lot hybride 12 au-delà du plafond ACVRAM_HYBRID_SLOTS=4` (aussi 8, 9, 11 : tout lot > 4 en eager) ; **certifie b=12** (12 créneaux, graphes) : **16,87 ms/pas · 711,3 t/s · 0,5208 J/j · 370 W**, sm 2 932-2 940 (17/09, régime classé gemv : 568 / 0,700 → +25 % t/s, −26 % J) ; **prefill L=2048 : 5 739 j/s** (σ 133, 356,9 ms/passe, 0,0704 brut / 0,0570 net J/jeton ; réf. P1 5 502 : +4 %) ; **capture** : 8,39 / 43,2 / 61,2 / 87,9 ms/pas à b=1/2/8/16, rapportée « 4/4 ok » — faux : `[graphe] repli eager — lot hybride 2|8|16 au-delà du plafond ACVRAM_HYBRID_SLOTS=1` (seul b=1 était capturé)
verdict : **FAUX** — (a) le régime SERVI au défaut donnait 155 t/s à b=12 parce que le plafond des créneaux hybrides (4, `graphs.py:159`, MLA compté comme hybride) est en dessous de `--max-batch 12` : tout lot > 4 retombe en eager, silencieusement pour l'usager du serveur ; le 568 du 17/09 était un régime d'instrument (certifie pose 12). Océane 24b6565 : plafond = `--max-batch` → **G1-bis à rejouer, attendu ≈ 711 au harnais égal**. (b) Même capturé, GLM b=12 = 711 < 858 et 0,52 > 0,397 : la cellule reste **derrière vLLM Marlin** ; le poste est le pas lui-même : **8,4 ms à b=1** (113 t/s ; vLLM 183,5) contre 3,3 ms pour Coder à paramètres actifs comparables — 47 couches MLA, expert partagé, 64 experts top-4 : à budgéter par noyau (nsys décodage b=1, 5 min), pas à deviner. (c) Instrument : `capture-godets-17-09.py` posait `ACVRAM_HYBRID_SLOTS` par godet APRÈS l'import de `graphs.py`, qui le lit une fois → plafond 1 → godets 2-16 en eager rapportés « ok » (le contrôle lisait le drapeau du moteur, pas le chemin du pas) : corrigé (posé avant l'import au plus grand godet ; `_raisons_eager_vues` non vide rend « ok » faux) ; les captures Coder de la journée ne sont pas touchées (pas de couche hybride : jamais ce plafond)
suite : G1-bis sur main `7aebbfa` (chaîne `scratchpad/glm-b12-bis-19-09/`, harnais b=12 ×2 + capture corrigée {1,2,8,12,16}) dès que Jérôme rend la carte ; Sage : cellule GLM b=12 à publier avec son régime (« servi défaut ≥ 7aebbfa » une fois G1-bis tenu à ≈ 711) ; Océane : poste du pas GLM b=1 (8,4 ms) — je propose un nsys décodage b=1/b=12 GLM (5 min) après le nsys prefill ; Jérôme : `[graphe] repli eager` d'un serveur = ligne du comparatif invalide, à grep dans tout log de cellule

## Addendum 18 h 26 — G1-bis sur main `7aebbfa` (Océane 24b6565 : plafond hybride = `--max-batch`) : servi défaut b=12 **725 t/s · 0,491 brut · 0,389 J net** — t/s **FAUX** (< 858), J **TENU** (≤ 0,397) ; 0 repli eager ; capture vraie 5/5

instrument : `scratchpad/glm-b12-bis-19-09/chaine.sh`, arbre `ace1199` (= main `7aebbfa` + verdicts), prise `carte.sh` 18:23:20-18:26:34, aucune variable `ACVRAM_*`, moteur `graphes=on(hybrides≤12)` ; capture godets {1,2,8,12,16} avec l'instrument corrigé (variable posée avant l'import, `_raisons_eager_vues` vide exigé — équivalent de la version 03769a75 d'Océane, adoptée pour la suite) ; harnais égal b=12 ×2 (mêmes réglages que G1) ; charge 12 → 10 (nvcc des pairs, hors carte)
scellé (inchangé) : b=12 t/s ≥ 858 ET J ≤ 0,397 ; attendu ≈ 711 (certifie de 18 h 21)
mesuré : capture **5/5 vraie** (`replis` = [] partout) : **8,47 / 9,62 / 12,80 / 14,81 / 14,99 ms/pas** à b=1/2/8/12/16 ; harnais p1/p2 : **719,6 / 730,6 t/s · 0,4967 / 0,4909 brut · 0,3922 / 0,3850 net** (24 576 jetons, 2 lots chacun), `repli eager au log : 0` ; moyenne **725,1 t/s · 0,494 brut · 0,389 net**
verdict : **t/s FAUX, J TENU** — la cellule servie GLM b=12 au défaut ≥ 7aebbfa est **725 t/s · 0,389 J net** (vLLM Marlin 858 / 0,397) : −15 % en vitesse, −2 % en joules ; la prédiction « ≈ 711 » tient (+2 %) ; le 155 de 18 h 12 était bien le plafond hybride, pas le moteur. Le poste restant est le pas lui-même (8,5 ms à b=1, 14,8 ms à b=12 pour 12 séquences : le pas n'est que ×1,75 à ×12 — la lecture des experts domine à b=1 et se partage mal jusqu'à 12) : budget nsys décodage b=1/b=12 en cours
suite : Jérôme — comparatif : « GLM-4.7-Flash acvram servi défaut (≥ 7aebbfa) b=12 : 725 t/s · 0,494 brut · 0,389 net ; b=1 : 113 t/s · 2,04 brut · 1,37 net (G1) ; prefill 5 739 j/s » ; Sage : cellule publiée telle quelle, régime dans le nom ; ma file : nsys décodage GLM (lancé 18:27) → MTP-exact → nsys prefill Coder → nsys prefill GLM + C13-a → C2/C5/C4 → porte A8+W8r → statics → NARROW_GEMM → P2+éco
