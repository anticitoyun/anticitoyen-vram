# Verdict — CAUSE TROUVÉE : le chemin à formes fixes rend des logits **bf16** (`model.py:2146` et `:2153`, `self.lm_head(h)` en bf16) alors que l'eager les calcule en **fp32** (`model.py:2097-2099`, correctif `ACVRAM_LOGITS_BF16`) ; à 133 de magnitude la résolution bf16 est 1,0 et GLM départage ses jetons à 0,3 près

- **instrument** : `sonde-ligne128-16-09.py` — un processus, deux moteurs sur `loaded` (graphes / eager), même séquence (prompt 128), 3 émissions ; comparaison ligne à ligne des 47 caches latents (`static_export` contre `gdn_states`) après les pas 1 et 2, et des logits émis (top-3, valeurs) ; sorties dans le journal `ligne128d-poste3`
- **commit** : poste4 **277393a** (poste3-qa) ; régime W4A16, `-k48`, b=1, `HYBRID_SLOTS=1`
- **régime** : graphes actifs côté fixe ; le même défaut est reproduit par `ACVRAM_GRAPHS_EAGER=1` et par sync+graphes (bras précédents), pas par l'eager
- **scellé** (hook de poste4) : (a) formes/len → static_load ; (b) lignes 0-127 → copie du prefill ; (c) ligne 128 → écriture du pas 1 ; (d) tout égal → lecture du pas 2
- **mesuré** : **(d) et au-delà** : après le pas 1 puis le pas 2, **les 47 caches latents sont identiques au bit** (129 puis 130 lignes, max |Δ| = 0, `len` 129 → 130, palier 256) ; jetons émis fixe [553, 83, 50] / eager [553, 83, 1237] ; logits : emit 0 identiques ; **emit 1 : fixe top-3 = [134,0 · 133,0 · 133,0], eager = [133,62 · 133,38 · 132,99]** ; emit 2 : fixe [133,0 · 133,0 · 133,0] (trois ex-æquo → argmax = plus petit indice) contre eager [132,96 · 132,95 · 132,89] ; cos 0,99996, max |Δ| 0,5 = un demi-pas de bf16
- **verdict** : ni MLA, ni créneaux, ni capture, ni pipeline : **la tête de sortie du chemin fixe est en bf16**. Le modèle calcule juste (états identiques à chaque couche), le jeton choisi est faux parce que les logits sont arrondis à l'unité. GLM (logits ≈ 130, marges 0,1-0,4) est massacré ; Coder (logits plus petits) l'est peu (25/28) ; la PPL teacher-forcée n'y voit que ±0,5 sur le logit cible (+0,3 %) ; les ties sur invites synthétiques (marges ≈ 0) basculaient au moindre bruit

## 1. Fichier:ligne
- `acvram/engine/model.py:2097-2099` (`forward`, eager) : `logits = self.lm_head(x.to(target, dtype=torch.float32))` — correctif « logits fp32 » avec repli `ACVRAM_LOGITS_BF16=1`, et un avertissement si le noyau rend autre chose que fp32.
- `acvram/engine/model.py:2146` (`forward_fixed`, branche avec `add_norm`) et **`:2153`** : `return self._logits_finaux(self.lm_head(h))` / `(self.lm_head(x))` — **`h`/`x` en bf16, le correctif n'y a jamais été porté**. Correctif d'une ligne chacune : `self.lm_head(h.to(torch.float32))` (même chemin que 2098), puis rejouer l'arbitre (graphes, GRAPHS_EAGER, eager doivent coïncider) sur GLM et Coder.
- Conséquences à réévaluer après correctif : tous les chiffres de **texte** produits sous graphes (duel GLM b=1/4/12, ties du 15/09 sur Coder) ; les **temps** sous graphes restent des temps (le lm_head fp32 coûte ~0,1 ms de plus par pas, à mesurer) ; la PPL de prefill n'est pas concernée.

## 2. Ce que la journée enseigne (pour REGLES)
Une différence de **précision de sortie** entre deux chemins se voit sur l'argmax et pas sur la PPL ; l'arbitre prefill contre décodage la révèle, les états intermédiaires l'innocentent, et les **valeurs** des logits (pas seulement leur écart) la nomment : 134,0 / 133,0 / 133,0 est une signature bf16.
