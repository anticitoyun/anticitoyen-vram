# Protocole — porte qualité W4A4 du prefill MoE (poste7, poste7-lecture-profils-coder-17-09) : fausse quantification NVFP4 des activations en torch, zéro noyau

instrument : `ACVRAM_PREFILL_A4=off|gateup|both` (model.py `fausse_quant_nvfp4` : échelle globale par ligne amax/(448×6), échelle de bloc 16 en UE4M3, E2M1 au plus proche, déquantifié ; appliqué à l'entrée de gate/up, et de down pour `both`, dans `_forward_prefill_grouped` avant la GEMM B0 `groupe` ; variable dans `regime_ligne` ; erreur relative 9,5 % sur du gaussien) ; `ppl-acvram-17-09.py`, régime classé ; Coder : `corpus-prive/tranches-coder` × bf16 `ppl-refonte-17-09/coder-prive-tr*-bf16` (géo 11,735) ; GLM `GLM-4.7-Flash-srcbf16-nvfp4-k48-calibA` : `corpus-encode-brut-glm/prive-tr*` avec `[gMASK]<sop>` × bf16 `ppl-refonte-17-09/glm-prive-tr*-bf16` (bras A du 17/09 : 1,0150) ; chaîne `scratchpad/porte-a4-17-09/chaine.sh`.
bras : Coder off / gateup / both ; GLM off / gateup. Bras qui doit différer : gateup ≠ off (sinon la fausse quant n'est pas sur le chemin : vérifier `regime_ligne` et le compteur `fpg`).
scellé (poste7) : gateup ≤ 1,020 → ouvre le noyau W4A4 experts (MMA mxf4nvf4) ; both ≤ 1,020 → ouvre les deux GEMM ; gateup > 1,025 → ferme W4A4 définitivement (préfill reste B0). Prédiction poste7 : gateup 1,017-1,022, both 1,022-1,030. Repère : off attendu ≈ acvram classé (Coder 1,027 sur ce corpus le 16/09 ; F1+F2 depuis).
