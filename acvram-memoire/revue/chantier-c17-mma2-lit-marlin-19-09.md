# Chantier C17 — mma2 lit la disposition Marlin (fiche, aucune ligne de code ; `sage-c1-route-i-c17-19-09` § 2 : dégelé et scellé après Mesure 1 ; rang : après C14/C15 et le ncu de C1)

## Objet fixé par Mesure 1 (`verdict-mesure1-mma2-marlin-19-09`)
À b=12, mma2 sur la pile naturelle fait le MoE en **×0,88 (45 distincts) / ×0,75 (27)** du temps de Marlin à octets égaux, au même plafond de 400 W (donc −12/−25 % de J), et perd à b ≤ 2 (×1,23 / ×1,40). La disposition unique reste Marlin (le prefill en dépend : C1 route (i)). C17 = **le même noyau mma2, lisant les tuiles Marlin au lieu de la pile naturelle**, choisi au décodage quand b ≥ ~4 (le croisement est entre u=16 et 27), Marlin GEMV en dessous.

## Scellé (Sage § 2, recopié avant toute ligne)
(1) sortie **au bit** égale à mma2 sur pile naturelle (même arithmétique, seul l'ordre de lecture change : tout écart est un bogue) ; (2) t par couche à u=45 dans le harnais de Mesure 1 ≤ **0,92 × Marlin** (Mesure 1 dit 0,88 : la permutation à la lecture coûte ≤ 4 %) ; (3) `-Xptxas -v` : registres ≤ mma2 + 8 (`<128,4,64>` = 118, `<16,4,64>` = 55), 0 déversement ; (4) capture {1, 2, 8, 12, 16} 5/5 ; (5) `certifie` b=12 ABAB : pas ≤ défaut − 0,35 ms. Réfutation qui gênerait Sage : (2) > 0,92 — la permutation casse la coalescence des `cp.async`, la vraie forme est un repack à la volée par tuile.

## Ce que la lecture à sec établit — deux faits qui touchent le scellé
* **(1) « au bit contre mma2 naturel » n'est pas tenable tel quel : la disposition Marlin ne porte pas les échelles de la pile naturelle.** `marlin_port.traiter_echelles_nvfp4` (`__init__.py:139`) ré-encode l'échelle UE4M3 de bloc en un octet « S0E5M3 » = moitié haute de `half(s × facteur × 2⁷) << 1`, et **annule toute échelle avec s·facteur·2⁷ < 2** (les sous-normaux E4M3, s < 2⁻⁶/facteur) ; `depaqueter_marlin` (`:264`, branche C1) le dit : « seule exception, les échelles que le repack a annulées restent nulles ». Compté à sec sur 72 tenseurs d'experts réels de Coder (couches 0/23/47 × 8 × gate/up/down) : **450 échelles annulées sur 7 077 888 = 0,0064 %** (pire tenseur 0,24 %, facteur médian 1,0). Ce sont des blocs de 16 poids dont l'échelle est < 2⁻⁶ × l'échelle globale (≤ 1/64 du bloc maximal) : leur contribution est ≤ 2⁻⁶ relative par bloc — c'est ce que la GEMV Marlin (P1) calcule déjà aujourd'hui, et c'est pourquoi P1 n'était « pas au bit à v1 » (`sage-p1-situ-verdict` § 4). **Scellé (1) réécrit, à faire trancher par Sage** : au bit contre mma2 naturel **sur tous les blocs dont l'échelle n'est pas annulée par le repack** (99,994 %), et, sur l'ensemble, égal à la **GEMV Marlin** à l'ordre de sommation près (≤ 2 ulp fp32 par ligne : le juge de `test_gemv_marlin`, `TOL_HORS`) — un écart ailleurs est un bogue. Sinon : la disposition Marlin devrait porter les échelles UE4M3 brutes (un second tableau d'échelles, +3 % d'octets, repack modifié) et le « au bit » redevient possible — c'est une option, pas la voie par défaut.
* **La permutation des quartets est une table constante par tuile**, pas un repack : `repack_torch` (`__init__.py:248`, branche C1) = tuiles 16 k × 64 n de 128 mots (512 o), `idx` (`_indices`) réordonne les 1 024 quartets d'une tuile en [32 voies × 4 mots × 4 octets × 2 moitiés] — une voie t porte les colonnes {w·16 + t/4, +8} (w = 0..3) aux k {2·(t%4) + 0, 1, 8, 9}. Le fragment B de `mma_mxf4nvf4` (`acvram_kernels.cu:3371`) veut, par fil (g, tq), 8 k consécutifs d'une colonne dans un mot (`lo` k = 8·tq.., `hi` = +32) : dans la tuile Marlin ces 8 k viennent de **4 mots de 4 voies** (les paires {0,1},{2,3},{4,5},{6,7} sont chez t%4 = 0,1,2,3, même position de colonne). Lecture globale → shared : **inchangée et coalescée** (`cp_async16` de 512 o par tuile, exactement les octets de la tuile, `:3314`) — la coalescence ne casse pas ; ce qui change est l'adressage shared → registres : 4 LDS.32 + 4 extractions (`prmt`/décalage) + 3 assemblages par mot de fragment au lieu de 1 LDS.32, soit **+40 instructions par MMA k64** ; les échelles : 1 LDS.8 + conversion S0E5M3 → UE4M3 (LUT 256 par tenseur, ou `(octet >> 1) − biais(facteur)` puisque facteur est une puissance de 2 : 2-3 instructions) par bloc de 16.
* **Chiffrage contre (2) ≤ 0,92 × Marlin** (Mesure 1 : mma2 = 0,877 × Marlin à u=45, gate+up 64,0 µs, down 28,3) : mma2 est borné DRAM à 1,24-1,41 To/s avec 0,15 inst/octet (9 % des créneaux d'émission, `verdict-ncu-m1-m2`) ; +40 instr/MMA porte à ≈ 0,40-0,45 inst/octet (25-30 % des créneaux) : **t inchangé si l'émission reste sous la bande** — prédiction t(C17)/t(Marlin) = **0,88-0,92 à u=45** (tenu de justesse), **0,75-0,80 à u=27**, et **W monte** (le modèle du 14/09 fait payer l'instruction : +10 à +25 W sur les 400 déjà atteints… au plafond, W ne monte plus, c'est l'horloge qui baisse : 2 730 → 2 500-2 600 MHz, ce qui est le mécanisme de la réfutation de Sage, (2) > 0,92). Issue qui me gênerait : (2) entre 0,92 et 1,0 par l'horloge — alors C17 ne vaut que sous éco 2 700 (où l'horloge est déjà tenue) et sa cellule se mesure à l'horloge du défaut servi (2 700 depuis le 19/09 20 h 22 : c'est le régime de Mesure 2).
* **(3) registres** : +4 mots temporaires + 1 pour la LUT d'échelle → 118 → ≈ 123-126 (≤ 126 = 118 + 8 tenu de justesse à BT=128 ; à BT=16 55 → 60, large) ; 0 déversement attendu ; ptxas tranche avant toute carte (`cuobjdump --dump-resource-usage`, section sm_120f, comme `chantier-c1-routes` § 4).
* **Sélection** : `MoEBlock._forward_grouped` (model.py) choisit déjà GEMV Marlin sous P1 ; C17 ajoute la branche « t ≥ seuil (défaut 4 lignes ? — à mesurer : le croisement u=16/27 vaut b ≈ 2-4) → mma2 sur tuiles Marlin », variable `ACVRAM_MOE_DECODE_MMA=seuil` dans `regime.VARIABLES` (0 = jamais, témoin), ligne de régime.

## Ordre de marche proposé (après C14/C15 et le ncu C1, Sage tranche le scellé (1))
1. Jumeau torch (`gemv_marlin_torch` existe : `marlin_port`, branche C10) : mma2-naturel contre mma2-sur-Marlin simulé — les 0,0064 % d'échelles annulées identifiées bloc par bloc, égalité stricte ailleurs (test à sec cassant : un quartet permuté à la main casse). 2. Noyau : `nvfp4_gemm_grouped_mma2_kernel<BT,S,KS,MARLIN=true>` — même corps, `src` de `cp_async16` sur la tuile Marlin (`table_qw` = pile Marlin, foulée 512 o par 16 k × 64 n), fragment B par 4 LDS.32 + `prmt`, `sfb` par conversion d'octet ; ptxas. 3. Carte (Manon, 10 min) : harnais Mesure 1 u=45/27 (t, W, horloge, rapport cyclique) + capture 5/5 + `certifie` ABAB b=12 sous éco 2 700.

## Code livré et carte (19/09 22:12, oceane-11 6c4aa24d + ce commit) — noyau **tenu au bit**, route opt-in, registres : (3) faux à BT=128 (164), tenu à BT=16 (64)
* Noyau : `nvfp4_gemm_grouped_mma2_kernel<BT,S,KS,MARLIN>` (`acvram_kernels.cu`), tuiles Marlin 512 o en shared (foulée 528, mêmes `cp.async` coalescés), fragment B depuis 4 mots de 4 voies (ordre tourné par g), échelles S0E5M3 → UE4M3 en fil (`gm2_ue4m3_depuis_marlin`, `decal = 15 + log2 facteur`, sous-normales survivantes reconstruites) ; `nvfp4_gemm_grouped_mma(…, marlin=decal)`. Jumeau torch de l'index (`marlin_port/disposition.py`) prouvé à sec (`tests/test_c17_disposition_marlin.py`, 5 : quartets 100 %, échelles hors annulées, témoins cassants).
* **Carte** (`tests/test_c17_mma2_marlin_carte.py`, 3 passed, 22:12) : (1) noyau Marlin contre noyau naturel, mêmes xq/xsf/tuiles, E=8/N=256/K=512 et E=16/N=768/K=2048 : **au bit sur toutes les lignes dont aucune échelle n'est annulée**, ≤ 2 ulp bf16 sur les colonnes touchées, decal faux → sortie différente (cassant) ; (2) bloc MoE jouet sous la disposition unique, `ACVRAM_MOE_DECODE_MMA_MARLIN=1` : `_forward_grouped_mma` prend `decode_mma_marlin` et rend la sortie du décodage MMA sur pile naturelle (> 99 % au bit, ≤ 2 ulp), à 0 rend None (la GEMV Marlin sert). Le premier passage FAUX (23 868 écarts) était **mon test** : `preparer_pile` lit `bs` en E4M3, je lui passais des octets (×56 = la valeur de l'octet 0x38) — corrigé, dit ici.
* Registres (cuobjdump, sm_120f) : `<16,4,128>` 50 → **64** ; `<128,4,64>` 118 → **164** (> 118 + 8 = 126 : **scellé (3) faux pour BT=128**, la tuile du préfill — sans objet au décodage qui tourne à BT=16 (`_MOE_DECODE_MMA_BT`), où (3) est tenu ; à trancher : (3) porte sur la tuile servie) ; 0 déversement, 0 local.
* Route : `_forward_grouped_mma` (le décodage MoE par MMA, `MOE_DECODE_MMA=1` depuis le 15/09, `MIN_T=5`) lisait `pg[1] is None → None` sous P1 ; avec `ACVRAM_MOE_DECODE_MMA_MARLIN=1` il continue sur `_gemm_mma_marlin` (tables w_marlin/s_marlin, `gs` naturel à l'épilogue, `decal` par pile) ; AWQ/Hadamard (GLM) → None (GEMV Marlin), non couvert par C17.
* Reste (scellé § 1 de `sage-c17-scelle-mesure1-ter`) : (2) t/couche ≤ 0,92 × Marlin à u=45 sous 2 700 dans le harnais Mesure 1 (mode `mesure1` à étendre : bras `mma2-marlin`) ; `ppl-decode-kv` ± 0,002 ; capture {1, 2, 8, 12, 16} 5/5 ; `certifie` b=12 ABAB ≤ défaut − 0,35 ms — Manon, ~25 min, sur main après fusion (l'extension recompile 9 min : `.cu` modifié).

## Verdict et fermeture (`verdict-c17-19-09` Manon e5907ea ; `sage-c17-faux-ferme-20-09`) — **C17 FERMÉ**, `ACVRAM_MOE_DECODE_MMA_MARLIN` reste 0
* Fenêtre scellée (Manon, 22:22-22:33, éco 2 700 posé par le moteur, hôte propre) : **(a)** t(C17)/t(Marlin) sous 2 700 : u=16 **0,909**, u=24 **0,917**, u=27 **0,897**, **u=45 1,234** (Marlin 111,5 µs/couche à 2 167 MHz contre C17 137,6 à 2 692) — ma prédiction 0,88-0,92 à u=45 **réfutée** : à 45 tuiles (le lot réel b=12), la lecture des tuiles Marlin (4 LDS + extractions par fragment, +40 instr/MMA) coûte plus que ce que la disposition naturelle donnait ; la carte tombe à 300-325 W : elle attend (latence, pas bande) ; **(c)** capture 5/5 (seule ligne tenue) ; **(d)** `certifie` b=12 **10,89 ms contre 8,73 (+2,15 ms, +25 %)**, 1 102 t/s contre 1 374, −4 % de J ; **(b)** `ppl-decode-kv` lot 12 : notée **+2,2 %** (12,6227 → 12,9010), 11 témoins +0,7 % en moyenne, 9/11 dégradés — au-delà de l'arithmétique du lot (± 0,2-0,3 %).
* **(b), documenté comme défaut non expliqué du chemin opt-in** (chiffres ci-dessus ; arbre main `eb66b3e`+C17 ; chaîne `scratchpad/c17-19-09/chaine.sh` sur la branche de Manon, `ppl-decode-kv-3seq` avec `PPL_REMPLISSAGES=11`, tranche 1 Coder 8 192 + 512, NLL par jeton dans le .json). Ce que les tests carte ont prouvé : le noyau MARLIN = le noyau naturel au bit hors échelles annulées (0,0064 %, ≤ 2⁻⁶ relatif par bloc : pas +2 % de PPL) sur E=8/16 et sur le bloc jouet. **Hypothèse à tester, non vérifiée** : le chemin `_forward_grouped_mma` quantifie les activations en **E2M1 (W4A4 au décodage, `nvfp4_quant_act`)** là où la GEMV Marlin servie lit x en bf16 (W4A16) ; l'A4 coûtait +0,010 (+1 %) au préfill (`verdict-w4a4-a-sec`) ; la « PPL 0,9995 » du décodage MMA du 15/09 a été mesurée en teacher forcing à b=1 — où `MOE_DECODE_MMA` n'est pas pris (`MIN_T=5` : REGLES § 4 bis, la leçon de Manon sur le niveau 2) : **le coût A4 du décodage MMA n'a jamais été mesuré au lot où le chemin est pris**. Test qui tranche (10 min carte, hors ordre) : arbre sans P1 (`ACVRAM_GEMV_LAYOUT=naturel`), `ppl-decode-kv` lot 12 sous `MOE_DECODE_MMA=1` contre `=0` : +0,7 % → c'est l'A4, pas C17 (et le décodage MMA naturel du 15/09 portait le même coût caché) ; ≈ 0 → le défaut est dans le chemin Marlin (decal, échelles) et se cherche sur les 12 lignes réelles.
* Fermé (Sage) : comme candidat au défaut et comme chantier — à u=45 il n'a pas d'objet, son objet à u ≤ 27 (−8 à −10 %) ne paie pas une recherche de défaut. Ce qui reste utile : le noyau `mma2<MARLIN>` (au bit, 64 regs à BT=16) pour un préfill sur disposition Marlin si un jour la disposition unique bascule ; le poste `mma2-MARLIN` du harnais Mesure 1 ; MECANISMES (Sage) : *un noyau prouvé au bit sur petites formes se juge à l'unité du pas réel (45 experts distincts, 96 lignes) avant tout défaut — C17 passait 3 tests carte et perdait 23 % au pas.*
