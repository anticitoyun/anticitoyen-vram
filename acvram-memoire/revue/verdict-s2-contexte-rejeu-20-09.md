# Verdict — § 3b S2-contexte rejoué sur la chauffe de contexte (main 645f87df, `ContexteNonTenu` au chargement + garde budget KV) : **18/19 sans 500 silencieux** (9 TENU, 8 refus nommés par la chauffe, 1 refus nommé par le budget KV avant même la chauffe) — **1 divergence réelle : GLM k48 (ctx défaut 32768) chauffe TENUE (32768/32768) mais 500 CUDA OOM à la requête** (318 Mio demandés, 298 libres) ; `ctx+64` → 400 nommé 19/19, zéro troncature ; **une colonne à corriger : `gemma-4-31B-it-nvfp4-vision` porte `ctx=262144`**, refusée dès le budget KV (123 840 Mio manquants) — jamais tenable sur cette carte, la colonne ment avant même la chauffe

instrument : `scratchpad/s2-contexte-20-09/chaine.sh` réécrit deux fois depuis le verdict d'hier (19:07) : (1) arbre pinné par `ACVRAM_ARBRE`/`PYTHONPATH` **ET `cd "$L"`** (le premier rejeu, deux fois, tournait sans le `cd` : `python -m` résout le paquet du **cwd** avant `PYTHONPATH`, donc sur le worktree `poste2` = ancien code sans chauffe — jeté avant publication, mémoire écrite `pythonpath-sans-cd-cwd-shadow`) ; (2) lecture du refus au CHARGEMENT (`ContexteNonTenu`, rc 2, message « max_model_len=N demandé, T jetons tenus » — mon premier regex avec `é` cassait sur l'encodage UTF-8, corrigé ; JSON du refus écrit par un script Python dédié, `ecrire-verdict-mort.py`, plus de concaténation bash fragile) ; trois prises (16 texte + 3 vision), en-tête ≤ charge 0,66 (aucun pair)
scellé (poste7, avant) : ctx−64 acceptée, un jeton décodé ; ctx+64 → 400 nommé ; **0 × 500 sur 19** ; toute troncature silencieuse = défaut bloquant
mesuré : **TENU (9)** Coder nvfp4, DeepSeek, gemma-4-26B, gemma-4-12B-vision, Jan-v2-VL, LFM2.5, Nemotron, Qwen2.5-Coder-14B, Qwen3-VL-2B-vision — tous 200/32704/1/length puis 400 nommé ; **DÉFAUT-CHAUFFE nommé (8)** Coder i8c (32768→15360), GLM calibA/base/Grande (32768→31744), Kimi-Linear (→15360), Ornith (→15360), Qwen3.5-35B (→31744), Qwen3.8-calibA (→31744) — refusés au chargement, jamais servis, aucun 500 ; **REFUS BUDGET KV nommé (1)** gemma-4-31B-vision : `ctx=262144` (colonne), « budget KV insuffisant … 123 840 Mio manquants », refus avant même la chauffe ; **DÉFAUT réel (1)** GLM k48 : chauffe dit 32768/32768 tenus, la requête réelle OOM quand même (500, 318/298 Mio) — la chauffe (séquence `[1]*(L-2)`, tokens répétés) ne subit pas les mêmes effets de spéculation n-gramme/fragmentation qu'une requête à tokens aléatoires
verdict : la porte de contexte fonctionne à deux niveaux (budget KV à la planification, chauffe au chargement) et couvre 18/19 cas — un progrès net sur les 8/16 d'hier ; **le scellé « 0 × 500 » n'est pas tenu au sens strict** (GLM k48) : la chauffe utilise une séquence homogène qui ne reproduit pas exactement le pire cas d'une requête réelle, à resserrer (séquence de chauffe non répétitive, ou marge de sécurité sur `tenu`) ; la colonne `ctx=262144` du 31B vision est une erreur de saisie/config à corriger (§ 3a, poste9) avant tout futur test — indépendante du moteur
durée : prise 1 21:46:22-22:03:56 (17,6 min) + prise 2 05:21:12-05:28:24 (7,2 min) + prise 3 05:46:41-05:48:07 (1,4 min) = 26,2 min de carte ; HEAD `poste2-qvl` 645f87df
suite : poste7 : trancher si GLM k48 (1/19) bloque le feu vert 0.6.34 ou si la chauffe reste « au mieux, pas garantie » ; poste1 : resserrer `chauffer_contexte` (séquence non répétitive ou marge) ; poste9 : corriger `ctx=262144` de gemma-4-31B-it-nvfp4-vision dans le TSV ; ma file : trou 10 min à poste9 [4ee9ab] + chef (heure lue), puis § 1b, feu vert 0.6.34, n=60, --decoder parc, P3 (3)(4)

## Rejouable
`PRISE=1|2|3 ACVRAM_ARBRE=<arbre pinné> poste2=<poste2> PYA=<venv>/bin/python ACVRAM_MODELES=/mnt/AI_GENERATOR/models_acvram CPUS=0-15 bash <poste2>/scratchpad/s2-contexte-20-09/chaine.sh` (≤ 18 min par prise selon les défauts-chauffe, plus lents à dichotomier).
