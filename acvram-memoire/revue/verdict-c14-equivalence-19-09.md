# Verdict — C14 (grille de `mla_1p_kernel` par lot, demi-tuile à b ≤ 2 ; branche `oceane-c14-mla-1p-grille` b765f1b2, compilée à sec) : **noyau tenu sur mêmes entrées (1,9-8,3 ulp fp32 de l'amplitude, ≪ ± 1 ulp bf16)** ; « jetons identiques 256 pas » **faux** (divergence au pas 5) par amplification chaotique, pas par le noyau ; l'écart 1p/référence à L=2048 (10,8-24,7 ulp) est celui de l'ANCIEN 1p aussi — étape 4 (nsys ≤ 15 µs/couche) chez Manon

instrument : `donnees-c14-19-09/{chaine.sh,equiv.py}` — GLM-4.7-Flash k48-calibA, prefill de la tranche privée 1 (moteur, graphes coupés), sonde sur `_mla_decode` au premier pas de décodage, 3 couches {0, 15, 30} × b {1, 12} (à b=12 sans graphes le chemin est `decode_static` par créneau : 564 = 47 × 12 appels, genre b1) × L {300 → godet 512, 2 000 → 2 048} ; (a) nouveau `mla_decode_1p` contre `mla_decode` de référence dans le même processus ; (b) **mêmes entrées** (q, cache, len capturés dans l'arbre d'AVANT, rejoués) contre la sortie de l'ancien 1p ; (c) 256 pas greedy b=1 sous graphes, jetons comparés entre arbres ; ulp = |Δ| / (2⁻²³ · max_h|o_h|) ; `ACVRAM_MLA_CORE=fp32` des deux côtés (l'arbre C14 précède le défaut tf32), `ACVRAM_ECO=off`
commit : avant = oceane-11 9e453576 (`<20,32,4>`), après = export de b765f1b2 dans `scratchpad/c14-build-19-09`, extension cache `kernels-9afa451e02c4` (`mla_1p_kernel<20,16,2>` **120/124 regs, 0 déversement, 0 local** ; `<20,32,4>` 170 ; `<32,16,2>` 174-177) ; carte 21:25-21:33 sous verrou, PID 4284 llama-server étranger présent avant/après
régime : GLM calibA, `experts_layout=marlin(33/46)`, cœur fp32, graphes off pour la capture, on pour les 256 pas
scellé (`sage-glm-decode-budget`, recopié) : `mla_1p` ≤ 15 µs/couche à b=1 (étape 4, nsys, Manon), b=12 ≤ actuel, **sortie = référence ± 1 ulp bf16**, capture 5/5 ; fiche C14 : ≤ 8 ulp fp32 de l'amplitude, jetons identiques 256 pas
mesuré : (b) mêmes entrées, nouveau contre ancien noyau : **1,89 / 2,63 / 3,91** (b1 L512, c0/c1/c2) · **2,89 / 3,44 / 7,18** (b1 L2048) · **1,89 / 4,49 / 3,31** (b12 L512) · **2,82 / 4,02 / 8,29** (b12 L2048) ulp fp32 — 11/12 ≤ 8, un 8,29 ; en ulp bf16 : ≤ 0,0003 ; (a) 1p contre référence : L=512 4,4-8,8, **L=2048 10,8-24,7 — identique sur l'arbre d'avant** (ancien 1p 10,8-24,7 aussi) ; (c) jetons : sha `ea6efad9…` (avant) contre `c1f52f72…` (après), **divergence au pas 5** ; sorties des couches 15/30 entre arbres à 10⁵-10⁶ ulp fp32 (0,01-0,1 relatif) alors que le noyau seul est à ≤ 8,3 : l'écart se construit couche après couche
verdict : **noyau tenu** au scellé de Sage (± 1 ulp bf16 : tenu à 10⁻⁴ près) et au mien à 8,29 contre 8 sur un point (b=12 L=2048, couche 30 : je ne rouvre pas, Sage dit si 8,29 vaut 8) ; **jetons identiques : faux, et non tenable pour un tuilage différent** (même mécanisme que split-K 19/09 et P1 : un ulp d'ordre de sommation, amplifié) — le juge qui reste est `ppl-decode-kv` 3 tranches ± 0,001, ou l'arbitre de distances de P2 (± 5 %) ; **l'écart 1p/référence à L=2048 (jusqu'à 24,7 ulp) préexiste à C14** et n'a jamais été mesuré sur des entrées réelles (la fiche disait 4,7 à sec sur du synthétique) : à noter dans `sage-glm-decode-budget`, ce n'est pas C14 ; étape 4 (t ≤ 15 µs/couche) et 5 (b=12, capture 5/5) : Manon, sur l'arbre `scratchpad/c14-build-19-09`
