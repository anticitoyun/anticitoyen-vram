# Sage — duel GLM arrêté : couches à formats mixtes (2 944 nvfp4 + 64 int4_awq) ; (a) maintenant, garde au convertisseur dans le même commit (15/09 soir)

1. **(a) maintenant, Manon** : reconversion `srcbf16` avec **`snr_floor 0` sur les experts seulement** (tous nvfp4, aucune promotion par expert), projections MLA et `lm_head` en **int8** comme prévu (1aj : `lm_head` refuse le W4). L'homogénéité se décide à la conversion, pas au chargement : c'est là que le format d'une couche est une propriété **de la couche**. Juge inchangé : **PPL ≤ étalon bf16 × 1,01** (réfuté > 1,02) + **accord top-1 ≥ 97 % / 2 048 jetons** ; si réfuté, les 64 experts avaient besoin de leurs bits → repli scellé d'avance : **promotion par couche entière** (toute la couche prend le format du pire expert, int4_awq), octets en plus mais piles gardées — pas de retour aux 64 isolés.
2. **Même commit, la garde qui rend « faux »** : le convertisseur **refuse** d'écrire une couche MoE mixte (`piles_ok` calculé à la conversion et exigé vrai, sinon erreur nommant la couche et les experts) ; test qui casse en réinjectant un manifeste à 2 944 + 64. Pas de (b) qui répare en silence au chargement : un loader qui « dépile » ou « ramène au format majoritaire » **fabrique** un régime que personne n'a mesuré. Le loader garde son repli par expert pour les anciens dossiers, mais il l'**annonce dans l'en-tête de mesure** (`régime : DÉGRADÉ, piles_ok=False`), et `energie.py`/le duel **refusent** une cellule dégradée — 38,7 ms / 6,36 J se publient comme « régime dégradé », jamais comme une cellule du duel.
3. **(c) non** : l'ancien `GLM-4.7-Flash-nvfp4` (srcQ5_K_M) n'est pas la source appariée à GadflyII ; un duel dessus n'est pas le livrable et son chiffre ne s'y transporterait pas (REGLES §4, régime).
4. **Bead durable** : le P2 « uniformiser l'échelle AWQ » du 14/09 (famille A6) est la cause ; il se ferme par le point 2, pas par une règle d'usage. Manon ~1 h + PPL ; Laure reprend la prise A dès `piles_ok=True` dans le manifeste, vérifié par `acvram doctor`, pas par le nom du dossier.

## Amendement (15/09 soir, cause exacte trouvée par Manon) : bloc MTP `layers.47`, pas une couche MoE mixte

1. Les 192 tenseurs int4_awq sont **tous** dans `model.layers.47.*` = le bloc MTP de GLM (`num_nextn_predict_layers=1`), rangé sous l'index `num_hidden_layers` ; `convert.py:263-315` ne reconnaît le MTP que par `.mtp.` et `layer_index()` rend 47, hors plan → défaut `int4_awq` codé en dur. Donc **aucune couche réelle n'est mixte** ; **`snr_floor 25` reste, comme le validé** — `snr_floor 0` était le remède d'une cause qui n'existe pas, je le retire. Plan (a) amendé : MTP détecté par **index == nombre de couches réelles**, format **hérité** (nvfp4 experts / int8 projections : on le convertit, la spéculation MTP est un chantier nommé, colibrì −32 %), **plus aucun défaut silencieux** (erreur nommée si un tenseur n'a pas de règle), et la garde « couche MoE mixte refusée » **reste** (elle peut rendre faux demain).
2. **5e bogue possible, à Océane, avant la prise A** : le loader ne doit **pas** exécuter `layers.47` comme une 48e couche au décodage. Contrôle qui rend faux : nombre de couches exécutées = 47 compté dans le forward (pas lu dans le config), tenseurs MTP **absents de la VRAM** au service (relevé `memory_allocated` avant/après chargement, écart = 0 pour le bloc), et un test qui casse si l'on force le bloc dans la pile. Si le loader le charge déjà comme couche, la PPL de Manon le dira (elle serait fausse de plusieurs points) — c'est pour cela que le juge reste la PPL du modèle entier, pas l'équivalence à deux couches.
3. Laure reprend la prise A dès `piles_ok=True` **et** le contrôle du point 2 vert ; 38,7 ms / 6,36 J restent publiés comme régime dégradé, cause : MTP en int4_awq par défaut de routage.
