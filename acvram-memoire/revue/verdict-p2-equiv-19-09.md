# Verdict — P2 ligne 1, équivalence décodage / préfill sur le converti i8c (`_int_mm` = `ACVRAM_PREFILL_INT8=cublas`, chemin ÉCRIT aujourd'hui, il n'existait pas) : distances **tenues** (médiane ×0,9996, p99 ×1,005 de la référence) ; top-1 : **B 5 divergents hors égalité contre A 2 — la référence échoue elle-même le « 0 »** (REGLES § 7), non tranché ; capture godets {1, 2, 8, 16} **4/4** après un correctif d'engine (le GEMM étroit Triton sortait de la mémoire partagée sur G = K)

instrument : `scratchpad/p2-equiv-19-09/equiv-decode-prefill.py` — tranche privée `tranche1.txt` (sha256 `1316df41799986c7…`, 106 086 o), préfixe 128 + 255 positions ; bras décodage = teacher forcing sous graphes b=1 (logit à chaque pas, le pas de préfill du préfixe exclu) ; bras préfill = une requête `ids[:p+1]`, `max_tokens=1`, par position (chemin paginé, 255 préfills) ; par position : top-1 égal hors égalité (|top1−top2| ≤ 10⁻³ dans un bras = égalité), max|Δlogit| ; `capture-godets-17-09.py 1,2,8,16` ; `chaine.sh` (scellé en tête)
commit : `oceane-11` ce commit (= 2c6c806 + cublas + garde gemm_etroit) ; A 16:27-16:30 carte vide ; B 16:36-16:37 avec **un processus étranger hors verrou : PID 95462 `./YALS` (498 + 250 Mio, lancé ≈ 16:29, pas à moi, laissé en place)** — mesure de qualité, publiable (REGLES § 4) ; les ms/pas des godets ne le sont pas
régime : A = classé `Qwen3-Coder-30B-A3B-nvfp4`, `[régime] défaut ACVRAM_GEMV_LAYOUT=marlin …` (prefill_int8=bf16) ; B = `…-nvfp4-qkvo-i8c`, `[régime] ACVRAM_PREFILL_INT8=cublas ACVRAM_GEMV_LAYOUT=marlin …` ; décodage de B = GEMV int8 par canal (`int8_gemv`, groupe = K = 2048, inchangé) ; préfill de B = A8 par jeton + `torch._int_mm` ; `.venv` du worktree (fla absent, sans effet : Coder sans GDN)
scellé (Sage § 2, écrit avant en tête de `chaine.sh`) : B 0 top-1 divergent hors égalité ; médiane et p99 de max|Δlogit| de B dans ± 5 % de A ; godets 4/4 ; prédiction : A lui-même > 0 divergent (graphes contre préfill paginé) → le « 0 » serait un seuil que la référence échoue ; distances B/A 0,95-1,10
mesuré : **A** 2 divergents (p=142 marges 0,107/0,041 ; p=231 0,031/0,024), médiane 0,4071, p99 1,817, max 2,90, PPL décodage 11,660 / préfill 11,697 · **B** 5 divergents (p=143 0,040/0,040 ; 167 0,050/0,039 ; 170 0,109/0,102 ; 208 0,050/0,132 ; **218 0,218/0,679**), médiane 0,4070 (×0,9996), p99 1,826 (×1,005), max 2,68, PPL décodage 11,854 / préfill 11,769 · godets B : 1 ✓ 2 ✓ 8 ✓ 16 ✓ (`warm_graphs`, 20 pas chacun)
verdict : **distances tenues** (les deux dans ± 5 %) ; **top-1 non tranché** — le seuil « 0 » est réfuté par la référence contre elle-même (2/255, REGLES § 7 du 17/09), et B en compte 5 dont un à marges nettes (p=218) : pas du bruit démontré, pas un défaut démontré sur 255 positions ; **je ne rouvre pas le seuil : Sage tranche** (proposition : même instrument sur les 3 tranches, ou seuil « divergents(B) ≤ 2 × divergents(A) » scellé avant) ; godets tenus. Manon peut prendre la carte (lignes 2-4) : le chemin `cublas` est servi et `regime_ligne()` le porte

## Ce qui a été écrit aujourd'hui (le chemin n'existait pas : `git grep _int_mm` sur `main` = conversion seule)

* `acvram/kernels/__init__.py` : `ACVRAM_PREFILL_INT8=cublas` — `_i8c_poids` (copie int8 signée q−128 construite une fois par tenseur, ≈ 0,9 Gio pour q/k/v/o de Coder), `gemm_i8c_cublas` (A8 par jeton via `quantifier_a8`, `torch._int_mm`, échelles fp32 ligne × colonne) ; éligible seulement si groupe = K, zéros = 128, M > 16, K et N multiples de 8 — sinon repli a8/bf16, **sortie inchangée pour tout converti classé**. `regime.VARIABLES` : `PREFILL_INT8` accepte `cublas`.
* `acvram/kernels/gemm_etroit.py` : `eligible(t)` = `group_size ≤ 128` — **défaut d'engine trouvé par la capture** : le GEMM étroit Triton (Poste C, 2 ≤ b ≤ 16) prend le groupe comme tuile K ; à G = 2 048, 393 Kio de mémoire partagée demandés pour 101 Kio → `OutOfResources` au godet 2 et dans le préfill par position (première passe B, 16:31). Dimension « taille de groupe » jamais posée (MECANISMES). Repli : `narrow_gemm` CUDA / `int8_gemv`.
* `tests/test_prefill_int8_cublas.py` (4 tests à sec, `_int_mm` a une implémentation CPU) : exactitude contre l'arithmétique en fp64 (< 10⁻⁵), écart à la déquant fp32 < 2 % (l'A8 seul), dispatcher sous régime, témoins cassants (affine par groupes → None, zéro ≠ 128 → None, M ≤ 16 → None), `gemm_etroit.eligible` refuse G = 2 048 et accepte 128.

## Non vérifié / à dire

* `MoE égal` non instrumenté (pas de trace de routage sous graphes) : un désaccord de routage se verrait dans les distances, qui sont égales à A à 0,5 % près.
* 255 positions d'une tranche ; les 5 contre 2 ne se départagent pas sans les 3 tranches.
* PPL décodage B 11,854 contre A 11,660 (+1,7 %) sur ces 255 jetons : ce n'est pas la ligne 2 (3 tranches, Manon), c'est un signe à regarder — le GEMV int8 lit une échelle fp16 par canal sur K = 2 048.
* Prefill `cublas` : vitesse non mesurée ici (ligne 3, Manon).
