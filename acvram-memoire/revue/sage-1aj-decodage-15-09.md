# Sage — 1aj au décodage : D tient la qualité, pas le temps ; un noyau, borné, avant de fermer ; la régression b=1 se bissecte au routeur fp32 d'abord (15/09 soir)

**(1) 1aj ne se ferme pas encore, mais il ne vaut qu'un jour.** Mes seuils (≤ 3,6 ms, J ≤ 1,10) sont réfutés : −19 % d'octets → −4,2 % de temps, parce que les projections passent par `nvfp4_gemv` (`kernels/__init__.py:497,522-533`), relecture de W par ligne, sans tranches — sur N=512 (k/v) la grille est trop courte pour la carte : même plancher de latence que le § 2 de `sage-mma-lot`. Deux marches, dans l'ordre :
1. **Laure, 20 min, sans code** : `ACVRAM_NVFP4_GEMV_MAX=8` (bascule tensorcore `:555-560`) sur D à b=12, ABAB 20 s. Scellé : **17,92 → ≤ 16,4 ms** (D revient sous le témoin 16,25 ± 1 %) et J ≤ témoin ; réfuté si > 16,9 → le chemin tensorcore a le même défaut à M=12 et le noyau de la marche 2 doit couvrir b=12 aussi.
2. **Laurine, 1 j, après route+pack** : `nvfp4_gemv` dense par tranches (K=2048, NV ≤ 16, même dessin qu'`int8_gemv` seuil 80), q/k/v fusionnés en **un** lancement (N = 4096+512+512) pour sortir du plancher. Scellé à b=1 sur D : **pas ≤ 4,10 ms (−8,5 %), J ≤ 1,33 (−11 %)** — c'est ce que valent les 0,45 Go à 1 050 Go/s ; **réfuté si > 4,25 ms** : les projections sont alors bornées par autre chose que W (activations, normes, RoPE : le 25,4 % hors poids de A8), et 1aj se ferme à « −4 %/−6,6 % à b=1, D non servi à b=12 ». Conversion du parc en D **seulement** si b=1 ≤ 4,25 ET b=12 ≤ témoin ; d'ici là D reste un dossier de banc.

**(2) Régression b=1 (233 → 223 t/s, −4,3 %, non thermique) : Laure bissecte, 6 commits, ordre par probabilité.** 1er suspect : **7f3f422, routeur et biais en fp32** (`model.py:1100-1117`, fusionné dans main ce matin via 2a5c56d) — il vaut pour **tous** les MoE, pas GLM seul : par couche, un cast de x en fp32 et un `F.linear` fp32 sur 128×2048 (1 Mo au lieu de 0,25), ~2 lancements de plus → **~0,2-0,4 ms/pas à b=1 = 4-9 %**, la taille exacte de l'écart. Puis v0.6.1/2/3 (une garde de lot par couche, < 1 %), 6-pré, carte.sh en dernier (aucun chemin de calcul). **Seuil** : un commit est coupable s'il rend ≥ 0,13 ms à b=1 (≥ 3 des 4,3 %), mesuré ABAB 20 s sur le même témoin (`good` = le commit du chiffre 233 du 14/09, `bad` = main) ; le correctif, si c'est le routeur, est **fp32 seulement quand `router_scoring == sigmoid` ou sur détection d'ex-aequo**, pas pour tout le monde — Océane, avec l'équivalence GLM qui doit rester 15/16. Prédiction écrite : 7f3f422 explique ≥ 0,2 ms ; réfuté si < 0,1 ms → le suspect suivant.
