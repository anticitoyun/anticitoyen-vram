# Sage — W4A4 GLM : la métrique W4A4 aggrave (1,0229 > 1,0183), toutes les variantes sont à 1,018-1,031 et le plancher sans échelle est 1,020 → le coût est l'E2M1 bloc 16 sur des activations à valeurs aberrantes ; chantier Hadamard à sec, en parallèle du comparatif (16/09)

1. **Pas de PPL de décodage diagnostique** : le prefill suffit à réfuter, et le décodage suit (+1,4 % mesuré). 20 min de carte pour un chiffre qui ne décide rien.
2. **Ce que les cinq chiffres disent ensemble** — sans AWQ 1,020 ; alpha-commun 1,028 ; indépendant 1,018 ; k_x=4 saturant 1,019 ; métrique W4A4 1,023 : **aucune échelle par canal ne descend sous le plancher sans échelle**, et l'écrêtage aide. L'AWQ (par canal, colonne entière) ne peut pas corriger un défaut **par bloc de 16** : dans un bloc, un canal aberrant fixe l'échelle E4M3 et les quinze autres tombent dans 12:1. La correction qui agit à l'échelle du bloc est la **rotation** (QuaRot/SpinQuant) : `x' = x·H`, `W' = W·Hᵀ`, produit inchangé, aberrants étalés sur tous les canaux du bloc.
3. **Chantier, à sec, deux membres en parallèle, aucune carte avant la PPL** :
   * **Manon, convertisseur (1 j)** : rotation des poids d'experts avant quantification — `gate/up` : `W·H` sur K = 2 048 (H = Walsh-Hadamard normalisée, bloc-diagonale `H_512 ⊗ I_4` pour n'avoir qu'une taille) ; `down` : K = 1 536 = 3 × 512, même `H_512` bloc-diagonale. Pas d'AWQ sur les experts (le plancher 1,020 est celui-là, et l'échelle par canal ne sert plus). Manifeste : `rotation: hadamard-512`. Le test à sec : `x·H · (W·H)ᵀ = x·Wᵀ` à 1 ulp fp32 sur un expert réel.
   * **Laurine, noyau (1 j)** : la transformée `H_512` **dans `nvfp4_quant_act`**, entre la division fusionnée et la quantification — la ligne est déjà en mémoire partagée (noyau par ligne, § 7) : 9 étages papillon sur 512, ~0 trafic mémoire ; drapeau par pile lu dans le manifeste. Même chose aux deux sites (`x/s` et `act/s_d`), et, parce que les poids tournés exigent l'entrée tournée partout, le chemin GEMV W4A16 applique aussi `H` à l'entrée (en torch, par bloc de 512 ; coût ≤ 0,2 ms/pas). Test : noyau = référence Python (E2M1 après FWHT) au bit.
4. **Scellés** : PPL NOMINAL MMA=1, **prefill ≤ 1,010 et décodage ≤ 1,010** (attendu 1,006-1,012 ; QuaRot récupère la plus grande part du coût W4A4 sur Llama avec des groupes plus grossiers que nos blocs de 16) ; W4A16 sur le converti tourné = W4A16 non tourné à ± 0,002 (la rotation seule ne coûte rien) ; pas b=12 MMA ≤ 19,0 ms (−2,3 attendus, + 0,3 de FWHT), prefill ≥ 12 000 j/s (×3). **Réfuté** (> 1,015 aux deux sites) → le W4A4 est abandonné sur GLM : régime W4A16 publié comme définitif, prefill ×6 assumé dans la table à cinq, et le levier suivant du décodage n'existe plus sur ce modèle — on le dit.
5. **Ordre** : le comparatif à cinq (`sage-comparatif-16-09`) démarre maintenant sur la carte (Laure) et les installs (à sec) ; Hadamard se développe à sec en parallèle, sa PPL prend la carte quand elle est libre. Coder-30B en bénéficie ensuite (+0,9 % → attendu ≤ +0,3 %), même conversion.

## Ordre

* **Jérôme** : `ETAT.md` — comparatif en tête de carte, Hadamard à sec (Manon + Laurine), rien d'autre ; INDEX.
* **Manon** : rotation dans le convertisseur, test 1 ulp, reconversion `srcbf16` sans AWQ experts, `rotation: hadamard-512`.
* **Laurine** : FWHT 512 dans `nvfp4_quant_act` + entrée tournée sur le chemin GEMV, noyau = référence au bit.
* **Laure** : comparatif (installs TRT-LLM, mesures), puis la PPL Hadamard.
