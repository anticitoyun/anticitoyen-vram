# Verdict — Nemotron-3.5-Lightning-30B-A3B acvram calibA (poste2 72ace16, bras A sur la précision officielle, SNR 23,2 dB) : PPL **1,4301 × bf16** (1,3294 / 1,4872 / 1,4794 ; officiel non calibré 1,0304, srcbf16 1,0632, vLLM 0,987) → **la calibration casse le modèle : + 40 points, uniformément sur les 36 fenêtres, prédiction poste7 1,015-1,025 réfutée de 0,4** ; vitesses saines : b=1 211,6 t/s (officiel 209,0), b=12 **963,8 t/s 0,405 J** (officiel 798,9 en GEMV : + 21 % par le GEMM dense Triton par défaut)

instrument : `scratchpad/nemotron-calibA-17-09/chaine.sh` (= verdicts srcbf16 / officiel : `ppl-acvram-17-09.py` 3 tranches × bf16 géo 13,416, `certifie-b12` b=1 ×1 / b=12 ×1) ; arbre poste3 = main ≥ 72ace16 (FLA, `DENSE_NVFP4=triton` défaut) ; converti `models_acvram/Nemotron-3.5-Lightning-30B-A3B-nvfp4-calibA`
régime : classé, graphes on, NOMINAL ; b=12 bridé 390 W
scellé (poste7) : PPL 1,015-1,025, classée si ≤ 1,020 ; vitesses attendues b=1 ≈ 209, b=12 ≥ 799
mesuré : PPL 20,428 / 19,802 / 17,459 (bf16 15,367 / 13,315 / 11,801), fenêtres de la tranche 0 toutes entre 1,25 et 1,5 × celles de l'officiel, 0 « explosée » (dégradation uniforme, pas un accident de fenêtre) ; b=1 pas 4,725 ms 211,6 t/s 1,617 J 342 W ; b=12 pas 12,45 ms **963,8 t/s 0,405 J** 390 W bridé
verdict : **calibA Nemotron réfuté : les échelles calibrées dégradent la sortie de 40 % de PPL de façon uniforme — ce n'est pas du bruit de calibration (qui vaudrait ± 1 point), c'est une échelle appliquée au mauvais tenseur ou dans le mauvais sens sur toutes les couches (les trois bogues en cascade corrigés à sec — renommage `backbone→model`, dispatch mamba/moe/attention, expert partagé sous mauvaise clé — laissent au moins un appariement faux que le SNR global (23,2 dB, calculé contre le bf16 tenseur par tenseur ?) ne voit pas s'il est mesuré sur les poids et non sur les activations).** À poste2 : contrôle tenseur par tenseur des échelles appliquées (nom du tenseur cible ↔ nom du tenseur statistique), en commençant par l'expert partagé et le `mixer.in_proj` FP8 (46 projections : si une échelle NVFP4 est appliquée à un tenseur FP8, c'est exactement + 40 %) ; un jouet à 2 couches avec PPL avant/après suffit. Les vitesses sont le seul fait utile : b=12 964 t/s = la précision officielle sous le GEMM dense Triton (+ 21 % sur 799 ; vLLM 1 799 : × 1,9), cellule à porter sur la ligne « officiel ». Colonne PPL calibA : « refus : conversion fausse (1,4301) ».
