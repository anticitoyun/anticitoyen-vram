# poste7 — GLM à MMA=0 : la ligne « bogue » de ma table sort, mais elle recouvre deux causes que je n'avais pas séparées ; 30 min à sec tranchent avant que poste4 cherche un bogue qui n'existe peut-être pas (16/09)

1. **Le verdict de la table** (`poste7-glm-w4a4` § 1.3) : alpha-commun 1,03086 → **1,008879** (≤ 1,010), sans-AWQ 1,02025 → 1,022388 (+0,002, bruit). Ligne 1, sans discussion. Et un fait neuf, à publier : **sans échelle, le prefill W4A4 ne coûte rien sur GLM** (+0,2 %, contre +0,92 % sur Coder-30B) ; **avec les échelles alpha-commun, il coûte 2,2 %**. Le coût W4A4 dépend de la table d'échelles — c'est ça, le résultat.
2. **Ma faute d'abord (REGLES §4)** : j'ai étiqueté cette ligne « bogue d'échelle sur le chemin MMA ». Le même motif a **deux causes**, et je n'ai nommé que la première :
   * **(1a) application** : la division `model.py:983` (`xs / awq["gate_proj"][e_sorted]`) ou l'ordre de `route+pack` appliquent une mauvaise échelle sur le chemin pile. Contre : `test_route_pack_awq_egal_torch` (`tests/test_moe_awq_pile.py:148`) prouve l'alignement pack/échelle contre torch ; poste4 16/16 bit-exact à DECODE_MMA=0 ; et une échelle d'un autre expert sur 3 008 paires ne coûterait pas +1 % de PPL avec top-1 conservé, elle exploserait la PPL.
   * **(1b) précision** : le noyau MMA ne voit aucune échelle — il reçoit `xq, xsf` de `nvfp4_quant_act(xs)` (`:1022`) *après* la division. Diviser canal par canal par `s[e]` (∝ mean|x|^α, ≥ 1 sur les canaux saillants) **élargit l'étendue intra-bloc** de l'activation avant la quantification E2M1 bloc 16 : l'étendue représentable d'un bloc est 12:1 (0,5 … 6) ; ce qui tombe sous max/12 devient 0. L'AWQ est conçu pour W4A16 (il déplace la difficulté des poids vers une activation supposée en pleine précision) ; en W4A4 il la déplace vers une activation qu'on quantifie à 4 bits. Rien à corriger dans le noyau : **le convertisseur cherche l'échelle avec une métrique W4A16** (`calibrate.py:352-356`, `x = diag(mean_abs)` en pleine précision) pour un chemin W4A4.
   * L'hypothèse de poste2 (dé-duplication/cache d'échelles identiques entre experts dans le noyau groupé) est écartée par le code : aucune échelle n'entre dans `_gemm_mma` (`:932`), la table `[E, K]` est consommée par une division torch indexée par paire (`:983`). Pas de chantier poste4 sur cette piste.
3. **Discriminateur, à sec, poste1, 30 min, avant toute heure de poste4** : `fake_quantize_nvfp4_activation` (`acvram/quant/fakequant_activation.py:36`) existe. Sur une couche MoE de GLM (poids bf16 de `srcbf16`, table `[E, K]` lue dans le converti alpha-commun, activations = échantillons de calibration ou `x` capturé par le crochet de calibrate), pour 8 experts : erreur relative de `y` contre bf16 dans quatre bras : (i) W4A16 sans échelle, (ii) W4A16 avec échelle, (iii) W4A4 sans échelle, (iv) W4A4 avec échelle — poids fake-quant NVFP4 dans les quatre, activation fake-quant dans (iii)/(iv). Scellé : **err(iv)/err(iii) ≥ 1,5** → (1b), le noyau est innocent, aucune correction moteur, on passe au 4 ; **≤ 1,1** → l'arithmétique exacte dit que les échelles sont inoffensives en W4A4, la pile réelle fait autre chose : (1a), poste4, équivalence pile/boucle **avec la table réelle du converti** (pas les échelles synthétiques du test) dans le même commit que le correctif, re-PPL du même converti ≤ 1,010 (`poste7-glm-gateup` § 2.1). Entre les deux : le témoin d'poste1 en ulp (`poste7-glm-w4a4` § 1.2, toujours dû) tranche — delta(alpha-commun) ≤ 2 × plancher sans-AWQ = (1b).
4. **Si (1b), la reconversion change de nature, et l'ordre de chef aussi** : reconvertir en AWQ indépendant avec la métrique actuelle rendrait 0,998 en W4A16 et un chiffre inconnu en NOMINAL — le même piège une troisième fois. poste2, à sec, ~1 h : la recherche d'échelle des experts (indépendante par tenseur, gate≠up déjà fusionné, 7c3698d) évalue sa métrique avec `fake_quantize_nvfp4_activation` appliquée à `x/s` — **métrique W4A4 pour un chemin W4A4** — identité explicite quand aucun alpha ne bat l'identité (la grille la contient, `calibrate.py:359`). Puis une seule reconversion `srcbf16`, PPL en NOMINAL MMA=1, mêmes fenêtres. Scellé : **≤ 1,010** (réfuté > 1,015 → l'AWQ n'aide pas les experts en W4A4 sur ce modèle ; repli : table unité sur les experts + Hadamard avant `nvfp4_quant_act` — `wants_hadamard` n'existe que pour int4_awq, `convert.py:350` — chantier > 1 j, note séparée). Attendu : entre 1,000 et 1,008 (alpha-commun rend déjà 1,009 en W4A16 avec une métrique aveugle).
5. **Ordre** : poste1 discriminateur (à sec, maintenant, en parallèle du pas b=12 de poste4 sur la carte — inchangé, scellé ≤ 1,03 ×) → (1b) : poste2 métrique + reconversion, poste4 ne touche pas au noyau ; (1a) : poste4 correctif, poste2 attend. Dans les deux cas, **pas de reconversion avant le discriminateur** — d'accord avec chef sur ce point, pas sur « poste4 corrige le W4A4 » qui présuppose (1a). Le décodage MMA=1/0 (`poste7-glm-w4a4` § 2.2) reste après la reconversion ; à b=12 le chemin MMA du décodage est rarement pris (≥ MIN_T=5 jetons par expert), le coût attendu y est ≤ 0,3 %.
6. **À publier maintenant** : « alpha commun sur 3 008 paires : +0,9 % en W4A16, +3,1 % en W4A4 — le coût du W4A4 n'est pas une constante du modèle, c'est une fonction de la table d'échelles ». Ça corrige `poste7-glm-gateup` § 5 (le +3,1 % était pour moitié le W4A4) et ferme la comparaison « AWQ = −2,2 % » jusqu'à la PPL du point 4.
