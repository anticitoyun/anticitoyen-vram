# poste7 — MMA=1 réfuté à b=1 (+56 % ms, +14 % J) : bascule au lot, critère et défaut (15/09 soir)

1. **Ma prédiction (−4/−9 % ms) est retirée, et l'arithmétique la réfutait avant la mesure** : la glue du chemin B est fixe, 32 µs/couche × 48 = **1,54 ms/pas**, soit +34 % d'un pas de 4,48 ms avant même les GEMM sous-occupés à M=1 (8 experts, 1 ligne utile par tuile de 64). J'ai raisonné « la GEMV n'est pas au plafond » sans écrire le coût fixe (REGLES §5). Le défaut MMA=1 de v0.6.1 a dégradé b=1 toute la journée : le conditionnement au lot de poste4 (v0.6.2, `ACVRAM_MOE_DECODE_MMA_MIN_T`) est la bonne forme, même geste que `GardeSpeculation`.
2. **Critère de bascule, scellé** : `t` = jetons réels du pas (pas le godet) ; MMA dès que **J/jeton(MMA) ≤ J/jeton(GEMV)** ET **ms(MMA) ≤ 1,02 × ms(GEMV)** — la première condition est l'objectif, la seconde interdit d'acheter des joules avec du débit. Défaut = le plus petit `t` mesuré qui satisfait les deux.
3. **Prédiction pour poste3 (b=2/3/4/6, ABAB 20 s, ~15 min, relatif à la même session)** : le coût fixe de 1,5 ms s'amortit sur t et les experts touchés croissent moins vite que t (8 → ~22 à b=4, ~28 à b=6 : concentration poste1) — ms : b=2 **+20/+30 %**, b=3 **+5/+15 %**, b=4 **−3/+5 %**, b=6 **−8/−14 %** ; J : croisement un cran plus bas (MMA baisse les W). Attendu : **défaut 4 en J, 6 en ms strict** ; réfuté si b=6 est encore ≥ 1,02× en ms → la glue n'est pas le seul coût fixe, le seuil attend route+pack.
4. **D'ici la mesure : défaut provisoire 6, pas 4** (aucun b ne doit être pire que ce matin), et **le seuil se remesure après route+pack** (§ 2 de `poste7-reprise-15-09-b`) : la glue de 22 µs/couche qui fait la moitié du coût fixe tombe avec lui, le croisement descend d'un cran ou deux. Le certifié b=12 (c6377d5) n'est pas touché. Réserve de poste3 notée : A1 223/1,51 contre 233/1,42 le 14/09, carte chaude après 90 min de ncu — ABAB dans la même session, relatif seulement.

## § 2 — Courbe b=2/3/4/6 (poste3) : réfutée à tous les lots ; d'où vient le fixe hors glue (15/09 soir)

1. **Hypothèse la plus probable : un plancher de latence par GEMM, pas de la bande passante.** À M ≤ 16 chaque expert touché tient dans une tuile ; le noyau lance `experts × tuiles_N` CTA (b=1 : 8 × 6 = 48 pour gate/up, 8 × 16 = 128 pour down ; b=6 : ~28 experts → 168 / 448), soit **une vague** jusqu'à b≈6 : le temps du noyau est celui d'**un** CTA qui parcourt tout K (2048/64 = 32 itérations à ~0,3 µs, quatre étages cp.async, un seul chargement en vol par fil — la même cause que le 39 % de borne du 14/09), **~10-15 µs par GEMM quel que soit b**, ×3 GEMM × 48 couches = **1,4-2,2 ms fixes**. Avec la glue (1,0-1,5 ms sous graphes) on retrouve les 2,0-2,3 ms de poste3 ; et à b=12 le fixe ne bouge pas pendant que la GEMV, elle, croît avec 96 paires — d'où le croisement tardif. Les deux autres candidats sont petits : `quant_act` sur x = 5,6 µs/couche (déjà dans la glue mesurée) ; les tuiles vides sautées coûtent un lancement de CTA (< 1 µs).
2. **Mesure de 30 min qui tranche (poste4, ncu, `--launch-count` borné, une passe)** : durée et `launch__grid_size`, `sm__warps_active.avg.pct_of_peak` des trois GEMM à **b=1, 6, 12**. **Scellé** : gate/up ≥ 8 µs à b=1 et **b=6 ≤ 1,3 × b=1** (plancher de latence, grille sous une vague) → hypothèse 1 confirmée ; **réfuté si b=6 ≥ 2 × b=1** (le temps suit les octets, le fixe est ailleurs : lancements/graphe ou `quant_act`, à reprendre avec `nsys` sur le rejeu). Contrôle qui rend faux : `dram__throughput` < 40 % du pic à b=1 attendu ; ≥ 70 % réfute.
3. **Ordre : la mesure (30 min) → route+pack (1 j, reste la première marche : −1 ms sûr à tous les lots, réutilisé par le port) → port avec, dans le plan de découpe, l'exigence chiffrée « ≥ 170 CTA à b=1 » (split-K par tuile ou K-tuile plus courte — c'est ce que fait b12x : 26,8 µs à 1 jeton contre nos ~3 × 12 + glue).** L'occupation ne passe pas devant route+pack : sans plan de découpe elle n'est pas une marche, c'est le port.
4. **Défaut MIN_T=9 (v0.6.3) confirmé** jusqu'au rescellement après route+pack et split-K ; prédiction rescellée alors, pas avant. La courbe de poste3 (reproductible au millième, T 45-55 °C) est le nouvel étalon des lots intermédiaires : toute marche se juge sur B/A à b=1/2/3/4/6/12, pas sur b=12 seul.

## § 3 — Recourbe avec route+pack (poste3 d505d52) : le seuil se lit au godet, et il se prouve à son bord

1. **Sous graphes le choix est une propriété du godet, pas de `t`** : le graphe capturé pour le godet 8 sert b=5-8 en entier. Le critère (J ≤ ET ms ≤ 1,02×) doit donc tenir **au pire cas du godet, son plus petit b** — b=5 pour le godet 8 (la MMA y paie le même godet qu'à b=6 pendant que la GEMV paie 5 jetons sur 6). **Décision : MIN_T = 5 si et seulement si la cellule b=5 passe le critère** (2 min, poste3, même session ABAB) ; sinon MIN_T reste 9 — il n'y a pas de valeur entre les deux, les bords sont ceux de la grille des godets. Prédiction b=5 : ms **−4 à −8 %**, J **−7 à −10 %** (entre b=4 en godet 4 et b=6 en godet 8, avec B quasi constant sur le godet) ; réfuté si ms ≥ 1,02× → MIN_T=9 et on le dit.
2. Godets 2 et 4 restent GEMV en mode moyen (b=4 +3,5 % ms, critère à la lettre). **Mais J y est déjà à −5,5/−10 %** : pour le mode **eco** (089ec1f), proposer `MIN_T=2` sous réserve d'une mesure croisée (eco + MMA à b=2 et b=4, 20 s : J ≤ eco seul − 4 % ; réfuté si l'horloge basse d'eco annule l'écart, ce que rien n'exclut). Les modes portent le seuil, pas la vigilance — `acvram serve --mode eco` fixe MIN_T, on ne le pose pas à la main.
3. Nouvelle référence des lots intermédiaires : la courbe d505d52 (20 cellules, ± 0,003 ms). Le port b12x (mesures en cours) se juge sur b ≥ 9 + GLM comme scellé ; s'il tient, cette courbe se refait une dernière fois avec lui et MIN_T se rescelle par le même geste — bord du godet mesuré, pas extrapolé.
