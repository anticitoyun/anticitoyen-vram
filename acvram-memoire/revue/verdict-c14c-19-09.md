# Verdict — C14-c (`mla_1p_kernel<5,8,1>` en grille B×S×G à b ≤ 2, combine réécrit ; oceane-c14c-grille-170 a6e6a98b) : **scellé TENU** — b=1 **`mla_1p` 5,2 µs/couche (≤ 15), combine 2,1 µs (≤ 3), noyaux du pas 6,67 → 6,12 ms (−0,55 ≤ −0,5)** ; équivalence **nouveau vs ancien sur mêmes entrées ≤ 8,31 ulp fp32 (un cas > 8), ≤ 1 ulp bf16 (les 12)** ; vs le noyau de référence à L=2 000, nouveau ET ancien s'écartent de 10-30 ulp fp32 (famille 1p, pas C14-c) ; capture GLM **5/5** (b=1 6,91 ms/pas) ; b=12 inchangé de forme (`<20,32,4>` SASS identique), 51,5 µs/couche mesurés contre 47,4 il y a deux heures : dérive de mesure, combine `<1>` 3,0 µs

instrument : chaîne d'Océane `scratchpad/c14c-carte-19-09/chaine.sh` sur ma copie `manon-c14c` (a6e6a98b ; **deux défauts corrigés avant tout chiffre** : AVANT par défaut = arbre oceane sans C14 ; `carte.sh` exporte `AVANT`/`APRES` (état de carte) et écrasait les siens → `C14C_AVANT`/`C14C_APRES`, repris par Océane sur sa branche), `C14C_AVANT` = manon `986b795` (main `cadfb52b`, C14 `<20,16,2>`), `C14C_APRES` = manon-c14c (extension compilée à sec) ; (1) `equiv.py` : 3 couches {0, 15, 30} × b {1, 12} × L {300, 2 000}, cœur fp32, nouveau vs référence `mla_decode` et vs binaire d'avant sur MÊMES entrées, puis 256 pas greedy ; (2) `decode-nsys.py` b=1 et b=12 (50 pas, `--cuda-graph-trace=node`) sur APRÈS ; (3) `capture-godets.py` {1,2,8,12,16} — **sa chaîne l'a lancé sur Coder** (pas d'`ACVRAM_MODELE_MESURE`) : rejoué sur GLM par moi ; prise `carte.sh` 23:10-23:14 + 23:16, `ACVRAM_ECO=off` (chaîne d'Océane), `fla absent` sur l'arbre C14-c (`GDN=torch`, sans effet sur GLM)
scellé (Sage, avant) : ≥ 170 blocs à b=1 ET combine ≤ 3 µs/couche → `mla_1p` ≤ 15 µs/couche, −0,5 ms sur le pas b=1 ; capture 5/5 ; équivalence ≤ 8 ulp fp32 vs avant (Océane), ± 1 ulp bf16 (Sage) ; b=12 ≤ actuel ; prédiction Océane : 8-12 µs + combine 2-3 ; issue gênante combine 3-5 µs
mesuré : (1) nouveau vs ancien (mêmes entrées) : ulp32 **2,1-4,5 à L=300, 3,0-8,31 à L=2 000** (b12_L2000_c2 8,31 : 1 cas sur 12 au-dessus de 8), **ulp16 ≤ 1 partout** ; vs référence : nouveau 4,8-7,5 ulp32 à L=300 (tenu), **10-28 à L=2 000** (ulp16 jusqu'à 7) — ancien : 5,3-7,6 à L=300, **10,8-29,7 à L=2 000** (ulp16 jusqu'à 8) : même famille d'écart, hérité de la recombinaison 1p ; jetons 256 pas : divergent au pas 46 (non exigé) ; (2) **b=1 : noyaux 6,12 ms/pas ; `mla_1p_kernel<5,8,1,…,2>` 5,2 µs/couche ×47 ; `mla_1p_combine<8>` 2,1 µs** (C14 : 16,8 + 8,4 ; main : 44,5 + 3,1) ; **b=12 : 14,05 ms/pas ; `<20,32,4>` 51,5 µs ; `combine<1>` 3,0** (C14 21:33 : 13,33 ms, 47,4 µs, 3,3) ; (3) capture GLM 5/5 : **6,908 / 8,354 / 12,551 / 14,779 / 16,378 ms/pas** (C14 : 7,414 / 8,627 / 12,039 / 13,885 / 15,194)
verdict : **TENU sur le scellé de Sage (b=1)** : la grille pleine fait ce qu'elle annonce — `mla_1p` ÷3 (16,8 → 5,2) et le combine ÷4 (8,4 → 2,1), le pas b=1 rend 0,55 ms de noyaux de plus (C14 en rendait 1,06 : cumul −1,6 ms sur 7,73, −21 %), capture 6,91 ms (−7 % sur C14) ; **équivalence : ± 1 ulp bf16 tenue vs l'ancien ; le « ≤ 8 ulp fp32 » d'Océane manque de 0,31 sur un cas** ; l'écart à la référence à L=2 000 (10-30 ulp) n'est pas de C14-c, l'ancien noyau l'a déjà ; **b=12 : forme identique mais 14,05 ms contre 13,33 deux heures avant — sous `ECO=off` cette fois (13,2-14,05 selon les runs, ± 6 %) : « b=12 ≤ actuel » n'est ni tenu ni faux à cette résolution, à rejouer dans une même prise si Sage le veut** ; capture 12/16 : 14,78 / 16,38 contre 13,89 / 15,19 en C14 (+6 %, même remarque)
suite : Sage : C14-c au défaut (b=1) ; b=12 : ABAB même prise C14 vs C14-c (5 min) si le « ≤ actuel » doit être tranché ; Océane : ok ; Jérôme : fusion ; ma file : PPL GLM lot 12 trois bras (Sage § 2) → nsys graph+osrt certifie GLM b=1 → concurrents 2 700 → C1 noyau (chaîne d'Océane, nue, 9 min de compilation) → C9

## Rejouable
`C14C_AVANT=<arbre C14> C14C_APRES=<arbre C14-c compilé> O=<sortie> ACVRAM_TYPE=mesure outils/carte.sh bash manon-c14c/scratchpad/c14c-carte-19-09/chaine.sh` (4 min) ; capture GLM : `ACVRAM_MODELE_MESURE=$G capture-godets.py` sur l'arbre C14-c.

## Addendum 23 h 35 — terme ppl-decode-kv (Sage, addendum 04 h 35) : **Δ géo +0,32 % > +0,2 % → FAUX à la lettre, non résolu en substance** — GLM 3 tranches préfixées, 8 192 + 512 notés, b=1 (chemin `mla_1p` : le noyau C14-c), **prefill fp32 des deux côtés** (`ACVRAM_MLA_CORE=fp32` : l'arbre C14-c est antérieur à la règle des 2 048 clés et prefille en tf32 à toute longueur — la première passe « défaut » contre « défaut » donnait +0,32 / −0,97 / +5,20 %, géo +1,48 % : c'était la dispersion TF32-8k, pas C14-c) : avant (C14) **15,8057 / 16,8423 / 14,4689**, C14-c **15,7881 / 16,7702 / 14,6894** → **−0,11 / −0,43 / +1,52 %, géo +0,32 %** ; même ordre que C14 contre son prédécesseur (−0,9 / −0,5 / +0,9 %, géo −0,18) : un tuilage différent déplace chaque tranche de ± 1 %, la géo de 3 tranches a une SE ≈ 0,6 % — le seuil ± 0,2 % n'est pas résoluble par cet instrument ; ce qui est établi : C14-c ne dégrade pas plus que C14 ne le faisait, et l'équivalence par noyau (± 1 ulp bf16 sur mêmes entrées, 12/12) reste la preuve forte
suite : Sage : trancher sur l'équivalence par noyau (12/12) ou exiger 12 tranches (10 min) ; Jérôme : C14-c à la fusion selon Sage ; règle d'instrument à écrire : tout arbre de chantier antérieur à 1eb9e283 doit être comparé à `MLA_CORE=fp32` des deux côtés dès que le préfixe dépasse 2 048
