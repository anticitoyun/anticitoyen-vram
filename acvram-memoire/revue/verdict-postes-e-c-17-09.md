# Verdict — postes E et C (Triton, opt-in) : **E ne compile pas** (`attn_paginee.py:60` : `PAGE_C` global lu dans le noyau `@jit`, Triton exige `tl.constexpr`) ; **C hors scellé** — pas dense b=12 Triton 6,59 ms contre cuda 4,86 (seuil ≤ 1,0 ms), seul `lm_head` gagne (×3,03 à b=12)

instrument : `outils/banc-attn-paginee-17-09.py` et `outils/banc-gemm-etroit-17-09.py` (Laurine, avec les noyaux ; 5 chauffes, événements CUDA, écart hors 2⁻⁸ compté) sous `carte.sh` — `scratchpad/bancs-ec-17-09/banc-{attn,etroit}.log`
commit : arbre laure 7dee6bf (= main avec E 1d998b2, C b9282ab, 13/13 CPU verts) ; défauts inchangés (`ACVRAM_PAGED_ATTN=triton`, `ACVRAM_NARROW_KERNEL=triton` opt-in) ; Triton 3.8.0, sm_120
régime : noyaux seuls, hors moteur ; formes Coder-30B (q 4096×2048, k/v 512×2048, o 2048×4096, lm_head 151 936×2048), b=12 et b=1
scellé (Sage) : E ≤ 1,5 ms par pas à b=12 ctx 2048 ET ≤ 0,72 ms à b=1 ctx 300, ± 2⁻⁸ ; C dense b=12 ≤ 1,0 ms par pas (48 couches + tête), ± 2⁻⁸
mesuré : **E : `NameError: Cannot access global variable PAGE_C from within @jit'ed function`** à `acvram/kernels/attn_paginee.py:60` (`debut_page = (debut // PAGE_C) * PAGE_C`) — aucune mesure, rc=1 après 3 min 28 (le temps de la compilation Triton avant l'erreur) · **C b=12** : q ×0,95 · k ×0,38 · v ×0,42 · o ×0,90 · **lm_head ×3,03 (0,659 → 0,217 ms)** ; pas dense **cuda 4,863 ms, Triton 6,586 ms**, hors 2⁻⁸ : 0 ; **b=1** : q ×0,35 · k ×0,32 · v ×1,49 · o ×0,37 · lm_head ×0,94 ; pas cuda 4,732, Triton 8,666
verdict : **E : non mesuré, refus de compilation à corriger (`PAGE_C` doit être `tl.constexpr` ou passé en argument `constexpr`) — dit à Laurine, banc à relancer (5 min) dès le correctif fusionné. C : réfuté au scellé (6,59 ms ≫ 1,0 ; le cuda actuel est lui-même à 4,86 dans ce banc, où chaque projection est chronométrée seule : en situ, `narrow_gemm` + `lm_head` font 3,0 ms) ; les projections k/v (512 lignes, N petit) sont 2,5× plus lentes en Triton — la tuile n'est pas faite pour N = 512 ; un seul gain réel : `lm_head` int8 b=12 ×3 (− 0,44 ms par pas, ≈ 3 % du pas de 13,2 ms). Exactitude tenue (0 hors 2⁻⁸) dans tous les cas. Recommandation : garder C opt-in fermé, ne retenir que le chemin `lm_head` Triton à b ≥ 8 si Laurine le sépare (`ACVRAM_NARROW_KERNEL=triton` par forme) ; le scellé 1,0 ms exige une autre approche (fusion q/k/v en un GEMM, déquant en registres) que des tuiles Triton génériques.**
