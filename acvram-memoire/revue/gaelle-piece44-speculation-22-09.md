# Pièce 44, dispersion b=1 — H2 (plafond MAX_GRAPHS) RÉFUTÉE, H7 réfutée : la chute « décroissante puis plateau » est la garde glissante de la spéculation n-gram qui se désactive pour la vie du serveur — 22/09 (Gaelle)

* instrument : `scratchpad/gaelle-p44-22-09/sondes-b1.py` (même séquence et mêmes invites que `banc-llamacpp-16-09.py decode` à b=1 : chauffe 5 s, repos 8 s, fenêtre 20 s à 1 024 jetons, 7 sondes de 128 jetons) + `/metrics` complet, horloge SM et température AVANT chaque sonde ; `run.sh` (bras 1 MAX_GRAPHS=16, bras 2 `ACVRAM_MAX_GRAPHS=64`), `run3.sh` (bras 3 = bras 1 + `ACVRAM_TRACE_STEPS=1`), `run4.sh` (bras 4 = sondes en ordre inverse 6→0) ; journaux `scratchpad/gaelle-p44-22-09/`
* commit : arbre `2c90672f` (= main b7c537c8 + carnet + scripts ; code identique à main, lancé depuis le worktree — `python -m acvram.cli serve`, cwd en tête de sys.path, donc pas dépendant du piège venv de Laure) ; alias officiel `Qwen3-Coder-30B-A3B-nvfp4-qkvo-i8c`, serve `--max-batch 12 --max-model-len 2304`, ACVRAM_CPUS=0-15
* régime : b=1, `graphes=on sampler=graphe rapatriement=epingle experts_layout=marlin eco=2700` (SM 2 670 MHz constant sur les 44 relevés, température 39-42 °C, 157-194 W) ; spéculation **`--speculative auto` → n-gram** (`cli.py:806-812`, défaut de `serve`), **absente de la ligne de régime** ; compute-apps début = fin (llama-server 8081 seul) ; verrou tenu 161 + 64 + 63 s
* scellé (gaelle.md 2c90672f, avant la carte) : H2 vraie ssi graphes_nombre = 16 avant la 4e sonde ET chute ≥ 15 % ET bras 64 sans chute ; H7 ssi compteurs figés dans les deux bras avec horloge/température corrélées
* mesuré : série reproduite 3/3 (bras 1 : 360,7 / 338,5 / 331,7 / **268,9** / 269,9 / 275,5 / 275,9 ; bras 2 : 352 / 338 / 343 / 276 / 271 / 271 / 273 ; bras 3 : 363 / 339 / 344 / 274 / 272 / 273 / 270 ; chute 22-23 %). **graphes_nombre = 8 → 9 dès la 1re sonde puis figé à 9 dans les DEUX bras** (clés capturées : (1,1,{16,32,64,128,256},0), (1,2,{32,64,128},0), (1,3,32,0)), jamais 16 ; `repli_eager` absent, aucune ligne eager sous trace. **Le compteur qui bouge est `steps`** : sondes 1-3 = 128 jetons en **46 / 67 / 67 pas** (`proposed_tokens = accepted_tokens` = 82 / 61 / 61, acceptation 100 %), sondes 4-7 = 128 jetons en **128 pas, 0 proposé**. Temps moteur par pas : 6,6 → 4,8 ms (pas spéculatifs à 2-3 jetons) puis 3,26 ms/pas à 1 jeton (3,7 ms mur, client compris). **Bras 4 (ordre inverse)** : 337,0 / 272,8 / 274,6 / 272,0 / 268,8 / 271,6 / 266,8 — seule la PREMIÈRE sonde (invite p=6, 68 pas, 60 acceptés) spécule, les six suivantes (dont p=0,1,2, rapides en ordre direct) sont à 128 pas : la bascule dépend de l'historique, pas de l'invite. La fenêtre de 20 s elle-même : 8 192 jetons en **4 120 pas = 1,99 jeton/pas** (383-386 t/s).
* verdict : **H2 réfutée, H7 réfutée, H1 et H4 réfutées** (captures figées après la 1re sonde, horloge et charge constantes, bras 64 identique). Cause : `GardeSpeculation` (`speculative.py:49-93`) — moyenne glissante sur 32 pas spéculatifs du gain jetons/pas ; sous `gain_min = 1,05` elle pose `_desactive = True`, et la seule réactivation est une transition de lot > `lot_max` (2) puis retour sous (`eligible`, l. 77-83) — à b=1 servi seul, **désactivation pour la vie du serveur** ; le plateau 270 t/s est le vrai débit à un jeton par pas, les 330-360 des premières sondes (et les 383 de la fenêtre) sont la spéculation n-gram sur des sorties qui bouclent. Conséquences à trancher par Jerome : (1) la cellule b=1 380,8 t/s (`verdict-t1-poste-1030-20-09`) a été mesurée par la même chaîne, même fenêtre, `serve` au défaut — donc **avec spéculation n-gram, à ≈ 2 jetons/pas**, face à des concurrents sans spéculation ; son régime doit le dire, ou la cellule se remesure `--speculative none` (prédit ≈ 270-280 t/s) ; (2) la ligne de régime et `/metrics` ne portent ni `speculation=ngram|none` ni l'état de la garde : à exposer (moteur, ≤ 20 lignes + test, comme la pièce 44 (a)) ; (3) la garde à repenser (réarmement après N pas ou par séquence) — décision d'algorithme, pas ce soir.
* durée : 288 s de carte en 3 prises (`tenue=161s, 64s, 63s`), verrou rendu 19 h 29, carte LIBRE pour Océane
