# Verdict — `ACVRAM_NARROW_GEMM=1` à b=12 (Coder classé, défaut éco 2 700 puis `ECO=2100`, ABAB harnais égal) : **FAUX, reste à 0** — au défaut B = A (**1,002 × t/s, J net 0,995 ×** : rien), à 2 100 B **0,982 × et J net 1,038 ×** ; et la preuve du chemin manque : `CHEMINS_INT8` ne compte **aucun `narrow_cuda`** sous B (les projections int8 restent en `etroit_triton`), la voie NVFP4 étroite (kernels/__init__.py:606) n'a pas de compteur — la variable est **au mieux inerte, au pire −2 %**

instrument : `scratchpad/narrow-gemm-b12-19-09/chaine.sh` (commit sur poste2), arbre `def8fd4`+ (main `8f35244`+ : `load_model` pose 2 700 ; bras 2 100 = `ACVRAM_ECO=2100`, nommé `eco=2100(2070)`), prise `carte.sh` 20:58:49-21:07:02 ; capture godets {1,2,8,16} sous `NARROW_GEMM=1` : 4/4 (3,51 / 5,09 / 6,60 / 9,00 ms/pas) ; harnais égal b=12 (`serve` par `serveur-chemins.py` + `banc-llamacpp-16-09.py`, 1 024 jetons, 20 s) : défaut A → B → A → B, puis 2 100 A → B ; ligne de régime `eco=2700(2670-2685)` / `eco=2100(2070)` (lecture sous charge de b820901b), `horloge_min/max` 2 497-2 692 et 1 890-2 092 ; **réserve** : indexeur `leann` (388 % CPU) pendant toute la prise, charge 6-8 → les absolus du harnais sont bas (1 200 contre 1 341 à 20 h 30), seul l'ABAB relatif vaut
scellé (chef/poste7, avant) : B t/s ≥ 0,98 × A ET J net ≤ 0,95 × A aux deux horloges → entre au défaut ; sinon reste 0 ; prédiction poste2 : B = A à 1 %, FAUX sur le J
mesuré : **défaut** A1 **1 212,7 t/s · 0,2042 net**, B1 **1 213,1 · 0,2039**, A2 **1 185,1 · 0,2099**, B2 **1 188,5 · 0,2082** → A 1 198,9 / 0,2071, B 1 200,8 / 0,2061 (**1,002 × / 0,995 ×**) ; **2 100** A **1 053,3 · 0,1717**, B **1 034,2 · 0,1783** (**0,982 × / 1,038 ×**) ; chemins par bras (dernier relevé) : A `{'dequant': 4032, 'gemv': 1656, 'etroit_triton': 1764}`, B `{'dequant': 3840, 'gemv': 1466, 'etroit_triton': 1856}` — **0 `narrow_cuda`** dans les quatre bras B
verdict : **FAUX** — le J net ne baisse pas de 5 % (0,995 × au défaut, +3,8 % à 2 100) et à 2 100 la vitesse touche la borne (0,982) ; surtout, **rien ne prouve que la variable ait agi** : le chemin int8 étroit à M=12 est déjà Triton dans les deux bras (`narrow_choix` prend Triton avant `narrow_cuda`, __init__.py:938-951), et la seule voie que `NARROW_GEMM=1` ouvre sans compteur est la NVFP4 étroite dense (:606) — B = A à 0,2 % dit qu'elle ne change rien à b=12 sur le classé ; `NARROW_GEMM` reste 0 ; la ligne « classé b=12 : 1 341 » du comparatif ne bouge pas
suite : poste1 : un compteur `CHEMINS_NVFP4['etroit']` sur la voie :606 (une porte sans compteur est inerte par construction) ; poste7 : NARROW_GEMM clos à b=12 ; ma file : vérification éco 3 bras sur main ≥ b199c5c9 (chef) → C13-c à sa fenêtre

## Rejouable
`ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/narrow-gemm-b12-19-09/chaine.sh` (8 min) — sans indexeur pour des absolus publiables.
