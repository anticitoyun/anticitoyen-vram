# Protocole — bloc de carte unique (GO utilisateur, poste7) : (1) porte cuBLASLt a8 · (2) banc Marlin P1 · (3) harnais égal Coder — machine calme (load1 < 16 vérifié en tête et entre les étapes)

1. porte cuBLASLt a8 : `scratchpad/bloc-p1-18-09/porte-cublas-a8.py` — `torch._int_mm` sur q (2048×2048→4096), k, v (→512), o (4096→2048), M = 2 048, × 48 couches, médiane de 20 rejeux (événements CUDA), bf16 `@` en témoin. Scellé : total ≤ 16 ms → in situ a8-cublas (scellé ≥ 11 000 j/s inchangé) ; > 16 ms → P0-a8 fermé définitivement. Prédiction : 12-18 ms (cutlass bf16 fait 19,4 ; INT8 tensor cores ≈ 2× en crête, rarement atteint sur N = 512).
2. banc Marlin P1 : `outils/banc-marlin-p1-18-09.py` (poste4 80feac6, port vLLM v0.29.0) — `--compiler-seulement` à sec (CUDA_VISIBLE_DEVICES="") AVANT le verrou, puis sous carte.sh ; le banc s'invalide s'il compile sous le verrou ; témoin B0 attendu 90-100 TFLOPS. Trois bandes (poste7) : X ≥ 137 → intégration, scellé ≥ 15 700 j/s ; 110 ≤ X < 137 → intégration une passe, ≥ 0,95 × formule(X) = 2048 / (0,076 + 7,43/X) ; X < 110 → P1 fermé, semaine annulée. GLM : même passe, formule avec `--glm-fixe-ms` si poste4 l'a donnée, sinon trois hypothèses du banc.
3. harnais égal : `scratchpad/harnais-egal-18-09/chaine.sh` (protocole-harnais-egal-coder-18-09 + addendum) — contrôle llama-server b=1 341,4 ± 3 % puis `acvram serve` b=1 (275-295) et b=12 (1 150-1 200 bridé, `ignore_eos`, valide si n_jetons = lots × 12 × 1 024).
