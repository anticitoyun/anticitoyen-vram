# Verdict — nsys `graph` + `osrt` sur `certifie` GLM b=1 (main 28441096 : C14 + C15-1, éco 2 700) : **le pas servi b=1 n'est pas borné par l'hôte** — pas attesté **9,21 ms** (sous nsys), rejeu de graphe **8,57 ms de GPU (93 %)**, hors GPU **0,64 ms (7 %)** ; mais les noyaux du même pas ne font que **6,1-6,3 ms** : **≈ 2,3 ms d'espaces ENTRE les nœuds du graphe** (2 163 lancements × ~1 µs de latence de lancement, même rejoués) — c'est ce que C15 attaque (2 622 → 2 163 → niveau 2 1 552), pas Python

instrument : `outils/gpu/mesure/certifie-b12.py` b=1 (rondes, invite 256, ctx 2 048) sous `nsys profile -t cuda,osrt --cuda-graph-trace=graph` (le graphe = un lancement), export SQLite : `CUPTI_ACTIVITY_KIND_GRAPH_TRACE` (durée GPU par rejeu), `OSRT_API` (appels système) ; arbre `de432a0`+ (main `28441096`), prise `carte.sh` 23:37-23:39, `eco=2700(2685)` (SM 2 685 constant), 246 W, `ACVRAM_MODELE_MESURE` GLM calibA ; référence noyaux : verdict-c14-etape4 (C14 : 6,67 ms), verdict-c15 (C15-1 : 6,34), verdict-c14c (C14-c : 6,12, hors de cet arbre)
scellé : aucun (poste7 § 1 : nommer la part hôte du b=1 servi) ; prédiction poste2/poste7 (verdict-glm-propres) : ≈ 3 ms hors GPU sur 8,9 — **réfutée**
mesuré : certifie **9,209 ms/pas · 108,6 t/s · 2,267 J/j · 246 W** (osrt gonfle le pas : sans nsys 8,2 ms / 122 t/s, C15) ; rejeux de graphe **n = 1 687, moyenne 8,14, médiane 8,573 ms, min 6,967, p90 8,759** ; OSRT : `poll` 8 393 appels / 228 s cumulés (fils d'attente, hors chemin critique), `clock_nanosleep` 53 / 66 s, `pthread_cond_timedwait` 482 / 63 s, `read` 8 904 / 1,9 s — rien sur le fil du pas
verdict : **hors GPU = 0,64 ms par pas (7 %), pas 3** ; le pas GLM b=1 servi vaut 8,2-8,6 ms de GPU pour 6,1-6,3 ms de noyaux : **2,2-2,5 ms d'inter-nœuds dans le graphe** (2 163 nœuds à ≈ 1 µs : le rejeu d'un graphe n'efface pas la latence de lancement par nœud sur cette carte) ; conséquence : (1) la cellule C14 servie (112,7 → 122,5 avec C15-1) n'était pas « hôte-bornée » : elle est bornée par le nombre de nœuds — C14/C14-c rendent des ms de noyaux, C15 rend des nœuds, les deux comptent ; (2) le levier restant à b=1 est le compte de lancements : niveau 2 (1 552) vaudrait ≈ −0,6 ms de plus, les fusions nommées par poste1 (division AWQ, routeur Triton, moe_reduce) encore −0,5 ; (3) verdict-glm-propres à corriger : « hôte-borné ≈ 3 ms » → « espaces inter-nœuds ≈ 2,3 ms »
suite : poste7 : régime GLM b=1 « graphe de 2 163 nœuds, 8,4 ms de GPU dont 6,3 de noyaux » ; poste1 : C15 niveau 2 est la bonne cible (avec le déviant d'aiguillage à nommer) ; chef : indexer, corriger la ligne « hôte » ; ma file : concurrents 2 700 → C1 noyau → C9

## Rejouable
`ACVRAM_MODELE_MESURE=$G nsys profile -t cuda,osrt --cuda-graph-trace=graph -o … certifie-b12.py x … 1` sous `carte.sh` (2 min) ; `sqlite3 … "select (end-start)/1e6 from CUPTI_ACTIVITY_KIND_GRAPH_TRACE"`.
