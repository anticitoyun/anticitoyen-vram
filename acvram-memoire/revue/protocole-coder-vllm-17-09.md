# Protocole — cellules Coder-30B × vLLM sur le checkpoint communautaire ModelOpt, deux régimes (W4A4 CUTLASS, W4A16 Marlin)

instrument : `ppl-vllm-17-09.py` (prompt_logprobs=0, 3 tranches, cibles 1024..2047, géo + médiane, TRITON_ATTN) ; `decode-vllm-17-09.py` (fenêtre 20 s, b=1 et b=12, énergie NVML nette) ; `prefill-vllm-17-09.py 2048` (7 répétitions, médiane) — dérivés génériques de `decode-glm-vllm-16-09.py` / `prefill-glm-vllm-15-09.py`.
commit : arbre poste3 e574d5a (main fusionné) ; vLLM `/opt/ia/vLLM/.venv` ; checkpoint `models_vllm/Qwen3-Coder-30B-A3B-Instruct-FP4` (NVFP4/…-FP4, ModelOpt 0.33.1, `kv_cache_quant_algo: FP8`, `quant_algo: NVFP4`) ; sha256 des 4 shards dans `scratchpad/coder-vllm-17-09/checkpoint.sha256`.
régime : deux dossiers. **W4A4** = checkpoint tel quel → `CutlassNvFp4LinearKernel` + `'VLLM_CUTLASS' NvFp4 MoE backend`, KV `auto` → fp8_e4m3 (imposé par le checkpoint : c'est le régime « communautaire par défaut »). **W4A16** = dossier jumeau `…-FP4-a16` (liens symboliques vers les mêmes shards ; `config.json` et `hf_quant_config.json` recopiés avec `quant_algo: W4A16_NVFP4`, `input_activations: null`) → `modelopt.py:1044,1289,1425` : `MarlinNvFp4LinearKernel` forcé (`kernels/linear/__init__.py:1053-1055`) et seul Marlin survit côté MoE (`activation_key=None`). Preuve exigée dans chaque log : la ligne `Using <Kernel> for NVFP4 GEMM` et la ligne `NvFp4 MoE backend`.
bras KV : les deux régimes sont aussi mesurés en PPL privée avec `BANC_KV=bfloat16` (3 tranches) pour séparer la part du cache fp8 de celle des activations 4 bits.
scellé (avant mesure, 16/09 14h) :
- W4A4 fp8 : PPL public géo 1,10-1,14 (4 fenêtres du 14/09 : 1,123) ; b=1 190-210 t/s ; b=12 1 350-1 500 t/s, 0,25-0,30 J ; prefill 2048 : 30 000-45 000 j/s.
- W4A16 fp8 : PPL privé géo **1,015-1,035**, public 1,010-1,030 (la perte de 15 % du W4A4 est attribuée aux activations 4 bits, non aux poids) ; b=1 200-235 t/s ; b=12 750-900 t/s, 0,40-0,50 J ; prefill 2048 : 14 000-20 000 j/s.
- KV bf16 vs fp8 : écart de PPL privée ≤ 0,010 dans les deux régimes (le cache fp8 pèse peu à 2048).
falsification : W4A16 privé ≥ 1,05 → les poids du checkpoint communautaire (pas les activations) portent la perte, la cellule reste non classée quel que soit le noyau ; W4A16 privé ≤ 1,02 → classé, et le W4A4 est un choix de noyau, pas un défaut du checkpoint ; log sans la ligne Marlin → régime non prouvé, mesure rejetée ; écart KV > 0,010 → le régime « communautaire » doit être publié avec son KV nommé.
ordre : vitesses (2 régimes) → PPL W4A16 fp8 (privé, public) → PPL W4A4 public → PPL KV bf16 (privé, 2 régimes). ~2 h 15 sous `carte.sh`, chaîne détachée `scratchpad/coder-vllm-17-09/chaine.sh`.
