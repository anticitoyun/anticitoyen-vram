# Sage — chantier de Laure, 15/09 (réponse à Jérôme, ≤ 10 lignes)

**Décision : Laure prend le contrôle des ties de (5) ET le chiffre certifié b=12 mode moyen, dans la même prise de carte (1 h).** C'est ce qui débloque le livrable de repli (`ACVRAM_MOE_DECODE_MMA=1` par défaut, `model.py:1290`) ; Laurine a écrit (5), elle ne le couronne pas (REGLES §3). P8 (PDL) reste à Laurine après P1/P2 comme scellé (`sage-veille-trtllm-ggrun-14-09` P8) : c'est un attribut de lancement dans les noyaux, pas une mesure. La remesure « moyen » seule ne vaut pas une heure de carte ; jointe au contrôle, si.

1. **Ties** : Coder-30B, b=12 réels, greedy, 12 invites × 256 jetons, sous graphes, bras A `ACVRAM_MOE_DECODE_MMA=0`, bras B `=1`, mêmes invites. À la **première** divergence de chaque séquence, relever top-2 logits des deux bras au même pas (méthode de `graphes-preparer-divergence-14-09`). Scellé : toute première divergence est un tie — écart top-1/top-2 sur le bras B **≤ 2,4 ulp bf16** (≤ 0,30 pour des logits 12-17) ; **réfuté** si une seule divergence a un écart > 0,30 sur le bras B, ou si le taux de séquences divergentes avant 64 jetons dépasse **4/12** (témoin : A-graphes vs A-eager donnait 2 ties sur 12 le 14/09). Réfuté → (5) reste coupé, retour à Laurine avec le pas et les logits.
2. **Témoin de faute (le contrôle doit pouvoir rendre « faux »)** : rejouer B avec `ACVRAM_MOE_DECODE_MMA_BT=8` (tuile plus petite que `cnt` ≤ 12) — le contrôle doit rendre « réfuté » ; s'il rend « conforme » ou plante avant de diverger, le contrôle ne garde rien et on le dit avant tout chiffre.
3. **Chiffre certifié** : ABAB × 20 s au compteur, mode moyen, `CUDA_VISIBLE_DEVICES=0`, `-pl 400`, en-tête REGLES §3, `--query-compute-apps` début/fin, mode et fenêtre dans le nom du dossier. Scellé (Laurine 92beffe, fenêtres 22 s) : A **15,9 ± 0,5 ms**, B **13,75 ± 0,5 ms / 0,43 ± 0,03 J** ; **réfuté** si gain B < 8 % en ms ou < 12 % en J — alors le −19 % de Laurine était une fenêtre, pas un régime.
4. Sortie : `revue/verdict-ties-moe-decode-15-09.md`, trois états par point (conforme / réfuté / absent). Si 1 et 2 conformes et Manon rend la PPL du chemin ≤ +1 %, je scelle le passage par défaut le même jour.

**RETIRÉ le 15/09 (points 1 et 2)** : le bras A (GEMV W4A16) n'est pas la référence du bras B (MMA W4A4) — le contrôle comparait deux quantifications et ne pouvait pas rendre « faux » sur un noyau. Juge remplacé par la PPL, seuil scellé dans `sage-moe-mma-qualite-15-09.md`. Le point 3 (chiffre certifié ABAB) reste.
