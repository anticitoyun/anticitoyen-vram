# poste7 — T4 69 rouges : (A) une branche à sec maintenant, (C) quatre seuils à tenir, huit exemptions nommées (20/09, 12 h 08, horloge machine)

Source : tri d'poste1 12 h 01 (`scratchpad/t4-20-09`, 02a0a855), lu dans le journal ; fichiers et lignes ci-dessous relus par moi.

## (A) 46 fixture/enveloppe + (B) 11 données — oui, une branche poste1, à sec, maintenant
La cause de famille 1 est structurelle et c'est la bonne correction : `conftest` ne pose `TRITON_INTERPRET=1` que si `CUDA_VISIBLE_DEVICES=""` — un test « à sec » qui dépend de ce que l'enveloppe lui laisse voir n'est pas à sec (REGLES § 7 : « une protection qui vit dans l'habitude n'est pas une protection »). Fixture `a_sec` autouse par marqueur : `TRITON_INTERPRET=1` + `get_extension → None` + tenseurs CPU, **quelle que soit la carte visible** ; les 22 tests « régime marlin masque le régime testé » posent leur régime par `monkeypatch` (test = son régime, jamais le défaut du jour) ; 3 plan KV → `_kv_plan_override` ; 3 ligne de régime → textes à jour ; 2 compteurs à zéro → `skip` nommé « chemin non pris sous <régime> » (trois états, jamais un pass vide) ; `test_menus` ×4 → `racine_modeles()` (pas un TSV repointé : le TSV est une donnée, la fonction est la règle). Preuve de famille 1 : les 8 fichiers relancés avec `CUDA_VISIBLE_DEVICES=""` = **15/15 verts** (prédit) — à faire **dans le trou entre M3 et M4 ou après M4**, sous `nice -n 19 ionice -c3`, jamais pendant une mesure de poste2.

## (C) 12 code — un par un, fichier et ligne
| test | nature | décision |
|---|---|---|
| `test_int8_gemv_lot.py:76-90` `test_sortie_fp32_egale_au_bit_au_chemin_x_fp32` ×4 (b=12/16 × 5120/151936) | **seuil au bit, chemin servi** (tête lm_head `sortie_fp32`, 54d5b281 15/09 ; noyau inchangé depuis) — un logit fp32 qui diffère du chemin x fp32 est la faute § 13 (argmax basculé) | **À TENIR, bloque T4.** poste1 : (1) `.so` plus récent que le `.cu` ? (REGLES § 6, ccache) ; (2) les 4 seuls dans un trou, 30 s ; toujours rouge → `int8_matmul`/`quantize` ont bougé (C11 i8c per-canal `group_size=K`, `poste1-11` 2803800) : bisect à sec impossible (carte) → correctif sur le chemin, test inchangé. Prédit : instrument (binaire) 60 % ; défaut C11 40 % — issue gênante : c'est un défaut servi depuis le 19/09 sur la tête, T1 PPL Coder 1,0094 n'a rien vu parce que ± 1 ulp fp32 sur un logit ne bouge la PPL que par l'argmax. |
| `test_mla_flash_causal_c13c.py:71-82` ×8 (6,5-9,5 ulp carte, témoin « excès max 3,0 » **mesuré à sec**) | `MLA_CORE=flash` **opt-in, clos** (C13-c forme 1 × 26, forme 2 non servie) ; juge calibré sous `TRITON_INTERPRET` (REGLES § 7 : un vert interpréteur ne prouve rien du noyau) | `xfail(strict=True, reason="C13-c forme 2 non servie : juge calibré à sec, carte 6,5-9,5 ulp — recalibrer sur carte avant toute reprise, 20/09")`. Ne bloque pas. |
| `test_mla_niveau2_jumeaux.py:109` (déviant pas 15, 2 ulp bf16, chemin `=2`) | chemin `MLA_GLUE=2` **clos** : le déviant EST l'arithmétique du chemin `=2` (`poste7-niveau2-clos-retrait-regles-20-09`) — le test rouge dit vrai | `xfail(strict=True, reason="niveau 2 : arithmétique du chemin =2, 4-8 ulp/couche, clos 20/09")` ; si un jour vert → le strict le dira. Ne bloque pas. |
| `test_budget_kv_mla_runner.py:85-96` (256 blocs, plan 64) | **seuil à tenir** : un budget de plan non lu est la famille `_finish("length")` silencieuse (REGLES § 6) ; test CPU → **bisect à sec possible** (`git bisect run pytest -q tests/test_budget_kv_mla_runner.py`, ≤ 20 min, hors prise ou sous nice) ; suspects : 24b6565 (plafond hybrides = `--max-batch`), hote-0631, C14 | **À TENIR.** Sur-allocation, pas troncature (T1 b=12 atteste lot 12 sur 100 % des pas) : ne touche pas les cellules ; correctif à sec avant le rejeu T4. |
| `test_route_prep.py:153` (`|Δtopw| ≤ 4 ulp fp32`, carte 5 ; experts, eid, compteur **au bit tenus** l. 108-110) | seuil écrit à sec le 20/09 03 h 32 (« 0 ulp » à sec) **après** la règle § 7 du 20/09 00 h 26 (juge ulp = par ligne, d ≤ d(réf)+6, témoin publié) : test non conforme à sa naissance, pas un seuil réfuté | Réécrire au juge § 7 (référence fp64 par ligne, +6, témoin imprimé). Je le dis tel quel : 5 passe sous +6 — ce n'est pas un seuil rouvert, c'est la règle antérieure appliquée ; ce qui compte pour un routeur est tenu au bit (experts, eid). M4 juge pièce 3 sur (0) top-k/eid identiques + ratio des termes < 1, cohérent. |

## Chemin de « terminé » recalculé
T4 rejoué **une fois, sous enveloppe, après M4** (≈ 13 h 00), avec (A)+(B) fusionnées, 9 xfail nommés, route_prep au juge § 7, budget_kv corrigé à sec, int8 tranché (instrument ou correctif). Prédit : **0 failed / ≥ 1 890 passed / xfail 12** ; terminé **13 h 40** si int8 = binaire, **15 h** si défaut C11 (correctif + 4 tests carte 30 s + PPL filet 3 × 512). Faux si le rejeu rend un rouge hors de ces 69 : famille non nommée → tri, pas de rejeu en boucle (3 hypothèses max).

## Ordre
* **poste1** — branche `poste1-t4-fixtures` maintenant, à sec : fixture `a_sec` hermétique, régimes posés par `monkeypatch`, `_kv_plan_override`, textes, skips nommés, `test_menus` → `racine_modeles()`, 9 `xfail(strict)` nommés/datés, route_prep au juge § 7, **bisect à sec de budget_kv et correctif** ; aucun pytest pendant une mesure de poste2 (trou M3/M4 ou après M4, `nice -n 19 ionice -c3`) ; int8 : `.so` vs `.cu` lu maintenant, les 4 tests dans le premier trou, verdict « instrument / défaut » à chef en une ligne.
* **poste2** — M3 → M4 inchangées ; après M4 : les 4 int8 (30 s) si poste1 ne les a pas eus, puis T4 sous enveloppe sur main fusionné (≤ 15 min), sept lignes.
* **chef** — fusion `poste1-t4-fixtures` dès son pointeur ; ETAT : « T4 attend fixtures + int8 tranché » ; rien d'autre sur le poste pendant les prises.

## Addendum 12 h 20 — M3 TENU rend `MLA_GLUE=2` servi : le xfail « jumeaux » est retiré
M3 (poste2 9ee0c00a, 3b84f9c1) : `ACVRAM_MLA_GLUE=2` au défaut → **`test_mla_niveau2_jumeaux.py:109` teste désormais un chemin servi ; on ne l'exempte pas.** Sa clause « AU BIT contre `decode_static` » (l. 130) est le mauvais juge, pas un seuil réfuté : le chemin `=2` a un autre ordre de somme par construction, et M3 (0) l'a jugé par les termes (ratio < 1 sur 7 520/7 520, max 0,4994 = plancher d'arrondi) ; 2 ulp bf16 au pas 15 sur 32 est cet ordre, pas un déviant. Réécrire le test au **juge des termes d'poste1** (même juge que 2a-bis, à sec, par ligne) ou au juge § 7 (référence fp64 par ligne, d ≤ d(réf)+6, témoin imprimé) — pas un xfail. Les 8 `flash_causal` restent xfail (chemin non servi). Total xfail nommés : 8, pas 9.
Prédiction M3 dépassée sur (A−B)/ΔN : 1,34 µs/nœud contre 0,5-1,0 prédit — lecture : `=2` retire aussi le temps GPU des noyaux de glue (stack/cat/index ≈ 0,2 ms/pas), pas seulement la latence de rejeu ; la loi « µs/nœud » compte les deux, à écrire ainsi dans MECANISMES (§ « somme des noyaux d'un graphe ») — chef.
0.6.32 = **un seul .deb** : `=2` au défaut (+ `ROUTEUR_FUSE=1` si M4 tenu), `DEFAUTS_PAR_VERSION["0.6.32"]`, trois bras une fois ; GLM b=1 servi prédit **163 ± 3** (rapport servi/certifie de 0.6.31 : 143,8/145,5 = 0,988 × 165,2), faux si < 160 ; comparatif GLM b=1 publié après bras sous `0.6.32, poste=20-09-1030`.

## Addendum 12 h 40 — 0.6.32 reste l'arbre livré
Question de chef : les 2 lignes `model.py:1428` (`colle = None` avant la branche « permut », témoin jamais pris au défaut marlin/mma) sont postérieures au .deb 0.6.32 (1f1d96bf). **Décision : 0.6.32 reste l'arbre livré ; pas de 0.6.33 pour « terminé ».** Un .deb se prouve par ses bras sur son arbre (fait, f43534e6, GLM b=1 servi 169,0 : prédiction 163 ± 3 tenue et dépassée) ; refaire trois bras et une installation pour deux lignes d'un chemin témoin serait une preuve de rien. Contrôle qui peut rendre faux, fait par moi 12 h 42 : `git diff --stat 1f1d96bf..f1069eee -- acvram/ kernels/` = `acvram/engine/model.py | 2 ++` seul (`colle = None`, branche non atteinte sous `DEFAUTS_PAR_VERSION["0.6.32"]`) ; une ligne de plus, ou une ligne sur un chemin servi → 0.6.33 avec bras. ETAT porte : « T3 = 0.6.32 (1f1d96bf) ; T4 = main f1069eee = 1f1d96bf + 2 lignes témoin `model.py:1428` ». Les deux lignes partent dans le prochain .deb, quel qu'en soit le motif.
