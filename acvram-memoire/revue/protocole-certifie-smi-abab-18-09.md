# Protocole — contrôle de l'instrument certifie (sage-profil-verdict-18-09) : ABAB Coder b=1 ancien instrument (nvidia-smi dans la fenêtre) contre nouvel instrument (Océane, NVML en processus ou hors fenêtre)

prérequis : le nouvel instrument d'Océane ; à défaut, bras B = `certifie-b12-15-09.py` lancé avec la lecture `smi` neutralisée par variable (à demander à Océane : `CERT_SANS_SMI=1` ou équivalent) — je ne modifie pas l'instrument moi-même.
instrument : ABAB Coder b=1 (`ACVRAM_HYBRID_SLOTS=1`, rondes 20 s, régime classé, défauts du jour), A = ancien, B = nouveau, ordre A B A B, même fenêtre ; juge = pas ms des rondes (et t/s) ; J/jeton informatif.
scellé (Sage) : écart A − B attendu 0,10-0,14 ms/pas (deux appels de 27 ms par ronde de 1 787 pas ≈ 0,135 ms) ; < 0,06 ms ⇒ autre cause (le smi n'explique pas l'écart pur/rondes) ; > 0,20 ⇒ l'instrument porte plus que le smi. Prédiction : 0,12-0,14.
inventaire préalable : `revue/inventaire-cellules-certifie-18-09.md` (197 passes, 34 verdicts ; biais 0,135/pas_ms : ≥ 2 % sur 48 passes, toutes b ≤ 2 ; max 3,9 % ; cellules étiquetées, non effacées).
addendum (Océane 6e00289, oceane-11, pris dans laure par checkout des deux fichiers ; Sage REGLES §7) : bras A = `CERT_SMI_TEMOIN=1` (ancien chemin, sous-processus, exempté témoin), bras B = défaut (NVML en processus, GardeSmi active) ; le verdict cite `appels_smi_fenetre` des deux bras : A > 0 (attendu 2 par ronde de 20 s : ≈ 5 700 pas / 200 → ~28) et B = 0 ; A = 0 ⇒ la garde ne voit pas les sous-processus, correctif non prouvé. GardeSmi compte par remplacement de `subprocess.run` (pas par pgrep). Ordre A B A B, Coder b=1, régime classé, défauts du jour ; fusion dans main après cet ABAB.
