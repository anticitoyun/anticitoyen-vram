# Protocole — in situ RPW (Sage, sage-rpw-in-situ-gui-18-09, règle 9) : ABAB `ACVRAM_GROUPED_RPW=1` / `=4`, Coder b=12, ms/pas nu premier juge, J/jeton second

instrument : `scratchpad/gemv-experts-rpw-18-09/situ-abab.sh` — pour chaque bras, dans l'ordre A(rpw1) B(rpw4) A B : `certifie-b12-15-09.py` (b=12 bridé 400 W : pas ms, t/s, J/jeton brut, `regime_ligne` + `engine_regime` dans le JSON = ligne [régime]) puis `profil-pas-coder-17-09.py … b12` (`ms_par_pas_nu`, `tps_nu`, 40 pas sans profileur, `regime_ligne`) ; modèle `Qwen3-Coder-30B-A3B-nvfp4`, régime classé, graphes on ; arbre laure = main d2670cb.
scellé (Sage) : seuil unique — moyenne des deux B en ms/pas nu ≤ 0,95 × moyenne des deux A → tenu, sinon faux ; second juge J/jeton (certifie), même seuil 0,95, informatif. Prédiction (mienne, du banc −11 % sur 7,88 ms d'experts dans un pas Coder b=12 ≈ 18-20 ms) : −4 à −6 % nu, autour du seuil.
contrôle GUI (Sage, ± 10 %) : `/metrics` `energie.j_par_jeton` contre energie.py sur la même fenêtre — le champ n'existe pas dans `acvram/server/app.py:823-841` à d2670cb ; à faire dès que l'ajout est fusionné, sur une fenêtre de serveur séparée (certifie tourne le moteur en processus, sans serveur).
