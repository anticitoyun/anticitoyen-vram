# Verdict — clause 4 (poste7 `poste7-p3-1-clause4-int8-20-09`) « n = 60 » kv=int8/kv=bf16 sur Qwen3-VL-2B, arbre 829a7ef0 : **le protocole demandé ne produit PAS n = 60 échantillons indépendants** — les 3 répétitions sont **bit-à-bit identiques** (déterminisme du moteur, greedy + graphes CUDA fixes) ; **n réel = 20, clause 4 reste NON RÉSOLUE à la même résolution qu'hier** (SE ≈ 1,54 %), l'intervalle calculé sur les 60 valeurs triplées (SE ≈ 0,87 %) est **fabriqué** et ne doit pas remplacer celui du 20/09

instrument : `scratchpad/mm-qvl-20-09/sortie-n60/{bf16,int8}-{1,2,3}.json` — `mesure-c.py` (Engine direct, régime servi, IMAGES_MAX=20, N_GREEDY=8) lancé 3 fois pour `ACVRAM_KV_FORMAT=bf16` puis 3 fois pour la valeur par défaut (int8), refs fp32 réutilisées (sha inchangé), un seul `carte.sh`, en-tête sans pair
scellé (poste7, avant) : géo − 2 SE > +1 % → `kv=bf16(vision)` en 0.6.35 ; géo + 2 SE ≤ +1 % → réserve levée ; sinon la réserve reste
mesuré : les trois essais bf16 sont **identiques au bit** (`lp_desc_par_jeton` égal à 16 décimales pour les 20 images, écart max 0,0) ; idem pour int8. Les statistiques « n = 60 » obtenues en regroupant ces triplets : geo +2,018 %, sd 6,75 %, **SE 0,871 %**, intervalle [+0,26 ; +3,81] % — cet intervalle est **faux** : il traite trois copies exactes de 20 observations comme 60 observations indépendantes (autocorrélation ρ = 1 à l'intérieur de chaque triplet), il sous-estime la vraie incertitude d'un facteur √3. La statistique honnête reste celle du 20/09 à n = 20 : geo +2,02 %, SE ≈ 1,54 %, intervalle [−1,1 ; +5,1] %, qui couvre encore les trois zones du scellé
verdict : **NON RÉSOLU** — pas par manque de marge (le scellé aurait pu trancher à cette résolution) mais parce que le protocole « 3 répétitions » proposé n'ajoute aucune information sur un moteur entièrement déterministe (rien à mesurer dans la reproductibilité : c'est un vrai résultat, elle est parfaite, mais ce n'est pas la variance qui manque à la clause 4) ; la réserve sur `kv=int8` **reste posée telle quelle** en 0.6.34 ; pour un vrai n = 60, il faut 40 échantillons DISTINCTS (`legendes-50.tsv` fournit 50 légendes supplémentaires sur les mêmes 20 images — contenu différent, pas une répétition) ou 20 images de plus, pas une troisième exécution
durée : mesure ≈ 1,5 min de carte (déjà rendue, faite entre 08:58 et 08:59) ; HEAD `poste2-qvl` 829a7ef0
suite : chef : la réserve kv=int8 reste posée, aucun changement en 0.6.34/0.6.35 sur cette base ; si un vrai n = 60 est voulu, je le fais avec `legendes-50.tsv` (≈ 2 min de plus) ; ma file : trou 3 min à poste1, puis attente du SHA fusionné pour les 4 alias

## Rejouable
`ACVRAM_ARBRE=<poste2-qvl 829a7ef0> ACVRAM_MODELE_MESURE=<Qwen3-VL-2B-Instruct-bf16-vision> [ACVRAM_KV_FORMAT=bf16] MAX_LEN=2048 IMAGES_MAX=20 N_GREEDY=8 <venv>/bin/python <poste2>/scratchpad/mm-c-20-09/mesure-c.py <images-20> <sortie.json>` — refaire une seule fois suffit, le moteur est déterministe.

## Addendum 09:07 — en-tête manquant, recouvrement signalé par chef
Cette prise n'a pas imprimé d'en-tête charge/cpu (script ad hoc sans le préambule habituel de `chaine.sh`) — défaut de ma part, noté. chef signale que la suite de tests de poste3 (261 s) a couru 09:00-09:05, chevauchant possiblement la fin de cette prise (fichiers écrits 08:58-08:59, mais le `carte.sh` englobant a pu tenir le verrou plus tard). Sans en-tête, la contamination éventuelle ne peut être ni confirmée ni exclue pour ce résultat précis — de toute façon disqualifié pour la raison statistique ci-dessus (répétitions identiques au bit) : aucune conséquence sur le verdict rendu. Le n = 40 réel (légendes distinctes) qui suit imprimera son en-tête et sera pris sur une carte vérifiée calme.
