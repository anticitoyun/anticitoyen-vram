# poste7 — Le 1 % n'est dans aucun noyau : la table GLM se publie maintenant, et le 1 % se cherche couche par couche contre HF, à sec, borné à un jour (17/09)

Entrée : `verdict-bissection-w4a16-17-09` (poste3, 3deb1bf, main d66e988). Scellé de `poste7-vllm-a16-verdict-17-09` § 2 : (i) inutilisable (chemin torch cassé), (ii) témoin tenu, déquant 0 ulp, GEMM CUDA forcée 1,0264. L'issue « qui me gênerait » est celle qui est arrivée : la perte est dans la mathématique commune aux deux chemins.

## 1. Ce que j'ai relu contre HF, et qui est égal

`modeling_glm4_moe_lite.py` (transformers 5.0.0rc0, `/opt/ia/vLLM/.venv/...`) : routeur `:400-423` (logits fp32, sigmoid, biais pour le choix seul, `n_group = 1` donc masque inerte, gather des scores, normalisation, × 1,8) = `model.py:1343-1353` ; `kv_a_layernorm` sur le latent seul, pas sur `k_pe` (`:331` = `mla.py:222-227`) ; échelle `256^-0,5` sans mscale (`:290`, `rope_parameters` absent = `mla.py:140`) ; `q_a_layernorm` (`:325` = `mla.py:185-188`). La formule n'est pas fausse à la lecture. Ce qui n'a pas été basculé par poste3 et que je ne peux pas lire : les normes hors MLA, le résidu, l'embedding, la norme finale, le RoPE de la partie `pe` (HF `rope_interleave`, `:337-340`, absent du `config.json` → défaut de la classe HF, à relever ; nous : paires adjacentes, `mla.py:200-211`). Après deux relectures vaines, on pose un instrument (REGLES § 5).

## 2. Décision : (b) pour la table, (a) par un seul instrument, (c) comme bogue séparé et borné

* **Table GLM : publiée maintenant, telle quelle.** acvram `-k48-calibA` 1,015 classé, vLLM W4A16 Marlin 1,016 classé (b=12 858 t/s / 0,397 J, prefill 18 117 j/s), vLLM W4A4 1,072 non classé, llama.cpp 1,015, TRT-LLM W4A4 non classé, TabbyAPI « MLA non implémenté ». Ligne sous la table : « à poids identiques, acvram W4A16 rend 1,028 contre 1,016 pour Marlin ; cause non localisée au 17/09, chantier ouvert ». Une limite dite n'est pas une limite cachée. Le 1 % ne bloque pas la publication : il bloque le jour où on voudra revendiquer la qualité.
* **Instrument, poste2, à sec, CPU, ≤ 1 jour** : `outils/equivalence-glm-2couches.py` étendu en **profil par couche** — `GLM-4.7-Flash-srcbf16-bf16` (nos poids bf16 purs, même arithmétique que HF) contre HF bf16 sur la source, même invite réelle ≥ 256 jetons avec préfixe, erreur relative de l'état caché en sortie de chaque couche (‖Δh‖ / ‖h_HF‖), et à l'intérieur de la couche fautive, par sous-bloc (norme d'entrée, attention, résidu, norme post, routeur, MoE, expert partagé). Poids bf16 des deux côtés : ce qui reste est la mathématique, pas la quantification. **Scellé** : erreur relative ≤ 0,5 % par couche, croissance ≤ ×1,5 d'une couche à la suivante. **Prédiction** : une couche ou un sous-bloc saute d'un facteur ≥ 3 — un site, pas une dérive ; candidats nommés dans l'ordre : RoPE `pe` (interleave), norme finale / normes hors MLA (`rmsnorm_bf16`), résidu. Issue qui me gênerait : dérive diffuse sans site — alors le 1 % est l'accumulation bf16 et il n'est pas récupérable sans fp32 sur le résidu, ce que vLLM ne fait pas non plus, et il faudrait chercher ailleurs (instrument PPL lui-même : deux scripts, `ppl-acvram` / `ppl-vllm`, à faire converger sur un bras HF commun avant tout).
* **`ACVRAM_DISABLE_KERNELS=1` cassé (PPL 10⁸)** : bogue séparé, **poste4, à sec, ≤ 2 h, avant ses correctifs de calibration** — reproduire sur le banc CPU 2 couches avec `-vllm-direct` (tenseurs CT en passage direct sous le chemin torch : premier suspect, `iter_tensors(direct_nvfp4=True)` n'a peut-être pas de jumeau torch), corriger, et **un test qui fait tourner le repli sur un NVFP4 passage direct** et casse si on réintroduit la faute (REGLES § 7 : c'est le chemin qu'empruntent les tests, et il ne rendait rien de faux parce que personne ne le lançait sur ce format). Inventaire, une ligne dans le verdict : quels tests passés dépendaient de ce repli sur un converti en passage direct — je prédis aucun (le format a deux jours), à prouver par `git log` des tests, pas par souvenir.
* Le correctif du 1 %, s'il existe, se remesure sur `-vllm-direct` (scellé ≤ 1,020, = Marlin ± bruit) **et** sur `-k48-calibA` (prédit ≤ 1,008), commit avec test d'équivalence. Après un jour sans site : la ligne sous la table reste, le chantier passe en veille, on ne relance pas de carte dessus.

## Ordre

1. chef : publier la table GLM (§ 2, ligne « 1 % non localisé » incluse) ; ETAT — chantier « 1 % » = profil par couche poste2 (≤ 1 j, à sec), bogue repli torch poste4 (≤ 2 h, à sec) ; ModelOpt propre clos ; carte libre sauf demande.
2. poste2 (à sec, CPU) : profil par couche bf16 vs HF (§ 2), verdict `verdict-profil-couches-glm-17-09` avec le tableau par couche et, si site, par sous-bloc ; relever le défaut HF de `rope_interleave` pour `Glm4MoeLiteConfig` (fichier:ligne) dans le même verdict.
3. poste4 (à sec) : repli torch sur passage direct (§ 2), verdict `verdict-repli-torch-passage-direct-17-09` ; ensuite `poste7-calibration-verdict` § 3.
4. poste3 : PPL Coder EXL3 déjà rendue (1,0006, classé) ; veille, rappelée sur le correctif du 1 % (deux PPL de 20 min).
