# Sage — ordre du jour du 15/09 (décideuse technique ; Jérôme coordonne, l'utilisateur tranche ce qui engage)

Objectif B (`sage-objectif-14-09`). Livrable de la semaine : duel MLA apparié GLM-4.7-Flash ; livrable de repli : pas complet MoE MMA sous graphes. Règles du jour, non négociables : toute énergie à **≥ 20 s au compteur**, **`CUDA_VISIBLE_DEVICES=0` sur les deux bras**, mode et fenêtre **dans le nom du dossier**, `nvidia-smi --query-compute-apps` au début et à la fin, **`-pl 400` remis après chaque bras** qui le change, aucune édition de l'arbre pendant une manche, omniroute éteint.

**File de carte (dans cet ordre) :** 1. Laure, colonne vLLM à une carte (b=1, b=12 ; 30 min) → 2. Laurine, bt=32 par noyau (1 h) → 3. Laure, llama.cpp 4a899373 b=1/2/12 (1 h) → 4. Laure, plafonds 300/600 W et repos bloqué (1 h) → 5. Laurine, pas complet MoE MMA sous graphes dès que (ii) est fusionné (1 h) → 6. Manon, conversion GLM `srcbf16` dès la fin du téléchargement (heures, soir/nuit) ; sinon duel llama.cpp pp2048/b=12.

| session | tâches, dans l'ordre | seuil scellé / ce qui rend « faux » |
|---|---|---|
| **Laurine** | (a) bt=32, octets par noyau (gate, up, down) + `cnt.max()` de la manche : le +13 % vient-il de la tuile ? (b) pas complet sous graphes, MoE MMA seule, Coder-30B, 20 s (repli du livrable). (c) démarre **1aj** (W4A4 projections + `lm_head`, > 1 j : autorisé) — après (b), pas avant. Godet 16 : levier (1) clos (±0, L2). | (a) 4,33 → 3,83 Go ± 3 % ; sinon nommer le noyau. (b) réf. 19,0 ms / 0,619 J / 390 W : **15,5-16,3 ms, 0,50-0,53 J**, W inchangé ; réfuté > 17,3 ms ou ≥ 0,56. (c) eager −1,5 ms ; tenu −2,0 ; réfuté < −0,8. |
| **Océane** | (a) finir (ii) grille fixe `t×k`, `tile_n=0` sauté, **test d'équivalence dans le même commit** : logits jeton décodé en MMA = prefill MMA à égalité près ; livrer à Laurine avant 14 h. (b) `energie.py` : invalidations `moyenne > plafond` et `duree < 10 s`, test qui casse (1 s pleine charge invalidé, 20 s accepté) ; `puissance_nvml.py` rend la moyenne, médiane à côté. Pas de carte. | (a) test rouge si on retire le saut n=0. (b) le banc de Laure du 14/09 (1,2-1,5 s, 401-435 W) doit sortir **invalidé** en rejeu. |
| **Laure** | (a) vLLM b=1 et b=12 à une carte : corrige la table 20 s et retire le créneau b=1 si confirmé. (b) llama.cpp b=1/2/12, Q4_K_M vs NVFP4, même instrument (2c). (c) modes : **mesurer avant d'écrire** — `-pl 600` et `-pl 300` à b=1 et b=12, puis repos serveur chargé et bloqué sur la prise vs carte vide ; le code `--energie` (serve, restauration sur signal, doctor) seulement après, demain. | (a) vLLM b=1 **1,42-1,45 J** ; b=12 **0,27** ; réfuté ≥ 1,55. (b) ≥ ×1,3 débit et ≥ ×1,2 J sur llama.cpp à b=12, sinon rien à revendiquer. (c) 600 W : t/s +15-20 %, J ±5 % ; 300 W : J −15/−20 % ; repos bloqué ≤ 30 W (réfuté ≥ 60). |
| **Manon** | (a) A7 alpha commun gate/up (sans carte, matin). (b) **dès le bf16 complet : conversion GLM-4.7-Flash `srcbf16` → NVFP4**, PPL étalon bf16 par exil des experts (nuit ; la PPL survit à la lenteur, pas le débit). (c) duel llama.cpp pp2048/b=12 seulement si le téléchargement n'est pas fini. | (b) PPL NVFP4 ≤ étalon × 1,01 (A4 experts +0,919 % sur Coder-30B) ; réfuté > 1,02 → bloque le duel, on le dit. |
| **Katy** | (a) relevé 3080 Ti (services 8081-8083, W, PID). (b) **audit « appareil »** : quels bancs et scripts appellent `Energie()`/`repos()` sans `CUDA_VISIBLE_DEVICES` — une fraction (« N sur M »), fichier et ligne, puis le correctif : `energie.py` refuse de sommer deux cartes sans `ACVRAM_CARTES_SOMMEES=1`. Pas de carte. | (b) le script vLLM 20 s (`campagne-20s-vllm-14-09.py:45`) doit être dans la liste, sinon l'audit est aveugle. |

**Duel GLM, deux bras vLLM** (Laurine a préparé le bras) : fp8 dynamique sur la source bf16 (prédiction scellée dans `sage-objectif`) **et** le NVFP4 téléchargé (même format, autre calibration) — scellé maintenant : bras NVFP4 b=12 entre **0,7 et 1,3× notre débit** ; ≥ 1,3× → (a) tombe pour GLM aussi et l'utilisateur l'apprend le jour même. Aucun chiffre du duel avant que Manon ait la PPL.

**Mise à jour 14/09 soir** : points 1 et 3 faits par Laure (acd91a7), (ii) livré par Océane (c8096c0, capture vérifiée, 869 tests). Projet **mis en pause** par l'utilisateur ; cet ordre s'applique **à la reprise**, avec la file de `sage-organisation-14-09.md` : 0 Laurine, llama.cpp b=1 sous ncu (instr/octet, J par noyau, repos chaud) → 2 Laurine bt=32 → 4 Laure modes → 5 Laurine pas complet → 6 Manon GLM. Le duel llama.cpp décodage est fait (Laure) : Manon ne garde que pp2048. Le code `--energie` va à Océane, Laure ne fait que mesurer.
