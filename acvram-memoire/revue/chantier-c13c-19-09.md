# Chantier C13-c — cœur d'attention MLA fusionné et causal au préfill GLM (Triton, saute la moitié masquée) ; chiffrage à sec (`sage-bras-f-faux-c13c-19-09` § 2) ; forme 1 fp32 CODÉE à sec le 20/09 (§ Code livré), chaîne carte prête

## Scellé (Sage, recopié)
Budget servi de F (bf16, faux à 8 175 < 8 300) : ≈ 250 ms/pas = cœur bf16 ≈ 105 + hors cœur ≈ 107 + hors noyaux 25-40. Un cœur fusionné qui **saute la moitié masquée** (scores causaux : `masked_fill` 9 + softmax 10 + ½ des deux einsum) et garde le softmax en fp32 : cœur **≤ 55 ms** → ≈ 195 ms → **≈ 10 500 j/s**. Scellé : prefill servi **≥ 9 500 j/s** ET ΔPPL contre fp32, 2 000 paires : point ≤ +0,002 ET borne IC95 ≤ +0,005 (indécidable si σ_moyenne > 0,002 → doubler, ne pas conclure) ET capture inchangée → défaut. Réfutation : cœur fusionné > 70 ms (le masque n'était pas la moitié du coût : la latence des 376 lancements l'était → fusionner d'abord, sauter ensuite).

## Ce qui existe (fichier:ligne, `mla.py` après C13-a/b)
Préfill chunké (`:351-368`) : par morceau de 256 requêtes, `scores = einsum('thr,sr->ths', q_eff, C)` [256, 20, 2 047] fp32 (`_dt_coeur`/`_tf32_coeur`), `masked_fill` causal, `softmax`, `einsum('ths,sr->thr', probs, C[:, :512])`, puis y = v_b·o_lat ; non chunké (`:369-378`) pareil sans boucle. Régime : `ACVRAM_MLA_CORE` tf32 défaut, bf16 = F. Par couche à L=2 047 : 8 morceaux × (2 einsum + masque + softmax + cast) = 40 lancements, 47 couches → **≈ 1 900 lancements par pas** (Sage compte 376 = 8 × 47 pour masque+softmax seuls).

## Chiffrage à sec
* **Travail** : scores 2 × L² × nh × (r + rope) = 2 × 2 047² × 20 × 576 = 96,5 GFLOP + o_lat 2 × L² × nh × r = 85,8 → **182 GFLOP par couche, 8,4 TFLOP par pas** (Sage : 8,6 ✓). Causal : la moitié + diagonale → **4,3 TFLOP**. Aux tensor cores bf16 (Triton `tl.dot`, acc fp32) à l'efficacité de Marlin (72 % de 209,5 plancher = 150 TFLOPS) : **29 ms** ; à 50 % : 41 ms ; ≤ 55 ms = ≥ 37 % du plancher, **tenable** ; > 70 ms = < 29 % : ce serait la réfutation « la latence, pas le masque ».
* **Octets** : C [2 047, 576] bf16 = 2,4 Mo par couche, relu par morceau de requêtes (8 fois) : 19 Mo/couche, 0,9 Go/pas — négligeable (L2). Les scores fp32 [256, 20, 2 047] = 42 Mo par morceau écrits puis relus par masque/softmax/einsum : **≈ 0,5 Go écrits + 1,0 lus par couche = 70 Go par pas** → à 1,5 To/s ≈ 47 ms : **c'est ce trafic, pas les FLOP, que la fusion retire** (flash : scores jamais en HBM) — la prédiction de Sage (≈ 105 → 55) est cohérente avec « la moitié du trafic + la moitié des FLOP ».
* **Forme** : flash-attention causal Triton, une tuile de requêtes [64-128, 576] × une tuile de clés [64, 576] : scores en registres, m/l en ligne (fp32), P bf16 × V_lat [64, 512] ; boucle sur les tuiles de clés jusqu'à la diagonale seulement ; grille (L/BM) × nh × morceaux ; sortie o_lat [L, nh, 512] fp32 ou bf16. Particularité MLA : K et V sont le MÊME tenseur latent C (V = C[:, :512]) : la tuile de clés sert aux deux produits, une seule lecture — c'est ce qu'aucun flash-attention générique n'exploite (ils lisent K et V séparément) : −50 % des octets de la tuile.
* **Précision** : scores bf16 aux entrées (q_eff, C en bf16 : le bras F) avec accumulation fp32 et softmax fp32 = F sur les scores + o_lat ; F = +0,14 % point, IC95 ≤ +0,5 % (3 × 12 fenêtres, non résolu) : le scellé exige 2 000 paires. Si le point de F sort > +0,002 à 2 000 paires, C13-c hérite du plafond (dit par Sage) — alors la variante `tl.dot` en tf32 (Triton `allow_tf32`) reste possible au même noyau : ~2 × plus lent sur les FLOP (29 → 58 ms), encore ≤ 70.
* **Capture inchangée** : le préfill n'est pas capturé ; le décodage ne change pas (`MLA_CORE_DECODE` fp32).
* **Chemin d'écriture** : `acvram/kernels/attn_mla_causal.py` (Triton, à côté de `attn_paginee.py`), branché dans `mla.py:351` sous `ACVRAM_MLA_CORE=flash` (4e valeur, régime nommé), jumeau torch = le chemin actuel fp32 (`MLA_CORE=fp32`) ; test à sec sous `TRITON_INTERPRET=1` sur formes réduites (L=256, nh=4) : sortie = fp32 ± 1 ulp bf16 par ligne ET témoin cassant (masque décalé d'une position → diverge) ; sur carte : formes réelles, nsys du cœur (≤ 55 ms), cellule servie, PPL 2 000 paires (Manon).

Non vérifié : tout temps ; l'efficacité Triton sur sm_120 pour `tl.dot` bf16 à K=576 (non multiple de 64 : 576 = 9 × 64 ✓, mais r=512 + rope 64 mêlés dans C — la tuile de clés porte les deux, le produit q·k utilise 576, p·v 512 : deux `tl.dot` sur la même tuile).

## Correction Sage (`sage-fiches-c5b-c13c-c14b-20-09`) : **forme 1 en fp32 d'abord** — la fusion retire les 70 Go/pas de scores et la moitié masquée SANS changer la précision
Forme 1 = le même flash causal Triton avec `tl.dot` en **fp32** (opérandes fp32, `allow_tf32=False`) : mêmes valeurs que le chemin fp32 à l'ordre de sommation près (± quelques ulp fp32 par ligne, jamais bf16), donc **aucune porte de PPL** (juge : sortie = fp32 ± 8 ulp de l'amplitude par ligne, comme C14), valable à toutes longueurs (la règle des 2 048 clés ne s'applique qu'aux régimes réduits) ; scellé Sage : cœur 220 → **≤ 110 ms**, prefill servi **≥ 9 000 j/s à toutes longueurs**, capture inchangée. Chiffrage : FLOP fp32 SIMT (pas de tensor core en fp32 plein sur sm_120 : `tl.dot` fp32 = FMA, ≈ 105 TFLOPS pic, ~60 % atteignable) : 4,3 TFLOP causal → **≈ 70 ms de FMA** + le trafic de C (0,9 Go, négligeable) — les 70 Go de scores disparaissent (≈ 47 ms aujourd'hui) et la moitié masquée aussi : 220 → ≈ 70-90 ms ; ≤ 110 tenable, la marge vient du calcul, pas de la bande. Forme 2 (tf32/bf16 dans le même noyau) ensuite, sous la règle des 2 048 clés, avec la PPL 2 000 paires ; le noyau porte les deux par un paramètre de dtype des opérandes. Code après C15 / C14-c / C1 (ordre Sage).

## Code livré à sec (Océane, branche `oceane-c13c-flash-causal`, 20/09 ; AUCUNE exécution GPU)
* **Noyau** `acvram/kernels/attn_mla_causal.py` : `attention_mla_causale(q_eff [t, nh, W] fp32, C [total, W] bf16|fp32, passe, scale, rank, operandes="fp32")` → o_lat [t, nh, rank] fp32 ; grille (⌈t/BM⌉, nh), **tuile BM = 64 requêtes × BN = 64 clés**, `num_warps` 8. **Tuilage de W = 576 = 9 × 64** : `tl.arange` exige une puissance de 2, donc q·kᵀ contracte par tranches BK = 64 (9 tranches, la dernière masquée si W % 64 ≠ 0 — rope 16 des formes réduites), un seul accumulateur de scores [BM, BN] ; p·v par tranches de 64 sur rank seulement (8 tranches sur GLM, 1 à rank 64), **8 accumulateurs explicites [BM, 64] fp32** (l'acc complet [64, 512] = 128 Ko ne tient pas en un bloc ; 128 registres/fil à 8 warps : le point non vérifié n° 1). Softmax en ligne fp32 (m, l ; `exp2` avec `scale·log2 e` plié dans le score), boucle sur les tuiles de clés **jusqu'à la diagonale seulement** : `n_plein = (passe + q0 + 1) // BN` tuiles sans masque, puis les tuiles diagonales jusqu'à `min(total, passe + q0 + BM)` avec le masque exact `pos_k ≤ passe + i`. **Lectures** : la tuile C [64, 576] est lue une fois par tranche pour les scores puis relue par tranche pour p·v (V = C[:, :512], la même tuile) ; la relecture touche les adresses qu'on vient de lire (74 Ko bf16 < L1) et C entier (2,4 Mo/couche) reste en L2 : **une lecture HBM de C par couche contre 8 aujourd'hui, et zéro octet de scores** ; q [64, 576] fp32 est relu par tranche à chaque tuile de clés (295 Ko/programme, L2) — c'est le trafic restant, pas compté dans la fiche initiale. Forme 1 : `tl.dot(…, input_precision=PRECISION)` avec PRECISION = "ieee" (≡ `allow_tf32=False` ; le mot-clé est déprécié en Triton 3.8 et, implicite, cède à `TRITON_F32_DEFAULT` — d'où l'explicite). **Forme 2 réservée** : `operandes="tf32"|"bf16"` lève NotImplementedError (signature prête, aucun chemin silencieux).
* **Branchement** `mla.py` : `ACVRAM_MLA_CORE=flash` (4e valeur ; `regime.py` note, `_MLA_CORE_DECODE` ne l'accepte pas — préfill seul), `_flash_prefill()` appelé une fois par couche dans le chemin chunké (`forward`, `passe = total − t`), la boucle de morceaux ne tourne pas, `y = v_b·o_lat` inchangé (règle VB, fp32 sauf MLA_CORE_VB) ; sous flash `_regime_coeur` rend « flash » à toute longueur (fp32 plein : la règle des 2 048 clés ne réduit rien). Repli **nommé** : Triton absent / ni carte ni interpréteur / erreur du noyau → `_FLASH_REPLI` + `[mla] …repli fp32 einsum` imprimé + ligne de régime `mla_core=flash(repli fp32: raison)` ; sinon `mla_core=flash(fp32)` (`regime_coeur_texte()`, partagé par `regime.regime_ligne()` et `runner._mla_core_texte()`). Décodage et capture non touchés (le préfill n'est pas capturé).
* **Prouvé à sec** (`tests/test_mla_flash_causal_c13c.py`, `TRITON_INTERPRET=1`, **10 passed en 4,2 s**) : formes réduites nh 4, rank 64, rope 16, cache bf16, (t, passe) ∈ {(128, 0), (128, 100), (256, 0), (256, 100), (200, 100)} : noyau = chemin einsum fp32 de mla.py au juge de C14 ; témoins cassants : masque décalé ± 1 → > 800 ulp ; passe − 1 lancé en contournant la garde du lanceur → diverge de la référence ET = référence décalée de −1 ; tuile pleine [0, 64) retirée → diverge ; garde `total == passe + t` ; le .py ne fait jamais de TF32 (9 `tl.dot` sous PRECISION, "ieee") ; `flash` dans la table et la ligne de régime, repli nommé ; `MLAttention.forward` jouet (t 200, cache 100) : flash contre fp32, noyau appelé une fois par forward, cache au bit, y à 1 ulp bf16 par ligne, repli forcé = sortie fp32 au bit. Voisins : `test_mla_tf32_c13`, `test_regime_noyaux`, `test_regime_garde`, `test_mla_glue_c15`, `test_mla_decode_batch` : 32 passed, 10 skipped (carte).
* **Précision du juge, mesurée** (à retenir pour Sage) : à t = 256, passe = 0, la référence einsum fp32 (CPU) s'écarte **elle-même de 8,74 ulp du float64** (sommes de 256 clés en un autre ordre) quand le noyau en est à 3,6 — noyau contre fp32 : 9,17 ulp, deux lignes sur 1 024. Un seuil que la référence ne tient pas contre elle-même n'est pas un contrôle (REGLES § 7) : **juge à trois bras** — ligne tenue si ≤ 8 ulp contre fp32, sinon si contre float64 le noyau est ≤ 8 ulp ET pas plus loin que la référence. À |scores| ≈ 19 (entrées × 6), référence et noyau sont tous deux à 26-28 ulp du fp64 (rounding de exp : |x|·ε) : le seuil de 8 ne juge que des scores modérés, et le test l'écrit. Reste vrai : aucune porte de PPL (fp32 plein), et les témoins cassent de deux ordres de grandeur.
* **Chaîne carte prête** `scratchpad/c13c-carte-19-09/chaine.sh` (à lancer NUE : refus si `ACVRAM_CARTE_TENUE`, chaque étape prend `carte.sh` ; `C13C_ARBRE` obligatoire, ≈ 25 min) : (1) `equiv-carte.py` formes réelles nh 20, rank 512, rope 64, cache bf16, (t, passe) ∈ {(2 047, 0), (4 096, 0), (2 047, 2 049), (1 000, 100)}, juge à trois bras + témoins ± 1, chronomètre par appel (prédiction du cœur × 47) et balayage BM/BN/warps (chaque variante jugée) — équivalence fausse = arrêt ; (2) nsys prefill GLM 2 047, bras jumeaux fp32 et flash dans la même fenêtre (`prefill-nsys.py`), cœur = `_flash_causal_kernel` + GEMM fp32 restantes + softmax/masque, scellé ≤ 110 (110-130 issue c, > 130 réfutation) ; (3) `prefill-abab` L = 2 048 et 4 096, D défaut tf32 ×2 / F flash ×2 / A fp32 ×1 ; F ≥ 9 000 ; **`[mla] repli` dans un log = résultat NUL** (la ligne `[régime]` est imprimée avant le premier préfill, le repli après).
* **Non vérifié** : tout temps (cœur, prefill) ; l'efficacité de `tl.dot` fp32 IEEE sur sm_120 (émulé en FMA : 105 TFLOPS pic, 50-60 % supposés) ; la pression de registres (acc 128 Ko/programme, spills possibles → étape 1 le dira en ms/appel) ; la relecture L1 de la tuile pour p·v ; le noyau sur carte (un juge Triton vert sous l'interpréteur n'est pas une preuve, REGLES § 7) ; la compilation Triton 3.8 pour sm_120 de `input_precision="ieee"`.

## Juge (Sage `sage-c13c-juge-fp64-ordre-fin-nuit-20-09` § 1), précisé par la mesure à sec — ce commit
Distance au float64 par ligne, pas « ± 8 contre fp32 » (la référence fp32 est elle-même à 7,4 ulp du fp64 à t=256). Mais **« d(noyau) ≤ d(référence) + 1 ulp par ligne » est réfuté à sec** par deux ordres de somme légitimes : t=128 : 15/512 lignes où le noyau est plus loin que la référence de > 1 ulp (excès max 3,0), t=256/passe 100 : 3/1024 (1,3) — alors que le noyau est **plus proche en médiane** (1,40 contre 1,64 ; 1,53 contre 2,44 ulp) et au max (4,86 contre 4,00 ; 3,91 contre 7,40). Juge retenu (test et chaîne, `_juge`/`juge`) : par ligne d(noyau, fp64) ≤ d(réf, fp64) **+ 4 ulp** ET médiane(noyau) ≤ médiane(réf) ET max(noyau) ≤ max(réf) + 1 — le noyau n'est jamais pire que le chemin qu'il remplace, ni par ligne au-delà de 4 ulp, ni en distribution ; 10 passed. **Filet PPL** ajouté à la chaîne (étape 1 bis, `PARTIES` contient `p`) : prefill GLM 3 tranches privées (`ppl-acvram-17-09.py`, corpus préfixé) flash contre fp32, |Δ géo| ≤ 0,0002 prédit (fp32 des deux côtés). Sage tranche le « + 4 ».

**Tranché (Sage § 1 bis, 06 h 50)** : seuil par ligne **+ 6 ulp** (2 × le témoin à sec, excès max 3,0 sur 15/512), témoin publié à côté du seuil (`temoin_exces_max` dans la chaîne, `[juge]` dans le test), médiane/max inchangés, filet PPL inchangé — test et chaîne sur ce juge.

**Tuile par carte (Manon 20/09, chaîne arrêtée à l'étape 1)** : 64-64-w8-s2 demande 319 488 o de shared (Triton garde q [64, 576] et la tuile de clés en fp32 entières) contre 101 376 par bloc sur sm_120 → `OutOfResources` ; sonde des tuiles : 32-64-w4-s1 est la plus grosse qui se lance. Correctif (ce commit) : `tuile_par_carte()` choisit BM/BN/warps/stages par `shared_memory_per_block_optin` (≥ 200 Ko : 64-64-8-2 ; ≥ 96 Ko : **32-64-4-1** ; sinon 16-32-4-1), `ACVRAM_MLA_FLASH_TUILE=BM,BN,w,s` force (diagnostic, régime), la ligne de régime nomme la tuile (`mla_core=flash(fp32,32x64w4s1)`) ; test à sec du choix. Le juge fp64 et les temps de la fenêtre sont ceux de cette tuile ; forme suivante si le cœur > 110 ms : q par tranches BK en shared (pas W entier) pour retrouver 64×64.

## Verdict carte, forme 1 : **FAUX sur les deux lignes** (`verdict-c13c-19-09`, Manon 4e3c429e, tuile 32×64-w4-s1)
* Juge fp64 faux aux 4 formes : L=2 047 noyau/fp64 **27,5 ulp contre réf 18,2**, 2 178/40 940 lignes au-delà de +6 ; L=4 096 36,2/25,6 (11 444/81 920) ; t2047/passe2049 41,8/27,4 ; t1000/passe100 19,9/12,8. Temps : **121 ms par appel à L=2 047 contre 4,6 ms pour l'einsum fp32 (× 26)**, 464 ms à 4 096 → cœur ≈ 5 690 ms contre ≤ 110.
* **Cause** : `tl.dot(input_precision="ieee")` fp32 n'a pas de tensor core sur sm_120 — Triton l'**émule** (passes bf16 ou FMA) : c'est le « non vérifié à sec » de la fiche (efficacité de `tl.dot` fp32 IEEE sur sm_120) qui tombe, et l'accumulation par tranches de 64 en émulation s'écarte plus du fp64 que cuBLAS. La forme 1 « fp32 sans porte » n'existe pas sur cette carte.
* Instrument, deux défauts corrigés (ce commit) : `equiv-carte.py` lisait `lignes_arbitrees` (clé absente depuis le juge § 1 bis → KeyError après calcul) → `lignes_plus_loin` ; le témoin −1 rendait nan (une ligne sans clé) et était noté « NE CASSENT PAS » (nan non compté) → nan = inf = casse.
* **Avant toute forme suivante (Manon) : mesurer à sec le débit de `tl.dot` fp32 IEEE contre cuBLAS fp32 et tf32 sur un GEMM 2 048² sur sm_120** — si l'IEEE est 3 passes bf16 (≈ 1/3 du tf32) la forme 2 (tf32/bf16) ne sauvera que si les tensor cores tf32 sont pris par Triton ; forme 2 = opérandes tf32 (`input_precision="tf32"`) sous la règle des 2 048 clés, avec la porte PPL (2 000 paires) que Sage voulait éviter ; ou bf16 = F (faux sur la vitesse à 8 175 j/s, la fusion ajouterait ≈ −40 ms). C13-c est fermé pour la nuit ; Sage tranche la forme 2.

**Erratum (Manon f2dec8cf, `sonde-tldot.py`, GEMM 2 048²)** : `tl.dot` ieee rend **49,9 TFLOPS = 0,88 × cuBLAS fp32** — ce n'est PAS une émulation ; tf32 84,8-86,5 = 0,92 × cuBLAS tf32 (forme 2 autorisée par le seuil de Sage) ; tf32x3 0,21-0,29 × (exclue). Ma cause « fp32 émulé sur sm_120 » est **fausse** : le noyau flash à 0,75 TFLOPS (121 ms pour ≈ 91 GFLOP) perd par sa **structure** à 32×64/s1 (q rechargé par tranche de W, 8 accumulateurs, softmax par tuile de 64 clés, un bloc par SM sous 99 Ko), pas par la précision — une forme 2 tf32 à structure égale gagne ≤ 1,8 ×, donc ne sauve rien. **Plomberie forme 2 livrée mais sans objet tant que la structure n'est pas refaite** (43956722 : `MLA_CORE=flash` = TF32 ≤ 2 048 clés vues, fp32 par morceaux au-delà, `ACVRAM_MLA_FLASH_OPERANDES`). Prochain geste (pas cette nuit) : mesurer le plafond de la structure — même noyau en bf16 d'entrée (sans exactitude) à 64×64 avec q par tranches BK en shared ; > 110 ms → aucune précision ne sauve C13-c, fermé ; sinon réécrire la structure (q en registres par tranche, un accumulateur [BM, rank] par 128, deux blocs par SM) avant toute forme 2. Médianes ulp/fp64 du GEMM (Manon) : cuBLAS fp32 2,88, ieee 5,59, tf32x3 1,54, tf32 6 493.

**Clôture de nuit (20/09 00 h 35 machine, `sage-suite-nuit-00h30-20-09` § 2)** : le bras bf16 64 × 64 de la sonde de structure **ne se lance pas** sur sm_120 : q [64, 576] + la tuile de clés [64, 576] entières en shared = 2 × 73 728 o ≈ **147 Ko même en bf16 > 99 Ko** (`shared_memory_per_block_optin` = 101 376) — la tuile n'est pas un paramètre qui sauve, c'est la structure « W entier en shared » qui plafonne à 32 × 64 (≤ 1,8 × par le débit tf32 mesuré → > 10 ms/appel). **C13-c fermé cette nuit par la clause** (7 268 j/s servis contre 18 117 vLLM). Structure qui tient, nommée pour la réécriture (≥ 2 h, chantier du jour suivant) : **q par tranches BK en registres (jamais W entier en shared), un accumulateur [BM, rank] par 128 colonnes, deux blocs par SM**. Ordre de preuve : (1) **sonde de temps à bras bf16 sans exactitude, scellé ≤ 2 ms/appel** sur la forme réelle (t = 2 048, W = 576, rank 512) — > 10 ms → aucune précision ne sauve C13-c, fermé ; (2) seulement ensuite la forme 2 (cœur ≤ 70 ms, prefill ≥ 9 500 j/s, juge contre l'einsum TF32 des 2 048 clés). Question au bilan (utilisateur) : un noyau MLA de prefill externe (FlashInfer / FlashMLA, dépendance qui engage) contre la réécriture maison (≈ 10 000 j/s promis).
