# Protocole — P1 in situ complet en UNE fenêtre (poste7, après le correctif MoEBlock de poste4) : `ACVRAM_GEMV_LAYOUT=marlin` + `ACVRAM_PREFILL_GROUPED=marlin` (disposition unique)

instrument : `scratchpad/p1-situ-complet-18-09/chaine.sh`, régime classé + les deux variables posées (pas encore défauts), arbre poste3 = main + poste4 (correctif), .so Marlin en cache, extension compilée seule avant, charge < 16 en tête ; dans l'ordre :
1. capture godets {1, 2, 8, 16} : `capture-godets-17-09.py` (warm_graphs + 20 pas par godet) — AVANT tout défaut (REGLES §3) ; arrêt si une capture échoue ou si graphes ≠ on.
2. décodage harnais égal (jumeau `banc-llamacpp-16-09.py`, `acvram serve` 8094, client sous `CUDA_VISIBLE_DEVICES=0`, ignore_eos, 256+1024, cartes [0]) : **b=12 ≥ 0,97 × 1 126 = 1 092 t/s ET J brut ≤ 1,03 × 0,347 = 0,357** ; **b=1 ≥ 0,97 × 366 = 355 t/s** ; ligne [régime] du serveur citée (`experts_layout`, `GEMV_LAYOUT=marlin`).
3. `ppl-decode-kv-17-09.py` Coder 256+1024 : **5,5426 ± 0,002** (témoin du jour).
4. prefill : `prefill-glm-acvram-15-09.py w4a16 2048` Coder (7 rép., σ) **≥ 15 700 j/s** ; PPL prefill privé 3 tranches (`ppl-acvram-17-09.py`) **= B0 ± 0,002** (B0 = 1,0148) ; GLM (`GLM-4.7-Flash-srcbf16-nvfp4-k48-calibA`) même passe : prefill 2048 (témoin B0 4 661 j/s, formule poste4 6740d18 à donner) et PPL 3 tranches `corpus-encode-brut-glm/prive-tr*` avec `PPL_PREFIXE="[gMASK]<sop>"` = B0 (1,0150) ± 0,002.
décision (poste7) : les deux régimes passent en défaut ENSEMBLE seulement si tout tient ; sinon rien ne passe et le verdict nomme le poste qui a lâché. Prédictions : décodage b=12 ≈ 1 126 × (6,91 / 6,13 pondéré par la part experts 66 %) ≈ 1 200-1 230 t/s, b=1 ≈ 375-385 ; ppl-decode-kv identique ; prefill Coder 15 900-16 400 (poste4).
addendum 18h05 : relance sur poste4 a3e1aa6 (disposition unique : réserve :1706 retirée, pile naturelle libérée après repack, experts_layout=marlin|naturel, les deux variables obligatoires ensemble) ; à surveiller séparément : couches exilées résiduelles (hors périmètre poste4).
