# Verdict — 0.6.25 (main c5367682 = arbre du .deb, blob 0ae48d49, sha256 91435cf3b625af9b…, 841 Ko ; `.cu` identique à 0.6.24 → pas de recompilation) : **trois bras éco conformes ; `test_paquet_charge_utile` 4 passed ; la ligne de régime nomme désormais `glue=compact(8)`** — feu vert possible

instrument : `scratchpad/verif-eco-19-09/chaine.sh` (serve réel sans variable → ligne de régime + `acvram eco etat`, SIGTERM → libre ; `-lgc` à la main + `ACVRAM_ECO=off` → la ligne lit la carte ; une requête servie dans chaque bras), 06:00-06:01, prises `carte.sh` ; test du paquet : copie temporaire de `tests/test_paquet_charge_utile.py` pointée sur `acvram_0.6.25_amd64.deb` (nom figé dans le test, cf. verdict-paquet-0623), hors carte, 06:01 ; copies `scratchpad/cellule-0625-19-09/`
scellé (Jérôme/Sage, avant) : trois bras conformes, charge utile passée → feu vert utilisateur
mesuré : (1) réel : `eco=2700(2692)`, `etat {"mode": "2700", "pid": 811421}`, requête servie à 2 670 MHz, après SIGTERM `horloge=libre` (487-2 812 en charge légère, stable) ; (2) `-lgc` à la main + `ECO=off` : `eco=off(2670: verrou 2700 posé hors processus)`, après SIGTERM `lgc2700?` (posé hors acvram, non rendu : juste) ; ligne de régime des deux serveurs : **`glue=compact(8)`** présent, `graphes=on(hybrides≤4)`, `chemin_moe=mma-a4`, `experts_layout=marlin` ; `4 passed in 0.80s` ; `dpkg-deb -f Version` = 0.6.25 ; blob = `origin/main`
verdict : **TENU** — rien à corriger ; la remarque (a) du verdict 0.6.24 est levée (le régime porte la glue compacte par son nom) ; reste (b) `test_marlin_prefill_p1::test_le_regime_nomme_la_disposition` faux hors carte (préexistant, Océane) et le nom figé du .deb dans `test_paquet_charge_utile`
suite : Jérôme : feu vert utilisateur ; Sage : 0.6.25 = 0.6.24 + régime nommé ; ma file (reprise, ordre Sage) : fenêtres d'Océane — niveau 2 biais + (β) (13894ca6, 3 + 10 min) — puis hygiène dans les trous (C5-b nsys, déterminisme ON/OFF, PPL longue)

## Rejouable
`bash scratchpad/verif-eco-19-09/chaine.sh` (1,5 min) ; paquet : `sed 's|acvram_0.6.0_amd64.deb|acvram_0.6.25_amd64.deb|' tests/test_paquet_charge_utile.py > tests/tmp.py && CUDA_VISIBLE_DEVICES= <python> -m pytest tests/tmp.py -q ; rm tests/tmp.py`.
