# Verdict — rang 6 de la clôture : vLLM KV fp8 contre bf16 (`auto`) sur la forme de `ppl-decode-kv` (préfixe 8 192 + 512 notés, tranches Coder 0-2) : **géo +0,18 %, SE 0,47 % — indécidable** (tranches −0,37 / −0,19 / +1,11 %) ; en face, acvram int8 par jeton contre bf16 = **+0,44 % géo** (verdict-c5-kv-int8, même forme, +0,62 / +0,74 / −0,05) : **les deux formats compressés coûtent la même classe (≤ 0,5 %), aucun des deux n'est séparable de zéro à trois tranches**

instrument : `scratchpad/vllm-kvfp8-19-09/ppl-vllm-decode-kv.py` (nouveau : vLLM `LLM(prompt_logprobs=0)` sur les 8 704 premiers jetons de chaque tranche, NLL des jetons 8 192..8 703 — l'attention lit le cache paginé écrit dans le même passage, donc en fp8 quand `kv_cache_dtype=fp8` ; **ce n'est pas un décodage pas à pas** : le préfixe et les notés sont traités en un prefill, ce qui est la seule manière d'avoir la lecture fp8 sans réécrire un harnais de décodage vLLM), `attention_config TRITON_ATTN` (la dorsale du 17/09 : FlashInfer absent, `auto` refuse), modèle `Qwen3-Coder-30B-A3B-Instruct-FP4-a16`, `max_num_seqs=1`, six prises `carte.sh` 05:51-05:58 (2 min chacune, chargement compris ; la notation dure 0,8 s)
scellé (poste7 poste7-feu-vert-0621 § 5, avant) : « vLLM KV fp8 sur ppl-decode-kv (10 min) » — la ligne de comparatif KV vLLM ; issue attendue par moi : fp8 ≤ +0,5 % comme int8 acvram
mesuré : auto **15,3653 / 14,2655 / 7,8541** ; fp8 **15,3086 / 14,2386 / 7,9411** ; ratios **−0,37 / −0,19 / +1,11 %**, géo **+0,18 %**, SE **0,47 %** ; acvram sur les mêmes tranches (C5, 19/09) : int8/bf16 +0,62 / +0,74 / −0,05 %, géo +0,44 %, et bf16 acvram 13,2572 (tr0, C5-b) contre vLLM bf16 15,3653 — **convertis différents** (nvfp4 k48 calibA contre FP4-a16 AWQ) : les PPL absolues ne se comparent pas, seuls les ratios KV comptent
verdict : **INDÉCIDABLE au signe, même classe que l'int8 acvram** — fp8 vLLM n'est ni meilleur ni pire que bf16 à trois tranches (SE 0,47 % contre un point à +0,18 %), l'int8 acvram est à +0,44 % (SE ≈ 0,25 %) ; pour le comparatif : « KV compressé : acvram int8 +0,4 % ± 0,3 ; vLLM fp8 +0,2 % ± 0,5 » — et rappeler que la forme vLLM note en prefill ; C5-b (canal) à +0,205 % reste la meilleure ligne, mais à +43 % de temps
suite : poste7 : ligne du comparatif KV ; chef : indexer ; ma file : rang 8 (.deb 0.6.25 à ton sha)

## Rejouable
`cd /opt/ia/vLLM && BANC_KV=fp8 ACVRAM_TYPE=mesure ~/travail/poste2/outils/carte.sh .venv/bin/python ~/travail/poste2/scratchpad/vllm-kvfp8-19-09/ppl-vllm-decode-kv.py <modele_vllm> <tranche.txt> <sortie.json>` (2 min par prise).
