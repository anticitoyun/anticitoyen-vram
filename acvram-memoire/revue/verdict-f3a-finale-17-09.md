# Verdict — F3a (rope_kv) dernière passe (protocole Laurine 17/09 06h20) : **arrêt à l'étape 2 — `test_rope_kv` 3/6 rouges sur carte (main f09db2b) ; l'étape 1 (sonde en situ) rend 62/62 IDENTIQUE mais son bras B a tourné en régime DÉGRADÉ sans graphes : non probante** → F3a clos réfuté selon le protocole, code en F3b, pas de deuxième fenêtre

instrument : étape 1 `outils/sonde-rope-kv-situ-17-09.py 256 64` (71eab9f, `ACVRAM_KV_FORMAT=int8`, régime classé) ; étape 2 `pytest tests/test_rope_kv.py` sous `carte.sh` ; journaux `scratchpad/f3a-finale-17-09/1-sonde.log`, `2-pytest.log`
commit : arbre laure f2da860 (acvram/ et tests/ = main 71eab9f ; noyau `rope_kv.py` = f09db2b « retour au noyau d462c24 », test = 389d8dc « référence des codes int8 = cache écrit par kv_write_int8 »)
régime : `ACVRAM_MOE_MMA=0 ACVRAM_MOE_DECODE_MMA=0 ACVRAM_NARROW_GEMM=1`, cache Triton tel quel (536 entrées) ; sonde : A = chemin actuel (rope_inplace + kv_write_int8) NOMINAL graphes=on ; B = rope_kv fusionné **DÉGRADÉ graphes=off, 25/48 couches et 3 283/6 144 experts exilés** (le moteur A n'est pas libéré avant le chargement de B : `del engA; empty_cache()` ne rend pas la VRAM des graphes)
scellé (Laurine) : étape 1 ~100 % IDENTIQUE, zéro DIFFÉRENT/NUL/STALE ; étape 2 6/6 ; une étape hors seuil ⇒ F3a clos réfuté
mesuré : étape 1 : `DÉCOMPTE {'IDENTIQUE': 62}` (positions 210-271), aucun DEMI-ENTIER relevé, rc 0 — mais B hors graphes : la corruption de F3a n'existe que SOUS graphes (f09db2b : eager sain), donc la sonde n'a pas mis le bras B dans le régime qui casse ; étape 2 : **3 failed, 3 passed** — `test_q_k_et_cache_suivent_la_reference[8-2-128-128-True-True]` et `[2-2-32-32-True-False]` : « jeton 0 : codes ou échelles ≠ kv_write_int8 » ; `test_le_demi_entier_exact_arrondit_comme_kv_write_int8` : AssertionError ; les trois cas verts sont ceux sans écriture int8 comparée
verdict : **F3a est clos réfuté par son propre protocole : le noyau de main (retour à d462c24, seul sain en situ) ne produit pas les codes int8 de `kv_write_int8` (test 389d8dc) — la version qui les produit (ece3bed, libdevice.rint) corrompt le cache sous graphes, la version qui ne corrompt pas ne les produit pas ; les deux moitiés n'ont jamais tenu ensemble.** La sonde en situ, une fois B libéré correctement (deux processus, ou `ACVRAM_ROPE_KV` posé par processus), reste l'instrument pour F3b : en l'état elle compare un bras sous graphes à un bras en eager exilé et ne peut rien voir. Étapes 3-6 non lancées (protocole : arrêt à la première étape hors seuil). Carte rendue au palier 2.
