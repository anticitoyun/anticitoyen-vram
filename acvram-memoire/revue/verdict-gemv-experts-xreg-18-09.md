# Verdict — dernier geste GEMV experts (poste4 07cc4b8, `ACVRAM_GROUPED_XREG=1`) : porte ncu gateup + down = **7,80 ms/pas** (témoin XREG=0 : 7,37) → **FAUX** (> 6,2) ; bit-exact tenu (20/20) ; chantier fermé à 1 262 t/s nu

instrument : `pytest tests/test_gemv_experts_v2.py` (20 cas dont `test_xreg_egale_v1_au_bit`, compilation réelle) ; passe ncu = commande du script 07cc4b8 rejouée par `scratchpad/xreg-18-09/chaine.sh` (17 métriques, 4 noyaux, skip 192 / count 96, `banc_decodage_moe.py gemv 12`), régime dans le wrapper cible `py-xreg{1,0}.sh` (classé, rpw=4, LC_ALL=C) ; tableau complet `scratchpad/xreg-18-09/tableau.txt`
commit : a8f5c67 (poste3 = main 4b83a2f + poste4 07cc4b8), 07:56-08:29, XREG=1 puis témoin 0 dans la même fenêtre, `fg=288 mma=0` dans les deux
régime : classé, rpw=4, b=12, graphes off (profileur), horloges verrouillées par ncu
scellé (poste7) : gateup + down ≤ 6,2 ms/pas ; prédiction poste4 gateup 68-76 µs, down 55-60, somme 5,9-6,5, mio/short ÷ 3, occupation 6 → 3-4 blocs
mesuré (médianes XREG=1 | témoin 0) : gateup **105,2 | 88,3 µs** (+19 %), down **57,2 | 65,1 µs** (−12 %) → **5,05 + 2,75 = 7,80** | 4,24 + 3,13 = 7,37 ms/pas ; registres/thread 96 | 40 (gateup), 56 | 40 (down) ; limite d'occupation par registres 2 | 6 blocs (gateup), 4 | 6 (down) ; warps actifs 32,0 | 86,8 % (gateup), 62,0 | 92,0 % (down) ; mio_throttle 0,15 | 4,66 et short_scoreboard 0,60 | 4,50 (gateup) — la prédiction « ÷ 3 » est dépassée (÷ 30) ; long_scoreboard 6,75 | 4,36 (gateup), 7,60 | 4,08 (down) ; issue actif 34,9 | 56,0 % (gateup) ; DRAM 34 | 40 % ; tests 20 passed (xreg = v1 au bit, 3 modes × 2 formes)
verdict : **FAUX** — 7,80 > 6,2 de 1,6 ms et pire que le témoin de 0,43 ms ; la prédiction down (55-60) est tenue (57,2), la prédiction gateup (68-76) est réfutée de 30 µs ; chantier GEMV experts fermé à 1 262 t/s nu (rpw=4 défaut), pas d'in situ

## Lecture
- Le mécanisme visé marche : x en registres supprime les files de la mémoire partagée (mio_throttle 4,66 → 0,15, short_scoreboard 4,50 → 0,60). Il se paie en registres : 96/thread sur le gate/up → 2 blocs par SM au lieu de 6, 32 % de warps actifs au lieu de 87 %, et la latence DRAM n'est plus couverte (long_scoreboard 4,4 → 6,8) — le noyau passe de « limité par la mémoire partagée » à « limité par l'occupation ». Le down (56 registres, 4 blocs, 62 %) garde la moitié de l'occupation et gagne 12 %.
- La piste qui resterait (hors chantier) : xreg sur le down seul (2,75 contre 3,13 ms/pas, −0,38 ms/pas ≈ −4 % du pas Coder) ou un gate/up xreg à ≤ 64 registres (`__launch_bounds__(256, 4)`), qui rendrait 4 blocs — mais poste7 a fermé le chantier au verdict de cette porte ; c'est noté pour un autre jour.
- Instrument : le script 07cc4b8 (`sudo -n env $ENVS ncu`) ne peut pas tourner sur ce poste — sudoers n'autorise que `/usr/local/cuda-13.4/bin/ncu` (rc 1 vérifié) ; le wrapper cible reste le seul remède (REGLES §6, 88e8472).
