# Verdict — C5-b (clés KV int8 à échelle E4M3 par canal sur chaque bloc de 16, `ACVRAM_KV_INT8_CANAL=1`, Océane oceane-c5b-k-par-canal a77eb722) : **PPL : FAUX de justesse par la règle, +0,205 % contre bf16 (SE 0,071 %, IC ± 2 SE [+0,06 ; +0,35]) contre ≤ +0,2 %** — canal enlève **deux tiers** du coût int8 (jeton +0,630 %, SE 0,21 %) ; **capture 5/5 tenu** ; **pas b=12 NON MESURABLE** : le planificateur KV compte les blocs en octets « par jeton » (16 640 o) et le bloc canal en fait 17 156 (+3,1 %) → budget 23 824 jetons pour 12 × 2 048 = 24 576, une séquence tronquée, `certifie` refuse (deux fois) ; **test carte 1/16 faux** (`test_ecriture_prefill_un_par_un_et_decoupe_au_bit_contre_le_jumeau` : « un par un ligne du bloc 2 »)

instrument : chaîne d'Océane `scratchpad/c5b-carte-19-09/chaine.sh` (nue, chaque étape sous `carte.sh`), worktree `manon-c5b` a77eb722, .so compilé par la chaîne (`kernels-d409deb4a317`, `kv_write_int8_kernel` 34 regs, 0 pile) ; (1) tests à sec 19 passed ; **tests carte : l'étape posait `ACVRAM_TYPE=test`, refusé par `carte.sh` (rc 64) — rejoués à la main sous `mesure` 01:11** (`tests-carte.log`) ; (2) `ppl-decode-kv-17-09.py` préfixe 8 192 + 2 048 notés, 3 tranches privées Coder, bras jeton / canal / bf16 ; (3) `certifie-b12.py` A B A B (A défaut, B canal) ; (4) `capture-godets.py` sous canal {1, 2, 8, 12, 16} ; prise 00:51-01:10, éco 2 700 posé par le moteur (`eco=2700(2685)`), hôte propre ; copies sur manon `scratchpad/c5b-19-09/`
scellé (Sage, sage-c5-kv-int8-faux § 3 recopié en tête de chaîne, et sage 01 h 00 lecture à trois états, avant) : ΔPPL canal contre bf16 (3 tranches, géo) ≤ +0,002 ET pas b=12 canal ≥ défaut − 1 % ET capture 5/5 → défaut ; tenu si point ≤ +0,2 % ET SE ≤ 0,2 % ; SE > 0,2 % → indécidable, opt-in avec borne ± 2 SE ; prédiction Océane : canal ≤ +0,15 %, pas −1 à −3 % possible (issue gênante)
mesuré : (1) carte **14 passed, 2 failed** : `test_kv_canal_c5b_carte.py::test_ecriture_prefill_un_par_un_et_decoupe_au_bit_contre_le_jumeau` (`AssertionError: un par un ligne du bloc 2`, `:73`) et `test_attn_paginee.py::test_triton_egale_le_noyau_cuda_actuel` (`([37, 0, 300], 0, 1, 1.83)` : 1 élément hors 2⁻⁸) — **ce second échec est aussi sur l'arbre manon sans C5-b** (`1 failed, 6 passed`, 01:12) : antérieur, pas de C5-b ; (2) bf16 [13,2572 ; 11,4176 ; 11,2466] ; jeton [13,3313 ; 11,5348 ; 11,2809] = [+0,56 ; +1,03 ; +0,30] %, géo **+0,630 %**, SE 0,211 % ; canal [13,2687 ; 11,4557 ; 11,2684] = [+0,09 ; +0,33 ; +0,19] %, géo **+0,205 %**, SE **0,071 %**, IC ± 2 SE [+0,06 ; +0,35] ; canal − jeton [−0,47 ; −0,69 ; −0,11] % ; blocs 16 640 → 17 156 o (+3,1 %), capacité 38 016 → 36 880 jetons ; (3) A1 **8,595 ms · 1 396 t/s · 0,280 J/j · 391 W** (sm 2 549 moy), A2 8,597 · 1 396 · 0,281 ; **B1, B2 : `REFUS : 1 sequence(s) tronquee(s) par budget KV epuise`, `kv_budget=23824/12`** (A : 24576/12) ; (4) capture canal 5/5 : 4,48 / 6,09 / 7,54 / 8,50 / 10,15 ms
verdict : **PPL FAUX de justesse (+0,205 contre ≤ +0,200, SE 0,071 : la règle dit faux, l'intervalle contient le seuil)** — la prédiction « ≤ +0,15 % » est manquée mais la thèse tient : **les canaux de K portaient les deux tiers du coût int8** (+0,63 → +0,21 %) ; le tiers restant est ailleurs (V, ou l'échelle E4M3 elle-même). **Vitesse non jugée** : le planificateur KV n'est pas averti du format (REGLES § 6, le régime doit porter la taille de bloc) — sans cela le pas b=12 sous canal ne se mesure pas au budget nominal ; défaut de plan, pas de noyau. **Test carte faux** sur l'écriture prefill un par un au bloc 2 : le noyau et son jumeau divergent sur la découpe d'un bloc entamé — à corriger avant tout défaut. Reste **opt-in** avec sa borne publiée
suite : Océane : (a) `test_ecriture_prefill_un_par_un…` : lire l'assertion « ligne du bloc 2 » (bloc entamé puis prolongé un jeton à la fois) ; (b) planificateur KV : octets par bloc selon `KV_INT8_CANAL` (17 156) dans `Plan.kv_max_tokens` ; (c) `chaine.sh` : `ACVRAM_TYPE=mesure` pour les tests carte ; puis rejouer (3) seul (10 min) ; Sage : 9 tranches à la reprise si la borne doit se resserrer sous 0,2 % ; `test_triton_egale_le_noyau_cuda_actuel` faux sur main (1 élément 1,83 > 2⁻⁸ à lens [37, 0, 300]) — à qui de droit (poste E) ; Jérôme : indexer ; ma file : sonde témoin niveau 2 (Océane 6354dfc5, 2 min) → rang 3 niveau 3 Coder (45 min) quand Sage a tranché le scellé nœuds

## Rejouable
`cd ~/travail/manon-c5b && PYA=<python venv> bash scratchpad/c5b-carte-19-09/chaine.sh` (20 min, .so en cache) ; tests carte : `ACVRAM_TYPE=mesure outils/carte.sh <python> -m pytest tests/test_kv_canal_c5b_carte.py tests/test_attn_paginee.py -q`.

## Addendum 20/09 03:42 — d3a07614 (trois correctifs d'Océane) : **les deux tests rejoués sont verts** — carte `test_kv_canal_c5b_carte.py` **9 passed** (dont `test_ecriture_prefill_un_par_un_et_decoupe_au_bit_contre_le_jumeau`, faux à 01:11 : `_memes(ecrites)` ne compare plus la ligne LIFO recyclée) ; à sec `test_kv_canal_budget_c5b.py` **3 passed** (`test_octets_par_jeton_egalent_le_bloc_du_format` 16 640 / 17 156, `test_le_surcout_canal_est_celui_du_bloc`) et `test_kv_canal_c5b.py` 9 passed ; prise `carte.sh` 03:41 (`tests-carte-d3a07614.log`), .cu inchangé depuis a77eb722 → **C5-b peut se fusionner en opt-in** (Sage) ; le terme vitesse (certifie B au budget 17 156 o/bloc) reste pour la reprise

## Addendum 20/09 05:45 — terme vitesse (rang 4 de la clôture, d3a07614, budget KV corrigé) : **FAUX, et de loin — canal 12,28 / 12,29 ms/pas · 977 t/s · 0,406-0,408 J/j contre défaut 8,57 / 8,57 · 1 400 · 0,281-0,282 : +43 % de temps, +45 % de joules** (scellé : canal ≥ défaut − 1 % ; prédiction Océane −1 à −3 %)
instrument : chaîne C5-b `PARTIES=3` (certifie-b12 A B A B, 12 séquences, ctx 2 048), worktree `manon-c5b` d3a07614 (.so en cache d409deb4a317), 05:39-05:44, éco 2 700 posé par le moteur (2 677-2 692), 393-398 W (plafond), hôte propre ; B servi cette fois sans refus (budget 17 156 o/bloc pris en compte)
scellé (Sage sage-c5-kv-int8-faux § 3, en tête de chaîne, avant) : pas b=12 canal ≥ défaut − 1 % ; issue gênante nommée par Océane : −1 à −3 % (lecture par la variante CUDA CANAL au lieu du Triton du poste E)
mesuré : A1 **8,569 ms · 1 400,4 t/s · 0,281 J · 393,5 W** ; B1 **12,278 · 977,3 · 0,406 · 397,1** ; A2 8,574 · 1 399,6 · 0,282 · 394,5 ; B2 **12,291 · 976,4 · 0,408 · 398,0** ; régime B `kv=int8-canal16` ; les deux paires reproductibles à 0,1 %
verdict : **FAUX** — le chemin canal ajoute **3,7 ms par pas** à b=12 : ce n'est pas « −1 à −3 % », c'est la lecture des clés par canal qui coûte autant qu'un tiers du pas (l'échelle par canal se multiplie sur `q` par bloc de 16 comme prévu, mais le noyau qui le fait n'est pas celui du poste E : `+43 %` = un noyau non tuilé ou un repli) ; à ce prix, C5-b reste **opt-in pour la seule qualité** (+0,205 % de PPL contre +0,63 % par jeton) et ne peut pas servir ; la fiche « +0,4 % d'instructions » est réfutée par un facteur 100
suite : Océane : nsys d'un pas B contre A (quel noyau porte les 3,7 ms : `paged_attention` variante CANAL, ou un repli torch — la ligne de régime le dirait si elle nommait le noyau d'attention) ; Sage : C5-b = opt-in qualité, hors défaut vitesse, à la reprise ; Jérôme : indexer ; ma file : rang 6 (vLLM KV fp8 ppl-decode-kv)

Rejouable : `cd ~/travail/manon-c5b && PARTIES=3 PYA=<python> bash scratchpad/c5b-carte-19-09/chaine.sh` (5 min).

## Addendum 20/09 08:39 — hygiène : nsys d'un pas b=12 (Coder, 50 pas, graphes ON) défaut contre canal : **noyaux 6,55 → 7,59 ms/pas (+1,04)** — le canal lit l'attention par `paged_attn_partial_kernel` CUDA **1,156 ms** + `paged_attn_reduce` 0,078 au lieu du Triton fusionné `_partiel_reduit` 0,446 (**+0,79 ms**), ajoute 3 noyaux élémentaires torch par couche (144/pas, **+0,22 ms**) et `kv_canal_plan` 0,07 ; Marlin −0,10 (bruit) ; **les 2,7 ms restants des +3,7 ms du certifie ne sont pas des noyaux** (hôte, synchronisations ou espaces entre nœuds : à voir en `--cuda-graph-trace=node` + osrt)
instrument : `decode-nsys.py` (b=12, 50 pas, invite 256) sous `nsys -t cuda --cuda-graph-trace=node`, arbre manon 3f679f26 (C5-b opt-in dans main), `ACVRAM_KV_INT8_CANAL=0|1`, prises `carte.sh` 08:37 (2 × 30 s) ; CSV `scratchpad/c5b-19-09/nsys/{defaut,canal}_cuda_gpu_kern_sum.csv`
scellé (Sage, hygiène « quel noyau porte les 3,7 ms ») : nommer le noyau ; issue : rien dans les noyaux → hôte
mesuré : défaut 327,4 ms / 50 pas = **6,55 ms/pas** ; canal 379,3 = **7,59** ; différences par pas : `paged_attn_partial_kernel<128, bf16>` +1,156 (48/pas, absent du défaut), `_partiel_reduit_kernel` −0,446 (absent du canal), `elementwise_kernel` +0,224 (144/pas), `nvfp4_gemv_marlin` −0,097, `kv_write_int8` −0,088 ↔ `kv_canal_write` +0,088, `paged_attn_reduce` +0,078, `kv_canal_plan` +0,070
verdict : **nommé pour un tiers** — la variante CUDA « CANAL » de l'attention paginée coûte 2,7 × le Triton fusionné (1,23 contre 0,45 ms/pas) : c'est la prédiction d'Océane (« −1 à −3 % possible ») mais × 4 ; le reste (+2,7 ms/pas au certifie) est hors noyaux et c'est la plus grosse part — la ligne de régime dit `graphes=on(hybrides≤12)` pour les deux, donc pas un repli eager : à chercher dans les espaces entre nœuds (3 élémentaires + plan + write par couche = 5 nœuds de plus × 48 = 240 nœuds/pas, ≈ 0,25 ms à 1 µs) et dans une synchronisation hôte que `kv_canal_plan` pourrait imposer (`.item()` sur le plan ?) — l'osrt le dirait
durée : 2 prises 1 min 10 s (08:36:48-08:37:58) ; rédaction 3 min
suite : Océane : (1) port de la lecture par canal dans `_partiel_reduit` (Triton) au lieu de la variante CUDA ; (2) `kv_canal_plan` : y a-t-il un `.item()`/`.tolist()` par couche ? (nsys osrt, 1 min) ; Sage : C5-b reste opt-in qualité ; Jérôme : indexer ; ma file : C14-b P1 au pointeur
