# Verdict A/V — cellule b=12 vraie alternance : acvram DERRIÈRE vLLM de 8,3 % aujourd'hui, la référence figée 1 596 (21/09) est contredite — poste4, 22/09, sans ménagement

instrument : relecture `verdict-cellule-b12-av-vraie-vllm-22-09.md` (poste2 1f2b968c), `chaine-cellule-b12-vraie-vllm.sh` (le mien, 22/09), `scratchpad/decode-vllm-17-09.py` (harnais T1 du 21/09) ; 0 min de carte
commit : main à jour (5d541f8a)
régime : A1 V1 A2 V2 A3 V3, vLLM encadre chaque paire, b=12, checkpoint identique à T1 (`Qwen3-Coder-30B-A3B-Instruct-FP4-a16`)
scellé : médiane acvram ∈ [1590;1660], réfuté < 1596 — **scellé écrit contre un plancher absolu, pas contre vLLM lui-même** : c'est le trou de méthode que ce verdict corrige
mesuré : **acvram médiane 1 634,0 t/s (A1 1604,3, A2 1641,2, A3 1634,0) — vLLM médiane 1 782,0 t/s (V1 1895,6, V2 1773,7, V3 1782,0) — ratio acvram/vLLM = 0,917, acvram DERRIÈRE de 8,3 %.** Horloges par paire 3,5/4,4/2,3 %, les deux bras au plafond de puissance (`bridages: puissance`, comme trtllm hier) : comparables, publiées avec MHz/W, pas un rejet REGLES §3 (même raisonnement que la cellule trtllm du 22/09). vLLM passe de 1 596,1 (T1, 21/09) à 1 782,0 (ici) : **+11,65 % ≈ le « +12 % » nommé.**
verdict : **le scellé « TENU » de poste2 est correct sur SON critère (≥1596) mais trompeur lu seul** : il compare acvram à une référence figée d'hier, pas au vLLM réellement mesuré aujourd'hui dans la même fenêtre — **acvram est derrière vLLM de 8,3 % sur cette cellule, sans ambiguïté.** Cause du +12 % de vLLM à isoler avant toute conclusion sur acvram : **T1 (21/09) lançait `decode-vllm-17-09.py` — un script Python autonome (`LLM().generate()` en boucle, API offline batch)** ; **cette cellule (22/09) lance `vllm serve` (serveur HTTP OpenAI, ordonnancement continu) avec `banc-llamacpp-16-09.py` en client** — DEUX chemins de code vLLM différents, pas la même mesure malgré le même checkpoint. Le backend d'attention n'est PAS la variable (les deux forcent TRITON_ATTN pour Coder, `decode-vllm-17-09.py:64`). L'ordonnancement continu du serveur HTTP (moins de synchronisations bloquantes qu'une boucle `generate()` séquentielle) est le candidat le plus probable pour expliquer un vLLM plus rapide EN SERVI qu'en offline — mais ce n'est qu'un candidat, pas vérifié ici.
durée : 0 min de carte
suite : chef : publier « acvram derrière vLLM de 8,3 % à b=12 (22/09) », retirer/corriger toute mention « devant vLLM » qui s'appuyait sur 1596,1 ; poste2/poste1 : rejouer T1 avec **`vllm serve` (même chemin de code que cette cellule)** pour savoir si le +12 % est un effet de harnais (offline vs servi) ou un vrai gain vLLM depuis le 21/09 — sans ce rejeu, la cause reste nommée, pas tranchée ; objectif b=12 (devant vLLM) NON atteint en l'état, à rouvrir comme chantier.
