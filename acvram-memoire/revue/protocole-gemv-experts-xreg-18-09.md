# Protocole — dernier geste GEMV experts (Laurine 07cc4b8 : `nvfp4_gemv_grouped_xreg/_gateup_xreg`, x en registres par tranche, LDS.128 ; `ACVRAM_GROUPED_XREG=1`, défaut 0 témoin ; sage-gemv-experts-dernier-geste-18-09)

instrument : (1) `pytest tests/test_gemv_experts_v2.py` (compilation réelle, cas `test_xreg_egale_v1_au_bit`) ; (2) passe ncu bornée = commande de `outils/ncu_gemv_experts_rpw_18-09.sh` (07cc4b8 : 17 métriques, filtre 4 noyaux, `--launch-skip 192 --launch-count 96`, `banc_decodage_moe.py gemv 12`) rejouée par `scratchpad/xreg-18-09/chaine.sh` avec le régime posé dans un wrapper cible (`py-xreg{1,0}.sh` : classé + rpw=4 + XREG) — `sudo -n env …` du script est refusé par sudoers (seul `/usr/local/cuda-13.4/bin/ncu` est NOPASSWD), vérifié `rc=1` ; `LC_ALL=C` posé dans le wrapper ; témoin XREG=0 dans la même fenêtre ; arbre laure = main 4b83a2f + laurine 07cc4b8 (30299a3).
scellé (Sage) : porte gateup + down (médianes ncu × 48) ≤ 6,2 ms/pas tenu, > 6,2 faux → chantier fermé à 1 262 ; tenu → in situ Coder b=12 nu ≥ 1 300 (Laurine ≈ 1 330) puis fermeture. Prédiction Laurine : mio/short_scoreboard ÷ 3, occupation 6 → 3-4 blocs, gateup 87,9 → 68-76 µs, down 65 → 55-60, somme 5,9-6,5. Témoin attendu (rpw=4, XREG=0, même passe) : 87,9 + 65,2 = 7,35.
