# Protocole — P0 prefill (Laurine 9cb0294, revue/p0-prefill-int8-colle-a-sec-18-09) : GEMM W8A8 `ACVRAM_PREFILL_INT8=a8` et colle MoE Triton `ACVRAM_COLLE_MOE=triton`, Coder 2048 — trois bras (Sage), suites GPU d'abord, cache Triton vidé

préalables (Sage, REGLES §6-7) : (0) la chaîne attend `load1 < nproc/2` (16) ; (1) `pytest tests/test_gemm_w8a8.py tests/test_colle_moe.py` SUR CARTE avant tout bras ; (2) `~/.triton/cache` vidé avant la mesure (817 entrées, 310 Mo au 18/09 13h15) ; arbre laure = main + laurine 9cb0294 + oceane-11 (07567a2).
instrument : `scratchpad/p0-prefill-18-09/chaine.sh` — pour chaque bras : `prefill-glm-acvram-15-09.py w4a16 2048` (7 rép., médian j/s) puis `profil-pas-coder-17-09.py … prefill` (postes GPU) ; bras A témoin (bf16 / torch = défauts), bras B `PREFILL_INT8=a8`, bras C `a8 + COLLE_MOE=triton` ; régime classé, défauts du jour, `regime_ligne()` en tête de chaque JSON ; puis PPL privé Coder 3 tranches (`ppl-acvram-17-09.py`, `corpus-prive/tranches-coder`, × bf16 12,462 / 11,269 / 11,508, témoin off 17/09 1,0148) sur le bras retenu (C si les deux gestes tiennent, sinon B).
scellés (Sage / Laurine, prédictions séparées à venir de Laurine avant la fenêtre) : a8 ≈ −20 ms (int8_dequant 11,2 → 0, cutlass 19,4 → 9-11) ; colle ≈ −6 ms (8,3 → 2-3) ; total 197 → ~171 ms ≈ 12 000 j/s : **tenu ≥ 11 000 j/s, faux < 11 000** (→ lire le profil) ; colle : si elle rend < 3 ms, « moins de lancements n'est pas un gain » (REGLES §4), pas de défaut ; PPL privé du bras retenu **≤ 1,020 tenu** (prédiction ≤ 1,017 ; faux > 1,020 → A8 par groupe de 128). Le défaut = le bras qui tient les deux juges.
addendum (Laurine 81cf5d9, prédictions séparées, noms des bras T/A/B) : T témoin 197 ± 2 ms ; A a8 seul −17 à −23 ms ⇒ 11 400-11 800 j/s ; B a8 + colle −5 à −7 ms de plus ⇒ 168-174 ms, 11 800-12 200 j/s. Verdicts : P0 sur B ≥ 11 000 ; colle en défaut seulement si B − A ≥ 3 ms ; a8 en défaut si A − T ≥ 15 ms ET PPL privé ≤ 1,020. Issue gênante : A − T ≈ −11 (déquant seule, GEMM int8 Triton pas plus rapide que cutlass bf16 → profil avant toute ligne). Ordre de la fenêtre : suites GPU → cache vidé → une chauffe hors chrono (JIT) → T, A, B avec profil chacun → PPL 3 tranches sur le bras retenu.
