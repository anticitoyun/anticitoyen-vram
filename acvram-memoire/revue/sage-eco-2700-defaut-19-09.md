# Sage — éco 2 700 par défaut (décision utilisateur, 19/09 20 h 22) : le serveur pose le verrou d'horloge pour sa durée de vie et le rend à l'arrêt, la ligne de régime porte demandé ET effectif, six cellules à remesurer au défaut courant, .deb 0.6.18 (19/09, 20 h 24, heure du commit)

Source : Jérôme 22 h 50 (utilisateur : « oui, éco 2700 par défaut ») ; `verdict-eco-lgc-b12-19-09` (E1), `verdict-eco-lgc-b1-genou-19-09` (E1-bis : genou faux, pas de gouverneur) ; `chantier-c8-19-09` (relâchement à 30 s, opt-in, preuve carte non faite) ; `acvram eco {2700|2100|off}` (Océane, à sec) ; `sage-c16bis-puissance-mesure1-19-09` § 3 ; REGLES § 1 (sonder un réglage = toucher la carte), § 6 (tout lanceur de serveur prend le verrou).

## 1. Conception — quatre réponses
1. **Qui pose 2 700 : le processus qui sert.** `acvram serve` (et `certifie`, `capture-godets`, tout instrument qui charge un moteur) pose `sudo -n nvidia-smi -i <carte servie> -lgc 2700,2700` **après** avoir pris le verrou de carte et avant le premier chargement ; il rend `-rgc` dans `Engine.fermer`, à SIGTERM/SIGINT et en `atexit` — le verrou d'horloge suit la durée de vie du serveur, pas celle du système (ni postinst, ni unité systemd, ni crochet de session). **Pas de relâchement à l'oisiveté par défaut** : un cycle rendre/reposer change le régime des premiers jetons après une pause (rafale à 3 000 MHz, puis 2 700) et coûte un aller-retour sudo par réveil ; C8 (30 s) reste opt-in `ACVRAM_GOUVERNEUR=1` jusqu'à sa preuve carte. Le genou est faux (E1-bis) : **2 700 à tous les lots**, 2 100 reste régime nommé « éco fort » sur demande.
2. **Sans le droit sudo, on sert quand même, mais bruyamment** : la ligne de régime porte toujours `eco=<demandé>(effectif <MHz lus dans nvidia-smi -q -d CLOCK>)` ; `demandé ≠ effectif` (refus sudo, `-lgc` non pris) est un état nommé, **jamais silencieux** : `acvram doctor` le dit, le serveur l'imprime au démarrage, et **`certifie` / `capture-godets` refusent de publier** une cellule dont l'éco demandé n'est pas effectif (même garde que « repli eager vu »). Rien ne bloque le service : un refus de démarrer pour une horloge serait pire que l'horloge libre.
3. **Configuration** : `config.json` `"eco": "2700"` par défaut (`"2100"`, `"off"` acceptés) ; `acvram eco off` l'écrit et rend l'horloge tout de suite si un serveur tourne ; variable `ACVRAM_ECO` dans `regime.VARIABLES` pour les bras A/B seulement, jamais pour le service. Tests à sec : faux `nvidia-smi` (script) qui enregistre les appels — pose au démarrage, rendu à `fermer`, rendu sur signal, refus → état nommé ; l'épreuve doit casser si on retire le `-rgc` de `fermer`.
4. **Paquet** : `.deb` **0.6.18** — `/etc/sudoers.d/acvram-nvidia-smi` restreint aux deux formes exactes (`nvidia-smi -i * -lgc *,*` et `-rgc`), `visudo -c` dans postinst, retiré à la purge ; `install.sh` imprime la ligne au lieu de l'installer (arbre de dev : acte de l'utilisateur) ; `acvram doctor` vérifie le droit ET l'effet (`-lgc` puis lecture) sous le verrou ; GUI : sélecteur éco (2700 / 2100 / off) dans les options. Un artefact par numéro, jamais installé par une session.

## 2. Cellules — ce qui se remesure, avec la prédiction et l'issue qui gênerait
Les lignes éco publiées sont sur 0.6.13 (avant P2, avant tf32) : **le défaut 0.6.18 = éco 2 700 sur le défaut courant**, six cellules au harnais égal (ctx 1 024, cartes `[0]`, en-tête avec plafond, horloge SM moyenne, `eco=2700(effectif 2700)`), et `certifie` pour les cellules moteur, chacune deux passes :

| cellule | défaut off (publié) | prédiction éco 2 700 | issue qui gênerait |
|---|---|---|---|
| Coder b=12 t/s · J net | 1 365 · 0,2243 | **1 340-1 365 · 0,200-0,207** | J > 0,215 : E1 ne se transporte pas à P2 |
| Coder b=1 | 363 · — | **350-360 · −25 % J** | < 340 (llama.cpp) |
| Coder prefill j/s · J | 18 850 | **17 000-18 300 (−3 à −10 %) · −10 à −15 % J** | < 16 400 : le prefill, borné par le calcul, paie l'horloge — à porter à l'utilisateur avec le chiffre, le défaut est le sien |
| GLM b=12 | 725 · 0,389 | **700-725 · 0,34-0,36** | J > 0,38 |
| GLM b=1 | 113 · 1,37 | **108-113 · −20 % J** | < 105 |
| GLM prefill (tf32) | 7 188 · 0,0466 | **6 600-7 100 · −10 % J** | < 6 500 |

PPL : ne dépend pas de l'horloge — contrôle d'une ligne, une tranche Coder sous 2 700 identique à la quatrième décimale (REGLES § 4 : « la perplexité survit… », vérifié ici pour l'horloge) ; si elle bouge, un noyau choisit autrement selon l'horloge et tout le comparatif PPL est à rejouer. Comparatif : chaque ligne « défaut 0.6.18 » porte « éco 2 700 » dans son nom ; les lignes « off » restent publiées comme régime nommé.

## Addendum 23 h 20 — première cellule et un défaut de ligne
Prefill Coder (i8c, défaut P2) sous `-lgc 2700` (SM 2 692 MHz relevé) : **17 853 / 17 716 → 17 784 j/s (−5,7 % sur 18 850), J net 0,0152 / 0,0160** (Manon) — dans la fourchette (17 000-18 300), au-dessus de 16 400 : **rien à porter à l'utilisateur, le défaut éco tient sur le prefill**. Défaut d'instrument vu par Manon : la ligne de régime imprimait `horloge=libre` sous `-lgc 2700` — elle lit l'état que `acvram eco` a mémorisé, pas la carte. C'est le cas « demandé ≠ effectif » du § 1.2 à l'envers (effectif sans demande) : la ligne doit lire **la carte** (`nvidia-smi -q -d CLOCK`, verrou d'horloge et SM courant), jamais une mémoire d'acvram ; jusqu'au correctif, l'en-tête `nvidia-smi` de chaque bras fait foi (les cinq cellules restantes le portent) et aucune cellule ne cite la ligne pour l'horloge. À corriger dans le lot éco d'Océane, avec son test (faux `nvidia-smi` rendant 2 700 → la ligne dit 2 700 sans qu'`acvram eco` ait été appelé).

## Addendum 23 h 45 — les six cellules (`verdict-eco-2700-defaut-19-09`, Manon 02997d0) : le défaut tient, deux prédictions à corriger
| cellule | off (publié) | **éco 2 700** | verdict |
|---|---|---|---|
| Coder b=12 | 1 365 · 0,2243 | **1 341 · 0,2005** | tenu (−1,8 % · −10,6 %) → **devant llama.cpp (1 065 · 0,2136) et vLLM (1 198 · 0,202) en vitesse ET en énergie à b=12** |
| Coder b=1 | 363 | **354 · 0,446** | tenu (llama.cpp 340 · 1,151) |
| Coder prefill | 18 850 | **17 784 · J −2 %** | t/s tenu ; **ma prédiction « J −10 à −15 % » est fausse** : borné par le calcul, le prefill rend du temps à horloge basse, pas des joules (J ≈ constant sous plafond) — à écrire dans MECANISMES à côté de « sous plafond, W est une constante » |
| GLM b=12 | 725 · 0,389 | **689 · 0,304** | J tenu (mieux que 0,34-0,36) ; t/s **faux de 1,5 %** (< 700) : reste faux, le défaut ne dépend pas de cette ligne ; vLLM 858 · 0,397 : derrière en vitesse (−20 %), devant en énergie (−23 %) |
| GLM b=1 | 113 · 1,37 | **106,3 · 0,999** | t/s **faux de 1,6 %** (< 108, une passe 104,8) : le pas b=1 GLM est borné par la latence (`mla_1p`, C14) et la latence suit l'horloge — c'est le prix d'éco sur ce pas, C14 le rend |
| GLM prefill | 7 188 · 0,0466 | **7 268 · 0,0435** | +1 % t/s, au-dessus de ma fourchette (6 600-7 100) : la même physique que C16-bis, le verrou supprime l'oscillation |

PPL Coder au bit sous 2 700 : le contrôle d'une tranche tient, l'horloge ne choisit pas de noyau. **Non prédit et à nommer avant toute revendication** : `certifie` Coder b=12 **8,37 ms · 384 W sous 2 700 contre 8,97 · 399 W libre** (+7 %, −4 % W → J −11 %), alors que le harnais égal rend −1,8 % : 7 % entre les deux instruments à 2 700, 2 % à horloge libre — contexte (256→2 044 contre 1 024), durée de fenêtre ou HTTP : une passe de Manon (10 min, même contexte dans les deux) avant que « +7 % » n'entre dans une ligne ; d'ici là le comparatif porte le harnais égal seul. Décision : **le défaut éco 2 700 est confirmé par les cellules** (J −10 à −27 % à ≤ 2 % de vitesse, prefill −5,7 %) ; 0.6.18 s'installe après la vérification carte à trois bras (dont la lecture d'horloge). Revendication b=12 Coder réécrite par Jérôme sur ces chiffres : devant les deux concurrents en vitesse et en énergie ; GLM : devant en énergie et en qualité, derrière en vitesse.

## Ordre
* **Océane** — code § 1 (serve/instruments : pose, rendu, état nommé, `config.json`, `doctor`, tests à sec avec faux `nvidia-smi`), ≤ 1 h 30, commit + pointeur ; **avant C17** ; C14/C15 sous-agents continuent ; **plus l'addendum 23 h 20** (la ligne lit la carte).
* **Manon (23 h 45)** — vérification carte 3 bras ; puis l'écart `certifie`/harnais à 2 700 (10 min, contexte égal) ; statics, NARROW_GEMM.

## Addendum 00 h 05 — le « +7 % » est mort, et il laisse deux choses : une règle sur l'horloge libre, une charge hôte à sortir des fenêtres
`verdict-ecart-certifie-harnais-19-09` (Manon def8fd4, même prise, ABAB) : `certifie` 2 700 = **1 430 / 1 438 contre libre 1 460 (0,982 ×)** ; le « +7 % » comparait deux heures — le certifie **libre** va de **1 337 (20 h 19, carte chaude oscillant au plafond, 8,97 ms · 399 W) à 1 460 (20 h 47, 8,22 ms)**, quand 2 700 rend 1 430-1 438 trois fois. Lecture, et règle pour REGLES § 4 : *à horloge libre sous plafond, une cellule dépend de l'état thermique de la carte — 9 % d'étendue en 28 minutes sur le même arbre ; une cellule libre porte température et minutes depuis la dernière charge, et seules les cellules au défaut 2 700 (étendue 0,5 %) se comparent entre elles.* C'est la seconde raison du défaut éco, après les joules : la reproductibilité. L'écart propre `certifie`/harnais reste 5-7 % (HTTP/SSE, `nvidia-smi` en sous-processus) : deux instruments étiquetés, jamais mélangés — rien à corriger.
**Charge hôte** : un indexeur `leann` à 383 % CPU depuis 20 h 38 (hôte, pas carte) a touché le harnais HTTP (1,15 × d'écart) et les passes p2 de GLM (charge 5-6 : 678 / 104,8 contre p1 propres 700 / 107,8). Le garde de `energie.py` (load1 > nproc/2 = 16 sur 32 fils) ne voit pas un processus à 4 cœurs : **REGLES § 2, une ligne** — l'en-tête publie les trois premiers consommateurs CPU hors instrument (`ps -eo pid,pcpu,comm --sort=-pcpu | head -4`), et un consommateur > 100 % pendant une fenêtre HTTP invalide la cellule de temps (la PPL survit, REGLES § 4). Conséquences : (1) **Jérôme identifie et arrête l'indexeur** (quatre `leann_mcp` vivent, un par session : c'est une reconstruction d'index lancée par quelqu'un — pas de `leann build` ni `reconstruire.sh` pendant une fenêtre, à écrire) ; (2) **les deux cellules GLM ne sont pas publiables en l'état** (une passe propre, une sous charge : une cellule = deux passes du même travail, REGLES § 4) → Manon rejoue p2 de GLM b=12 et b=1 (5 min) une fois l'hôte propre ; d'ici là le comparatif GLM porte « p1 seule, sous réserve » ; les verdicts (faux de 1,5 / 1,6 %) restent écrits tels quels. `eco etat` : première lecture froide 225 → 1 980 → lire jusqu'à stabilité (Océane, transmis).
* **Jérôme (23 h 45)** — comparatif « défaut 0.6.18 = éco 2 700 » avec la table ci-dessus et les revendications réécrites ; MECANISMES : « le prefill borné par le calcul rend du temps, pas des joules » ; ETAT ; INDEX ; commit + push.
* **Manon** — après le ncu du tampon C1 : les six cellules § 2 avec `acvram eco 2700` sous le verrou dès maintenant (le code du serveur n'est pas requis pour mesurer : la ligne de régime lit déjà l'horloge), plus le contrôle PPL d'une tranche ; verdict avec la table ; `-lgc`/`-rgc` uniquement sous `carte.sh`.
* **Jérôme** — ETAT + REGLES § 1 (décision utilisateur, mot pour mot, heure machine) ; à la fusion d'Océane : .deb 0.6.18 (sudoers, doctor, GUI), comparatif « défaut 0.6.18 = éco 2 700 » avec les six cellules, revendications réécrites sur ces chiffres ; **si le prefill Coder tombe sous 16 400, le dire à l'utilisateur avec le chiffre** avant de publier le défaut ; INDEX ; commit + push.
