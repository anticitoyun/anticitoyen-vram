# Verdict — C corrigé (186177a) : reproduction b=12 passe, passe de capture {1, 2, 8, 16} sans erreur, **Coder b=12 sous E + C mixte : 997,8 t/s · 0,400 J** (+ 12,4 % sur E seul 887,7 ; + 34 % sur 743,4) ; b=1 inchangé 257,8

instrument : `certifie-b12-15-09.py` (reproduction 40 s puis rondes ×2, ctx 2048, invite 256, budget = slots) ; `scratchpad/capture-godets-17-09.py 1,2,8,16` (un moteur par godet : warm_graphs + prefill + 20 pas, ok ⇔ graphes=on et lot = b) ; `outils/banc-gemm-etroit-17-09.py` — `scratchpad/coder-c-fix-17-09/`
commit : arbre poste3 0ea0d03 (= main 186177a : masque `g < NG` sur x/q/s/z + assert `scales.shape == (N, ng)`, 13/13 verts ; cause : 16 groupes en 6 tranches de 3 = 18 → lectures hors tenseur, silencieuses au banc, fatales sous capture) ; défaut encore `cuda` (`regime.py:57`), `ACVRAM_NARROW_KERNEL=mixte` posé par l'appelant et nommé dans les JSON (`dense=triton≥2|cuda`)
régime : classé `ACVRAM_MOE_MMA=0 ACVRAM_MOE_DECODE_MMA=0 ACVRAM_NARROW_GEMM=1`, E défaut (`triton`), C `mixte` ; moteur `NOMINAL graphes=on 0/48 exilées`
scellé : poste4 : aucune erreur CUDA, sortie = cuda ± 2⁻⁸, dense b=12 ≈ 2,1 ms/pas ; moi (protocole E+C d'avant) : b=12 920-1 000 t/s, 0,42-0,46 J
mesuré : reproduction b=12 : **rc 0, 977,2 t/s** (le plantage de b34a1bb ne se reproduit pas) · passe de capture : godets **1 / 2 / 8 / 16 : ok** (4 captures chacun, graphes=on, lot = b ; 3,62 / 5,32 / 7,74 / 12,26 ms/pas) · rondes b=12 : **997,6 et 997,9 t/s, 0,4001 / 0,3989 J** (12,03 ms/pas, 21 444 jetons, 0 troncature) · banc étroit sous env `mixte` : cuda 2,028 = Triton 2,031 (le bras « cuda » du banc suit la variable d'environnement — non discriminant sous `mixte`, à corriger : forcer `cuda` dans le bras témoin ; la mesure discriminante reste celle de 9d384e5 : 4,11 → 2,07)
verdict : **condition 2 tenue : aucune erreur de capture sur les quatre godets, la ronde b=12 passe et rend 997,8 t/s · 0,400 J — dans ma fourchette 920-1 000, énergie sous la fourchette. Par rapport à la cellule publiée ce soir (887,7 · 0,449, E seul), C ajoute + 12,4 % de débit et − 11 % de J ; par rapport à ce matin (743,4 · 0,538), E + C font + 34 % et − 26 %. `mixte` peut revenir défaut dans son commit à part ; cellule à publier : b=12 **997,8 t/s · 0,400 J** (E + C), b=1 **257,8 · 1,214** inchangé (C = cuda à b=1). Leçon d'instrument : le banc de noyau doit forcer son bras témoin (`cuda`) indépendamment de la variable de régime, sinon il compare le noyau à lui-même.**
