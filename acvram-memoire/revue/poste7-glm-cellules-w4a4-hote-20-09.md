# poste7 — cellules GLM propres : b=12 660 ± 20 (à publier avec sa dispersion), b=1 servi 112,7 (+6 %, le pas servi porte 3 ms hors GPU : à nommer), L=8 192 1 905 j/s ; et une conséquence du verdict C17 : mma2 est W4A4 par construction, donc GLM sert aujourd'hui 13 couches en A4 à b ≥ 5 — mesure de PPL au lot de 12 avant les concurrents (19/09, 23 h 15, heure du commit)

Source : `verdict-glm-propres-19-09` (poste2, main cadfb52b) ; fiche C17 fermée (poste1 fe4168b7 : hypothèse (b) = `_forward_grouped_mma` quantifie l'activation en E2M1) ; `acvram_kernels.cu:2965` (`mma.sync…kind::mxf4nvf4…e2m1.e2m1` : les deux opérandes en e2m1) ; `verdict-budget-decode-glm-19-09` (`nvfp4_quant_act` ×39 à b=12) ; `verdict-w4a4-a-sec-19-09` (A4 fermé au prefill : +0,010/+0,013) ; `verdict-c17-19-09` (+2,2 % au lot de 12 sur 48 couches).

## 1. Cellules, telles quelles
| cellule (servi, 2 700, hôte propre) | mesuré | lecture |
|---|---|---|
| GLM b=12 | **657,6 · 0,320 net** (660,5 / 654,7) ; d'une heure à l'autre **660 ± 20** | ma « 700-725 » fausse deux fois ; la cellule se publie **avec sa dispersion** (± 3 %), Coder n'en a que 0,5 % : le pas GLM varie avec le routage (26-64 distincts par pas) — c'est un fait du modèle, à écrire dans le comparatif ; contre vLLM 858 · 0,397 : −23 % en vitesse, −19 % en J |
| GLM b=1 (C14 au défaut) | **112,7 · 1,075** (112,6 / 112,8) | +6 % sur 106,3 quand le noyau rend −12 % en processus : **le pas servi fait 8,9 ms dont ≈ 3 ms hors GPU** — Coder b=1 servi fait 2,75 ms en tout ; le chemin hybride (MLA, créneaux) a un coût d'hôte par pas que le chemin dense n'a pas. **À nommer avant tout chantier** : `certifie` GLM b=1 sous nsys `graph` + `osrt` (30 s) → ce que l'hôte fait entre deux rejeux ; prédiction : ≥ 2 ms de Python par pas dans la gestion des créneaux hybrides (liaison, états) ou l'échantillonnage ; réfutation : hors GPU ≤ 1 ms → les 3 ms sont des trous dans le graphe (lancements hors capture), autre chantier. **C16-GLM** s'ouvre si ≥ 1,5 ms d'hôte nommé : le gain possible est +30-40 % de b=1 servi (112 → 150-160), le plus gros levier GLM restant |
| GLM prefill L=8 192 | défaut (règle 2 048 clés) **1 905 j/s · 4,3 s · 0,170** ; fp32 pur 1 767 | la règle vaut +8 % ; à 8 192 le cœur pèse ∝ L² — C13-c forme 1 (fp32 fusionné, sans trafic de scores) est le levier du long contexte : cellule L=8 192 dans son scellé (prédiction ≥ 3 500 j/s) |

## 2. mma2 est W4A4 par construction : ce que GLM sert à b ≥ 5
`kind::mxf4nvf4` prend **les deux opérandes en e2m1** (`acvram_kernels.cu:2965`) : le chemin `_forward_grouped_mma` quantifie l'activation en NVFP4 (`nvfp4_quant_act`, ×39 par pas GLM à b=12) — **W4A4**, l'arithmétique fermée au prefill pour +1 % de PPL. Le verdict C17 (+2,2 % au lot de 12 sur 48 couches Coder) est cet effet, pas un défaut de disposition ; et Mesure 2 (« naturel au décodage ») aurait mis tout Coder en A4 — elle est morte deux fois. **Aujourd'hui GLM sert ses 13 couches distinctes en mma2 dès t ≥ 5 (`MOE_DECODE_MMA=1`, `MIN_T=5`)** : la PPL GLM publiée (0,998) est à b=1, où le chemin n'est pas pris. Mesure avant les concurrents (poste2, 15 min, en processus, hôte indifférent) : `ppl-decode-kv` GLM **au lot de 12** (corpus préfixé, 3 tranches, notée + témoins), trois bras sur main : **A** `MOE_DECODE_MMA=1` (défaut) ; **B** `MOE_DECODE_MMA=0` (xreg, W4A16) ; **C** `ACVRAM_MARLIN_DISTINCT=1` (C10 (b), Marlin W4A16, fusionné opt-in). Prédiction : A − B = **+0,4 à +1,0 %** (13 couches sur 46 ; Coder : 48/48 → +2,2 %) ; C = B ± 0,2 %. Règle écrite avant : A − B ≥ **+0,3 %** → **C10 (b) passe au défaut pour GLM** (les couches distinctes en Marlin W4A16 : qualité de b=1 rendue à b=12, et −0,1 à −0,27 ms/pas en prime), `MOE_DECODE_MMA` sans objet sur GLM ; < +0,3 % → rien ne change, dit. Issue qui me gênerait : A − B < 0,3 % (l'A4 sur 13 couches est absorbé) — alors le comparatif garde mma2 et la ligne « A4 sur 13/46 couches à b ≥ 5 » entre dans le régime publié. MECANISMES, une ligne : *un noyau MMA `mxf4nvf4` est W4A4 par sa définition (deux opérandes e2m1) ; « chemin_moe=mma » au décodage veut dire « activations en 4 bits », à porter par le nom (`mma-a4`) ; une PPL mesurée à un lot où le chemin n'est pas pris ne le juge pas.*

## Ordre
* **poste2** — après C15 et C14-c (fenêtres en cours) : (1) PPL GLM lot 12, trois bras (§ 2, 15 min) ; (2) nsys `graph` + `osrt` sur `certifie` GLM b=1 (30 s, § 1) ; (3) concurrents 2 700 ; (4) C9. Comparatif : GLM b=12 « 660 ± 20 · 0,320 », b=1 « 112,7 · 1,075 », L=8 192 « 1 905 ».
* **poste1** — `chemin_moe=mma` renommé `mma-a4` dans la ligne de régime (une ligne) ; si la règle § 2 tombe : C10 (b) au défaut pour les modèles à couches distinctes (`MARLIN_DISTINCT` défaut 1, `MOE_DECODE_MMA` ignoré quand Marlin couvre) ; C16-GLM après le nsys ; C1 noyau en cours.
* **chef** — ETAT : cellules GLM avec dispersion, hôte 3 ms à nommer, mma2 = W4A4 (MECANISMES), PPL lot 12 scellée, Mesure 2 morte deux fois ; comparatif ; INDEX ; commit + push.
