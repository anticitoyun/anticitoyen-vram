# poste7 — C13-c forme 1 faux : `tl.dot` fp32 IEEE n'a pas de chemin tensoriel sur sm_120 (émulation ×26, et pas plus juste) — mon ordre « fp32 d'abord » tombe ; forme 2 = même noyau en TF32 sous la règle des 2 048 clés, jugé contre l'einsum TF32 qu'il remplace ; forme 1-ter (tf32x3) à chiffrer pour le long contexte (20/09, 00 h 50, horloge machine)

Source : `verdict-c13c-19-09` (poste2 4e3c429e : 64 × 64 refusée, shared 319 488 o contre 101 376 ; à 32 × 64-w4-s1 juge fp64 faux aux 4 formes — L=2 047 : 27,5 ulp contre 18,2 pour la référence, 14 % des lignes au-delà de + 6 — et 121 ms/appel contre 4,6 pour l'einsum fp32 : cœur ≈ 5 690 ms) ; poste1 2e56557d (tuile choisie par `shared_memory_per_block_optin`) ; `poste7-fiches-c5b-c13c-c14b-20-09` § 2 et `poste7-c13c-juge-fp64-ordre-fin-nuit-20-09` (mon ordre fp32 d'abord) ; `verdict-tf32-8k-19-09` (TF32 : ± 3-6 % à 8 192 clés) ; règle des 2 048 clés (1eb9e283).

## 1. Ce qui est établi
* **`input_precision="ieee"` sur sm_120 = émulation** : × 26 en temps ET plus loin du fp64 que l'einsum (l'émulation n'est pas un fp32 exact). Ma prémisse (« la fusion en fp32 retire 70 Go de trafic, le calcul reste celui de l'einsum ») était fausse sur le second terme : sur cette carte, le calcul fusionné n'existe qu'en tensor cores — TF32, TF32 × 3 passes, bf16. Le trafic de scores (≈ 47 ms) reste vrai et c'est ce que la forme 2 encaisse.
* Le juge fp64 « + 6 ulp, jamais pire que la référence » a rendu faux pour la bonne raison (14 % des lignes) : il reste le juge de toute forme qui prétend à la classe fp32.
* Tuile : 64 × 64 impossible (shared) ; 32 × 64-w4-s1 se lance (2e56557d, sonde à l'exécution, tuile nommée sur la ligne).

## 2. Forme 2 — flash TF32, ≤ 2 048 clés, scellé écrit maintenant
Même noyau, `input_precision="tf32"`, tuile 32 × 64 (ou ce que la sonde retient), accumulation et softmax fp32, **sous la règle des 2 048 clés** (au-delà : einsum fp32, comme aujourd'hui pour tf32). Juge : **la même classe de précision que le chemin remplacé** (einsum TF32 = défaut actuel), donc distance au fp64 **contre l'einsum TF32** : par ligne ≤ + 6 ulp de sa distance, médiane et max jamais pires (le témoin = deux ordres de somme TF32, excès publié) ; filet PPL prefill 3 tranches flash-tf32 contre tf32 |Δ géo| ≤ 0,0005 ; capture inchangée. Temps : **cœur ≤ 70 ms à L=2 047** (poste1 chiffrait ≤ 55 pour la tuile pleine ; 32 × 64 coûte des relectures de q) → prefill servi **≥ 9 500 j/s** (7 268 aujourd'hui). Réfutation : cœur > 90 ms → la tuile 32 × 64 relit trop, forme « q en tranches » avant d'insister ; PPL |Δ| > 0,0005 → un bogue, pas un ordre de somme. Tenu → `MLA_CORE=flash-tf32` **au défaut ≤ 2 048 clés** (remplace tf32 einsum), cellule prefill L=2 048 réécrite. Fenêtre poste2 20 min quand poste1 pointe (à sec : test `TRITON_INTERPRET` sur le juge TF32, tuile par sonde, régime nommé).

## 3. Long contexte — forme 1-ter à chiffrer, pas à coder cette nuit
`input_precision="tf32x3"` (trois passes TF32 = ≈ 21 bits de mantisse) : sur tensor cores, ≈ 3 × le coût de la forme 2 (cœur ≈ 150-200 ms contre 220 einsum fp32 : gain faible à 2 048, **fort à 8 192 où le trafic de scores est × 16**). Juge : pas la classe fp32 (elle est hors d'atteinte sur cette carte) mais **la règle des 8 192 clés** (6 tranches : géo ≤ + 0,3 %, max |Δ| ≤ 2 %) et le filet à 2 048 ; prédiction : tenu (21 bits contre les 10 de TF32). Chiffrage à sec (poste1, fiche) : coût réel de `tf32x3` sur sm_120 par sonde de 1 min sous verrou `etat`, avant toute ligne. Si `tf32x3` n'est pas tensoriel non plus sur sm_120 → le long contexte garde l'einsum fp32 et c'est écrit.

## Addendum 01 h 00 — la sonde d'poste1 avant la forme 2, et la porte assumée
poste1 (166166d2) demande, avant toute forme 2, **1 min de carte** : débit de `tl.dot` en `tf32` (et `tf32x3`, § 3) contre cuBLAS fp32/tf32 sur un GEMM 2 048² — c'est la mesure qui aurait évité la forme 1 (« non vérifié à sec » de la fiche), elle passe en tête : **si `tl.dot` tf32 n'atteint pas ≥ 0,8 × cuBLAS tf32, la forme 2 ne s'écrit pas** (Triton n'a pas de chemin tensoriel utile sur sm_120 pour ce noyau et C13-c se ferme, dit). La forme 2 porte une porte PPL — oui, contre l'einsum TF32 de même classe (|Δ géo| ≤ 0,0005 à 2 048, 3 tranches), pas contre fp32 : ce n'est pas « fp32 sans porte », qui n'existe pas sur cette carte, c'est « TF32 déjà accepté, autre ordre de somme ». bf16 = F, refusé (8 175 < 8 300, indécidable en PPL) : pas de troisième voie cette nuit. C13-c **fermé pour la nuit** ; forme 2 à sec dès la sonde, fenêtre à la reprise ou dans un trou après C9.

## Addendum 01 h 25 (horloge machine) — la sonde retire ma ligne REGLES § 9 : `tl.dot` ieee n'est pas émulé, c'est la structure du noyau qui vaut × 26 ; la forme 2 attend le plafond de la structure
Sonde (poste2 f2dec8cf, GEMM 2 048², sm_120) : `tl.dot` **tf32 84,8-86,5 TFLOPS = 0,91-0,93 × cuBLAS tf32** (93,0) ; **ieee 49,9 = 0,88 × cuBLAS fp32** (56,8) ; tf32x3 19,5-27,2 (0,21-0,29 ×, la plus exacte : 1,54 ulp). **Retrait** : « `tl.dot` fp32 IEEE = émulation sur sm_120 » (ma ligne REGLES § 9 de 00 h 50) est faux — à ne pas écrire, ou à retirer si chef l'a déjà porté. Le × 26 du noyau flash (0,75 TFLOPS) est **structurel** : 32 × 64/s1 sous 99 Ko, q rechargé par tranche, 8 accumulateurs, softmax par tuile de 64, un bloc par SM — une forme 2 tf32 **à structure égale gagne ≤ 1,8 ×** (≈ 67 ms/appel contre 4,6 pour l'einsum), pas 50 : la règle des 0,8 autorisait le chemin tensoriel, elle ne dit rien de la structure. Arithmétique du plafond : à L=2 047 une couche fait ≈ 0,09 TFLOP causal ; à 85 TFLOPS tf32 c'est **≈ 1,1 ms/appel** (cœur ≈ 52 ms, le ≤ 55 d'poste1) ; la structure actuelle est **60 × au-dessus** de ce plafond. Décision : **forme 2 non autorisée tant que le plafond de la structure n'est pas mesuré** — poste2 a raison : **bras bf16 64 × 64 sans prétention d'exactitude** (1 min, verrou `etat`, la tuile qui tient en shared en bf16) → temps par appel. Règle écrite avant : **≤ 2 ms/appel en bf16 64 × 64** → la structure est saine et le mal est la tuile sous 99 Ko : forme 2 = tf32 avec une disposition qui tient (q en tranches, tuile de clés 32, accumulateurs réduits), scellé § 2 inchangé ; **> 10 ms/appel** → la structure Triton est fausse à W=576 (neuf tranches de K, pression d'accumulateurs : un « flash » à tête de 576 n'est pas un flash de 128) → **C13-c fermé**, et la question qui reste est un noyau MLA de prefill existant (FlashInfer/FlashMLA, dépendance externe : engage, chef la porte à l'utilisateur avec ses chiffres, pas cette nuit) ; entre 2 et 10 → chiffrer la forme « q en tranches » avant de l'écrire. C9 : M0 et cellule llama.cpp 119B b=1 cette nuit ; **C9-charge** (`ModelSpec` mistral4 absent de main : Mistral3 → llama dense seulement) est un chantier de code, à la reprise — acquis.

## Ordre
* **poste1** — bras bf16 64 × 64 pour la sonde de structure (à sec : compile, tuile), pointeur à poste2 ; forme 2 seulement après (addendum 01 h 25) ; fiche C13-c mise à jour (forme 1 fermée, retrait de la ligne « émulation ») ; niveau 2 : adresses de `mla_prep_batch` (01 h 10) ; niveau 3 fiche.
* **poste2 (01 h 25)** — sonde de structure bf16 (1 min, `etat`) dès le bras d'poste1 ; C9 : M0, cellule llama.cpp 119B b=1.
* **chef (01 h 25)** — REGLES § 9 : ne pas écrire « émulation » (ou retirer) ; ETAT : forme 2 suspendue à la sonde de structure, C9-charge à la reprise ; INDEX ; commit + push.
* **poste2 (01 h 00)** — sonde `tl.dot` tf32 / tf32x3 contre cuBLAS (1 min, verrou `etat`) avant C9 ; puis C9.
* **poste2** — sondes C15, PPL niveau 2 une tranche, **C9** (M0, chargement 119B) ; forme 2 (20 min) quand poste1 pointe.
* **chef** — ETAT : C13-c forme 1 faux (émulation × 26), forme 2 TF32 ≤ 2 048 scellée, forme 1-ter à chiffrer ; REGLES § 9 : « `tl.dot` fp32 IEEE = émulation sur sm_120 : ni tensoriel ni exact » ; INDEX ; commit + push.
