# Verdict — tête `lm_head` par `gemm_dense_etroit` (poste4 7c09b53, `DENSE_NVFP4=triton` défaut, MIN_M=4), Qwen3.8 calibA sous FLA : **b=12 392,3 / 391,3 t/s, 1,019 / 1,022 J** (palier 1 : 349 / 1,145) ; b=1 **70,0 t/s** (69,9) ; ppl-decode-kv **4,0882** (=) ; tests 19/20 (le rouge = jouet GDN sur hôte, test corrigé 0a0def0, pas le code) → **b=12 dans la bande 380-400 non définie par le scellé (≥ 400 tenu / < 380 faux) : ni tenu ni faux, à poste7 ; PPL et b=1 tenus** ; profil : la GEMV fp32 de la tête a disparu (5,6 → 0 ms), le pas passe de 32,5 à 28,9 ms, et la réduction des tranches K (`_reduire_kernel`, 3,3 ms, 494 lancements) est maintenant le second poste

instrument : `scratchpad/gemm-dense-17-09/tete.sh` (test sur carte, `certifie-b12` b=12 ×2 / b=1 ×1, `ppl-decode-kv` 256+1024, `profil-gdn-17-09.py b12`), défauts sans variable ; JSON et journaux dans le dossier ; arbre poste3 = main 7c09b53
régime : classé, FLA, graphes on, `DENSE_NVFP4=triton` (défaut), bridage puissance 399-400 W sur les trois rondes
scellé (poste7) : b=12 349 → ≥ 400 (faux < 380) ; ppl-decode-kv ± 0,0005 ; b=1 ± 3 %
mesuré : b=12 pas 30,59 / 30,67 ms → **392,3 / 391,3 t/s, 1,019 / 1,022 J/j** ; b=1 14,29 ms **70,0 t/s 5,72 J** ; ppl-decode-kv **4,0882** ; profil b=12 : 27,8 ms GPU / 28,9 nu (96 %), 3 272 lancements ; `_dense_etroit_kernel` **18,16 ms (65 %, 496 appels = 495 + la tête)**, `_reduire_kernel` **3,30 ms (12 %, 494)**, `fused_recurrent` 1,45 (5 %), elementwise 1,19 + 0,34, memcpy 0,63, rmsnorm 0,50, paged_attn 0,32 ; plus aucun `nvfp4_gemv_kernel` ; sous profileur 415 t/s nu (fenêtre courte sans bridage)
verdict : **+ 12 % (349 → 392) pour 5,6 ms de GEMV en moins : le gain est celui de la tête, exactement (32,5 − 28,9 = 3,6 ms de pas, la tête Triton en coûtant ~ 1 ms au lieu de 5,6) — et 392 n'atteint pas 400 parce que la carte est bridée en puissance (400 W) pendant la fenêtre : le profil, en fenêtre courte, lit 415.** Par la lettre du scellé : ni ≥ 400 ni < 380. Ce que le profil nomme ensuite : `_reduire_kernel` (réduction des tranches K, 3,3 ms, 12 %) est de la colle du GEMM — une réduction fusionnée dans le noyau (palier 2 la promettait, témoin non ouvert) rendrait ~ 3 ms → ~ 440 t/s ; puis les 18,2 ms du GEMM lui-même (1,1 To/s effectifs, plancher 11 ms). Cellule des menus : « acvram Qwen3.8 calibA b=12 392 t/s, 1,02 J (bridé) ». vLLM 621 reste à × 1,6.
