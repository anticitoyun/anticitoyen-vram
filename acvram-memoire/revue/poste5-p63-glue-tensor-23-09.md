# Pièce 63 — la glue du chemin tensor fusionnée : −0,21 ms/pas à b = 12 (6 410 → 6 200 µs, −3,3 %), un aligneur en un lancement (2,7 µs) qui absorbe le clamp, un reduce qui lit le bf16 ; et une trouvaille : la glue A4 servie n'était pas reproductible (aligneur vLLM par atomiques), la fusion l'est

* instrument : trace A4/2 (`tensor2-graphe`, `familles-noyaux --detail`) pour nommer les noyaux ; `frontiere-pas.py 12 200` sous graphes, `ACVRAM_MOE_TENSOR=1` avec témoin `ACVRAM_MOE_TENSOR_FUSION=0` (glue A4) puis `=1` (`scratchpad/poste5-p62-23-09/chaine-p63.sh`) ; nsys `--cuda-graph-trace=node` + familles sur le chemin fusionné (`p63-graphe`) ; `diag-aubit.py` (reproductibilité des deux glues, deux appels chacune) ; tests `tests/test_moe_tensor_glue_fusee.py` (6), `test_moe_tensor_decodage.py` (4), `test_aligneur_cuda_marlin_port.py` (4)
* commit : arbre de 962b0706 + ce commit (`moe_aligner_petit` v2 et `moe_reduce<bf16>` dans acvram_kernels.cu, `gemm_experts_tensor(fusion=)`, régime `+tensor` / `+tensor-glue-a4`) ; alias `Qwen3-Coder-30B-A3B-nvfp4-qkvo-i8c`
* régime : b = 12 pleins, graphes on, repli_eager=0 ; extension compilée à sec (148 s, drapeaux du service, `compiler-a-sec.py`) hors de toute prise ; carte 9 + 16 + 16 + 26 s ×2 + diags 1-3 s ; llama-server étranger (4627) présent, inactif ; poste2 entre mes prises (A5 rendue 03:26)
* scellé : « −0,2 à −0,3 ms/pas, au bit contre le chemin tensor actuel » (chef) ; 63/1 (carnet 03 h 1x) : −0,20 à −0,29, réfuté si < 0,12 → **RÉFUTÉ** (−0,094 : l aligneur v1 coûtait 5,4 µs, préfixe sériel) ; 63/2 (carnet 03 h 3x, avant) : aligneur 2,0-2,6 µs, cumul −0,21 à −0,25, réfuté si aligneur > 3,4 ou cumul < 0,12
* mesuré : noyaux par couche, A4 → 63 : clamp 0,7 → 0 ; aligneur 2,6 + 0,8 → **2,7** (v1 : 5,4) ; cast `d.float()` 1,9 → 0 ; reduce 1,0 → 0,9 (bf16) ; act 0,9 = ; nœuds 942 → 798/pas, trou 0,331 → 0,287 ; noyaux 6,189 → **5,990 ms/pas** ; frontière méd. **6 410,5 (témoin, glue A4) → 6 199,6 µs/pas (−210,9, −3,3 %)**, p90 6 517 → 6 300 ; cumul depuis le GEMV (A4/2 : 6 856) : **−657 µs/pas, −9,6 %** ; tests 14/14 verts après correction de deux tests (voir ci-dessous)
* verdict : **63/2 tenu** (−0,21 dans −0,21/−0,25 ; aligneur 2,7 : 0,1 au-dessus de la fourchette, sous le seuil 3,4) ; « au bit contre le chemin actuel » est **inapplicable tel quel** : `diag-aubit` montre que la glue A4 diffère d elle-même d un appel à l autre (t = 12 : 5 lignes, t = 16 : 2 lignes, max 2⁻⁹ ; lignes des experts répartis sur deux blocs, que l aligneur vLLM place par atomiques et que la GEMM Marlin ne rend pas au bit selon la place) — la fusion est reproductible (rang stable), **au bit avec A4 sur toute ligne où A4 est d accord avec lui-même**, sous 2⁻⁷·max ailleurs ; les tests `test_aligneur_cuda_egal_a_triton` (A2) et `test_aligneur_fusionne_egal_vllm` comparaient les tampons bloc par bloc — faux pour un expert à deux blocs, passés par chance jusqu ici ; réécrits sur l invariant lu par la GEMM (paire → expert)
* durée : 1 h 10 (dont ~2 min de carte, 2 × 148 s de nvcc hors prise) ; gênant nommé et arrivé : la première prédiction est tombée sur l aligneur v1 (un thread sériel sur 128 experts vaut 3 µs) ; gênant non prévu : la non-reproductibilité de A4, servie chez poste2 en A5 — sans effet sur son verdict (bruit 2⁻⁹, KL identique), mais à dire

## Reste sur la table (hors 63)
act 0,9 + reduce 0,9 + aligneur 2,7 = 4,5 µs/couche (0,22 ms/pas) ; le GEMM lui-même 3,145 ms/pas contre 3,23 sur l op vLLM. Fusion gate+up en une GEMM (N interfoliés, comme vLLM) : un lancement de moins par couche, pas de non-GEMM ; à chiffrer avant de coder.
