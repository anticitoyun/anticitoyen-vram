# Pièce 71 bis — gate·up fusés en une GEMM Marlin (w13) : RÉFUTÉ au banc, − 4,0 µs/couche seulement (69,1 → 65,1 sur 20 routages réels b=12, 33,4 experts distincts, L2 froid) = − 0,19 ms/pas, sous mon seuil de − 0,2 ; le coût fixe d une GEMM groupée n est pas ce qui sépare nos 65 µs/couche des 53 de vLLM en service

* instrument : `outils/gpu/mesure/banc-moe-w13.py` (graphe des 20 routages réels de la cellule b=12, `routages-cellule-b12-20.pt`, 8 jeux de piles NVFP4 en rotation, formes Coder E 128 K 2048 I 768 k 8) — chemin tensor actuel (`gemm_experts_tensor` : GEMM gate, GEMM up, `moe_act`, GEMM down) contre w13 (GEMM gate‖up N = 1 536 à échelle globale 1, `moe_act` appliquant gs_gate[e] et gs_up[e] avant l activation — `acvram_kernels.cu`, arguments optionnels ajoutés à `moe_act`, gardés) ; test au 2⁻⁷ (t = 8/12 tenus, t = 16 : 20 valeurs sur 262 144 à un ulp bf16, critère p57 ≤ 5e-4 tenu) ; frontière b=12 avec la pile w13 AJOUTÉE : hors mémoire (+ 8,9 Go, llama-server étranger 5,6 Go présent) — le jalon A s est fait au banc
* commit : 0dc96eb8 + ce commit (code opt-in retiré, banc gardé, moe_act à échelles gardé) ; carte 2 min 50 (`poste5-p71-tests`, `poste5-p71-front-W0/W1`, `poste5-p71-banc-w13`)
* régime : banc hors service, horloge libre ; échelles globales gate/up distinctes 12/12 sur l alias officiel (couches 0/23/47 × experts 0/1/7/127), appliquées dans moe_act comme proposé par chef
* scellé (note p71, carnet 06 h 0x, avant) : MoE 3,145 → 2,65-2,80 ms/pas (− 0,35 à − 0,50), réfuté si < − 0,2 ms/pas ou KL bouge
* mesuré : gate/up séparés **69,1 µs/couche**, w13 **65,1** : − 4,0 µs/couche, **− 0,19 ms/pas** (× 48) ; frontière b=12 témoin (w13 = 0) 6 198 µs/pas inchangée ; KL non jouée (frontière w13 = 1 hors mémoire)
* verdict : **réfuté** (− 0,19 < − 0,2, et loin des − 0,35 prédits) — supprimer un lancement de GEMM groupée à 33 experts ne rend que 4 µs : le coût par GEMM est surtout de l arithmétique et des lectures, pas du lancement ; l écart en service (nous 65,5 µs/couche, vLLM 53 avec le même noyau) tient donc à autre chose : leurs paramètres de noyau (`thread_k/thread_n`, `is_k_full`, `use_fp32_reduce`, bloc d alignement — nous passons − 1/− 1/− 1 et bloc 8-16), ou leur `silu_and_mul` + `moe_sum` contre notre `moe_act` + `moe_reduce`, ou le contexte de service (L2, horloge 2 700) que le banc ne voit pas ; à départager par une trace vLLM par noyau (p59, `familles-vllm.txt` : 26,6 µs par lancement Marlin pour 2 lancements/couche = 53) contre la nôtre (p63 : 3 lancements de 22,0 µs = 66) — **par lancement nous sommes plus rapides (22 contre 26,6), c est le nombre de lancements qui coûte, mais fusionner gate‖up ne le récupère pas au banc** : à vérifier EN SERVICE (frontière avec disposition w13 unique) avant de conclure, ce que la mémoire n a pas permis ici
* durée : 55 min (dont 2 min 50 de carte) ; gênant nommé (échelles) résolu ; gênant non nommé et arrivé : la mémoire (pile ajoutée + llama-server étranger)

## Reste
Le seul contrôle qui tranche : frontière b=12 avec la disposition w13 UNIQUE (jalon B : gate/up remplacées, GEMV des godets < 8 lisant des colonnes à foulée ou repli tensor, préfill sur w13) — coût ≈ une demi-journée ; à n engager que si la trace par noyau vLLM (p59) confirme 26,6 µs × 2 contre 22,0 × 3 en service, ce qui donnerait − 0,6 ms/pas au mieux. Sinon, l autre poste b=12 : projections + 0,26 (Marlin dense, sous seuil).
