# Protocole — profil Coder-30B, défauts 0.6.9+ (E triton, C mixte, B0 groupe, F1+F2) : prefill 2048 et décodage b=12 (poste7, priorité 2 : aucun noyau proposé avant ces deux profils)

instrument : `scratchpad/profil-pas-coder-17-09.py <sortie> b12,prefill` (torch.profiler, noyaux CUDA seuls, `device_type == CUDA` ; décodage : fenêtre pure b=12 sous graphes, 30 pas avant, 40 profilés ; prefill : eager L=2048, 3 passes nues puis 3 profilées) ; régime classé `ACVRAM_MOE_MMA=0 ACVRAM_MOE_DECODE_MMA=0 ACVRAM_NARROW_GEMM=1` + défauts ; b12 et prefill dans deux processus (allocateur) ; sortie `scratchpad/profil-coder-2-17-09/`.
en face : prefill acvram 9 913 j/s (B0) contre llama.cpp 15 717 (−37 %) et vLLM Marlin 22 067 fiche / 2 031… (÷2,1) ; décodage b=12 acvram 997,8 t/s contre Marlin 2 031 (÷2).
scellé (prédictions) : prefill 2048 — experts (grouped/`groupe`) 55-70 % du pas GPU, attention + norme + rope ≤ 15 %, déquantification W4A16 ≥ 10 % (la déquant bf16 par couche est le poste que Marlin n'a pas) ; le pas GPU ≥ 85 % du temps mur (sinon l'hôte est le poste) ; décodage b=12 — experts GEMV 40-55 %, `paged_attn` triton ≤ 15 %, ≥ 600 lancements par pas (dont ≥ 128 pour les experts : la boucle par expert reste) ; réfutation utile : un poste unique > 30 % hors experts (déquant ? routage ? norme ?) ou un temps mur ≫ somme des noyaux (hôte).
