# Chantier C15-prefill — la glue du préfill Coder (norm 9,1 + glue 8,2 ms sur 96,8 de noyaux) ; fiche seule (`sage-c1-ferme-c15-prefill-20-09` § 2), code à sec après le niveau 2

## Scellé (Sage, recopié)
Budget P2 i8c à L=2 047 (96,8 ms de noyaux) : **norm 9,1 + glue 8,2 = 17,3 ms (18 %) pour ≈ 2 Go de trafic** — la norm à × 8 sa borne DRAM, la glue en lancements ; vLLM fait 20 824 j/s avec le même noyau Marlin : son avance est hors experts. C15-prefill : rmsnorm + résidu + cast fusionnés en prologue des projections (9,1 → ≤ 2,0), glue du routeur et des permutations (8,2 → ≤ 3,0), attention paginée inchangée ; **noyaux 96,8 → ≤ 83 ms, prefill servi ≥ 20 500 j/s** (17 784 aujourd'hui, vLLM 20 824), **PPL au bit** contre le défaut sur 3 tranches (même arithmétique : tout écart est un bogue), capture inchangée ; réfutation : ≤ 83 ms de noyaux mais < 19 500 servi → le hors-noyaux du prefill (11 %) est le poste suivant, nsys `graph` avant d'insister. Prédiction Sage 20 500-21 500.

## Ce que les 17,3 ms sont (à sec, depuis l'attribution nsys P2 et le code)
* **Norm 9,1 ms** : `rmsnorm_bf16` × 2 par couche sur [2 047, 2 048] bf16 (8,4 Mo lus + 8,4 écrits par appel → 97 appels × 16,8 Mo = 1,6 Go à 1,5 To/s = **1,1 ms de borne** ; mesuré 9,1 = × 8) : le noyau `rmsnorm_bf16_kernel` (`.cu:5738`, un bloc par ligne ?) est **latence/occupation-borné aux formes du préfill** (2 047 lignes × 2 048 : un warp par ligne suffit, 2 047 blocs est trop peu de parallélisme par bloc si chaque bloc fait une réduction en deux passes). Deux leviers : (a) réécrire la norm pour le préfill (une ligne par warp, lecture vectorisée uint4, réduction par `shfl`, une passe : borne 1,1 ms → prédit **≤ 2,0**) — c'est le geste de Sage « ≤ 2,0 » sans même fusionner ; (b) la fusion en prologue de la projection suivante (`int8_gemv`/`_etroit`/cuBLAS) supprime aussi la relecture : −8,4 Mo par appel, mais la projection du préfill est une GEMM cuBLAS/`_int_mm` (M=2 047) qu'on ne peut pas prologuer sans la réécrire ; **(a) d'abord**, (b) seulement si (a) ne rend pas ≤ 2,0.
* **Glue 8,2 ms** : à L=2 047 le routeur (`F.linear` fp32 + softmax + top-k + `_route_fusee`), la permutation des lignes par expert (`argsort`, `bincount`, `index_select` de x [16 376 × 2 048] = 67 Mo → gather 0,1 ms), le dé-tri (`moe_reduce_trie`), les casts fp32↔bf16 des activations (x.to(f32) pour le routeur : 8,4 Mo), `moe_act`, `nvfp4_quant_act` ×2 (déjà noyaux), `kv_write`, RoPE, résidus — par couche ≈ 25 lancements de glue à 2-10 µs : **≈ 0,17 ms par couche = 8,2**. Leviers : routeur en 1 noyau (logits bf16→fp32 dans `_route_fusee`, −3/couche, −0,7 ms) ; casts supprimés (les GEMM Marlin lisent bf16 : −2/couche) ; `moe_route_pack` (déjà écrit pour le décodage : tri + gather + tuiles en 1 lancement, `.cu`, utilisé quand t·k ≤ 1 024 — à L=2 047 t·k = 16 376 > 1 024 : **lever la borne** ou la découper par morceaux) ; résidu+norm fusionnés (`add_norm`, déjà pris par `_res_differe` au décodage : à brancher au préfill). Prédit : 8,2 → **3,0-3,5**.
* Total : 96,8 − 7 (norm) − 5 (glue) ≈ **85 ms** — le « ≤ 83 » demande les deux leviers pleins (norm ≤ 2,0 ET glue ≤ 3,0) ; servi : 17 784 × 96,8/85 ≈ **20 250 j/s**, ≥ 20 500 si le hors-noyaux ne bouge pas (11 % = 12 ms : le scellé de réfutation de Sage). Ma prédiction : **19 800-20 800** ; issue qui me gênerait : la norm réécrite ne descend pas sous 4 ms (alors la borne n'est pas la latence mais l'écriture bf16 des 2 047 lignes deux fois par couche : la fusion (b) devient obligatoire).

## Preuve à sec prévue (au bit, aucune porte)
Norm : jumeau = `rmsnorm` torch fp32 → bf16 (le `.cu` actuel est au bit avec lui : `layers.py:578`) ; nouveau noyau au bit avec le témoin sur formes réelles (test CUDA saute à sec, mais la référence et le témoin cassant — eps décalé → diverge — existent à sec). Glue : compte de lancements par couche au préfill (TorchDispatchMode, comme `test_mla_glue_c15`) avant/après, jetons du prefill (logits du dernier jeton) au bit ; `moe_route_pack` par morceaux : sortie = tri torch au bit (test existant `test_moe_route_pack`). Chaîne carte : nsys prefill 2 047 (noyaux ≤ 83, norm ≤ 2, glue ≤ 3), prefill ABAB ≥ 20 500, PPL 3 tranches au bit.

## Fait le 20/09 (Océane, 06 h 01 - 06 h 45, à sec, branche `oceane-c15-prefill` depuis `oceane-11` = `ca0108de`)

### La prémisse corrigée avant de coder (lecture du CSV nsys P2 du 19/09, `scratchpad/nsys-budget-19-09/p2-cublas_cuda_gpu_kern_sum.csv`)
« Norm 9,1 ms = rmsnorm à × 8 sa borne » est faux : `rmsnorm_bf16_kernel` vaut **2,12 ms × 97** (21,9 µs par appel, 0,77 To/s sur 16,8 Mo — près de la borne, pas × 8). Les 9,1 de la famille « norm/élém » sont **l'épilogue du chemin cublas** : `gemm_i8c_cublas` (`kernels/__init__.py:801-803` sur `oceane-11`) rendait `bf16(f32(acc) · s_x · s_w)` par quatre noyaux torch sur [2 047, N] entiers — `MulFunctor<float>` ×384 **3,82 ms**, `direct_copy` int32→f32 ×432 **2,11**, `bfloat16_copy` ×192 **0,74**, `elementwise <128,4>` ×96 0,98 — soit **≈ 6,7 ms et 21 lancements par couche pour 88 Mo utiles**, plus `_quant_a8_kernel` ×192 (0,97 : la même ligne normée quantifiée trois fois pour q/k/v) et l'addition résiduelle `x + y` ×48 (0,32). La glue MoE 8,2 : `moe_act` 2,0 + `moe_reduce_trie` 1,65 + `vectorized_gather` 1,17 + `moe_route` 0,92 = **5,7 ms à la bande passante** (plancher), puis le tri **int64** de `topi` (8 passes radix, 1,70), **un second tri d'une liste déjà triée** dans `MP.aligner_blocs(e_sorted)` (`marlin_port/__init__.py:353-374` : argsort 4 passes 0,82 + bincount 0,14 + scan, **deux `int()` hôte par couche**), histogramme 0,56, index/scatter/arange ≈ 1,0. Le « glue ≤ 3,0 » du scellé est donc **hors de portée sans toucher Marlin** (fusionner le gather des lignes dans la lecture de A du noyau) ; le « norm ≤ 2,0 » vise en réalité l'épilogue.

### Régime
`ACVRAM_PREFILL_COMPACT` (défaut **0**, témoin ; `regime.py` `VARIABLES`, `kernels/__init__.py` `prefill_compact()`), `ACVRAM_PREFILL_COMPACT_ITEMS=epilogue|a8|residu|permut` pour la bissection, `prefill_glue=temoin|compact|compact(items=…)` sur la ligne de régime (`regime.prefill_glue_texte`) — commit `22f22347` ; `test_hors_regime_ne_cache_aucun_regime` passe.

### Les fusions (une par commit, chacune au bit avec son témoin, test dans le même commit : `tests/test_prefill_compact.py`, 21 tests à sec + 1 carte)
| # | commit | fusion | ce que c'était (fichier:ligne, `oceane-11`) | ce que c'est | ms prédits (B) | précision | test à sec | pas faisable à sec |
|---|---|---|---|---|---|---|---|---|
| 1 | `972c3e55` | `epilogue` | `gemm_i8c_cublas` 4 noyaux torch/projection, 6,7 ms | `gemm_w8a8.epilogue_i8c` Triton : cvt int32→fp32, ×s_x, ×s_w, →bf16 RNE **par la formule entière de c10::BFloat16** (l'interpréteur Triton TRONQUE `.to(bf16)` : 659,4 → 656, vu au premier test) ; échelle fp32 convertie une fois par tenseur | 6,7 → **1,0-2,5** (acc relu depuis le L2), −16 lancements/couche | même chaîne d'arrondis, même ordre : au bit | au bit bf16 et fp32 sur 104 000 éléments d'amplitude réelle (> 2²⁴) ; témoins : produits inversés ≠ sur > 10 % des fp32, troncature ≠ sur > 30 % ; dispatcher atteint le noyau (compté), ITEMS=a8 le laisse au témoin | le temps ; l'arrondi RNE sur carte = celui de l'interpréteur par construction (entiers) |
| 2 | `51a6783c` | `a8` | `Attention._proj` (`model.py:318`) : q, k, v quantifient chacun la même ligne (`_quant_a8` ×4/couche) | `kernels.int8_matmul_partage` : une A8, trois `_int_mm` ; décline (None → chemin d'avant) dès qu'un tenseur ne prendrait pas cublas sous le dispatcher | 0,97 → **0,35-0,60**, −2 lancements/couche | mêmes octets (même quantificateur, même produit) | trois `int8_matmul` = chemin partagé, un seul quantificateur compté ; M ≤ 16, affine, régime bf16 → None ; `_proj_i8c_partage` sur QuantLinear, biais → None | rien |
| 3 | `c0f58e97` | `residu` | `DecoderLayer.forward` (`model.py:2773`) rend `x + y` (add ×48, 0,32 ms) ; la couche suivante relit x | `forward_res` (x, delta) → `add_norm` de la norme d'entrée (rmsnorm_bf16 avec résidu, celui du décodage `decode_fixed_res`), dernier delta dans la norme finale ; réservé multiplicateur 1,0 | 0,32 → **0**, −1 lancement/couche | bf16(fp32(x)+fp32(delta)) puis norme = `model.py:2612` (docstring) | tiny llama : logits de tous les jetons au bit compact/témoin, `forward_res` compté par couche, ITEMS sans residu → chemin d'avant, multiplicateur 0,5 → chemin d'avant | l'égalité du noyau avec résidu (à sec `add_norm` est le repli add + norme) : PPL au bit de la chaîne |
| 4 | `94a4d082` + `baec346d` | `permut` | `_forward_prefill_grouped` (`model.py:1373-1383`, `1466`) : tri int64, second tri dans `aligner_blocs`, arange + repeat_interleave + gather pour `flat_t[ordre]`, `inv.to(int32)`, 2 syncs hôte/couche | tri stable sur les clés int32 telles quelles, comptes par `searchsorted` (`_comptes_tries`), lignes par `ordre // k`, `MP.aligner_blocs_tries(e_sorted, cnt)` **sans scalaire hôte** (sorties de taille fixe P = G + E·(bloc−1), −1 au-delà : `ops.cu:58` lit num_tokens_post_padded sur la carte, `:116` saute −1), inv int32 d'emblée | tri 3,72 → **0,9-1,6** ; glue 7,88 → **4,5-6,0** ; −12 lancements et **−2 synchronisations hôte par couche** | mêmes ordre / cnt / xs / blocs : au bit | `_comptes_tries` = bincount (int32/int64, experts vides ; faux sans tri) ; `ordre // k` = `flat_t[ordre]` ; tri int32 = tri int64 ; `aligner_blocs_tries` = `aligner_blocs` sur les `total` premières entrées (blocs 8/16/64), queue G / −1, comptes décalés → différent | `_forward_prefill_grouped` marlin compact = témoin **au bit** : test carte `test_prefill_moe_marlin_compact_au_bit_avec_le_temoin` (saute à sec, joué en tête de la chaîne) |

Non fait : la rmsnorm + A8 fusionnées en un noyau CUDA (≈ −1,3 ms : −16,8 Mo et −1 lancement par couche ; exige de rejouer l'ordre exact de la réduction fp32 du bloc de 1 024 fils pour rester au bit — faisable, écrit dans le plan, pas dans les 3 h) ; `_compter_routage` (5 petits noyaux/couche, ≈ 0,5 ms) ; le gather des lignes dans Marlin (−1,2 ms, chantier à part). Trois tests **déjà rouges sur `oceane-11`** à sec, vérifiés dans un worktree propre de `ca0108de` (`test_marlin_prefill_p1::test_le_regime_nomme_la_disposition`, `test_mla_glue_c15::test_compte_a_sec_par_couche_par_niveau`, `::test_glue_1_bit_identique…`) — pas de ce chantier.

### Prédiction (écrite avant la mesure, à recopier par Manon en tête du verdict)
* (a) nsys : **B = 85-88 ms** de noyaux (A 96,8 ± 1) ; par famille dans `familles.py` `PREDIT` (le même CSV en A et B rend FAUX sur les cinq familles touchées : le contrôle peut rendre faux). **Le « ≤ 83 » est prédit FAUX de 2-5 ms** : plancher de la glue MoE 5,7 à la bande passante, épilogue 88 Mo/couche à relire, rmsnorm inchangée 2,1. Issue qui me gênerait : `epilogue_i8c` > 2,5 ms (acc non relu du L2, alors c'est 4,2 Go à 1,5 To/s = 2,8) ; `tri_experts` > 1,6.
* (b) PPL 3 tranches **au bit** (sinon bogue → BISSECTION=1 nomme la fusion).
* (c) godets 1,2,8,16 : 5/5 (le décodage sous graphes ne passe par aucune fusion du préfill : `forward_res` et `_forward_prefill_grouped` sont eager, `int8_matmul_partage` exige n > seuil GEMV).
* (d) servi : deux mécanismes s'ajoutent — les noyaux (17 784 × 96,8/86 ≈ **20 000**) et les **96 synchronisations hôte par préfill retirées** (chacune vide la file pendant que l'hôte relance ~15 petits noyaux : 100-150 µs, soit 10-14 ms = à peu près le « hors-noyaux 11 % » du scellé). Prédiction **19 500-22 000 j/s** ; ≥ 20 500 tenu si le second mécanisme est réel, faux sinon — c'est lui que le résultat tranche, et il tranche aussi la clause de réfutation de Sage (« ≤ 83 mais < 19 500 → hors-noyaux ») : si B < 19 500 avec zéro synchronisation, le hors-noyaux n'est pas les synchronisations.

### Chaîne (Manon, ≈ 40 min, chaque mesure sous `outils/carte.sh`)
`bash scratchpad/c15-prefill-20-09/chaine.sh` (`BISSECTION=1` ajoute quatre nsys B-<fusion>) : (0) `pytest tests/test_prefill_compact.py` sur carte (Marlin compact = témoin au bit) + régime des deux bras ; (1) nsys préfill 2 047 A/B → `familles.py` (ms par famille, écart, scellé ≤ 83, prédictions TENU/FAUX, noms non classés) ; (2) PPL 3 tranches A/B (`ppl-acvram-17-09.py`, PPL_MIN_CTX=1023 PPL_MAX_TOK=24576) → `ppl-au-bit.py` (pertes fenêtre par fenêtre, premier écart nommé ; vérifié : une perte modifiée de 10⁻⁴ → FAUX) ; (3) `capture-godets.py` 1,2,8,16 sous B ; (4) `prefill-abab-19-09.py` L = 2 048, A B A B (le harnais des 17 784), alarme A hors ± 3 %.

### Reprise 07 h 00 - 07 h 15 (sage-reprise-rapide-20-09 § 3) : deux fusions de plus
| # | commit | fusion | ce que c'était | ce que c'est | ms prédits (B) | précision | test |
|---|---|---|---|---|---|---|---|
| 5 | `a3a79e70` | `norm` | `rmsnorm_bf16_kernel` un bloc de 1 024 fils par ligne, 2 047 blocs de 4 Kio, deux `__syncthreads`, 21,9 µs/appel (2,12 ms × 97) | `rmsnorm_bf16_warp_kernel<TH>` : un warp par ligne, 8 lignes par bloc de 256 fils, sans smem ; somme des carrés dans l'ordre EXACT du bloc (partiels par fil t = 32·wq + lane, arbre xor 16…1, somme séquentielle des TH/32 warps), `rsqrtf(ss / H + eps)` identique ; pris par `RMSNorm.forward` et `add_norm` (`layers.py` `_norme_warp`) dès 256 lignes — le décodage sous graphes garde le bloc | 2,12 → **1,0-1,5** | **SASS sm_120f comparé** (`nvcc -Xptxas -v`, mêmes drapeaux `-O3 --use_fast_math`) : les deux noyaux font `FFMA.FTZ` pour `ss += v·v`, `SHFL.BFLY` + `FADD.FTZ` pour l'arbre, une chaîne `FADD.FTZ` pour les warps, `MUFU.RCP` + `FFMA` + `MUFU.RSQ` pour rs, `FMUL` + `F2FP` en sortie — mêmes instructions ; **ptxas : 37 registres, 0 pile, 0 déversement, 0 barrière, 0 smem** (bloc : 22 reg., 1 barrière, 128 o smem) | carte (saute à sec) : warp = bloc au bit, H 2048/1024/256/4096/2560, R 2047/300/9, sans/avec résidu, témoin eps décalé ; à sec : seuil 256 lignes, témoin COMPACT=0 |
| 6 | `5555eef2` | `attn` | la copie de `rope_fusee` (`layers.py:799`) **n'existe pas** au préfill Coder : q y est la vue contiguë de `q_proj` (N = 4 096 = `t.shape[0]`, aucun rembourrage) ; les copies sont dans `Attention._prefill` (`model.py:524-552`) : `repeat_kv` matérialise K et V ×n_rep (expand + reshape, deux [2 047, 32, 128] bf16 par couche = les `direct_copy` ×96, 0,98 ms) et la sortie SDPA est recopiée dans `out = empty_like(q)` (16,8 Mo/couche) | `layers.attention(..., n_rep)` → SDPA `enable_gqa` (tête h lit la tête KV h // n_rep, le mappage de `repeat_kv` ; flash le fait par `h_h_k_ratio` sur les mêmes octets) ; à une séquence la sortie est rendue telle quelle | copies_autres 1,65 → **0,2-0,9** | même noyau flash, mêmes données : au bit (à prouver par la PPL de la chaîne — c'est l'issue nommée si elle tombe : bissection `attn`) | à sec : gqa = repeat_kv au bit (causal, décalée, fenêtre ; témoin têtes échangées) ; tiny llama 8 têtes / 2 KV sous ITEMS=attn : logits au bit |

**Prédiction recalculée (07 h 10, avant mesure)** : B = **83-86 ms** de noyaux (85-88 − 0,6-1,1 norm − 0,8-1,4 copies) ; le « ≤ 83 » reste prédit FAUX, de 0-3 ms ; servi **19 500-22 000** inchangé (les 96 synchronisations hôte retirées tranchent) ; `familles.py` `PREDIT` et l'en-tête de `chaine.sh` mis à jour, bissection à six bras.

### Premier passage carte (Manon, `d17a719d`, 07 h 18-07 h 22, `verdict-c15-prefill-19-09`) et ce que j'en corrige (07 h 25-07 h 40)
**(0) 4 failed / 29 passed** — trois fixtures fausses par construction, une plomberie de test :
* `test_epilogue_i8c_au_bit…[bf16|fp32]` : tenseurs CPU donnés au noyau Triton **réel** (« Pointer argument cannot be accessed from Triton ») — à sec l'interpréteur (conftest : `CUDA_VISIBLE_DEVICES` vide → `TRITON_INTERPRET=1`) lit le CPU, sur carte non. Corrigé : `DEV = cuda si disponible` pour tous les tenseurs des tests i8c.
* `test_gemm_i8c_cublas_compact…` : `appels == []` — même cause : `x` CPU sur carte → `gemm_i8c_cublas` prend la chaîne torch (`x.is_cuda or _interprete()` faux), pas le noyau. Corrigé (DEV) ; éligibilité nommée : M = 40 > 16, K = 256 et N = 96 multiples de 8, groupe = K, zéros = 128. Les tests A8 partagée passaient sur carte mais **hors chemin** (CPU) : sur carte, sous le seuil GEMV (80 lignes) le dispatcher prend le GEMV et le chemin partagé décline, comme il doit — les tests abaissent `_INT8_GEMV_MAX` à 16 (`_sous_le_seuil_gemv`), vérifient le cas nominal d'abord et le refus sous le seuil.
* `test_prefill_moe_marlin_compact…` : les deux appels directs `_forward_prefill_grouped` (témoin, compact) **ont passé au bit** ; c'est `bloc(x)` (mon ajout « compte d'usage ») qui tombait : `forward` reconstruit les piles tant que `_stack_state == "?"`, la seconde construction voit la pile naturelle **rendue** après le repack Marlin (vide, `size=(8, 0, 128)`, « pile hors CUDA »), refuse Marlin et part en MMA sur une pile vide (`model.py:1301`). Corrigé : le test pose `_stack_state = "oui"` comme `forward` le fait après une construction réussie.
**(1) A : OOM** à la construction Marlin (`_construire_marlin → preparer_pile → facteur_nvfp4`, 288 Mio refusés, **31,07 Gio pour ce processus** : 20,83 alloués + 9,61 réservés non alloués = la fragmentation du repack) ; B, même chargement, mêmes piles (rien de `PREFILL_COMPACT` n'est lu avant le premier préfill), passe 20 s plus tard ; même `kv_budget=63360/8` dans les deux journaux, aucun « budget KV borné par la VRAM libre ». Le témoin n'alloue pas deux fois : `_i8c_poids` (0,9 Gio) se construit au premier `_int_mm`, après le repack, dans les deux bras. **Rectification 07 h 35 (Manon) : mon hypothèse « processus étranger » était fausse** — le llama-server pid 4204 est sur la 3080 Ti (bus 02:00.0), la 5090 était à 15 Mio à 07 h 18 comme à 07 h 33 : l'OOM est entièrement dans A. Ce que les chiffres disent (07 h 45, lecture) : **l'alloué est nominal et n'a pas grossi** — 20,83 Gio = **17,05 de poids** (somme des safetensors du modèle qkvo-i8c) + **2,90 de KV** (63 360 créneaux × 48 couches × 2 × 4 têtes × 128 × 1 o int8 = 48 Kio/jeton ; le 19/09 : 64 768 créneaux = 2,97, **plus** qu'aujourd'hui) + ≈ 0,9 d'activations du préfill 2 047 et de la pile en construction ; rien de persistant n'est ajouté au chargement depuis `cce062b` (diff `loader.py` : `eco.poser_pour_ce_processus` seul ; `kv_canal` inactif, `ALLOC_EXTENSIBLE` vide ; `_i8c_poids` 0,9 Gio vient au premier `_int_mm`, après le repack, dans les deux bras). **Seconde rectification 07 h 48 (Manon) : mon hypothèse « fragmentation » tombe aussi** — sous `ACVRAM_ALLOC_EXTENSIBLE=1` les **deux** bras meurent (« expandable_segments: memory mapping failed », 31,1 Gio) ; et par lecture de Manon **la cause est dans la branche** : `oceane-c15-prefill` (depuis `oceane-11` = `ca0108de`) **ne contenait pas `c48c2b2c`** (`_rendre_le_cache_apres_la_pile`, main 0.6.29) — les 9,61 Gio « réservés non alloués » sont **le cache des piles Marlin non rendu** après le repack, le mécanisme déjà vu sur gemma ; le témoin, qui alloue plus de transitoires au préfill 2 047 que B (`repeat_kv` ×2, tampon `out`, chaîne torch de l'épilogue en fp32 [2 047 × 4 096] ×4…), n'a plus la place, B si — d'où « A tombe, B passe » avec le même chargement. Correctif : **`origin/main` (≥ `53ecbce4`, HEAD `e9b229db`) fusionné dans la branche à 07 h 48 (`e4f35f57`, sans conflit)** ; `model.py:1020` appelle désormais `_rendre_le_cache_apres_la_pile()` après chaque pile ; 71 tests à sec verts (`test_pile_rend_le_cache.py` compris). Le harnais garde `torch.cuda.memory_stats` (alloué, réservé, `inactive_split` petit/grand pool, segments, tentatives) après chargement / premier préfill / fin — **la garde est là** (Sage 07 h 40) ; le repli `ALLOC_EXTENSIBLE` est retiré de la chaîne. Deux hypothèses fausses en une heure sur la même OOM : la première hors processus, la seconde sur l'allocateur — toutes deux plausibles, aucune vérifiée ; la lecture de Manon (diff de la branche contre main) était la bonne méthode.

**Second passage carte (Manon, `14d66909`, 07 h 48)** : tests **35 passed** ✓ ; **B = 90,4 ms** (arrêt ≥ 90 maintenu par Sage) : **rmsnorm_warp 3,82 ms** (`edd44987`, +1,7 au-dessus du bloc 2,12, +1,3 au-dessus de ma première version 2,56) → **FAUX, `norm` retirée du défaut** (`PREFILL_COMPACT_DEFAUT` = toutes sauf norm ; le noyau reste, opt-in `ITEMS=…,norm`) ; glue_moe 6,81 (> 6,0, les index) ; experts_marlin 51,5 (49,4 le 19/09 : dérive de carte) ; épilogue 1,01 / quant_a8 0,45 / tri 1,43 dans les fourchettes. **Ce que je prédis mal sur la norme** — deux versions, deux fois plus lente que le bloc, dans deux régimes différents : (v1) 32 latences en série par ligne — juste ; (v2) la ligne en 64 registres par lane, chargements indépendants — **et pourtant 39 µs par appel** : ce que je n'avais pas compté, (a) **les accès** : le bloc lit H contigu par 1 024 fils (chaque instruction de warp couvre 64 o contigus, mais 32 warps du même bloc lisent 32 tranches contiguës en même temps → lignes de cache pleines), le warp-par-ligne fait lire à chaque lane `x[32·wq + lane + 1024·r]` : un warp couvre 64 o contigus par instruction et **saute de 2 Kio entre deux instructions** — 64 instructions de 64 o par ligne au lieu de 4 de 512 o ; en chargement par uint4 (8 bf16 par lane) on ferait 4 instructions par ligne, mais alors la découpe des partiels par fil (t, t + TH) n'est plus celle du bloc et l'ordre de la somme change — c'est la contrainte « au bit » qui force les accès de 2 o ; (b) **l'occupation** : 191 registres → 1 bloc de 8 warps par SM (au lieu de 12 possibles), 256 blocs pour 170 SM = 2 vagues dont la seconde à moitié vide ; (c) 2 047 lignes ne font que 2 047 warps : le bloc, lui, lance 65 000 warps. Conclusion : **un warp par ligne à 2 o par lane ne bat pas le bloc à 1 024 fils** ; la seule voie compatible avec l'ordre de somme serait de garder un bloc par ligne et de vectoriser SES accès (un fil lit 2 éléments : les mêmes qu'aujourd'hui, en un uint32 — x[t] et x[t + 1024] ne sont pas adjacents, donc non) — ou d'accepter un ordre de somme différent, ce que le scellé PPL au bit interdit. La norme reste au bloc (2,12 ms).

**Prédiction refaite (07 h 55, sans norm, Marlin à 51,5)** : B = 90,4 − 3,82 + 2,12 = **88,7 ms** ± 0,5 sur cette carte (86,6 si Marlin revient à 49,4) ; le « ≤ 83 » FAUX ; A attendu 96,8 + 2 (dérive Marlin) ≈ **98-99** ; B/A ≈ 0,90.
**(1) B : 88,6 ms de noyaux (somme CSV), 96,4 ms de passe, 21 233 j/s** — au-dessus de ma prédiction 83-86 et du scellé 83 ; −8,5 % contre 96,8 ; +19,4 % de j/s contre 17 784 (sous nsys, horloge libre : à ne pas comparer au servi). Par famille (`familles.py`, B seul — rendu robuste à l'absence de A) : epilogue_i8c **1,01** (prédit 1,0-2,5 ✓ : 0,90 ms × 192 à 4,7 µs, acc relu du L2), quant_a8 **0,45** ✓, résidu add **0** ✓, tri_experts **1,43** ✓ (radix ×192 0,91 : 4 passes), copies_autres **0,56** ✓ (0,2-0,9). **Ce qui manque** : (i) **rmsnorm_warp 2,56 ms** (26,4 µs/appel) — prédit 1,0-1,5, **plus lent que le bloc (2,12)** : ma première version bouclait `#pragma unroll 1` sur les 32 warps rejoués avec deux chargements par tour → 32 latences mémoire en série par ligne, et 2 048 warps pour 170 SM (12 par SM) ne couvrent rien ; (ii) **glue_moe 6,75** (prédit 4,5-6,0) : le plancher 5,73 (moe_act 1,97 + reduce 1,62 + gather 1,18 + moe_route 0,96) plus **1,0 ms d'index** (`flat_e[ordre]`, `debut_pad[e_sorted]`, `debut_tri[e_sorted]`, scatter de `inv`) que j'avais comptés 0,3 ; (iii) hors de mes fusions : Marlin **50,99** (49,44 le 19/09, +1,5), gemm int8 7,71 (+0,5), flash 12,82 (+0,1) — dérive de la carte (horloge libre), pas de la branche. Somme des trois : 88,6 − (83-86) = 2,6-5,6 ✓. Corrigé pour (i) : `rmsnorm_bf16_warp_kernel<TH, EPT>` charge la ligne entière en registres d'abord (64 par lane, chargements indépendants émis d'un bloc), même ordre de somme depuis les registres, sortie depuis les registres, réservé à H ≤ 2 048 (`NORME_WARP_H_MAX`, GLM garde le bloc) ; prédiction refaite ci-dessous. (ii) reste (les index sont la structure même de la permutation ; le gather dans Marlin est le chantier suivant).

**Prédiction refaite (07 h 40, avant la seconde carte)** : rmsnorm 2,56 → **1,0-1,5** (11 µs de bande + lancement) ; B = **87,0-87,6 ms** sur la même carte que le 88,6 (Marlin à 51), 85-86 si Marlin revient à 49,4 ; le « ≤ 83 » **FAUX** (structurel : glue MoE 6,7 + épilogue 1,0 + Marlin/flash/gemm int8 71,5 = 79 avant toute glue) ; servi B ≥ 20 500 : **TENU** prédit (21 233 sous nsys, horloge libre, 96,4 ms de passe pour 88,6 de noyaux : le hors-noyaux est tombé à 8 %) ; issue gênante : la carte sous éco 2 700 rend moins que sous horloge libre — le servi ABAB dit lequel.

### État à la pause (20/09 06 h 44, pause du groupe demandée par l'utilisateur à 06 h 43 ; reprise 06 h 54)
* **Fait** : régime `PREFILL_COMPACT` (défaut 0) ; quatre fusions au bit avec leurs tests (épilogue i8c, A8 partagée, résidu différé, permutations MoE + `aligner_blocs_tries` sans scalaire hôte + compte d'usage différé : commit `65139849`) ; chaîne carte `scratchpad/c15-prefill-20-09/chaine.sh` (+ `familles.py`, `ppl-au-bit.py`) ; fiche ci-dessus ; branche `oceane-c15-prefill` poussée ; 22 tests à sec verts (`tests/test_prefill_compact.py`), 1 test carte jamais joué (aucune carte prise).
* **En cours** : rien d'ouvert dans l'arbre (tout est commité) ; en lecture au moment de la pause : d'où viennent les 2 copies `elementwise <128,4>` de 10 µs par couche (0,98 ms, famille « copies_autres ») — `rope_fusee` fait `q = q.contiguous()` (`layers.py:799`), candidat non vérifié.
* **À faire** (prochain geste) : Manon joue `bash scratchpad/c15-prefill-20-09/chaine.sh` (40 min ; (0) les tests sur carte d'abord — si `test_prefill_moe_marlin_compact_au_bit_avec_le_temoin` ou la PPL au bit tombe : `BISSECTION=1` nomme la fusion, `ACVRAM_PREFILL_COMPACT_ITEMS` la retire) ; puis, selon le verdict : rmsnorm en un warp par ligne (ordre de réduction rejoué, ≈ −1 ms), copie de `rope_fusee`, gather des lignes dans Marlin (−1,2 ms), et le `_route` (moe_route 19 µs/appel).
