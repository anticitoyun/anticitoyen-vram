# Pièce 71, jalon 1 — Marlin dense pour qkv/o : RÉFUTÉ. Le port n a pas de GEMM dense ; la GEMM groupée à E = 1 est exacte mais 1,8 à 2,5 × PLUS LENTE que l int8 servi (qkv 16,95 contre 9,49 µs, o 25,5 contre 10,3, L2 froid, M = 12 : 80 et 32 blocs seulement) ; et la prémisse « + 0,57 ms de projections » se relit à + 0,26 (le reste était la tête et de la glue) — le vrai reste contre vLLM à b=12 est le MoE : 3,145 contre 2,551 ms/pas avec le même noyau Marlin (+ 0,59)

* instrument : `outils/gpu/mesure/banc-marlin-dense-e1.py` (harnais L2 froid de la p57 : 24 copies des poids en graphe, `chrono_froid`) — int8 servi `gemm_etroit(compact=True)` contre `MP.gemm_moe` à E = 1 (12 jetons routés vers l expert 0, bloc 16, top_k 1, poids `preparer_pile` sur `quantize_nvfp4`), exactitude ≤ 2⁻⁷·max par ligne contre x @ déquant fp32 ; relecture des traces p59 (`familles-vllm.txt`, `familles-acvram-officiel.json`) et p62 (`tensor2`, détail par noyau)
* commit : 7e5b37b8 ; carte 1 min 25 (`gaelle-p71-banc-dense`), horloge libre
* régime : banc hors service (horloge libre, carte vide) — les µs de service (eco 2700, L2 partagé) sont ceux des traces, pas du banc
* scellé (carnet 05 h 4x, avant) : qkv 5,5-7,0 µs, o 5,0-6,5 (≥ 25 % sous l int8 sur les deux formes, seuil Jerome) ; réfuté si qkv > 7,3 ou o > 7,7 ; gênant nommé : « 80 ou 32 blocs seulement à E = 1, o pourrait rester à 0,5 To/s »
* mesuré : int8 servi froid qkv **9,49 µs (1,13 To/s)**, o **10,3 (0,83)** ; Marlin E = 1 : qkv **16,95 µs (0,35 To/s)**, o **25,5 (0,19)** — 0 valeur hors 2⁻⁷ (exact) ; gain − 79 % et − 148 % ; relecture p59/p62 : nos projections à b=12 = 96 lancements `_etroit_reduit` ([80×4] 10,1 µs qkv + [32×11] 11,4 µs o) = **1,035 ms/pas** (10,8 µs/lancement), le « 1,345 » de la p59 comptait la tête (0,19) et 48 petits noyaux (0,06) ; vLLM `marlin::Marlin` 4 bits : 96 × 8,0 µs = 0,771 → écart projections **+ 0,26 ms/pas**, pas + 0,57 ; MoE : nous 3,145 (p63, 144 lancements : gate, up, down) contre vLLM 2,551 (96 lancements : gate·up fusés, down) → **+ 0,59 ms/pas**, le plus gros poste restant
* verdict : **jalon 1 réfuté, par le gênant nommé** — la GEMM groupée à un expert n occupe que N/64 blocs (80 ou 32 sur 170 SM) et lit à 0,19-0,35 To/s ; le Marlin DENSE de vLLM (`gptq_marlin_gemm`, non porté : ~2 000 lignes, stream-k sur les tuiles K×N) fait 8,0 µs en service sur ces formes, soit − 16 % (qkv) / − 22 % (o) contre notre int8 froid : **sous le seuil de 25 % même une fois porté**, et son gain plafonne à 0,26 ms/pas (≈ 4 % du pas, moins de la moitié des 9,1 %) ; la prédiction « − 0,4 à − 0,5 ms/pas » par les projections n est pas atteignable. Le poste qui porte l écart est le MoE lui-même, avec le MÊME noyau : 3 lancements par couche chez nous (gate, up, down : 65,5 µs/couche) contre 2 chez vLLM (gate·up interfoliés en une GEMM N = 1 536, puis `silu_and_mul`, down : 53 µs/couche en service).
* durée : 30 min (dont 1 min 25 de carte) ; gênant nommé et arrivé

## Pièce 71 bis proposée — gate·up fusés dans le chemin tensor (comme vLLM)
Une seule GEMM Marlin par couche pour gate et up : poids interfoliés par colonnes (w13, N = 1 536) préparés une fois au chargement (`_construire_marlin`, `preparer_pile` sur la pile concaténée — les échelles globales de gate et d up diffèrent : à porter par colonne, comme le fait `moe_wna16_marlin_gemm` avec `g_marlin` par expert… à vérifier, sinon égaliser les deux échelles globales à la conversion), puis `moe_act` lisant les colonnes interfoliées. 144 → 96 lancements/pas. **Prédiction** : MoE 3,145 → 2,65-2,80 ms/pas (**− 0,35 à − 0,50**, ≈ 55-58 µs/couche : le coût fixe d une GEMM Marlin à 33 experts ≈ 8-10 µs disparaît, l arithmétique ne change pas) ; au bit contre le chemin actuel : NON (l ordre des sommes gate/up ne change pas, mais les échelles interfoliées peuvent différer d un bit : 2⁻⁷·max + KL 5/5 comme scellé) ; réfuté si < − 0,2 ms/pas ou si le KL bouge. Frontière b=12 : 6 200 → 5 750-5 900.
