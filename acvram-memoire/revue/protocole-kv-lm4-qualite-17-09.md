# Protocole — KV lm4 qualité : PPL de décodage à préfixe 8 192 sur Coder-30B, 5 bras (bf16, int8, lm4, lm2, tq3+1), régime classé

instrument : `scratchpad/ppl-decode-kv-17-09.py` (dérivé de `outils/ppl-decode-mma-coder30b.py` : préfixe de 8 192 jetons de wiki-gptq entré par le prefill, puis 512 jetons notés un par pas en teacher forcing ; NLL par jeton dans le JSON ; `regime_ligne()`, `engine.regime_ligne()`, `cfg.dtype` effectif, `bytes_per_block`, `capacity_tokens`, `nvidia-smi memory.used` après chargement) ; chaîne `scratchpad/kv-lm4-17-09/chaine.sh`, un processus par bras.
commit : arbre laure 525895a (= main) ; `Qwen3-Coder-30B-A3B-nvfp4` ; régime classé `ACVRAM_MOE_MMA=0 ACVRAM_MOE_DECODE_MMA=0 ACVRAM_NARROW_GEMM=1`, b=1, graphes demandés (lm4 : `graphs.paged_ok` exige int8 → `graphs.py:299-300`, le décodage lm4 passe par torch — aucune vitesse n'est publiée).
bras : `ACVRAM_KV_FORMAT=bf16` · `int8` · `lm4` · `lm2` · `tq3+1`.
scellé (Sage, `sage-kv-lm4-et-campagne-menus-17-09` § 1, repris) : contrôles en premier — int8 / bf16 < 0,002 (sinon l'instrument ne voit pas le cache : arrêt) et lm2 hors tolérance (> + 0,010 : un instrument qui ne casse pas à 2 bits ne prouve rien à 4) ; lm4 / int8 ≤ 1,005 ET capacité ≥ 1,8 × int8 → régime opt-in nommé ; > 1,005 → réfuté à 4 bits LM. Prédiction Sage : lm4 / int8 = 1,002-1,004. Le scellé § 5 (+ 0,003 / réfuté > + 0,010) s'applique tel quel.
mes prédictions : lm4 / int8 = 1,003-1,006 (contexte 8 k : l'erreur de 4 bits s'accumule sur 8 192 clés relues à chaque pas — au-dessus de la fourchette Sage) ; lm2 / int8 ≥ 1,03 ; capacité exacte lm4 / int8 = 1,97 (`bytes_per_block` : 132 contre 260 octets par tête-jeton, mêmes échelles) et `memory.used` identique à ± 100 MiB (le budget KV du plan est fixe, seul le nombre de jetons change) ; **`tq3+1` n'existe pas dans l'arbre** (`acvram/memory/kv_lm4.py:27` `FORMATS = ("lm4", "lm3", "lm2")`, `tiering.py:49-50` ValueError) — bras rendu comme refus, pas mesuré.
falsification : int8 / bf16 ≥ 1,002 → l'étalon bf16 ou l'instrument est en cause, aucun verdict sur lm4 ; lm2 dans la tolérance → instrument aveugle au cache, aucun verdict ; lm4 > 1,005 → réfuté ; capacité < 1,8 → conception réfutée même à PPL tenue ; `tq3+1` chargé sans erreur → ma lecture de l'arbre est fausse.
