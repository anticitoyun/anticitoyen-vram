# Sage — duel GLM réfuté ×4 : on le publie tel quel, B se réduit à (c) comme scellé, et le chantier MLA a déjà sa cause dans le code — le cache latent est relu 40 fois par pas et par couche (16/09)

1. **Publier tel quel, maintenant.** `sage-objectif-14-09` l'avait écrit : « réfuté si vLLM ≥ notre débit à b=12 avec ≤ 1,5 × nos octets — alors B se réduit à (c) et il faut le dire ». C'est le cas (×4,08, ×6,1 au prefill, PPL 1,0018 comparable). Ce qui se revendique encore : rien contre vLLM sur les modèles qui tiennent chez lui ; la table du comparatif à cinq (`sage-comparatif-16-09`) reste l'objectif suivant. **Ce point engage l'utilisateur** : l'objectif « plus performant que tous les concurrents » n'a pas de date sur GLM ; je propose « parité MLA puis table publiée », il tranche.
2. **La cause n'a pas besoin d'un nsys pour être nommée** (`acvram_kernels.cu:4050-4180`, `engine/mla.py:1-10`) : l'absorption est déjà faite (bien), mais le décodage MLA est en **deux noyaux naïfs** : `mla_scores_kernel` lit le cache latent `[L, 576]` **une fois par tête** (grille `(H, L/128)`, un fil par ligne), écrit `scores [H, L]` en mémoire, puis `mla_reduce_kernel` relit le cache **une seconde fois par tête** pour `Σ p_r · cache[r]`. À b=12, ctx 2 048, 20 têtes, 46 couches : 20 × 2 × 2 048 × 1 152 o × 46 × 12 ≈ **52 Go lus par pas**, ≈ 30 ms à 1,7 To/s — c'est le 32,8 ms mesuré. En forme absorbée, les 20 têtes partagent **le même** cache (une tête KV de 576) : il se lit **une fois**, 1,3 Go par pas, ~1 ms. vLLM (~4 ms) en est la preuve d'existence sur cette carte.
3. **Chantier MLA, Laurine, 2 jours, note d'ordre = celle-ci** : un noyau **à une passe** par (séquence, couche) — tuiles de 64-128 lignes du cache lues une fois ; scores des 20 têtes par `mma` bf16 (M = 32 têtes rembourrées, K = 576) ; softmax en ligne par tête ; accumulation `o_lat [20, 512]` par `mma` sur la même tuile ; découpe de L entre CTA avec recombinaison (flash-decoding) ; table d'adresses `[B]` existante ; godets doublants inchangés. **Équivalence** (même commit) : top-1 identique et cos ≥ 0,9999 contre les deux noyaux actuels sur 16 positions × 2 couches ; PPL 3 tranches dans ± 0,004. **Scellé perf** : `mla_*` ≤ **6 ms** à b=12 ctx 2 048 (réfuté > 10 ms → pas borné par la bande passante, ncu alors, pas avant) ; pas b=12 ≤ **30 ms** (réfuté > 36 → 10 ms cachés ailleurs, nsys découpe). Attendu après : ~440-480 t/s = **0,55-0,6 × vLLM** — pas la parité : le reste (23 ms hors MLA contre 13,6 ms de pas Coder entier) est le second chantier, et il n'a pas encore de nom.
4. **Ce qui se lance maintenant, 15 min de carte, Laure** : (i) **PPL vLLM même corpus** (10 min) — la colonne « PPL comparable » n'a qu'un bras ; (ii) **nsys d'un pas b=12** (5 min) : pas pour trouver la cause, pour **l'en-tête du chantier** — MLA / MoE / denses / trous, en ms, avant et après le noyau. Pas de ncu.
5. **Océane et Manon** : la sonde par étape et cb2784b (`sage-glm-pile-correctif` § 8.3) passent **après** le noyau MLA — 2 % de PPL ne pèsent rien devant ×4.

## Ordre

* **Jérôme** : publier le verdict du duel (85c52a1) avec la ligne « B réduit à (c) » ; porter à l'utilisateur le point 1 ; `ETAT.md` : chantier MLA en tête, file de carte = (4) puis Laurine.
* **Laure** : PPL vLLM même corpus, nsys d'un pas b=12 (en-tête), 15 min ; puis veille.
* **Laurine** : noyau MLA à une passe (point 3), scellés écrits ci-dessus, 2 jours, worktree figé.
* **Océane, Manon** : veille.

## § 6 — nsys (ade242f) : ma cause du § 2 était fausse en grandeur ; le pas est borné par les lancements, pas par le cache ; et le duel n'était pas apparié en qualité

1. **Correction, la mienne** : j'ai attribué 32,8 ms à la relecture du cache par tête depuis le code ; le nsys de Laure donne **6,4 ms d'attention MLA** sur un pas de 43,9 ms, et **15 534 lancements** (Coder : 1 517) : 2 845 `int8_gemv<4,1>` à lot 1 (5,6 ms), ~5 000 cat/copies (10 ms), 8 ms d'élémentaires, 12 rmsnorm par couche (1,7 ms), 5,3 ms de trou. **Le chemin MLA du décodage tourne séquence par séquence.** L'en-tête que j'avais exigé avant le noyau a servi à ça : la lecture du code donne le site, la mesure donne la grandeur (REGLES §5 — je le range dans mon compte du jour, troisième).
2. **Périmètre du chantier MLA, réordonné** (Laurine, même 2 jours) : **commit 1 = batcher le chemin de décodage MLA au lot b** — projections int8 en un GEMM M = 12 (chemin `narrow`/int8 existant), normes, RoPE, `cat` et écriture du cache en noyaux `[B]` (les tables d'adresses `[B]` et `decode_static_batch` existent, `mla.py:324-400` ; le verrou est dans `peut_batcher_decode` et la boucle `model.py:1252`). Scellés : **lancements par pas ≤ 2 500** (même nsys) ; **pas b=12 ≤ 22 ms** (réfuté > 26 → le trou de 5,3 ms est hôte/synchronisation, chantier suivant) ; équivalence : top-1 identique et cos ≥ 0,9999 contre le chemin séquentiel sur 16 positions × 2 couches, PPL 3 tranches ± 0,004. **Commit 2** = noyau d'attention à une passe (§ 3, 6,4 → ≤ 3 ms). **Commit 3** = latent fp8 (`sage-avis-exterieur` § 6). Attendu après les trois : ~17-19 ms, **vLLM 15,1** — parité à portée, pas acquise.
3. **Qualité** : vLLM GadflyII rend **1,056** (KV fp8) / 1,064 contre nos 1,0018 — hors du seuil 1,02 du comparatif : **sa vitesse ne se classe pas**, et le verdict se republie avec cette réserve, mot pour mot : « bras vLLM à +5,4 points de PPL, non apparié ». Ce que la réserve **ne dit pas** : que le ×4 s'achète avec la qualité — mêmes octets NVFP4, le nôtre est dans 15 534 lancements. Pour un bras vLLM apparié : conversion ModelOpt NVFP4 par nous depuis `srcbf16` (Manon, dans le comparatif à cinq, pas avant), PPL scellée ≤ 1,02.
4. **Commit 2 (89bb888) : TENU sur ce qu'il visait** — le 6,4 ms de référence était l'attention (scores + reduce, les seuls noyaux `mla_*` de l'ancien chemin) ; son successeur est `mla_1p_kernel + combine = 2,33 ms ≤ 3`. `mla_prep_batch` (0,95) est un noyau **nouveau** qui remplace une part des 10 ms de cat/copies de l'ancien chemin : il se juge dans le pas ≤ 22 ms, pas dans le scellé de l'attention. Les deux chiffres se publient côte à côte avec la regex ; leçon : un scellé sur un regroupement nomme sa regex et sa liste de noyaux au moment où il s'écrit.

## § 7 — Bras D (de287c5) : D = C à six décimales → d'abord prouver que le fp8 a pris, puis l'équivalence contre la boucle d'origine ; pas de fusion avant ; le W4A4 coûte +1,42 % au décodage GLM → DECODE_MMA=0 sur GLM

1. **D = C exactement** a une explication plus simple que « le cache n'est pas lu » : **le chemin fp8 n'a pas été pris** (REGLES §3 : prouver que la configuration a pris, dans le processus qui mesure). Si le cache était ignoré, la PPL ne serait pas 8,00 : à ctx ≈ 300 l'attention sans passé rendrait un chiffre bien au-dessus. Contrôle 1, Laure, 5 min : dans la passe D, **compte de lancements de `mla_1p<fp8>` contre `<bf16>`** (nsys, même regex) et **octets alloués au cache latent** (÷ 2 attendu). Zéro lancement fp8 → bras D non jugé, on corrige la prise du drapeau et on rejoue (35 min) ; lancements fp8 présents et D = C quand même → alors le noyau lit autre chose que le cache qu'on écrit, et le test de Laure (relecture de `st["cache"]` après 100 pas contre le latent recalculé) devient le bon.
2. **La boucle d'origine contre le chemin batché : +0,21 % de PPL** (8,0175 / 8,0003) est **dans le bruit de l'instrument** (graphes/eager 0,0014 ; étendue 0,008 sur 3 tranches, `sage-narrow-verdict` § 6) : la PPL ne tranche pas. Ce qui tranche est le scellé d'équivalence du commit 1 que je n'ai pas vu appliqué **contre la bonne référence** : les « bit-identiques » de mla1-3 comparaient des chemins batchés entre eux — une équivalence nomme sa référence, et la référence est **le chemin qu'on remplace**. Contrôle 2, Laure, 10 min : `equivalence-glm-2couches` boucle d'origine (`MLA_BATCH=0`) contre batché, 16 positions × 2 couches, **delta en ulp de ligne, top-1 identique, cos ≥ 0,9999**. Ordre de sommation → quelques ulp, top-1 égal : les gains mla1-3 sont acquis, on fusionne ; deltas ≫ 10 ulp ou top-1 différent → le chemin batché lit ou écrit mal (cache, RoPE, norme), Laurine, **rien ne fusionne avant**.
3. **Fusion de `laurine` dans main : après le contrôle 2 conforme**, pas avant — c'est l'ordre inverse de (c).
4. **Chiffre séparé, et il décide quelque chose** : `MOE_DECODE_MMA=0` → 7,8886 contre 8,0003 : le **W4A4 au décodage coûte +1,42 % sur GLM**, au-dessus du scellé de `sage-glm-w4a4` § 2.2 (≤ 1,005, réfuté > 1,010 → MIN_T monte). Conséquence écrite d'avance, appliquée : **GLM tourne avec `ACVRAM_MOE_DECODE_MMA=0` dans le régime du duel** (le chemin MMA est pris plus souvent que je ne l'avais dit à b=12) ; le pas se remesure une fois dans ce régime (attendu +1 à +2 ms sur ~6 ms de MoE) et la colonne PPL du duel porte désormais **la PPL en mode décodage** (celle de l'instrument de Laure), pas celle du prefill. Le chantier « W4A4 GLM » (`sage-glm-pile-correctif` § 8.3) couvre les deux sites.

## § 8 — Sonde (4a130e0) : le chemin batché ne lit pas `st["cache"]` (logits identiques au bit après zérotage, len figé à 511) ; équivalence réfutée (top-1 53 %) ; main à sonder maintenant

1. **Rien ne fusionne** ; les 17 ms des commits 1-3 sont le temps d'un chemin qui ne calcule pas la bonne attention — ils ne valent rien tant que la sonde ne passe pas.
2. **Main ce71723 (régime du duel) à sonder, 5 min, Laure** : même geste. Touché → **erratum immédiat** sur le duel publié (chiffre retiré, régime invalide) ; sain → défaut propre à mla1-3.
3. **Pourquoi l'instrument était aveugle** : en teacher-forcing à prompt d'un jeton, les jetons décodés *sont* le corpus ; un chemin qui ne relit que ses propres jetons rend une PPL presque juste. **Règle** (REGLES §4 bis à ajouter) : tout changement d'un chemin de cache passe la sonde « cache zéroté → les logits doivent changer » et une PPL en mode décodage **à prompt ≥ 128**.
4. **Cause probable** (Laure) : tables `ptrs`/`len_ptrs` capturées dans le graphe et pointant des tampons réalloués après capture. Correctif attendu : tables `[B]` en tampons **statiques** du `GraphRunner`, remplies avant chaque rejeu ; `len` lu depuis un tampon statique mis à jour, jamais une valeur capturée. Scellé du correctif : sonde passée (logits changent au zérotage, `len` suit la longueur), équivalence contre la boucle d'origine ≤ quelques ulp, top-1 identique 768/768 hors ex-aequo ; ensuite seulement les temps de mla1-3 se remesurent.

## § 9 — Arbitre prefill (d70e002) : main sain, le duel tient ; le § 8 est retiré (sonde sur le mauvais tampon) ; l'arbitre devient le juge de toute équivalence de décodage

1. **Retiré** : « le batché ne lit pas le cache » et « ptrs capturés périmés » (§ 8.1, 8.4) — la sonde zérotait `DecoderBlock.statics` (un slot de warm-up), pas le magasin d'états ; les 53 % de top-1 étaient des bifurcations greedy sur quasi-égalités. Ma règle du § 8.3 (« cache zéroté → logits changent ») reste vraie mais **se pose sur le magasin d'états des séquences vivantes**, pas sur un tampon de bloc.
2. **Juge adopté** (règle de Laure, REGLES §4 bis) : toute équivalence de décodage se juge **contre le prefill du même texte** (prompt réel ≥ 128 + k jetons, MoE égal, W4A16 partout), top-1 et cos sur ≥ 84 points par bras ; jamais contre un autre chemin batché. Témoin de l'instrument : boucle 83/84, défaut 81/84 — l'écart d'un chemin correct est de cet ordre ; scellé pour un chemin nouveau : **≥ 80/84 et cos ≥ 0,9999**, réfuté en dessous.
3. **Go pour (2)** : rejuger `laurine` (mla1-3 + fp8) avec cet arbitre, 5 min ; conforme → fusion et les 17 ms sont acquis ; réfuté → Laurine, avec l'arbitre comme test du commit. **(1)** ensuite, 5 min : fp8 identique au bit = vérifier que la conversion s'applique au **magasin d'états** (le cache que le noyau lit), pas au seul `statics` — même erreur de tampon probable ; si le magasin est bien fp8 et les logits identiques, alors E4M3 sur 576 dims ne change rien à ctx 300 et ça se publie comme tel (avec un contrôle à ctx 2 048).

## § 10 — Erratum (23dc9f3) : le régime à 12 créneaux calcule faux (58/84, les 30 premiers jetons après le prefill), le ×4,08 est retiré ; localisation en 15 min avant que Laurine touche au code

1. **(a), tout de suite** : erratum formel sur le duel (chiffre b=12 retiré, cause en cours), Laurine en priorité absolue, tout le reste suspendu. **Les b=1 et b=4 du duel sont à revérifier aussi** : ils ont tourné dans le même processus à `HYBRID_SLOTS=12` ; arbitre à leur configuration exacte, 5 min chacun — ils restent publiés seulement s'ils passent.
2. **Ce que le motif dit déjà** : faux à k = 2-10, juste à k ≥ 32, même chiffre boucle et batché → ce n'est pas l'attention, c'est **l'état chargé au passage prefill → créneau** (`static_load`) qui est faux au-delà de 4 créneaux, et il ne compte plus dès que les jetons décodés dans le créneau dominent l'attention. Deux causes candidates, un test qui les sépare (Laure, 15 min, avant tout code) : arbitre à `HYBRID_SLOTS = 5, 8, 12` avec b = slots. **Faux dès 5** → `static_load`/magasin d'états au-delà de 4 (taille figée, indexation, `len`) ; **juste à 5 et 8, faux à 12** → le godet 16 et `_SID_REMBOURRAGE` (`graphs.py:520-537`) : les 4 créneaux fantômes écrivent ou lisent dans les 12 vrais. Laurine reçoit le résultat avec le fichier:ligne, pas une piste.
3. **Règles de commit, définitives** : tout arbitre à `HYBRID_SLOTS = b` visé ; tout chiffre de pas publié porte ses slots ET son verdict d'arbitre à ces slots dans l'en-tête ; les temps mla1-3 (17,2 ms) se conservent comme temps, ne se publient pas comme gain tant que l'arbitre à 12 ne passe pas.
4. **Ce qui ne bouge pas** : la colonne PPL de prefill du duel, le comparatif à cinq (suspendu, pas annulé), narrow ON en 0.6.7 (mesuré à slots 12 aussi — **à revérifier par l'arbitre avant la livraison 0.6.7**, une ligne dans `ETAT.md`).

## § 11 — Seuil de créneaux (14b121f) : faux dès b=1 aux graphes, juste en eager → c'est le rejeu du `GraphRunner` sur le lot MLA ; Laurine maintenant, un plancher eager de 10 min, pas de duel en eager

1. **(a)** : Laurine, priorité absolue, avec le fichier:ligne de Laure (`graphs.py:512-537`, `model.py:1708-1726`, `mla.py:345-355`, `:89`). Motif à expliquer : pas 1 juste, pas 2 faux → ce que le rejeu relit (ligne écrite au pas 1, `len`, palier `godet_mla`, table `ptrs` de `_mla_lot`) est celui de la capture, pas du créneau vivant. Test d'abord (à sec possible avec un créneau factice) : `len` / `data_ptr` / palier vus par le graphe contre ceux du créneau, à chaque pas de 1 à 4. **Scellé du commit : eager = graphes à l'arbitre**, à `SLOTS = b` pour b = 1, 4, 12 (écart ≤ 2/84, cos ≥ 0,9999), et la sonde « magasin d'états zéroté → logits changent » sous graphes. Coder-30B n'est pas touché (chemin sans `_mla_lot`, témoin graphes/eager 0,9986 du narrow) — à dire dans le commit, pas à supposer : l'arbitre y tourne aussi une fois (5 min).
2. **Pas de duel en eager** (1 h de carte pour un régime qu'on ne livre pas). **Un plancher, 10 min, Laure** : pas b=12 en eager, arbitre passé dans le même processus, publié comme « temps du chemin juste, eager » — il borne le correctif (le rejeu corrigé doit rendre ≤ ce temps) et il dit tout de suite si les 17,2 ms étaient l'ordre de grandeur d'un chemin juste ou celui d'un chemin qui sautait du travail.
3. **Publication** : erratum étendu à b=1 et b=4 ; les 43,9 → 17,2 ms restent des temps de rejeu faux, non publiables comme gain ; tout se remesure après le commit 1, dans l'ordre : arbitre → pas b=12 → duel (Laure, 1 h) → puis mla1-3 rejugés à l'arbitre à 12 avant toute fusion.

## § 12 — MLA innocenté (d154ca5) ; Coder aussi diverge sous graphes (25/28, un point à |Δ| 15,5) : (b) — Océane sur le générique avec un test qui le chiffre, Laurine reste sur GLM

1. **Deux faits, pas un** : GLM sous graphes est faux à médiane 5,8 dès le pas 2 (contre 1,24 eager) ; Coder sous graphes a **un** point franchement faux sur 28 (25/28 contre 27/28 eager, cos min 0,872). Le témoin existant borne le second : sur Coder, graphes/eager divergent déjà de 7,9 % en top-1 sur 24 k jetons **sur des quasi-égalités**, et la PPL graphes y est **meilleure** que l'eager (8,4989 / 8,5110, `verdict-ppl-narrow-b12`) — un défaut générique, s'il existe, est petit sur Coder et **n'invalide aucun chiffre publié de Coder-30B** (leur PPL sous graphes est juste). GLM ×6 n'est pas « la même chose amplifiée » tant que ce n'est pas montré : c'est une hypothèse de Laure, pas un fait.
2. **(b)** : **Océane rappelée** (règle : scellé réfuté) sur le générique, **Laurine reste sur GLM** (`_mla_lot`, § 11) — les deux en parallèle, la carte à Océane 10 min. Test d'Océane, scellé : Coder-30B, b=4 W4A16, **84 points** (pas 28) sous graphes et en eager, arbitre prefill ; on compte les points **hors quasi-égalité** (|Δ| > 1 logit sur la référence). Eager ≈ 0 et graphes ≥ 3 → défaut générique confirmé, et son **pas** (k) le localise ; alors, même processus : **`x` d'entrée du pas 2 sous graphes contre eager** (jetons, embedding, positions, bit à bit — c'est la question de Laure, elle se pose sur un tenseur, pas sur une piste). Graphes ≤ 1 et eager ≤ 1 → le 25/28 était du bruit d'échantillon, le générique est clos, tout est GLM.
3. **Ce qui ne bouge pas** : erratum du duel, plancher eager (§ 11.2), rien ne fusionne ; Manon reste en veille.

## § 13 — Cause (7bdde94) : tête `lm_head` en bf16 sur le chemin fixe (`model.py:2146, :2153`), fp32 en eager depuis a5fac1c ; go, scellés

1. **Go, Laurine, un commit** : `h.to(torch.float32)` aux deux lignes **et** le test qui casse si on le retire (REGLES 9) : à sec, `forward_fixed` et `forward` sur un mini-modèle rendent des logits **égaux au bit** (même dtype fp32 en sortie) ; sur carte, **arbitre graphes = GRAPHS_EAGER = eager** : GLM à `SLOTS = 12` **≥ 80/84**, cos ≥ 0,9999 (référence boucle 83, eager 84 attendu), Coder b=4 **≥ 26/28** ; **coût** : pas b=12 GLM **≤ +0,3 ms** (une conversion `[12, 2048]`, la tête est déjà en int8 avec accumulation fp32), réfuté au-delà → on cherche où le fp32 s'est propagé plus loin que la tête.
2. **Ce que ça rétablit, dans l'ordre** : (i) arbitre passé → le **×4,08 du duel n'est plus faux, il est confirmé** si le pas remesuré vaut 56,1 ± 0,3 ms (10 min, Laure) — l'erratum devient « chiffre confirmé après correctif de la tête, qualité rétablie » ; (ii) **mla1-3 rejugés** à l'arbitre à 12 créneaux avec la tête fp32 (10 min) → fusion, et le pas ≈ 17-19 ms se remesure ; (iii) **duel prise B** avec mla1-3 (Laure, 1 h) : scellé inchangé, vLLM 15,1 ms contre nous, attendu 0,8-0,9 × son débit, **réfuté < 0,7** → le second chantier (trou hôte 5,3 ms) avant toute republication.
3. **Leçon, REGLES §7 (une ligne)** : *un correctif porté sur un chemin (eager) se porte le même jour sur ses jumeaux (fixe, graphes) et son test tourne sur chacun* — a5fac1c n'avait de test qu'en eager ; le chemin fixe a servi trois jours de mesures avec la tête en bf16.
4. **PPL teacher-forcée** : elle n'a vu que +0,3 % d'une tête qui bascule l'argmax — elle reste le juge de la quantification, **l'arbitre prefill devient le juge du décodage** (règle de Laure, § 9.2, désormais avec un nom de cause).

## § 14 — Prise B (52c019c) : 0,70 / 0,79 / 0,65 × vLLM, scellé 0,8-0,9 réfuté de peu ; (a) publier tel quel, puis deux postes sans coût de PPL, et le W4A4 comme chantier qui déverrouille le reste

1. **(a), publier maintenant** : réfuté (b=12 à 0,65 < 0,7), avec la réserve qualité du bras vLLM (PPL 1,056) et le régime (W4A16 partout, arbitré à `SLOTS = b`). La conséquence écrite au § 13.2 (« trou hôte d'abord ») visait 5,3 ms ; il en reste 1,2 après mla1-3 — la prémisse est tombée, la conséquence se re-cible, et ça se dit. Un résultat réfuté se publie (REGLES 10). **MLA : parité atteinte** (2,3 ms d'attention, ~3,3 avec la préparation, vLLM ~4) — l'objectif (1) du 16/09 est tenu sur le poste, pas sur le pas.
2. **Chantiers, par ms gagnée sans PPL perdue** :
   * **(i) projections int8 : fusion q/kv/o** (4,4 ms, Laurine, 1 j) — attendu **−2,0 ms**, PPL inchangée (même arithmétique, ordre de sommation ; arbitre ≥ 80/84) ; réfuté < −1,0 ms → les projections sont bornées par les octets, pas par les lancements, et on s'arrête là.
   * **(ii) tête : GEMV int8 avec accumulation fp32 directe**, sans conversion de `h` (0,85 → ~0,2 ms, Laurine, ½ j, même commit ou le suivant) — attendu **−0,6 ms**, logits égaux au bit au chemin fp32 actuel (test à sec).
   * Scellé après (i)+(ii) : **pas b=12 ≤ 19,0 ms** (≈ 630 t/s, 0,79 × vLLM), réfuté > 20,0.
   * **(iii) MoE MMA au décodage (−2,3 ms) et prefill MMA (×6 au prefill) : NON tant que le W4A4 coûte +1,4 / +1,9 %** — c'est le même verrou, et il se lève par la qualité, pas par le bouton : **Manon, cb2784b** (métrique W4A4 dans la recherche AWQ des experts, à sec 1 h + PPL 20 min), scellé **PPL NOMINAL MMA=1 ≤ 1,010 en prefill et décodage** ; réfuté → Hadamard par bloc (note séparée, > 1 j). Tenu → MMA aux deux sites, attendu pas ≈ 16,5 ms (0,92 × vLLM) et prefill ×3 au moins.
3. **Énergie** : 0,774 J contre 0,445 (×1,74) suit le pas ; à 16,5 ms attendu ≈ 0,60 J (×1,35) — la parité énergétique n'a pas de levier propre nommé ; elle ne se revendique pas.
4. **Ordre** : Laurine (i) puis (ii) ; Manon cb2784b à sec en parallèle ; Laure mesure (i)+(ii) puis la PPL de Manon ; Océane, Katy en veille ; comparatif à cinq après le scellé (ii).

## § 15 — (i)+(ii) réfutés (96e5af7 : −0,23 ms) : (a), le poste projections est borné par la latence par lancement (387 × ~7 µs), on s'arrête ; le seul levier restant est le W4A4 par la qualité

1. **Réfuté des deux côtés, conséquence appliquée** : le GEMM étroit remplace des lancements à coût égal ; ce poste est **latence par lancement**, pas octets ni tensor cores. Ce qui resterait : fusionner `q_a` et `kv_a` en un seul GEMM (même entrée) = −47 lancements ≈ **−0,3 ms** — pas un chantier, une ligne si Laurine passe par là, sans mesure dédiée. **Pas d'autre piste avant Manon** : à 21,1 ms, MoE W4A16 7,1 ms est le seul poste > 5 ms, et sa réduction (−2,3 ms) est verrouillée par +1,4 % de PPL — cb2784b est la clé, pas un noyau.
2. **Tête** : Laurine confirme par le nom de noyau nsys si le 0,85 → 0,2 de ce matin visait un noyau distinct ; sinon le poste vaut 0,14 ms et se raye. Une ligne dans son carnet, pas de passe de carte.
3. **Attendus après Manon** : tenu (≤ 1,010 aux deux sites) → MMA décodage + prefill : pas ≈ 18,8 ms (≈ 0,73 × vLLM), prefill ×3 ; réfuté → Hadamard par bloc, note séparée ; **et dans les deux cas, on publie le pas 21,1 ms comme état 0.6.7 de GLM**, avec ses postes : MoE 7,1 · projections 2,6 · MLA 3,3 · tête 0,85 · trou 1,2 · reste.
