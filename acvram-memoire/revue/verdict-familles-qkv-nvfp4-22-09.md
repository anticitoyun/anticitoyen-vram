# familles-noyaux sur l'alias Coder qkv-nvfp4 (pièce 42) — nvfp4 confirmé mais régime confondu (experts_layout=naturel) — 22/09 (poste2)

* instrument : `nsys profile --cuda-graph-trace=node` + `familles-noyaux.py --detail proj_etroites_int8`, `ACVRAM_MODELE_MESURE=/mnt/AI_GENERATOR/models_acvram/Qwen3-Coder-30B-A3B-nvfp4-qkv-22-09` (**1er essai invalide** : oublié de poser cette variable, a mesuré par défaut l'alias périmé `Qwen3-Coder-30B-A3B-nvfp4` du 31/08 — repéré avant jugement, non publié)
* commit : main 6c6a2cc8
* mesuré : le noyau étroites est bien `_dense_etroit_kernel` (pas `_etroit_reduit_kernel` int8) — **confirme q/k/v/o servies en NVFP4** (cohérent avec le manifeste : `format: nvfp4`, clés `qweight/block_scale/global_scale/act_scale`). MAIS le pas complet mesure **8,619 ms/pas (mur 9,034)**, très au-dessus de la référence -qkvo-i8c (~6,7-6,8 ms) : `experts_marlin` 144 lancements (contre 96 en référence), nouvelle famille `experts_quant_a4` apparue (0,623 ms, 144 lancements, absente en référence), `glue_torch` ×10 (0,487 ms/207 lancements contre 0,043-0,044 ms/17). Ligne de régime : **`experts_layout=naturel`** (contre `experts_layout=marlin` sur tous les alias mesurés aujourd'hui) et `chemin_moe=mma-a4(atteint=decode_mma)` (atteint ici, alors que « non-atteint » ailleurs).
* verdict : **NON COMPARABLE en l'état** — `experts_layout=naturel` au lieu de `marlin` est un défaut de RUNTIME (`model.py:429`, repack au chargement, dépend de `PREFILL_GROUPED`/conditions non identifiées ici) qui change tout le régime des experts, pas seulement le format qkv. La comparaison qkv-nvfp4 vs qkv-int8 serait confondue par cette différence de disposition MoE — pas un résultat exploitable sur la pièce 42 telle quelle.
* durée : ~4 min de carte (2 essais nsys)

## Suite
Avant l'ABBA prévu : établir pourquoi ce nouvel alias charge en `experts_layout=naturel` (repack Marlin échoué ou non tenté — condition à poste1, `model.py:429` et voisinage). Sans ce point réglé, tout chiffre de débit comparerait deux régimes différents, pas le format qkv seul.

**Complément (à sec)** : les experts sont **au format `nvfp4` identique sur les deux alias** (vérifié manifeste, `model.layers.0.mlp.experts.0.gate_proj.weight` : `nvfp4, bpw 4.5` sur -qkvo-i8c ET sur qkv-22-09) — le format sur disque n'est PAS la cause. `_liberer_pile_naturelle()` (`model.py:401-424`) ne repack en Marlin que si `self._stacks[nom][0] == "nvfp4"` — une propriété construite EN MÉMOIRE au chargement, distincte du manifeste ; la divergence se situe donc dans la construction de `_stacks` au chargement (`experts_repli=identite` du nouveau manifeste, ou un autre champ, à isoler par poste1), pas dans la conversion elle-même.
