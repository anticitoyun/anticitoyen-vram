# Verdict — C13-a (`ACVRAM_MLA_TF32=1`, TF32 sur les deux einsum du cœur MLA au prefill, poste1 b6249d0) : **TENU** — prefill GLM L=2048 **7 191 j/s (+26,5 %, 285 ms/passe ; ≥ 6 200)**, J net −22 % ; PPL 3 tranches (corpus privé préfixé `[gMASK]<sop>`) **ratio B/A géo +0,00066 (± 0,001)**

instrument : `scratchpad/c13a-19-09/chaine.sh` (prefill ABAB : `prefill-abab-19-09.py` L=2048 ×7 passes, Engine direct sans graphes, J brut/net `energie.py` ; A défaut → B `MLA_TF32=1` → A → B) + `chaine-ppl.sh` (PPL `ppl-acvram-17-09.py`, `PPL_MIN_CTX=1023 PPL_MAX_TOK=24576`, corpus **`corpus-prefixe-glm/prive-tr{0,1,2}.txt`** = fenêtres déjà préfixées, preuve `premiers_ids_fenetre_0 = [154822, 154824, …]`), arbre `a3d4222` (main `16528d1` + verdicts), prises `carte.sh` 19:09:08-19:11:58 et 19:14:10-19:15:33, bras B = `ACVRAM_MLA_TF32=1` seule variable (`[régime] ACVRAM_MLA_TF32=1 …`), carte 0 vide, charge 0,9-1,3 ; **contrôle préalable** : nsys (verdict-budget-prefill-glm-19-09) montre les deux einsum en `tensorop_s1688gemm` (TF32) sous B, −64 ms de noyaux — la variable agit. **Première passe PPL invalidée par moi** : `corpus-prive/tranches-glm/` n'est PAS préfixé (PPL 26,3 / 35,9 / 17,3 : GLM sans `[gMASK]<sop>` s'effondre par fenêtre — leçon du 17/09) ; un ratio pris hors régime ne vaut rien, rejoué sur le corpus préfixé (ci-dessus)
scellé (poste7, avant) : prefill B ≥ 6 200 j/s (défaut 5 502 le 18/09, 5 739 en G1) ET PPL tranche(s) = défaut ± 0,001 ; prédictions : poste7 372 → 315-325 ms/pas ; poste2 330-345 ms (6 000-6 200), PPL +0,0002 à +0,0006
mesuré : **prefill** A p1/p2 **5 683 / 5 684 j/s · 360,4 / 360,3 ms · 0,0591 / 0,0575 J net** ; B p1/p2 **7 187 / 7 195 j/s · 285,0 / 284,6 ms · 0,0451 / 0,0460 J net** (σ 102-129) → **B 7 191 = 1,265 × A, −75 ms/passe, J net −22 %** ; **PPL** (corpus préfixé) A **16,6314 / 13,0408 / 10,7235**, B **16,5484 / 13,0871 / 10,7604** → ratio B/A par tranche **0,99501 / 1,00355 / 1,00344, géo +0,00066** ; (première passe non préfixée, pour mémoire : A 26,27 / 35,85 / 17,31, B/A géo +0,00021 — même sens, régime effondré, non retenue)
verdict : **TENU sur les deux lignes** — 7 191 ≥ 6 200 (poste7 prédisait 315-325 ms : mesuré 285, mieux ; ma fourchette 330-345 était trop prudente : le cœur était plus gros que je ne le comptais), et la PPL bouge de +0,0007 en géo — sous le seuil, mais **la dispersion par tranche (−0,005 / +0,0036 / +0,0034) dépasse ± 0,001 dans les trois tranches** : le TF32 (10 bits de mantisse sur les scores) déplace la PPL par fenêtre plus que le seuil ne l'imagine, l'agrégat compense ; à dire tel quel, pas à cacher dans la moyenne. Cellule à publier : « GLM-4.7-Flash prefill 2 048, `MLA_TF32=1` : 7 191 j/s · 0,0456 J net » ; le défaut moteur ne change qu'au mot de poste7
suite : poste7 tranche « MLA_TF32=1 au défaut GLM » (deux lignes tenues, réserve de dispersion dite) ; poste1 8ae21997 (3e produit `v_b·o_lat` au niveau 1, attendu −15 à −18 ms) → addendum bras B (5 min) dans ma file ; chef : cellule au comparatif avec le régime dans le nom ; ma file : C2 témoin → C5 → C4 → porte A8+W8r → statics → NARROW_GEMM → P2+éco → addendum C13-a sur 8ae21997

## Rejouable
`ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/c13a-19-09/chaine.sh` (prefill + PPL non préfixée, 3 min) puis `chaine-ppl.sh` (PPL préfixée, 1 min 30) ; `pplp2-*.json`, `prefill-*.json`.
