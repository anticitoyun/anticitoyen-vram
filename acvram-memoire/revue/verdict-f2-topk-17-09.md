# Verdict — critère poste7 sur F2 (`route_fusee`) : **0 écart de routage hors égalité sur 392 832 triplets** (1 024 pas × 48 couches × 8), distances fp64 des poids identiques à celles du chemin torch (médiane 6,28e-8 des deux côtés, p99 0,213 / 0,214, max 0,310 / 0,303) ; le « ≤ 1,1 sur 100 % des pas » n'est pas tenable à la lettre (59 % brut, 97 % avec plancher 2⁻²⁰) parce que **8 % des relevés ont une égalité exacte de logits bf16 au rang 8/9** ; `test_rope_kv` sur cuda : **3 échecs** (cache int8 ≠ référence)

instrument : `scratchpad/topk-route-17-09.py` (teacher forcing wiki-gptq, préfixe 256 + 1 024 pas, eager ; crochets sur `_router_logits`, `_route` et `route_prep.route_fusee` : logits fp32 + topw/topi par pas et couche, 49 104 relevés par chemin) ; `analyse-topk-route-17-09.py` (référence fp64 de chaque chemin depuis ses propres logits — même arithmétique que `_route` torch ; distance ‖topw − fp64‖/‖fp64‖ ; ensembles top-k ; écart admis ⇔ scores fp64 des deux experts à < 2⁻²⁰) ; `tests/test_rope_kv.py`, `test_route_prep.py` sur cuda (e29b282) — `scratchpad/topk-f2-17-09/`
commit : arbre poste3 bfa7e1d (= main e29b282) ; F1 (`ROUTE_PREP=1`, chemin torch/CUDA `moe_route`) contre F2 (`=2`, `route_fusee` Triton) ; eager (les crochets Python ne vivent pas dans un graphe)
scellé (poste7) : distance fp64 logits ≤ 1,1 × référence sur 100 % des pas ET ensembles top-k identiques par (pas, couche, jeton), ≤ 1 écart / 10⁴ triplets, tout écart hors égalité < 2⁻²⁰ = bogue
mesuré : **écarts top-k vs fp64 : F1 2 062, F2 2 107 relevés sur 49 104 (4,2 / 4,3 %), tous à écart fp64 = 0,00 (égalités exactes), 0 hors égalité des deux côtés** ; 4 001 relevés (8,1 %) ont le 8ᵉ et le 9ᵉ logit exactement égaux (logits bf16 → 2⁻⁸ de résolution ; fp64 prend l'indice bas, les noyaux prennent l'autre) · distances des poids : médiane **6,28e-8 = 6,28e-8**, p99 0,2127 / 0,2137, max 0,310 / 0,303 — les grandes distances sont les lignes d'égalité (un expert échangé) ; ratio par ligne : médiane 1,00, ≤ 1,1 sur 59 % brut (plancher fp32 des poids, 2⁻²⁴, le rapport de deux bruits), **97,0 % avec plancher 2⁻²⁰**, les 3 % restants = lignes où un seul des deux chemins a pris l'expert jumeau · F1 vs F2 directement : les logits divergent dès le pas 2 (dérive amont d'un ulp : poids d'experts à 2⁻²⁰ près), 5 245 relevés à top-k différents, **0 à logits identiques** · `test_route_prep` cuda : vert ; **`test_rope_kv` cuda : 3 failed sur 6** (`test_q_k_et_cache_suivent_la_reference[2-2-32-32-True-False]`, `[8-2-128-128-True-False]`, `[8-2-128-128-True-True]` : `assert torch.equal(c.k[blk, off], kc)`, ligne 99 — codes int8 du cache ≠ référence, sur cuda seulement)
verdict : **F2 tient le critère dans sa substance : le routage fusionné ne s'écarte du fp64 que sur des égalités exactes (0 écart hors égalité, seuil ≤ 1/10⁴ tenu), et ses poids sont à la même distance du fp64 que ceux du chemin torch (médianes égales à 10⁻¹⁰ près) ; la lettre « ratio ≤ 1,1 sur 100 % » ne peut pas tenir avec des logits bf16 : 8 % des lignes sont des égalités et le rapport de deux distances au plancher fp32 n'est pas un nombre — je propose à poste7 la forme : « distances médiane et p99 dans ± 5 %, 0 écart hors égalité », tenue ici. Les bascules de s8 (d462c24) sont donc des égalités exactes de logits, pas un défaut de F2. F3a : `test_rope_kv` corrigé pour cuda révèle 3 échecs sur les codes int8 écrits (formes 32 et 128, q normé) — le noyau `rope_kv` n'écrit pas le même cache que la référence sur la carte (l'interpréteur ne le voyait pas) : à poste4 avant tout défaut ; ma PPL décodage F2+F3a (0,9959) était compatible avec un cache légèrement différent.**

## Garde (3) de poste7 — « aucune queue neuve » (à sec, sur le JSON de ce verdict)
Relevés hors ratio 1,1 (plancher 2⁻²⁰) : **1 477** (3,01 %) — **tous à égalité exacte** (8ᵉ = 9ᵉ score fp64 du candidat). Parmi eux, **379 ont une distance > p99 de la référence (0,2127)** : à la lettre, la garde n'est pas tenue. Mais ce n'est pas une queue neuve : la référence a elle-même 492 relevés au-dessus de son p99 (par définition 1 %), le candidat 506 (1,03 %), et **le maximum du candidat (0,303) est sous celui de la référence (0,310)** ; ces 379 sont les égalités où la référence a tiré l'expert jumeau et le candidat l'autre — la distance d'une ligne d'égalité vaut la norme d'un poids échangé (0,2-0,3), identique des deux côtés. Formulation qui tient et garde le sens de la garde : « tout relevé hors ratio a une distance ≤ max de la référence, et l'effectif au-dessus du p99 de la référence est dans ± 10 % du sien » — **tenue (0,303 ≤ 0,310 ; 506 contre 492, + 2,8 %)**. Décision à poste7 : à la lettre non, en substance oui.
