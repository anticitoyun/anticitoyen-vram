# Sage — GLM : ni 1,020 en nominal ni 0,998 en dégradé ; l'échelle AWQ par expert entre dans la pile, duel demain matin (15/09 nuit)

1. **Pas de duel avec 1,020** : le seuil ≤ 1,01 a été écrit avant (réfuté > 1,02 → bloqué ; 1,020 est sur la ligne, et une ligne ne se déplace pas après lecture, REGLES §3). L'AWQ vaut −2,2 % de PPL sur les experts d'un MLA — ma prédiction +0,3 % et celle de Manon sont réfutées dans le bon sens ; c'est un résultat à publier.
2. **Chantier maintenant, ½ j Manon + Laurine, mais pas l'échelle commune : l'échelle PAR EXPERT dans la pile.** `_try_build_stacks` (`model.py:720`) refuse la pile dès qu'une échelle existe — c'est la contrainte artificielle, pas l'AWQ. Les lignes sont déjà rassemblées **par expert** par route+pack : appliquer **x_ligne ∗= 1/s[e]** (table `[E, K]` bf16, 0,5 Mo/couche, lue une fois par ligne) juste avant `nvfp4_quant_act`, dans route+pack ou dans le quant — zéro noyau nouveau, une ligne dans le chemin B, le loader garde `[E, K]` au lieu de refuser. C'est **exactement** l'AWQ par expert du chemin dégradé (0,998 mesuré), pas une approximation ; l'échelle commune (A7, `alpha_commun_gate_up`) ne devient utile que si ce coût dépasse 2 µs/couche — repli scellé, pas premier choix : elle demande une recalibration et ne promet que « entre 0,998 et 1,020 ».
3. **Scellé** : PPL du converti AWQ-experts en pile = **0,998 ± 0,002** (test d'équivalence pile vs boucle par expert, même jeton, ≤ 1 ulp bf16, même commit) ; régime **NOMINAL** (`piles_ok=True`, graphes, vérifié par `acvram doctor`) ; pas GLM b=12 ≤ 1,03 × le pas du converti sans-AWQ en pile ; **réfuté** si la PPL bouge de > 0,004 (l'échelle n'est pas appliquée où on croit) ou si le pas dépasse 1,05× (la mise à l'échelle par ligne coûte, on passe au repli commun).
4. **Le juge ≤ 1,01 tient** : il compare notre NVFP4 à **notre** bf16 de la même source ; la qualité de GadflyII (MMLU −1,3 pt) est celle du bras vLLM et se publie **à côté**, en colonne (PPL des deux NVFP4 sur le même corpus), jamais comme excuse pour desserrer le nôtre. Un duel apparié, c'est même source, même corpus, chacun sa qualité écrite.
5. Duel GLM prise A : **demain matin**, sur ce converti, dès les trois voyants (PPL 0,998 ± 0,002, `piles_ok=True`, contrôle MTP d'Océane vert). Rien ne le précède sur la carte.
