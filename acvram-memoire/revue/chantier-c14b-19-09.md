# Chantier C14-b — porter le noyau de C14 (grille par couche, partiels + combine) au décodage b=12 de GLM : GEMM mince + reduce + prep (2 ms/pas) ; fiche seule (`sage-niveau2-faux-c14-defaut-20-09` § 1 ; aucune ligne de code)

## État à la pause (09:08 machine, 20/09, utilisateur « pause du groupe ») — branche `oceane-c14b` (Manon 00f6fd06 fusionné), rien à moitié écrit dans le .cu
* **combine_vb, les 37 lignes** : FAIT — cause trouvée par lecture et rejeu à sec : **l'annulation**, pas la tranche vide (témoin `.cu:6068/6079` = fusionné `.cu:6154/6165`, même garde ; jumeau à tranches vides fini, témoin cassant NaN ; rejeu de 200 lignes d'annulation : milliers d'ulp de |y64| dans les deux sens, juge des termes tenu). Juge (d) réécrit à l'échelle des termes (§ 7) dans le test carte (formes à lens inégaux ajoutées) et `equiv-reel.py` (lignes fautives nommées, compte inverse). À FAIRE : P1 de Manon rejouée sur `2b077ad8` pour confirmer (attendu : ratio des termes < 1 partout, compte inverse ≈ compte direct ; une ligne fautive à len_b court ET ratio > 1 remettrait la tranche vide en cause).
* **`ACVRAM_MLA_PREP_GRILLE`** : FAIT — variable séparée (défaut 0, `mla.py` `temoin = not _MLA_PREP_GRILLE`, `regime.py`, `cli.VARIABLES_LUES`, test de câblage à sec vert), chaîne A = 0/0, B = 1/1, `familles.py` attend le témoin en A. À FAIRE : le nsys (a) de Manon (≤ 8 µs/couche, pas b=12 non perdu) → défaut 1 (commit séparé).
* **En cours : rien.** Prochain geste : P1 (10 min) puis P2/P3 par Manon ; à la reprise, lire (c) (+0,42 ms sous 310 W) à la lumière de (a)(b).

## Scellé provisoire (Sage, recopié)
Niveau 2 (TF32 au décodage) est **faux** : un seul site passe en `s1688`, `gemmSN_TN` fp32 0,53 ms + `reduce` 0,52 restent (cuBLAS sert ces GEMM minces à M=12 par un noyau SIMT que `allow_tf32` n'atteint pas) ; ce qui reste du poste : **1,05 ms (GEMM mince + reduce) + 0,97 (`mla_prep_batch`) = 2,0 ms/pas b=12** (14 % du pas 13,9 ms). C14-b : ces 1,05 + 0,97 → **≤ 0,8 ms**, `ppl-decode-kv` au lot de 12 contre l'ancien chemin Δ ≤ +0,002, capture 5/5. Contrôle de Manon (REGLES § 4 bis) : une PPL à b=1 ne juge pas un chemin pris à b > 4.

## Ce que `gemmSN_TN` ×93 et `reduce` ×233 calculent à b=12 (fichier:ligne, `mla.py`, chemin `decode_static_batch` `:485` sous graphes)
* `gemmSN_TN` (cuBLAS, 93 = 47 couches × 2 − 1) : les deux einsum du cœur par lot — **`q_abs = einsum('hrn,bhn->bhr', k_b, q_nope)`** (`:531`, [20, 512, 128] × [12, 20, 128] : 20 GEMM de [512 × 128] × [128 × 12], batchées par tête, M=12) et **`y = einsum('hvr,bhr->bhv', v_b32, o_lat)`** (`:544`, [20, 128, 512] × [12, 20, 512], M=12) ; à M=12 cuBLAS choisit `gemmSN_TN` (SIMT, « small N ») — TF32 sans effet (niveau 2 faux). Travail : 2 × 20 × 512 × 128 × 12 × 2 = 63 MFLOP par couche : rien ; le temps (0,53 ms / 93 = 5,7 µs par lancement) est **la latence du noyau SIMT à M=12** (deux vagues de 20 blocs).
* `reduce` (233 ≈ 47 × 5) : les `.sum` et réductions de `_prep_decode`/`_sortie_decode` par lot — norme RMS de kv_a (`:509` `kv_a_norm`), `q_a_norm` (`:264`), et les réductions de `torch.stack([… for i in range(B)])` (`:524` : B préparations par créneau puis stack : c'est la « préparation par créneau » que `mla_prep_batch` (`:537`, `_MLA_PREP_NOYAU`) remplace déjà à b=12 quand q est bf16) ; 233 × 2,2 µs = 0,52 ms : latence pure aussi.
* `mla_prep_batch` (0,97 ms / 47 = 20,6 µs par couche) : RoPE + einsum k_b + norme kv_a + écriture du latent pour B=12 en un lancement (`.cu:5600`) — déjà un noyau, mais 20,6 µs pour 12 lignes : **grille = B × H ?** (à lire : c'est le même défaut d'occupation que C14-bis, 12-240 blocs pour 170 SM, à vérifier par ncu avant d'écrire).

## Chiffrage à sec
* **Ce que C14 apporte** : `mla_1p_kernel` fait à b=1 en un lancement q·C, softmax en ligne et P·V ; à b=12 le chemin est `mla_decode_batch` / `<20,32,4>` (47,4 µs, inchangé). Les 2 ms ne sont PAS dans l'attention (le noyau) mais **autour** : deux GEMM minces (k_b avant, v_b après) et la préparation. **C14-b = fusionner k_b·q et v_b·o dans le noyau d'attention par lot** : q_abs = k_b·q_nope se calcule par tête dans le prologue du bloc (une GEMM [512 × 128] × [128 × 12] par tête : 0,8 MFLOP, en shared : q_nope [12, 128] fp32 6 Kio + k_b [512, 128] bf16 128 Kio — **trop pour la shared (99 Kio)** → k_b par tranches de 128 lignes ou lu depuis L2 en registres, 4 passes) ; y = v_b·o_lat dans l'épilogue du combine (v_b [128, 512] bf16 = 128 Kio par tête : idem, par tranches). Donc : k_b et v_b (2 × 20 × 128 Kio = 5 Mio par couche, 240 Mo par pas, déjà lus aujourd'hui par cuBLAS) sont lus par le noyau lui-même, la latence de 4 lancements cuBLAS + 5 réductions par couche disparaît : **−(0,53 + 0,52) = −1,05 ms si la fusion ne coûte que ses octets** (5 Mio/couche à 1,5 To/s = 3,3 µs/couche = 0,16 ms/pas) → **prédiction 1,05 → 0,2-0,3 ms** ; `mla_prep_batch` 0,97 → ≤ 0,5 par la grille (ncu d'abord) ; total 2,0 → **≤ 0,8 ms tenable**, −1,2 ms/pas b=12 (13,9 → 12,7 : +9 % t/s).
* **Précision** : k_b·q en fp32 acc (entrées bf16 k_b, q_nope fp32 → bf16 ? aujourd'hui `k_b.to(x.dtype)` bf16 × q_nope bf16 par cuBLAS avec acc fp32 : **même arithmétique** possible dans le noyau (mma bf16 ou FMA fp32 sur bf16 déplié) ; v_b·o_lat aujourd'hui fp32 × fp32 (`_v_b32`) : dans l'épilogue en fp32 FMA — à l'ordre de sommation près : ± quelques ulp fp32, **jamais au bit** → juge = `ppl-decode-kv` au lot de 12 (REGLES § 4 bis) ± 0,002, pas « jetons identiques ».
* **Registres/shared** : le noyau `<20,32,4>` est à 170 regs ; ajouter une GEMM de prologue par tranches pousse vers 200+ → 1 bloc/SM (déjà le cas : 67 Ko de shared) ; risque : déversement (`-Xptxas -v` avant carte).
* **Ordre** : (1) ncu de `mla_prep_batch` à b=12 (grille, occupation : 5 min, même chaîne que C14-bis avec `-k regex:mla_prep`) ; (2) fusion k_b dans le prologue (la moitié du gain, la plus simple : q_abs par tête est indépendant) ; (3) v_b dans le combine ; (4) `ppl-decode-kv` b=12, capture 5/5, cellule b=12 ABAB. Après C14-c (grille b=1), qui touche le même noyau : **C14-b s'écrit sur C14-c**, pas avant.

Non vérifié : le nom exact des 233 `reduce` (nsys de Manon les groupe : à relire par pile d'appel), la grille de `mla_prep_batch`, tout temps.

## Fait le 20/09 (Océane, sous-agent à sec, 07 h 17 → 08 h 05 machine ; branche `oceane-c14b` sur `oceane-11`)

### 1. Ce que le chemin b=12 lance AUJOURD'HUI (lecture, fichier:ligne) — le budget de la fiche est à corriger
Chemin : `model.py:2424` `_MLA_BATCH` défaut 2 → `:2686` `decode_static_batch_complet` (`mla.py:765`). Par couche MLA (47) :
* `mla.py:794` `ext.mla_prep_batch` (`.cu:6632`, noyau `:6434`) — **k_b·q_nope EST DÉJÀ DEDANS** (`.cu` « q_abs[b,h,r] = Σ_n k_b·q_nope accumulé fp32, arrondi bf16 ») avec RoPE, norme kv_a, cat : le premier einsum de la fiche (`q_abs = einsum('hrn,bhn->bhr')`, `mla.py:807`) n'existe plus que dans la branche torch `_MLA_PREP_NOYAU=0`. Grille d'avant : `nh·NR + B` = 20·8 + 12 = **172 blocs** de 256 fils (un bloc = 64 lignes de k_b[h] × tous les b), 22,0 µs par lancement (nsys c14c b=12, `scratchpad/c14c-sortie/glm-decode-b12_cuda_gpu_kern_sum.csv` : 2 350 instances / 50 pas, 51,6 ms → **1,03 ms/pas**).
* `mla.py:813` `mla_ecrit_latent` (1,7 µs) ; `:827` `_mla_decode_batch` → `mla_decode_1p` (`.cu:6290`) = `mla_1p_kernel<20,32,4>` (51,5 µs, 2,42 ms/pas) + `mla_1p_combine_kernel<1>` (`.cu:6040`, grille (240, 2), 3,0 µs, **0,14 ms/pas**).
* `mla.py:832` **`y = einsum('hvr,bhr->bhv', _v_b32, o_lat)` fp32** sous `_tf32_coeur(decode=True)` (inerte : `MLA_CORE_DECODE=fp32`) — c'est le seul einsum restant hors noyau ; cuBLAS le sert par `gemmSN_TN<float>` strided-batched (20 lots de [128 × 512] × [512 × 12]) : 11,5 µs ; puis `:836` `.to(x.dtype)` (une copie bf16, 0,7 µs) et `_o` (o_proj int8 étroit).
* **`gemmSN_TN` ×93 = 47 + 46** : les 47 einsum v_b ci-dessus **et les 46 routeurs MoE** — `model.py:1926` `F.linear(x.to(fp32), w)` (`_router_logits`, fp32 parce que sigmoid + biais départage le top-k) : [12 × 2 048] × [2 048 × 64] fp32 à M=12, même noyau SIMT, 11,6 µs. Preuve dans le verdict niveau 2 (`verdict-niveau2-tf32-decode-19-09`) : sous TF32 **un seul** site bascule en `s1688` (0,427 ms = 47 × 9,1 µs : l'einsum, sous le drapeau) et `gemmSN_TN` garde 0,533 ms = 46 × 11,6 µs : le routeur, hors drapeau. **Part MLA de `gemmSN_TN` : 0,53 ms, pas 1,05.** Le routeur relève de C15-3c (`_routeur_compact`, `model.py:1929`).
* **`reduce` ×233 n'est pas du MLA** : `at::native::reduce_kernel<128,4>` fp32 = `y.sum(0)` des tranches K du GEMM étroit Triton (`kernels/gemm_etroit.py:194`, témoin `_etroit_kernel` ×234 + `FillFunctor<float>` ×234 = `torch.zeros` `:189`) — les projections int8 qa_kv/q_b/o + couche dense ; `noeuds.py` de C15-3 le nomme déjà (« reduce<128,4> (y.sum de _etroit) »). Sous `ACVRAM_GLUE_COMPACT=1` (**défaut depuis le 20/09**, `kernels/__init__.py:1214`, `_etroit_reduit_kernel`) ces 233 + 234 lancements n'existent plus : le nsys c14c du 19/09 (et celui de Manon) sont au témoin. Le scellé « GEMM + reduce 1,05 → ≤ 0,35 » additionne donc 0,53 d'einsum MLA + 0,53 de routeur MoE, et le `reduce` 0,52 est déjà retiré par C15-3d : **C14-b ne peut agir que sur 0,53 (einsum) + 0,14 (combine) + 1,03 (prep) = 1,70 ms/pas**.

**Budget corrigé (b=12, 13,9 ms/pas, témoin c14c)** : `mla_1p` 2,42 · `mla_prep_batch` 1,03 · einsum v_b (`gemmSN_TN`) 0,53 · combine 0,14 · copie bf16 de y 0,03 · routeur (`gemmSN_TN`) 0,53 (hors C14-b) · `reduce` + `Fill` 0,68 (retirés par GLUE_COMPACT=1, hors C14-b).

### 2. Gestes écrits (à sec, aucune carte) et prédictions — SEUILS AVANT MESURE
* **(2) v_b·o_lat dans le combine** : `mla_1p_combine_vb_kernel<SL>` (`.cu:6125`), un bloc de 256 fils par (b, h) — 240 blocs : M, w_s, Lsum à l'identique du combine, puis les R=512 colonnes par morceaux de CH dans `o_sh` (même `acc·inv` colonne par colonne), puis un warp par ligne v de v_b (16 lignes/warp) : uint4 de 8 bf16 tous les 256 r, FMA fp32, arbre de shuffles, **bf16 par arrondi au plus proche** (= le `.to(bf16)` de torch sur le fp32 de l'einsum). Lancé par `mla_decode_1p(..., v_b=self._v_b_c())` (`.cu:6335` `fusion`, `mla.py:819`) : `o_lat` fp32 n'est plus écrit, y bf16 [B, nh, dv] va droit à `o_proj`. Numérique : v_b bf16 → fp32 exact (ce que lisait `_v_b32`), produits/sommes fp32, un arrondi : **± ulp fp32 avant l'arrondi bf16, jamais au bit** ; le fp32 n'étant jamais matérialisé, le test carte juge la sortie **bf16 ≤ 1 ulp bf16** de bf16(einsum) et les deux ≤ 1 ulp bf16 d'une référence float64 (`tests/test_mla_batch_fusion_c14b.py:186`, formes b=12/S=8/SL=1, b=3/L=4 096/SL≥2, b=1 FIN) — le « ≤ 4 ulp fp32 » du scellé n'est pas mesurable sur une sortie bf16, il est remplacé par ce juge plus strict à la frontière d'arrondi près (positions ≠ comptées, attendues ≲ 1 %).
  **Prédit** : −47 × (11,5 einsum + 0,7 copie + 3,0 combine) + 47 × combine_vb ; combine_vb ≈ 6-8 µs (v_b 128 Kio par bloc lu par uint4, 2,6 Mo uniques par couche en HBM = 1,7 µs, les 12 b en L2 ; la partie combine 3 µs) → **einsum 0,53 → 0, combine 0,14 → 0,30-0,38, net −0,35 à −0,45 ms/pas**. Faux si combine_vb > 12 µs (≥ einsum + combine : la fusion coûte plus que ce qu'elle retire) ; gênant : registres > 128 ou déversement (ptxas ci-dessous).
* **(3) grille de `mla_prep_batch`** : 172 blocs (1 par SM, 8 warps) et chaque fil lisait ses 32 bf16 de k_b un par un à foulée 4 depuis la globale (L1 dès b=1) — **latence** (une chaîne de 12 × 32 chargements dépendants du tour de boucle à 8 warps/SM), pas débit (2,6 Mo par couche = 1,7 µs à 1,5 To/s). Nouveau noyau (`.cu:6434`) : tuile k_b[h][r0..r0+64] en shared par uint4 coalescés (rangées rembourrées nope+8 : les 8 rangées d'un warp sur des banques distinctes, 17,4 Kio), q_nope du groupe en shared, **groupes de MLAP_BG=4 créneaux** : grille `nh·NR·⌈B/4⌉ + B` = **492 blocs à b=12** (≈ 3/SM, 24 warps), 172 inchangés à b=1 (C15 niveau 2). **Au bit** : même fil (r, quart), même ordre de somme sur n, mêmes deux shuffles, mêmes valeurs bf16 (shared ou globale) ; le noyau d'avant reste sous `temoin=True` (`mla_prep_batch_temoin_kernel`, `.cu:6540`) et `tests/test_mla_prep_regrille_c14b.py:52` compare `torch.equal` à B ∈ {1, 5, 12}, avec/sans RoPE. Aucun ncu à sec : la lecture ci-dessus est l'hypothèse, le ncu de la chaîne la juge.
  **Prédit** : 22,0 → **≤ 6 µs** par lancement (1,03 → ≤ 0,3 ms/pas, −0,7 ms). Tenu si ≤ 10 µs (≤ 0,5 ms/pas) ; faux si > 15 µs (la grille n'était pas la cause : la chaîne de FMA par (b, r) est le plancher, il faudrait découper n).
* **Total prédit b=12 GLM** : −1,05 à −1,15 ms/pas (13,9 → ≈ 12,8), scellé Sage « −0,7 ms au moins » tenable ; pas ≤ 13,2 ms au banc ABAB. Issue gênante : le pas ne bouge pas parce que le b=12 est borné ailleurs (experts nvfp4 marlin 3,6 ms, `mla_1p` 2,4) — le nsys par famille tranche avant le banc.
* **(4) régime** : `ACVRAM_MLA_BATCH_FUSION` (`regime.py:189`, `mla.py:217`), défaut 0, nommée sur la ligne dès 1 (`tests/test_mla_batch_fusion_c14b.py:137`) ; pris seulement si `MLA_UNE_PASSE` (ou fp8), v_b bf16, sortie bf16, `MLA_CORE_DECODE=fp32` (`mla.py:262` `_fusion_vb_possible`, `tests/test_mla_batch_fusion_c14b.py:112`). La grille de prep (3) n'a pas de variable : au bit, témoin par argument.

### 3. ptxas (`nvcc -Xptxas -v`, sm_120a, `-O3 --use_fast_math -std=c++20`, objet seul à sec, rc=0, 07 h 43)
| noyau | registres | pile / déversement | shared statique |
|---|---|---|---|
| `mla_1p_combine_vb_kernel<1>` (b=12) | 40 | 0 / 0 | 6 176 o (w_s 4 Kio + o_sh 2 Kio) |
| `mla_1p_combine_vb_kernel<2,4>` | 48 | 0 / 0 | 7 200 o |
| `mla_1p_combine_vb_kernel<8>` (b=1, S=64) | 48 | 8 o / 4 o stores, 4 o loads | 7 200 o |
| `mla_1p_combine_kernel<1>` (avant, inchangé) | 98 | 0 / 0 | 4 128 o |
| `mla_prep_batch_kernel` (regrillé) | 40 | 8 o / 4 o stores, 4 o loads | 128 o statique + 19 456 dyn. (nope 128, BG 4) |
| `mla_prep_batch_temoin_kernel` (avant) | 40 | 0 / 0 | 128 o + B·512 dyn. |
| `mla_1p_kernel<20,32,4>` (inchangé) | 170 | 0 / 0 | dyn. |
Le combine fusionné tient en 40-48 registres (le combine d'avant en prenait 98 : `#pragma unroll` des 8 partiels par voie sur 2 colonnes par fil ; ici une colonne par fil et par morceau), ≥ 5 blocs/SM résidents : les 240 blocs (b=12) tiennent en une vague. Les deux déversements de 4 octets (un registre, une fois par bloc : `mla_prep_batch_kernel`, `combine_vb<8>`) sont hors boucle chaude d'après leur taille ; à vérifier au ncu (`lts__t_sectors` / `l1tex__t_bytes_pipe_lsu_mem_local`), pas bloquant.

### 4. Tests
* À sec (`CUDA_VISIBLE_DEVICES=""`), `tests/test_mla_batch_fusion_c14b.py` + `tests/test_mla_prep_regrille_c14b.py` : 6 passés, 10 sautés (carte) ; `test_mla_decode_batch`, `test_mla_une_passe`, `test_mla_glue_c15`, `test_mla_niveau2_jumeaux`, `test_mla_1p_grille_c14`, `test_regime*` : 192 passés, 20 sautés — le défaut (FUSION=0) et le témoin de prep ne changent rien à sec.
* Carte (chaîne § 5, étape 0) : `test_combine_fusionne_contre_einsum_sur_carte` ×4 formes, `test_prep_regrille_au_bit_contre_temoin_sur_carte` ×6 (B ∈ {1, 5, 12} × RoPE oui/non) — chacun avec un témoin cassant (v_b décalé d'une tête ; q d'un créneau modifié ne touche que ce créneau).

### 5. Chaîne carte pour Manon — `bash scratchpad/c14b-20-09/chaine.sh` (3 prises ≤ 30 min, gabarit `outils/gpu/mesure/gabarit-chaine.sh`)
Sage 07 h 50 (main `48079e76`) a **retiré** le scellé « GEMM + reduce 1,05 → ≤ 0,35 » (faux par lecture, § 1) et posé un **scellé par poste**, écrit en tête de la chaîne avant toute mesure ; branche fusionnée avec `origin/oceane-c15-elementaires-glm` (sans conflit : tranches-9, `geo-sequentiel.py`, gabarit, `carte.sh` à `ACVRAM_DUREE_MAX` 1 800 s). Bras A = `FUSION=0`, B = `FUSION=1` (le prep regrillé, au bit et sans variable, est dans les deux).
* (a) `mla_prep_batch` ≤ 8 µs/couche (prédit ≤ 6 ; 22,0 avant) ; (b) einsum v_b retiré (`gemmSN_TN` 93 → 46/pas) ET `combine_vb` ≤ 0,32 ms/pas (6,8 µs/lancement ; prédit 6-8) ; (c) pas servi (`certifie-b12.py`, énergie NVML) B ≤ 12,9 ms contre 13,9 ET J_B ≤ J_A ; (d) ≤ 1 ulp bf16 vs bf16(einsum) ET juge fp64 par ligne d(noyau) ≤ d(réf) + 6 ulp — en ulp bf16 de la référence, la sortie étant bf16 — sur entrées réelles b=12 (`equiv-reel.py`, sans graphes, mandataire d'extension) et formes de test — forme RELATIVE précisée par Manon 08 h 10 : sur carte le noyau est à 2,0 ulp bf16 de fp64 là où l'einsum l'est aussi (résolution bf16 de la sortie, pas le combine), `≤ 1 absolu` était faux ; prep au bit (idem ; le test carte construisait une demi-table cos/sin [max_pos, rope/2] refusée par le lanceur — corrigé en pleine largeur comme `tables32`) ; (e) capture {1, 2, 8, 16} 4/4 ; (f) PPL lot 12, 9 tranches `tranches-9`, préfixe 2 048 + 512 (8 192 × 12 = OOM), `geo-sequentiel.py` « non établi pire de > 0,5 % » (10 ou 13 tenu, 11 faux). Tout tenu → `FUSION=1` au défaut.
* Prises et durées prédites : (c) compilation hors verrou 9 min (0 si le .so est là) ; **P1** tests carte 2 min + (d) réel 3 min + nsys A/B 5 min = **10 min** (arrêt si (d) tombe) ; **P2** PPL lot 12 : `ppl12-multi.py` (variante lot de `ppl-decode-kv-3seq`, un chargement par bras : `outils/gpu/mesure/ppl-decode-kv.py` **n'a pas de lot de 12**) 2 × (45 s + 9 × 13 s) ≈ **6 min** ; **P3** godets 4 min + certifie ABAB 4 × 1,7 min = **11 min**. Total carte ≈ 27 min.
* Réfutations écrites : (a)/(b) faux → le poste n'était pas les lancements mais la lecture de k_b/v_b (ncu `dram__bytes_op_read` sur `cible-b12.py`, 3 lancements, métriques explicites) ; (c) faux avec (a)(b) tenus → espaces inter-nœuds (niveau 3, pas C14-b : partiel) ; (f) faux → ordre des sommes non neutre pour ce nvfp4 : opt-in nommé.

### 6. Ce qui reste
* Mesures : tout (aucune carte ici). Défaut de `MLA_BATCH_FUSION` seulement après (2)-(4) tenus.
* Le scellé « GEMM + reduce 1,05 → ≤ 0,35 » est retiré par Sage (07 h 50) et remplacé par le scellé par poste du § 5 ; `reduce` = C15-3d (déjà retiré au défaut), routeur fp32 = C15-3c.
* Si le ncu montre `mla_prep_batch` encore > 10 µs : découper n (4 quarts → 8) change l'ordre de somme (≤ 1 ulp bf16, plus au bit) — chantier séparé avec son juge PPL.
* Le témoin `mla_prep_batch_temoin_kernel` se retire quand la chaîne a rendu son « au bit » (un commit, le test carte passe alors par le tenseur enregistré).

### 7. Verdict Manon 09 h 00 (77aabdae) et réponse — les 37 lignes, la variable `MLA_PREP_GRILLE`
* Mesuré (equiv-reel, 799 couches-pas b=12, 16 pas) : **prep regrillé au bit** (prep_faux 0) ; combine fusionné : 37 / 191 760 lignes à d(noyau, fp64) > d(einsum, fp64) + 6 ulp bf16 de |y64|, excès max 640, max_d_k 2 296, max_d_e 4 344, positions ≠ 8 770 / 49 M (0,018 %) ; certifie b=12 B +0,42 ms (+2,5 %), J +2,3 % sous 310 W bridés.
* **Lecture de la tranche vide** (prédiction de Sage) : `mla_1p_kernel` marque une tranche sans clé pour (b, h) par m = −inf, l = 0, o = 0 (`.cu:5867-5873`) ; le témoin la combine à poids 0 (`w = m != -INFINITY ? exp(m − M) : 0` `.cu:6068`, `inv = Lsum > 0 ? 1/Lsum : 0` `:6079`) et le fusionné porte **la même garde, ligne pour ligne** (`.cu:6154`, `:6165`) — o_sh (fusionné) et o (témoin) sont la même arithmétique colonne par colonne, dans le même ordre. Le jumeau torch de cette arithmétique sur des (b, h) à tranches vides (`tests/…fusion_c14b.py::test_tranche_vide_combinee_a_poids_nul_a_sec`) rend un o fini égal à la combinaison des tranches pleines, et le témoin cassant sans garde rend NaN ; le test carte exerce maintenant les vrais noyaux à lens INÉGAUX (b=3 [3900, 40, 700] à L=4 096 : S ≥ 32 tranches, la plupart vides pour deux b ; b=12 lens de 1 à 511). **La cause des 37 lignes n'est pas la tranche vide.**
* **La cause : l'annulation, et un juge à la mauvaise échelle.** max_d_e = 4 344 ulp dit que l'einsum cuBLAS lui-même est à 4 344 ulp de |y64| sur des lignes — deux ordres de somme fp32 exacts à leur borne (n·eps·Σ|termes|) diffèrent de milliers d'ulp de |y64| là où Σ_r v·o s'annule (|y64| ≪ Σ|v·o|), dans les deux sens ; `test_annulation_fait_des_milliers_d_ulp_de_y64_sans_faute` le rejoue à sec sur 200 lignes d'annulation (512 produits, ordre séquentiel contre 32 voies + arbre) : max d_k 5 892, max d_e 9 668, d_k > d_e + 6 sur 37 lignes, l'inverse sur 141 — et le juge à l'échelle des termes tenu partout. « Un rapport ne classe pas deux objets » : l'ulp de |y64| n'est pas une échelle où le résultat s'annule. **Juge (d) réécrit** : |y − y64| ≤ 1 ulp bf16(|y64|) + 32·eps32·Σ_r |v_b·o| (arrondi de sortie + borne d'accumulation, quel que soit l'ordre), noyau ET einsum, toutes lignes ; `equiv-reel.py` publie en plus le compte inverse (d_e > d_k + 6 : la symétrie qui dit « bruit ») et nomme les lignes fautives (pas, b, h, len_b, |y64|, Σ|termes|, ratios) — à confirmer sur carte : attendu ≈ 37 contre ≈ 37-150 inverses, ratio des termes < 1 partout ; si une ligne fautive a len_b court et un ratio des termes > 1, la tranche vide revient en cause.
* **(c) B +0,42 ms** : à lire après (a)(b) de P1 (prep 22 → ? µs, combine_vb ? µs) ; sous 310 W bridés seul le relatif compte ; si (a)(b) sont tenus et le pas perd 0,4 ms, c'est le combine_vb (240 blocs, v_b 128 Kio par bloc) qui coûte plus que l'einsum : réfutation (b) écrite en tête de chaîne.
* **`ACVRAM_MLA_PREP_GRILLE`** (défaut 0) : le prep regrillé seul, indépendant de `MLA_BATCH_FUSION` (qui ne porte plus que le combine) ; `mla.py` passe `temoin = not _MLA_PREP_GRILLE` ; `regime.py` VARIABLES, `cli.VARIABLES_LUES`, nommée sur la ligne dès 1 ; test de câblage `tests/test_mla_prep_regrille_c14b.py::test_cablage_variable_prep_grille`. Chaîne : A = 0/0, B = 1/1 ; (a) se lit sur la famille prep (`mla_prep_batch_temoin_kernel` en A, `mla_prep_batch_kernel` en B).
