# Verdict — T4 rejoué sous enveloppe etat sur main 7bcb75fc (⊇ f1069eee = poste1-t4-reste : `colle = None`, jumeaux au juge de 2a-bis, moe_fused contre float64, test_menus à trois états ; chef 12:30 « dernière prise avant terminé ») : **3 failed / 1 885 passed / 38 skipped / 11 xfailed / 2 xpassed en 267 s** (13 → 3 ; attendu 0) — les trois restent nommés, aucun ne touche le chemin servi : (c') **test_mla_niveau2_jumeaux::test_chaque_noyau_contre_son_jumeau_sur_carte** — la faute construite du nouveau juge ne casse pas : « le juge ne voit pas une tête déréglée de 2 % (5,09 ulp) ; assert 5,09 > 6 » = **le juge des termes de 2a-bis est trop lâche pour une tête à 2 %** (la constante 32·eps32 masque, exactement ce que M3 a dit « torch_masque_par_32 » ; un test qui exige que sa faute construite casse fait son travail : le juge doit être resserré ou la faute construite grossie, à poste1/poste7) ; (d') **test_moe_fused[64|128]** — « chemin B : 2 642 sorties hors tolérance vs float64 (max 4,56e-2) ; fusionné − B max 4,54e-2 » = **le chemin B lui-même est à 4,5e-2 de float64** sur ces formes, le fusionné est à 4,5e-2 de B : la tolérance du test (contre float64) n'est pas celle d'un chemin bf16 → à trier (tolérance en ulp bf16 ou juge des termes), pas une régression ; **test_menus ×5, marlin_et_groupe ×4, courriels : verts** ; 2 xpassed inchangés (à démarquer)

instrument : `outils/carte.sh` etat autour de `pytest -q -p no:cacheprovider -rs --timeout 300` sur `poste2-0631` = main 7bcb75fc, venv anticitoyen-vram, 12:35:31-12:39:58 (267 s), hôte calme, 3080 Ti pid 4286 (pair) ; **un premier rejeu 12:30:43-12:35:12 a tourné sur 1f1d96bf** (12 failed / 1 876 passed : le `git checkout` avait été refusé par mes sorties non suivies, non lu — 4,5 min de carte perdues, nommées) ; sorties `scratchpad/t4-rejeu-20-09/` de l'arbre, journal copié sur poste2
scellé (chef/poste7, avant) : 0 failed, skips et xfail nommés ; un rouge restant est nommé, pas retouché
mesuré : 3 failed 1 885 passed 38 skipped 11 xfailed 2 xpassed ; skips : 12 « à sec, carte visible » (joués à 89/89 sous `CUDA_VISIBLE_DEVICES=`, verdict-t4-6b1f9fe3), 2 « alias absent Qwen3-14B-nvfp4 », 1 « carte à 68 % » (test_regressions_0_4_4x:130, compte le pair sur la 3080 Ti), 1 « bt=128 » ×15, 4 « opt-in splitk », 2 « noyau w4a16 pas écrit », 1 « cas sans GPU »
verdict : la suite est verte sur tout ce qui sert et mesure ; les trois rouges sont deux **questions de juge** (un juge trop lâche pour sa propre faute construite ; une tolérance float64 posée sur un chemin bf16) — elles disent quelque chose de vrai (le 32·eps32 masque 2 %) et ne se ferment pas par une retouche de test : poste7 tranche entre resserrer la borne (et rejouer M3 (0) avec) ou nommer ces deux tests xfail avec leur raison
durée : 4,5 min de carte (+ 4,5 perdues sur le mauvais arbre)
suite : poste7 : (c') borne des termes ; (d') tolérance moe_fused ; poste1 : selon poste7 ; chef : indexer, fusionner ; ma file : vide — carte rendue 12:39:58

## Rejouable
`cd <worktree main 7bcb75fc> && ACVRAM_ARBRE=$PWD PYTHONPATH=$PWD ACVRAM_TYPE=etat outils/carte.sh <python venv> -m pytest -q -p no:cacheprovider -rs --timeout 300` (4,5 min).
