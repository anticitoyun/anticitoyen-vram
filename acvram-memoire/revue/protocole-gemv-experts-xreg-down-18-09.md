# Protocole — adoption du xreg sur le down seul (poste7, poste7-gemv-experts-clos-18-09) : ABAB Coder b=12, seuil unique pas ≤ 0,97 × témoin

prérequis : un aiguillage « down seul » n'existe pas au 07cc4b8 — `xreg_demande()` (`acvram/kernels/acvram_kernels.cu:2075-2078`) est lu par le gate/up (`:1872`) ET le down (`:4312`) ; `ACVRAM_GROUPED_XREG=1` allume les deux. Demandé à poste4 (via chef) : valeur `ACVRAM_GROUPED_XREG=down` (down seul), `1` inchangé (les deux), `0` témoin ; `regime.py:69` à jour. La mesure part dès que la valeur existe sur main (ou poste4).
instrument : `scratchpad/xreg-down-18-09/situ-abab.sh` = montage de l'ABAB RPW (verdict-gemv-experts-rpw-situ-18-09) : A = XREG=0, B = XREG=down, ordre A B A B ; chaque bras `certifie-b12-15-09.py` (bridé 400 W, J/jeton, ligne régime) puis `profil-pas-coder-17-09.py … b12` (ms/pas nu) ; rpw=4 défaut ; modèle Coder ; ~10 min.
scellé (poste7) : moyenne B (ms/pas nu) ≤ 0,97 × moyenne A → tenu ; sinon faux. Prédiction (ncu : down 65,1 → 57,2 µs, −0,38 ms/pas sur 9,51) : B/A ≈ 0,96, juste sous le seuil ; J/jeton informatif. Bit-exact déjà tenu (test_xreg_egale_v1_au_bit 20/20) ; ppl-decode-kv Coder XREG=down vs 0 en fin de chaîne (1 024 NLL identiques attendus).
addendum (poste4 465030a) : `ACVRAM_GROUPED_XREG=down` livré (xreg_mode : "down" → down seul, atoi ≠ 0 → les deux, absent/0 témoin) ; prédiction poste4 B/A 0,955-0,97 (−0,4 ms sur le down).
