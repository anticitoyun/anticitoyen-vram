# Verdict — poste E, référence : `paged_attn_partial` b=12 ctx 2 020 = **4,60 ms** (+ reduce 0,18) pour 1,21 Go de KV int8 → **263 Go/s, 14,7 % de crête** ; b=1 ctx 2 020 = 0,72 ms (7,8 %) ; b=12 ctx 300 = 0,81 ms (12,3 %) — le noyau souffre de sa latence par jeton, à tout lot et tout contexte ; seuil E ≤ 1,5 ms = ×3,1 (45 % de crête)

instrument : `scratchpad/attn-reference-17-09.py <b> <ctx>` (graphes, 20 pas profilés, noyaux seuls ; grille/bloc/registres/shared lus dans la trace chrome ; octets KV calculés depuis la disposition int8 `[NB,16,HKV,D]` + échelles fp16 `[NB,16,HKV]` ; crête 1 792 Go/s) — `scratchpad/poste-e-17-09/attn-b{12,1}-ctx{2000,280}.{json,log}` ; **ncu refusé** (`ERR_NVGPUCTRPERM`, compteurs réservés à l'administrateur ; aucun réglage système de ma part) : occupation réelle et octets DRAM mesurés restent à faire par l'utilisateur (`NVreg_RestrictProfilingToAdminUsers=0`) — les octets ci-dessous sont ceux du cache lu, pas ceux vus par la DRAM (L2 96 Mo : 25 Mo de KV par couche y tiennent, la redondance GQA ×8 est donc probablement servie par L2)
commit : arbre laure feb878b (= main) ; protocole `protocole-poste-e-reference-17-09.md` ; Coder classé, KV int8, chunk 64 (`acvram_kernels.cu:3866`), `PA_WARPS=4`
régime : `[régime] ACVRAM_NARROW_GEMM=1 ACVRAM_MOE_MMA=0 ACVRAM_MOE_DECODE_MMA=0 extension=oui`, graphes, budget KV pour le lot ; noyau : grille `(B·QL godet, HQ=32, C)`, bloc 128 fils (4 warps), **40 registres, 2 608 o de shared**, un jeton par warp et par itération, lectures d'un octet par élément (`kp[lane*4+i]`, `:1000-1002`), réduction warp par jeton ; `reduce` : grille `(B, HQ)`, 37 registres
scellé : Sage ≤ 1,5 ms à ctx 2048 / b=12 (ne bouge pas) ; moi : b=12 partial 4,8-5,4 ms à 12-15 % ; b=1 0,5-0,8 ms à 15-20 % ; ms par jeton de contexte égaux à ± 20 % entre b=1 et b=12 (« souffre du contexte ») ; b=12 ctx 300 ≈ 1,1
mesuré : **b=12 ctx 2 020 : partial 4,595 ms, reduce 0,177 ; grille (16, 32, 32) = 16 384 blocs (godet 16 : 4 lignes fantômes sur 16, `bucket_batch(12)`) ; KV 1 191 Mo + échelles 18,6 → 263 Go/s = 14,7 %** (prédit 12-15, tenue) · **b=1 ctx 2 020 : partial 0,717, reduce 0,130 ; grille (1, 32, 32) = 1 024 blocs ; 99,3 Mo → 141 Go/s = 7,8 %** (prédit 15-20, réfutée par le bas) · **b=12 ctx 300 : partial 0,812, reduce 0,089 ; grille (16, 32, 8) ; 177 Mo → 221 Go/s = 12,3 %** · par séquence et par jeton de contexte : b=1 **0,355 µs**, b=12 ctx 2 020 **0,190 µs**, b=12 ctx 300 0,226 µs (écart b=1 / b=12 = − 46 % : « ± 20 % » réfuté) · partiels écrits puis relus : 307 Mo par pas à b=12 ctx 2 020 (f32, `part/pm/pl`)
verdict : **le noyau n'est borné ni par la bande passante ni par le lot : c'est sa latence par jeton — un warp traite un jeton à la fois, en lisant 4 octets par lane à l'unité et en réduisant sur 32 lanes à chaque jeton — qui plafonne à 0,19-0,36 µs par (séquence × jeton), soit 8-15 % de crête partout ; le lot aide (b=12 remplit la grille que b=1 laisse à 1 024 blocs), le contexte n'aggrave pas le rendement (12,3 → 14,7 %). Forme de E qui en découle : lectures vectorisées 16 octets (`int4`), plusieurs jetons par warp avec une tuile de clés en shared, softmax en ligne par tuile, et godet sans lignes fantômes dans la grille ; réduction partielle à garder (C = 32 tranches, 0,18 ms, 307 Mo — à convertir en bf16 ou à fusionner si E vise 1,0 ms). Le seuil ≤ 1,5 ms exige 810 Go/s (45 % de crête) sur le même volume : atteignable pour un noyau de décodage vectorisé, hors de portée par réglage de `chunk` ou de `PA_WARPS`.**

| point | partial ms | reduce ms | grille (blocs) | Mo KV lus | Go/s | % crête | µs / (séq × jeton) |
|---|---|---|---|---|---|---|---|
| b=12, ctx 2 020 | 4,595 | 0,177 | (16,32,32) = 16 384 | 1 210 | 263 | 14,7 | 0,190 |
| b=1, ctx 2 020 | 0,717 | 0,130 | (1,32,32) = 1 024 | 101 | 141 | 7,8 | 0,355 |
| b=12, ctx 300 | 0,812 | 0,089 | (16,32,8) = 4 096 | 180 | 221 | 12,3 | 0,226 |

Réserves : occupation théorique à 40 registres × 128 fils = 16 blocs / SM possibles, non vérifiée (ncu) ; les 4 lignes fantômes du godet 16 sortent tôt (`start >= slen`) mais occupent un quart des blocs lancés ; « Go/s » compte une lecture par octet du cache — si L2 ne sert pas la redondance GQA ×8, la DRAM voit moins et la latence l'explique encore mieux.
