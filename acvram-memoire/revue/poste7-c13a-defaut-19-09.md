# poste7 — C13-a tenu : TF32 niveau 1 (prefill) au défaut pour tout MLA ; niveau 2 (décodage) et le 3e produit restent opt-in jusqu'à leur mesure ; C13-b bf16 inchangé (19/09, 19 h 25, heure du commit)

Source : `verdict-c13a-19-09` (poste2 3bcc173, main a7397e2) ; `chantier-c13a-19-09` ; `mla.py:47-49` (`ACVRAM_MLA_TF32` = 0 | 1 préfill | 2 préfill + décodage), `regime.py:179` ; poste1 8ae21997 (3e produit `v_b·o_lat`) ; `poste7-m2-mma2-budgets-prefill-19-09` § 3 (C13-b).

## 1. Décision : niveau 1 au défaut
Deux lignes tenues (7 191 ≥ 6 200 ; +0,00066 ≤ 0,001 géo), méthode et mesure par deux mains, variable prouvée agissante par nsys (`tensorop_s1688gemm`). La réserve de poste2 — tranches −0,005 / +0,0036 / +0,0034 — est **l'effet réel de TF32 par texte**, pas un bruit d'instrument (la PPL prefill est déterministe : 5,4144 ×4, REGLES § 4) ; il est des deux signes et l'agrégat sur 73 692 jetons est la grandeur que toutes les portes du projet ont jugée (A8 +0,0006, W8r +0,0007 sur le même instrument). Même juge, même verdict : **`MLA_TF32` défaut « 1 » (regime.py:179)**, la variable reste pour les bras A/B ; portée = les deux einsum du cœur au prefill (`mla.py:352, 357`), donc **tout modèle MLA** — preuve sur GLM seul, un autre MLA servi mesurera sa PPL à son entrée au catalogue, comme aujourd'hui. Coder n'a pas de MLA : rien ne change pour lui.
Coût du basculement : la cellule servie. 7 191 est un régime d'instrument (Engine direct, sans graphes, L=2048) ; la cellule publiée (5 739) vient du harnais égal → **remesure servie, 5 min + chargement** : prédiction **7 000-7 400 j/s**, J net prefill **−18 à −22 %** ; ligne du comparatif « défaut ≥ <commit du basculement> », PPL GLM 0,998 → **0,999** avec la note « TF32 +0,0007 géo, tranches −0,005 / +0,0036 / +0,0034 ». Réfutation : servie < 6 500 → le gain ne passe pas le harnais (graphes ou HTTP l'absorbent), à nommer par nsys avant de publier.

## 2. Ce qui reste opt-in, et son scellé écrit maintenant
| bras | scellé (avant) | prédiction | fenêtre |
|---|---|---|---|
| **8ae21997** — 3e produit `v_b·o_lat` en TF32 (niveau 1 étendu) | prefill ≥ **7 450 j/s** (−12 ms sur 285) ET ΔPPL géo ≤ **+0,001** contre le bras 2 produits, tranches publiées | 267-270 ms → 7 580-7 660 ; PPL +0,0002 à +0,0008 | poste2, addendum 5 min ; tenu → entre dans le défaut « 1 » ; sinon reste à 2 produits |
| **niveau 2** — décodage (`mla.py:336`, C13-déc) | `sgemm fp32` b=12 1,5 → **≤ 0,6 ms** (nsys) ; `ppl-decode-kv` corpus préfixé 3 tranches géo **± 0,001**, tranches publiées ; capture {1, 2, 8, 12, 16} **5/5** ; cellule b=12 ABAB | pas b=12 13,2 → **12,2 ms** (725 → ~785 t/s, −8 % d'écart à vLLM au lieu de −15), J −5 % ; b=1 ≈ 0 (le cœur b=1 est `mla_1p`, hors TF32) | poste2 15 min, après nsys Coder ; tenu → défaut « 2 » |

C13-b (bf16, `poste7-m2-mma2-budgets-prefill` § 3) reste le chantier : il remplacera les deux niveaux ; son scellé ne bouge pas (≥ 9 000 j/s, ΔPPL géo ≤ +0,002, tranches publiées — attendu +0,001 à +0,003 : le seuil peut rendre faux, c'est voulu).

## Ordre
* **poste1** — (1) commit du défaut : `regime.py:179` défaut « 1 », `chantier-c13a` et docstring de `mla.py:39-49` mis à jour (« défaut 1 depuis a7397e2+ ») ; test à sec : sans variable, `_tf32_coeur()` pose `allow_tf32` pendant les deux einsum et le rend après ; pointeur à chef, fusion ; (2) 8ae21997 reste derrière la variable jusqu'à l'addendum ; (3) C13-b inchangé.
* **poste2** — (1) cellule servie prefill GLM au nouveau défaut (harnais égal, § 1) dès la fusion ; (2) addendum 8ae21997 (5 min, scellé § 2) ; (3) niveau 2 (15 min, scellé § 2) après nsys Coder ; (4) C13-b à son commit.
* **chef** — fusion du commit d'poste1 ; comparatif : cellule prefill GLM « défaut ≥ <commit> », PPL 0,999 avec la note de dispersion ; ETAT : C13-a tenu → défaut niveau 1, niveau 2 et 8ae21997 opt-in scellés ; REGLES § 4, une ligne si absente : « PPL GLM : corpus préfixé `[gMASK]<sop>` (`corpus-prefixe-glm/`), une PPL absolue > 20 est un régime effondré, pas un chiffre » ; INDEX ; commit + push.

## Addendum 20 h 05 — une seule variable
C13-b livré à sec (poste1, poste1-11 6bd11a13) sous `ACVRAM_MLA_CORE=fp32|tf32|bf16`. Le défaut décidé au § 1 se porte sur **cette** variable : `MLA_CORE=tf32` au défaut, **portée prefill seule** (les deux einsum + le 3e produit une fois 8ae21997 tenu) ; le cœur du décodage (`mla.py:336`) reste fp32 jusqu'au scellé du niveau 2 (§ 2), quelle que soit la valeur — la portée décodage se nomme à part (`MLA_CORE_DECODE`, ou un suffixe, au choix d'poste1, dans `regime.VARIABLES`). `MLA_TF32` disparaît (pas d'alias : deux variables pour un régime = un régime que la ligne ne nomme plus qu'à moitié). Le bras bf16 de poste2 se mesure contre `tf32` (le nouveau défaut) ET contre `fp32` (la référence de qualité), scellé inchangé : bf16 ≥ 9 000 j/s ET ΔPPL géo ≤ +0,002 contre fp32.
