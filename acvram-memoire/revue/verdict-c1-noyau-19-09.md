# Verdict — C1 noyau (`nvfp4_gemm_grouped_int8`, Océane oceane-c1-noyau 2dd43da9, `ACVRAM_PREFILL_GROUPED=w4a8 ACVRAM_W4A8_GEMM=noyau`) : **FAUX** — (1) test carte **5 échecs / 19** : les quatre cas `768×2048` et `2048×768` (les formes du modèle) à `ks=128` (étages 3 et 4) → `CUDA error: invalid argument` ; accumulateur int32 : `addmm_cuda not implemented for 'Long'` (défaut du test, pas du noyau) ; (2) nsys L=2 047 : **T_experts(C1) = 1 147,9 ms par différence contre 27,2 scellés (× 42)**, noyaux nommés 160,9 ms (GEMM int8 93,7 + dépaquetage 67,2 + **A8 0 : la quantification d'activation reste en torch**, 5 noyaux élémentaires × 2 301 = 640 ms) contre Marlin 53,0 ; pas 1 290 ms / 1 587 j/s contre 153 / 13 355 ; (3) PPL 3 tranches **1,0174 tenu** (≤ 1,020, attendue 1,0157 ± 0,001 : +0,0017 au-delà), témoin torch tr0 **identique au bit** (12,656843307052744)

instrument : `scratchpad/c1-noyau-19-09/chaine.sh` (Océane, copié avec ses sorties sur manon ; `test_w4a8_noyau_carte.py` 19 cas ; `prefill-nsys.py` L=2 047 défaut puis C1, `classer-c1.py` ; `ppl-acvram-17-09.py` PPL_MIN_CTX=1023 PPL_MAX_TOK=24576, tranches Coder 0-2, témoin `W4A8_GEMM=torch` tr0), arbre `~/travail/manon-c1n` 2dd43da9, extension compilée à sec avant la prise, ptxas `REG:128 SHARED:1024 LOCAL:0`, prise `carte.sh` 23:52:21-23:58:43, **horloge libre** (arbre antérieur à l'éco par défaut), hôte : un pair tient 5,6 Gio (pid 4284) → allocateur en retry OOM (`free 220 Mio`) sur tous les bras, sans échec — le même sur défaut et C1, donc sans effet sur le rapport
scellé (Sage, sage-c1-route § 3 puis ordre fin de nuit § 3, avant) : test carte 19/19 au bit contre `torch._int_mm` ; T_experts(C1) ≤ 27,2 ms nsys (0,55 × Marlin 49,5) ; PPL ≤ 1,020 attendue 1,0157 ± 0,001 ; témoin torch = noyau au bit ; les trois tenus → défaut
mesuré : (1) `5 failed, 14 passed in 1.62s` — échecs `[4-128-128-768-2048-cnt0]`, `[4-128-128-2048-768-cnt1]`, `[3-128-128-768-2048-cnt0]`, `[3-128-128-2048-768-cnt1]` (`ks=128` : `(4,128)` et `(3,128)` paramétrés, `NOYAU_KS=64` par défaut passe), `test_accumulateur_int32_exact_a_travers_un_bf16_sans_perte` (Long en addmm) ; (2) défaut total noyaux **119,7 ms**, experts Marlin **53,0** (144 lancements), hors experts 66,7 ; C1 total **1 214,5 ms**, GEMM int8 **93,7** (2 301 lancements, 40,7 µs moy.), `_depaqueter_kernel` **67,2** (2 301), A8 **0 (0)**, marlin résiduel 0 ; six premiers hors nommés : `neg` 202,2 (4 602), `where` 188,6 (2 301), `add<float>` 122,5 (4 602), `floor` 120,5 (4 602), binaire float 80,2 (2 301), `clamp` 77,8 (4 602) = 791 ms d'arrondi/quantification élémentaire en fp32 par appel d'expert ; ratio 21,64 contre 0,55 ; ratio nommé seul 3,03 ; (3) PPL C1 tr0 12,6568 / tr1 11,4322 / tr2 11,7598, ratios [1,0157 ; 1,0145 ; 1,0219] géo 1,0174 ; témoin torch tr0 12,656843307052744 = noyau tr0
verdict : **FAUX sur (1) et (2), tenu sur (3)** — le noyau n'est pas au bit aux formes du modèle avec `ks=128` (lancement refusé : shared 80 Kio × 2 ou paramètre hors domaine, à lire dans `test-carte.log:56`), et même là où il se lance (ks=64) il est **1,8 × plus lent que Marlin sur ses seuls noyaux nommés** (160,9 contre 53,0) et **× 21 sur le pas** parce que la route C1 quantifie encore A8 en torch élémentaire, expert par expert (2 301 appels × 5 noyaux) ; le scellé 0,55 × Marlin supposait A8 fusionné et une seule passe — aucun des deux n'est là. La PPL tient et le témoin torch est au bit (mêmes tampons, même épilogue) : la route est **juste, mais 21 × plus lente** ; la porte W4A8 (verdict-porte-a8w8r) reste sans levier tant que A8 n'est pas un noyau
suite : Océane : (a) `ks=128` invalide aux formes 768/2 048 : `cudaFuncSetAttribute` ou grille hors domaine — un seul cas suffit ; (b) A8 dans le noyau (ou un seul noyau de quantification par couche, pas par expert) avant toute nouvelle prise : sans cela le scellé est inatteignable par construction ; (c) test accumulateur : `.to(torch.int64)` en addmm n'existe pas, comparer en int32 ou en CPU ; Sage : C1 noyau **défaut non**, reste chantier ; Jérôme : indexer ; ma file : niveau 2 C15 (b) + PPL sur f8a495a3 → C13-c → C9

## Rejouable
`cd ~/travail/manon-c1n && TRANCHES=~/travail/manon/scratchpad/corpus-prive/tranches-coder REF_HF=~/travail/manon/scratchpad/p2-hors-moteur-18-09 PYA=<python venv> ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/c1-noyau-19-09/chaine.sh > scratchpad/c1-noyau-19-09/journal.log` (6,5 min ; sorties : `test-carte.log`, `defaut-stats.log`, `c1-stats.log`, `c1_cuda_gpu_kern_sum.csv`, `ppl-*-tr*.json`).
