# Verdict — chauffe clampée + plan KV max (main 829a7ef0, `poste1-chauffe-2` 53839225 + `poste1-plan-kv-max` 52f03a02) sur les 4 alias en défaut d'hier : **0 × 500 sur 4 (au sens de la clause : aucune requête client ne reçoit un 500)**, mais **GLM k48 ne charge plus du tout** (crash avant la chauffe, à la capture des graphes CUDA) — pire qu'hier pour cet alias précis, à nommer avant tout feu vert

instrument : `scratchpad/s2-contexte-20-09/chaine.sh PRISE=5` (rejeu ciblé après contamination trouvée en `PRISE=4`, voir « Contamination » ci-dessous), arbre `poste2-qvl` pinné à 829a7ef0 (`cd "$L"` avant `python -m`), en-tête vérifié sans pair avant chaque lancement (`nvidia-smi --query-compute-apps` = 4286 seul, le pair 3080 Ti)
scellé (chef/poste7, avant) : ctx_tenu−64 → 200 + un jeton, ctx_tenu+64 → 400 nommé ; **0 × 500 sur 4** ; prédiction : clamp `ctx_tenu=31744(demandé 32768)` pour k48, `15360` pour i8c/Ornith/Kimi
mesuré : **GLM-4.7-Flash-srcbf16-nvfp4-k48** : SERVEUR-MORT — charge le modèle (46,7 s), puis à la capture des graphes CUDA (« mémoire avant capture : libre 9,87 Gio, réservé 20,84, alloué 19,06 ») **OOM à deux reprises croissantes** (583 Mio puis 1,12 Gio demandés, 150 Mio libres au final) : le crash survient **avant même que `chauffer_contexte()` ne s'exécute** (l'ordre `warm_graphs()` puis `chauffer_contexte()` dans `cmd_serve` place la capture de graphes en premier) — la chauffe ne couvre pas ce chemin, aucun refus nommé, le serveur ne bind jamais le port, aucune requête n'est possible (donc 0 × 500, mais 0 × service aussi) ; **Coder i8c** : DÉFAUT-CHAUFFE propre — `ctx_tenu=4096` (bien en-deçà de la prédiction 15360), −64 → 400, +64 → 400, les deux nommés « demandé 15360, tenu par la chauffe » ; **Ornith** : TENU — `ctx_tenu` non atteint (colonne 15360 tenue), −64 → 200/15296/1/length, +64 → 400 nommé ; **Kimi-Linear** : DÉFAUT-CHAUFFE propre — `ctx_tenu=9216` (prédit 15360), même schéma −64/+64 → 400/400
verdict : la clause « 0 × 500 » est **techniquement tenue** (aucun 500 client sur les 4) mais **incomplète** : GLM k48 ne sert plus AUCUNE requête (régression fonctionnelle, pas de refus nommé — un crash au démarrage, silencieux pour l'utilisateur GUI qui ne verrait qu'un échec de chargement) ; Coder i8c clampe à 4096 (÷3,75 par rapport à la colonne 15360) — conservateur, la marge de la chauffe (poste1 : ≥ max(5 %, 64 Mio)) semble punir plus fort les alias déjà proches de leur limite ; feu vert 0.6.34 **non bloqué par le sens strict du scellé**, mais GLM k48 est **fonctionnellement régressé** — à nommer à poste7 avant d'installer
durée : prise 4 (contaminée par un orphelin GPU de mon arrêt d'urgence antérieur, § 1b) 08:25:05-08:28:08 (3,1 min) + prise 5 (rejeu propre k48/i8c) 08:28:32-08:33:56 (5,4 min) = 8,5 min de carte, HEAD `poste2-qvl` 829a7ef0
suite : poste7 : GLM k48 (capture de graphes qui OOM avant la chauffe) — soit désactiver les graphes pour cet alias (`--no-cuda-graphs`), soit inverser l'ordre chauffe/graphes, soit accepter la régression documentée ; Coder i8c 4096 : vérifier si la marge de réserve est trop large ; ma file : « trou : 3 min » à poste1 et poste9, puis GLM b=1 (charge < 1) sur le SHA final, puis § 1b

## Contamination (avant le rejeu propre)
La première prise (`PRISE=4`, 08:25:05) a couru sur un processus GPU orphelin (~14,85 Gio) laissé par mon arrêt d'urgence de § 1b (13 min plus tôt : `pkill` du wrapper bash d'`acvram-serveur` sans tuer le `python -m acvram.cli serve` détaché en `setsid`, qui a continué à charger et à tenir la VRAM jusqu'à sa propre fin). GLM k48 (1er alias de la prise) est tombé sur un OOM à l'ALLOCATION (2 Mio demandés, 2,6 Mio libres) — signature nette de contamination, pas la même que le crash réel (583 Mio à la capture de graphes) ; Coder i8c (2e alias) a pu hériter d'un reliquat. Rejoués seuls après vérification carte vide.

## Rejouable
`PRISE=4|5 ACVRAM_ARBRE=<arbre pinné> poste2=<poste2> PYA=<venv>/bin/python ACVRAM_MODELES=/mnt/AI_GENERATOR/models_acvram CPUS=0-15 bash <poste2>/scratchpad/s2-contexte-20-09/chaine.sh` — **toujours vérifier `nvidia-smi --query-compute-apps` vide avant de lancer** après tout arrêt d'urgence d'un `-serveur` (le tuer par PID de processus GPU réel, `ss -tlnp` sur son port une fois qu'il l'a pris, jamais seulement le wrapper bash).
