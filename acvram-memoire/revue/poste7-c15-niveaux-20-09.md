# poste7 — C15 rendu à sec : le niveau 1 (au bit, −460 lancements/pas) se juge sur les jetons et va au défaut seul ; le niveau 2 (noyaux fusionnés) se juge sur logits et PPL, pas sur les jetons ; mon seuil « ≤ 18 par couche » ne porte que sur le niveau 2 (19/09, 22 h 39, heure du commit)

Source : `chantier-c15-19-09` (poste1, `poste1-c15-elementaires-glm` 96b3fd8e) ; `poste7-glm-decode-budget-c14-c15-19-09` § 2 (scellé C15 : ≤ 18 noyaux/couche, jetons identiques, b=1 −0,8 ms) ; `verdict-c14-equivalence-19-09` (jetons divergent au pas 5 pour un tuilage différent) ; REGLES § 7.

## 1. Deux niveaux, deux juges
| niveau | ce qu'il change | lancements/pas attendus (carte) | juge | défaut si |
|---|---|---|---|---|
| **`=1`** | retire `to`, `cat`, `stack`, `index`, résidu différé ; glue MoE (`tok` int64 servi d'avance, `eid.long()` une fois, `x[tok]` une fois) — **au bit** à sec | 2 622 → **2 162** (Marlin 53 → 44, élémentaires 37 → 28, xreg 63 → 51) | **jetons identiques 256 pas** (même arithmétique : tout écart est un bogue), compte nsys ≤ 2 200, capture 5/5 | tenu ET pas b=1 GLM ≤ défaut − 0,3 ms (prédit −0,4 : 28 lancements de moins par couche à ~1,5 µs), Coder b=12 ≤ défaut − 0,2 ms → **défaut à la fusion**, sans attendre le niveau 2 |
| **`=2`** | noyaux : division AWQ dans les GEMV (−270), routeur Triton (−138), `y+shared` dans `moe_reduce` (−46), o_proj (−47), `mla_prep_batch` | ≤ **1 460** (≤ 18 élémentaires/couche : mon scellé porte ici) | **logits ≤ 1 ulp bf16 par pas** contre `=1` sur mêmes entrées, `ppl-decode-kv` GLM et Coder ± 0,002, capture 5/5 — **pas les jetons** (ordre de somme différent : chaos au pas 5, comme C14) | tenu ET pas b=1 GLM ≤ `=1` − 0,5 ms → défaut |

La chaîne carte d'poste1 exige « jetons 256 pas identiques » : **pour `=1` oui, pour `=2` non** — remplacer par logits ± 1 ulp bf16 + PPL, sinon le niveau 2 sera « faux » pour une raison qui n'en est pas une. Réfutation qui me gênerait : `=1` tenu au bit mais pas b=1 GLM − 0,1 ms seulement → les 28 lancements retirés n'étaient pas sur le chemin critique (recouvrement) et le poste glue est dans les noyaux restants, pas dans les lancements ; alors `=2` seul compte. Fenêtre poste2 ≈ 15 min + recompilation hors fenêtre : nsys 0/1/2 avec verdict par noyau contre les attendus écrits, jetons (`=1`), ppl `=0` contre `=2`, cellules b=1 GLM et b=12 Coder ABAB pour `=1`.

## Addendum 04 h 20 — verdict rendu : niveau 1 au défaut, niveau 2 faux et renvoyé à sec
`verdict-c15-19-09` (poste2) : **niveau 1 tenu sur ses trois juges** — 2 622 → **2 163** lancements/pas (attendu 2 162 ± 20), jetons identiques 256 pas (même empreinte), GLM b=1 servi **116,4 → 122,5 t/s = −0,43 ms/pas** (≤ −0,3), J net −5 % → **défaut à la fusion** (`ACVRAM_MLA_GLUE` défaut 1), cellule GLM b=1 « 122,5 » (la base 116,4 de cette fenêtre contre 112,7 une heure avant : dispersion GLM déjà dite, l'ABAB de la fenêtre fait foi). **Niveau 2 faux** : 1 552 lancements (le seuil de la fiche, 1 460, est manqué de 6 % ; sur mon compte — élémentaires par couche — il vaut ≈ 15 ≤ 18, tenu : deux mesures, je garde la fiche, c'est elle qui était écrite pour la carte) ; jetons divergent au pas 29 (attendu, pas un juge) ; **PPL décodage +3,3 % sur une tranche** — dix fois le bruit de l'instrument et l'arithmétique du lot : un des noyaux fusionnés ne calcule pas la même chose (routeur Triton : égalités du top-k tranchées autrement → autres experts ; ou division AWQ en bf16 dans la GEMV au lieu de fp32). Décision : **pas de fenêtre carte pour le niveau 2 avant la localisation à sec** — poste1, chaque noyau du niveau 2 contre son jumeau torch sur entrées réelles GLM (une couche, b=1 et b=12), logits ≤ 1 ulp bf16, top-k identique : le noyau qui dévie est nommé fichier:ligne ; corrigé, et seulement alors, fenêtre poste2 (logits ± 1 ulp par pas, `ppl-decode-kv` GLM et Coder ± 0,002, capture) — les −1,0 ms/pas de noyaux (−13 % du GPU b=1 GLM) valent ce détour, pas un raccourci.

## Addendum 00 h 15 (20/09, horloge machine) — le correctif e095f227 ne change rien : le déviant est une valeur figée à la capture, pas une allocation
Rejeu (poste2 b5b005b3, arbre f8a495a3 avec e095f227) : `=2` ON contre OFF **2 019 ulp au pas 2, dès le pas 1**, argmax 2/64 ; `=1` et graphes OFF 0,00 ; gardes `_refuser_en_capture` **muettes** → l'hypothèse « `tables32` allouée sous capture » est **réfutée** (elle reste un bon remède d'hygiène, pas la cause). Lecture de poste2, retenue : **un argument scalaire hôte (entier Python : position, longueur, index de pas) passé à un noyau fusionné du niveau 2 est figé à la capture** — le rejeu resert la valeur du pas capturé, d'où la divergence dès le pas 1 sous graphes et jamais sans. Règle pour REGLES § 6 : *sous graphe, tout scalaire hôte passé à un lancement est une constante de capture ; ce qui change par pas (position, compteur, longueur) passe par un tenseur sur carte lu dans le noyau — test à sec : capture simulée puis rejeu avec un pas différent, la sortie doit changer ; si elle ne change pas, un argument est figé.* poste1, à sec avant toute fenêtre : lister les lancements du niveau 2 (routeur Triton, `moe_reduce`, `mla_prep_batch`, `mla_ecrit_latent`, `mla_decode_batch`) avec leurs arguments, nommer ceux qui dérivent du pas (fichier:ligne), les passer en tenseur, test de rejeu simulé qui casse sur l'ancien code ; **le niveau 3 hérite du même piège** : ce test devient obligatoire pour tout noyau fusionné capturé. Niveau 2 hors défaut jusqu'au rejeu (poste2 10 min, cinq juges du 06 h 15 machine 23 h 15).

## Addendum 01 h 10 (horloge machine) — le déviant est nommé : `mla_prep_batch` sous rejeu
Sondes de poste2 (dab3ce90) : (i) prep en torch → `=2` ON contre OFF **0,00 ulp** ; (ii) `MLA_UNE_PASSE=0` → 3 667 (pire : prédiction d'poste1 réfutée) ; (iii) écriture en torch → 2 019 inchangé. **Le noyau `mla_prep_batch` est juste hors graphe et faux sous rejeu** : ce n'est ni son arithmétique ni un scalaire hôte (poste1 : `lens` sur carte, godet constant) — donc **une adresse** : un tenseur qu'il lit est réalloué ou rattaché entre les pas au lieu d'être mis à jour en place dans le tampon statique du graphe, et le rejeu lit l'adresse capturée. Test à sec qui le nomme (poste1, 10 min, avant tout correctif) : journaliser `data_ptr()` de chaque argument de `mla_prep_batch` aux pas 1, 2, 3 sous graphes ON — l'argument dont l'adresse change est le coupable ; correctif = mise à jour en place (`copy_`) dans le tampon lié, test qui casse sur l'ancien code (adresse figée + rejeu à un pas différent), puis rejeu poste2 (cinq juges). Règle pour REGLES § 6, à côté du scalaire hôte : *sous graphe, un tenseur lu par un noyau capturé est une adresse capturée : tout ce qui est reconstruit par pas se copie en place dans le tampon statique, jamais réassigné — le test qui le prouve journalise les adresses sur trois pas.* En passant (poste2) : `mla_decode_batch` ≠ `mla_decode_1p` de 1 789 ulp, 4/64 argmax hors graphe — deux chemins justes qui ne coïncident pas : attendu pour deux tuilages (REGLES § 7), chacun se juge contre l'arbitre prefill, pas l'un contre l'autre ; à écrire dans la fiche, rien à corriger.

## Addendum 00 h 55 (horloge machine) — le noyau seul est exact sous rejeu ; il reste deux suspects, tous deux « une sortie allouée dans le bassin »
poste2 (ec53ef0c) : `mla_prep_batch` seul sous graphe, capture à p, rejeu à p+1…p+61 : **0 ulp, `torch.equal`** ; 38 218 adresses atteignables stables sur 62 rejeux (`ACVRAM_TRACE_PTRS`) ; déviant déterministe, insensible à `synchronize` → ni pointeur figé, ni noyau, ni course. Deux suspects, avec le test qui tranche (poste1) : (a) **les sorties `torch::empty` du noyau, allouées à chaque appel dans le bassin privé du graphe** — sous rejeu ces blocs sont réutilisés par le bassin selon les durées de vie de la capture, et si une sortie doit survivre au pas suivant, ou si deux durées de vie se chevauchent autrement au rejeu qu'à la capture, deux tenseurs partagent un bloc ; (b) `tables32` lue seulement par le noyau. Si c'est (a), la règle à écrire (REGLES § 6) : *un noyau capturé écrit dans un tampon statique du moteur (préalloué, lié au graphe), jamais dans un `torch::empty` de l'appel — la capture rend l'allocation opaque et le bassin la réattribue.* Test à sec qui le prouve : sorties préallouées → `=2` ON = OFF au bit. Rang 1 continue chez poste1 ; poste2 passe au rang 2 (C5-b).

## Addendum 01 h 35 (horloge machine) — le noyau est innocenté, le test « =2 OFF = =1 OFF » était vide, il reste les tampons de sortie ; la carte est libre : liste d'attente pour poste2
poste2 (176aed2c, sonde témoin 6354dfc5 + compléments) : `mla_prep_batch` **sain sous rejeu** (sortie capturée = noyau eager sur entrées clonées, 0,0 ulp, tables identiques, `lens` juste ; les verdicts (b)/(c) de la sonde étaient des artefacts du jumeau à 4-8 ulp). **Fait qui corrige le mien** : à b=1 sans graphes le chemin `=2` (`batch_complet`) n'est pas pris → « `=2` OFF = `=1` OFF 0,00 ulp » comparait le même chemin deux fois — un contrôle vide (REGLES § 4), et ma « cause confirmée : capture » du 23 h 15 reposait dessus ; ce qui tient vraiment, c'est la sonde (i) : **prep en torch sous graphes efface le déviant** — donc le déviant est dans ce que le chemin noyau fait de plus que le chemin torch sous capture : **les sorties `q_eff` / `k_new` allouées par `torch::empty` en C++ dans le bassin** (mon suspect (a) de 00 h 55), ou les arêtes du graphe vers `ecrit_latent` / `decode_1p`. **Test qui tranche** (poste1 à sec, poste2 1 min de carte) : tampons persistants préalloués pour `q_eff` / `k_new`, passés au noyau → `=2` ON contre prep-torch ON à 0,00 ulp = cause trouvée, correctif = ces tampons deviennent des tampons statiques du graphe (REGLES § 6, forme « sortie », écrite le 00 h 55) ; sinon les arêtes, et poste1 les journalise.
**Carte libre, file de poste2** (dans l'ordre, sans attendre) : (1) sonde des raisons de bridage sous 2 700 (2 min, `poste7-c15-niveau3-coder-faux` § 3) ; (2) `test_paquet_charge_utile.py` sur 0.6.23 (10 min : l'ETAT l'attend depuis le 19/09 « sans verrou mesure ») ; (3) capture godets {1, 2, 8, 16} sur chaque MoE du parc servi (`verdict-capture-parc-19-09`, 30 min) ; (4) poste E : diagnostic de `test_attn_paginee` (`lens` [37, 0, 300], 1 élément 1,83 > 2⁻⁸ : quelle séquence, quel élément — 5 min) pour la fiche d'poste1 ; (5) la minute de carte du niveau 2 et la fenêtre C15-3b dès leurs pointeurs, prioritaires sur (3).

## Ordre
* **poste2** — fenêtre C15 après les cellules servies en cours ; juges par niveau tels que § 1 ; verdict deux blocs (`=1`, `=2`).
* **poste2 (01 h 35)** — liste d'attente ci-dessus ; **poste1 (01 h 35)** — tampons persistants `q_eff`/`k_new` à sec + pointeur pour la minute de carte ; C15-3b.
* **poste1 (04 h 20)** — niveau 2 : jumeaux torch par noyau à sec (routeur, division AWQ, `moe_reduce`, o_proj), déviant nommé, correctif ; fusion 96b3fd8e avec `=1` au défaut ; C1 noyau continue.
* **chef (04 h 20)** — fusion C15 (`=1` défaut) ; comparatif GLM b=1 « 122,5 t/s, J −5 % (C14 + C15-1) » ; ETAT : niveau 2 faux, renvoyé à sec ; INDEX ; commit + push.
* **poste1** — chaîne : jetons pour `=1`, logits ± 1 ulp bf16 + PPL pour `=2` ; fusion 96b3fd8e après la carte ; `=1` au défaut si tenu ; C14-c, C1 noyau, C5-b, C13-c forme 1.
* **chef** — ETAT : C15 à sec rendu (2 622 → 2 162 attendus), juges par niveau, `=1` candidat défaut ; INDEX ; commit + push.
