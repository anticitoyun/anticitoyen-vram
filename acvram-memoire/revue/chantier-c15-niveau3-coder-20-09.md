# Chantier C15 niveau 3, Coder b=12 — ≤ 700 nœuds par pas (fiche seule ; `sage-concurrents-2700-verite-b12-20-09` § 2 : niveau 3 Coder et GLM après le niveau 2)

## Scellé (Sage, § 2, recopié) et point de départ
Écart b=12 contre vLLM W4A16 Marlin sous 2 700 (1 626 t/s · 0,136 J) : **glue 1,58 ms + ≈ 1,9 ms d'espaces entre nœuds** ; C15 niveaux 2-3 sur Coder prédits **≈ 1 850 t/s · 0,15 J**. Budget nsys b=12 (`verdict-nsys-coder-b12-19-09`, Manon, arbre du 19/09, 6,87 ms de noyaux, ~1 030 lancements/pas, 30 noms) : Marlin gate·up 2,38 ×48 · down 1,35 ×48 · `_etroit` (q/k/v/o int8 M=12) 1,06 ×97 · rmsnorm 0,23 ×97 · **élémentaires 0,41 ×490** · attention paginée `_partiel` 0,35 + `_reduce` 0,06 ×49 · `_route_fusee` 0,33 ×48 · **`reduce_kernel<128,4>` fp32 0,23 ×98** · tête wmma 0,13 + splitK 0,05 · `kv_write` 0,08 ×48 · rope 0,14 ×48 · `moe_reduce` 0,05 ×48.

## Attribution des ≈ 1 030 nœuds par couche (48 couches, GQA int8 paginé, MoE Marlin) et fusions nommées
| famille (par couche) | nœuds | quoi (fichier:ligne) | fusion | après | précision |
|---|---|---|---|---|---|
| bloc : rmsnorm ×2 + résidus ×2 | 2 | `add_norm` (DecoderLayer, `layers.py:585`) : déjà fusionné pour Coder | — | 2 | au bit |
| projections q/k/v/o : `_etroit` ×2 (qkv empilé, o) + `reduce_kernel<128,4>` fp32 ×2 (les réductions split-K de `_etroit` ? à confirmer par pile d'appel) | 4 | `gemm_etroit.py` (Triton, M=12) : la réduction fp32 des partiels est un 2e noyau par GEMM | split-K sériel dans le noyau (accumulateur unique : M=12 laisse la grille aux N colonnes, pas au K) : −2 | 2 | au bit si l'ordre de somme est celui du témoin, sinon ± 1 ulp bf16 |
| q_norm / k_norm (Qwen3 : RMSNorm par tête sur q et k) + rope + `kv_write_int8` + élémentaires de vue (cast, split, contiguous) | ≈ 6-7 | `model.py:353-356` (`k_norm`, `apply_rope`), `kvcache.py:420` | `_rope_kv_fusee` existe (`model.py:465` : RoPE + écriture KV en un) — vérifier qu'il est pris à b=12 (le budget montre `rope` ×48 ET `kv_write` ×48 séparés : il ne l'est pas) ; q_norm/k_norm dans le prologue du même noyau (norme par tête = 128 valeurs) : 6-7 → 1 | 1 | ± 1 ulp bf16 (norme fp32 dans le noyau comme torch) |
| attention paginée : `_partiel` + `_reduce` | 2 | `paged_attn_partial/reduce` (`.cu:944/1126`, split-K sur le contexte) | à b=12 et L=512 (256 + 50 pas), le split-K vaut-il ? 1 tranche par (b, tête kv) → `_reduce` inutile : −1 quand ctx ≤ 1 024 | 1-2 | au bit (même somme) |
| MoE routeur : `x.to(f32)` + F.linear (cuBLAS ×2) + `_route_fusee` + arange/index | 5-6 | `model.py:1885`, `route_prep.py:54` | logits dans `_route_fusee_kernel` (entrée bf16, produit fp32 dans le noyau) : 5-6 → 1 | 1 | ± 1 ulp bf16 |
| MoE experts : Marlin gate·up + down + `moe_reduce` + glue (tok/eid int64 — niveau 1 fait —, casts, gather x[tok]) | 5-6 (niveau 1) | `model.py:1730-1752` | `x[tok]` rassemblé DANS le GEMV Marlin (il reçoit `tok` déjà : `nvfp4_gemv_marlin_gateup(…, eid, tok, x, …)` lit x[tok[i]] — vérifier que le gather torch restant est bien le `x_g` AWQ absent sur Coder : alors −1), `moe_reduce` gardé | 3 | au bit |
| tête : norme finale + wmma bf16 + splitK + échantillonnage | ≈ 8 (hors couches ×1) | | tête wmma sans split-K à M=12 (−1), échantillonnage (top-k/greedy) en un noyau (−3 à −5) | 3-4 | |

Total par couche : ≈ 21 (mesuré ~1 030/48) → **8-9** ; 48 couches × 8,5 + tête 4 ≈ **410-440 nœuds** : le seuil ≤ 700 est **large** sur Coder (moins de glue que GLM : pas de MLA, `add_norm` déjà là). Espaces prédits : 1,9 ms × 430/1 030 ≈ **0,8 ms** (−1,1 ms), noyaux 6,87 − 0,4 (élémentaires) − 0,23 (réductions) − 0,1 (rope/kv fusion) ≈ **6,1 ms** ; pas ≈ 6,1 + 0,8 + hôte 0,5 = **7,4 ms → ≈ 1 620 t/s** (contre 1 374-1 434 aujourd'hui) — la prédiction de Sage (1 850) demande aussi les 3,73 ms de Marlin plus courts (C1 ne touche que le préfill ; au décodage Marlin reste : 1 850 t/s = 6,5 ms/pas, il faudrait Marlin −0,9 ms) : **je prédis 1 550-1 650 t/s · 0,155-0,16 J pour C15 niveaux 2-3 seuls**, et je dis que 1 850 n'est atteignable qu'avec un gain sur les experts eux-mêmes (le seul poste qui reste : 54 %).

## Ordre proposé (après le niveau 2 GLM)
(1) Instrument : nsys `--cuda-graph-trace=node` + pile d'appel (`nsys stats cuda_gpu_trace` avec `--sample` ou marqueurs NVTX par famille dans `_la_decode`/`_forward_grouped`) pour attribuer les 490 élémentaires par famille (le budget ne les nomme pas) — 10 min carte, avant toute fusion ; (2) routeur 1 nœud (Triton, partagé avec GLM) ; (3) `_rope_kv_fusee` pris à b=12 + normes q/k dans le prologue ; (4) split-K de `_etroit` sériel ; (5) tête + échantillonnage. Juge : jetons identiques si l'ordre de somme est gardé, sinon logits ± 1 ulp bf16 + `ppl-decode-kv` lot 12 avec SE ; capture 5/5 ; `certifie` b=12 ABAB (scellé : ≥ 1 550 t/s ET J ≤ 0,16 — le mien ; 1 850 : celui de Sage, réfuté si Marlin n'est pas touché).


## Fait le 20/09 (Océane, à sec — carte tenue par Manon ; branche `oceane-c15-niveau3-coder` depuis oceane-11 988a4492)

Point de départ relu dans le CSV du budget (`scratchpad/nsys-budget-19-09/coder-decode-b12_cuda_gpu_kern_sum.csv`, 50 pas) : **1 169 lancements/pas** (pas ≈ 1 030), **24 par couche** × 48 = 1 152 + tête 3 + échantillonnage 14. La table d'attribution ci-dessus se corrige ainsi, fichier:ligne à l'appui — les « ≈ 490 élémentaires » sont : 144 `direct_copy` (**3/couche** : les copies contiguës de k et v dans `kv_write_int8`, `acvram_kernels.cu:3828` à HEAD, `k.contiguous(), v.contiguous()` avant, et de q dans `kernels.paged_attention`, `kernels/__init__.py:1256` — des tranches de la projection q/k/v empilée), 97 `Fill<float>` (le `torch.zeros` de `gemm_etroit.py:189`), 96 `bfloat16_copy` (son `.to(bf16)`, `:195`), 48 `compare<long>` (`slots >= 0` recalculé par couche, `model.py:2799`) ; les 96 `reduce_kernel<128,4>` sont bien la somme torch des tranches de `_etroit` (`gemm_etroit.py:194` `y.sum(0)`, ATen `reduce_kernel<128, 4, ReduceOp<float, sum>>`) : **un GEMM étroit = 4 nœuds**. Le « wmma + splitK ×48 » n'est pas la tête mais le **routeur** (cuBLAS choisit `cutlass wmma bf16` + `splitKreduce` pour F.linear [12×2048]·[2048×128]) ; la tête est le 97ᵉ `_etroit`, à 1 tranche (N = 151k tuiles > 2 × 170 SM), donc sans somme.

Variable de régime **`ACVRAM_GLUE_COMPACT`** (`regime.py`, lue dans `kernels._GLUE_COMPACT`, `kernels.glue_compact()` à l'appel ; défaut **0** = témoin, le chemin d'avant au bit) : chaque fusion se débranche par elle. Commits (branche, dans l'ordre fait 1, 2, 4, 3) :

| item | commit | quoi | nœuds retirés par couche (prédits) | précision | test à sec | pas prouvable à sec |
|---|---|---|---|---|---|---|
| (1) routeur | 0ca85673 | `route_prep.route_logits_fusee` : logits x·Wᵀ par `tl.dot` (produits exacts, acc fp32) dans le noyau de sélection, arrondi bf16 rejoué là où `_router_logits` sortait du bf16 (softmax sans biais), fp32 sinon ; `MoEBlock._routeur_compact` | −2 (wmma, splitKreduce) → 1 noyau | logits ± 1 ulp bf16 (ordre de la somme seul) | `test_route_prep.py` : 9 cas contre F.linear fp32 + route_fusee (experts, eid, compteur au bit, poids 2⁻¹⁶), témoin cassant (ligne 0 = ligne du favori : égalité vers 0) ; `test_routeur_fp32_conditionnel.py` | l'arrondi bf16 (l'interpréteur ne porte pas bf16 : dot et cast rendent n'importe quoi, mesuré) — test marqué carte |
| (2) rope_kv + normes | 0965218e | **corrigé** : `_rope_kv_fusee` n'est pas pris parce que `ACVRAM_ROPE_KV=0` (`model.py:2284`, `regime.py:162`), **réfuté F3a** le 17/09 (`rope_kv.py:1-13` : cache corrompu sous rejeu, cause non identifiée) — pas repris à sec ; les normes q/k sont **déjà** dans `rope_inplace` (`layers.py:770-810`). À la place, au bit : `kv_write_int8` lit k, v par leur pas de jeton (`.cu`, `contigu_par_tete`), q passe sans copie au noyau Triton (il lisait déjà `stride_qb/qh`), `valid = slots >= 0` une fois par pas (`ACVRamModel.decode_fixed` → `decode_fixed_res(valid=)`) | −4 (3 copies, 1 compare) ; +1 par pas | au bit (copies et entiers) | `test_glue_compact.py` : vue q non contiguë = copie au bit, tranche voisine casse ; plomberie lanceur/kvcache/valid | le noyau CUDA à pas (compilé à sec, 222 s, sm_120 CUDA 13.4) : test carte « tranches = copies au bit, k/v échangés cassent » |
| (4) attention | fde1955c | **corrigé** : C ne dépend pas du contexte servi mais de la forme (`attn_paginee.py:232` `_tranches` : voulu = ⌈2·170/(12·4)⌉ = 8 sur N = 144 pages) — C = 8, jamais 1 à b=12 ; `_reduce` ne se saute pas. `_partiel_reduit_kernel` : le dernier programme de chaque groupe (b, tête KV) réduit les C tranches dans l'ordre 0..C−1 (compteur atomique acq_rel entre deux barrières, lectures .cg, auto-remise à zéro : aucun memset) ; corps partagé `_tranche` | −1 | ± 1 ulp 16 bits (ordre de `tl.sum` de lg) ; au bit à sec | `test_glue_compact.py` : 4 formes ≤ 1 ulp, fantômes nuls, compteurs à zéro ; témoin cassant (compteur non remis à zéro → groupe faux, autres intacts) | visibilité mémoire entre programmes (REGLES § 7 : Triton vert à sec ne prouve rien du noyau) |
| (3) GEMM étroit | 73dc22ed | **prouvé par lecture** (ci-dessus). `_etroit_reduit_kernel` : dernier programme par tuile N somme les tranches 0..tranches−1, écrit la sortie dans son dtype (bf16 : un arrondi), auto-remise à zéro ; `torch.empty` ; corps partagé `_acc_tranche`. La fiche se trompait : la grille EST sur K aussi (96 ou 32 tuiles N pour 170 SM → 4 et 11 tranches) ; une somme sérielle dans un programme aurait rendu le parallélisme | −6 (2 × zeros, sum, cast) ; tête −1 | bf16 ≤ 1 ulp (au bit à sec sur toutes les formes) ; fp32 à 2⁻²⁰ × Σ\|x·w\| (72 ulp fp32 mesurés pour 1,8e-7 absolu là où la somme s'annule : l'ulp fp32 n'y est pas un critère) | `test_gemm_etroit.py` : formes Coder b=12 (6144×2048, 2048×4096), b=1, tête 1 tranche au bit, découpe qui déborde ; témoin cassant (compteur) | visibilité mémoire (idem) |
| (5) tête, échantillonnage, gather | — | tête : couverte par (3) (memset) et (1) (le wmma/splitK ×48 étaient le routeur) ; `x[tok]` : absent sur Coder par lecture (`model.py:1745` sous `awq.get("gate_proj") or distinct` seulement, sinon `x_g = x` `:1763`) ; échantillonnage glouton : argmax + gather + logsumexp = 14 lancements/pas hors graphe (`sampler.py:105-110`) — **laissé** (un noyau Triton argmax + log p en une passe : −12, à faire) | 0 | — | — | — |

**Prédit après (B) : 11/couche** (rmsnorm ×2, `_etroit_reduit` ×2, rope, kv_write, `_partiel_reduit`, `route_logits_fusee`, marlin ×2, moe_reduce) × 48 = 528 + valid 1 + tête 2 + échantillonnage 14 = **545 lancements/pas** (nœuds de graphe ≈ 531) contre 1 169 (−624). **Le scellé ≤ 450 est réfuté d'avance par ce compte** : il demande en plus rope_kv (F3a, −48) et moe_reduce dans le GEMV down (−48) → 449. Dit tel quel ; la mesure (a) tranche si les 545 tiennent. Vitesse : prédiction de la fiche gardée (1 550-1 650 t/s · 0,155-0,165 J sous éco 2700) ; issue gênante : les noyaux fusionnés (compteur + relecture des tranches par un seul programme) coûtent plus que les 3 lancements retirés — visible dans (a) par µs/pas de `_etroit_reduit` contre `_etroit` + reduce + cast.

Chaîne carte pour Manon (scellés en tête, avant toute mesure) : `bash scratchpad/c15-niveau3-20-09/chaine.sh` sur la branche — (a) nsys A/B 50 pas b=12 → `noeuds.py` (listes de familles attendues, pas seulement le total) ; (b) `equiv-b12.py` A/B : 12 fenêtres de `tranche1.txt` en teacher forcing sous graphes (jetons, logits ± 1 ulp bf16 par pas, PPL ± 2 SE) + `ppl-decode-kv-17-09` b=1 ± 0,002 ; (c) `capture-godets` 1,2,8,16,12 sous B ; (d) ABAB serve b=12 banc decode 1 024 jetons (t/s, J net), horloge relevée à chaque bras. Tests à sec passés : `test_route_prep` 13, `test_routeur_fp32_conditionnel` 4, `test_glue_compact` 6 (+2 carte), `test_gemm_etroit` 27, `test_attn_paginee` 6 (+1 carte), `test_regime_noyaux`, `test_regime_garde` — lancés avec `ACVRAM_TESTS_PENDANT_MESURE=1` sous `nice 19` (carte tenue par Manon toute la session, charge 3/32 : dit ici). Non fait : le noyau d'échantillonnage (item 5), la preuve carte des trois noyaux fusionnés, rope_kv (réfuté, hors périmètre à sec).

## C15-3b, 20/09 (Océane, à sec, après `verdict-c15-niveau3-coder-19-09` FAUX et `sage-c15-niveau3-coder-faux-20-09`)

**(1) Pourquoi le routeur cuBLAS tournait encore sous COMPACT=1** — lu dans `model.py` de 0ca85673 (MoEBlock.forward) : le bloc compact était `if fusee and compact is not None: … route_logits_fusee(…)` **séparé**, et tout ce qui suit le routage (trace, `topi = eid.view`, `_forward_grouped`, `return y`) restait sous `elif fusee:` ; la branche compacte calculait donc topw/topi/eid par le Triton puis **tombait dans `topw, topi = self._route(x)`** (`_router_logits` = cuBLAS wmma + splitKreduce, `ext.moe_route`, puis `route_prep` + une copie) : le nsys B de Manon montre exactement ces 5 × 48 en plus (`moe_route_kernel` 48, `_route_prep_kernel` 48, `unrolled direct_copy` 48, wmma 48, splitKreduce 48) — le Triton s'ajoutait (1,03 ms) et son résultat était jeté ; d'où B = A numériquement + 1,46 ms. Correctif 876ffa6a : un seul `if fusee:` avec les deux routeurs en tête, chemin d'experts commun ; test à sec `test_c15_3b_le_routeur_triton_remplace_cublas_il_ne_s_ajoute_pas` (MoEBlock réel, compteurs sur F.linear / route_fusee / route_logits_fusee / `_route`, experts alimentés par le Triton) — vérifié cassant sur le model.py de d2ab8e62.

**(2) Routeur Triton 21,5 µs → grille** (3e62a9b5) : la version 0ca85673 avait grille `(⌈T/16⌉,) = 1` programme à b=12, 32 itérations sérielles de `tl.dot [16, 64] × [64, 128]` rechargeant W [128, 64] à chaque pas — 0,5 Mo lus par un SM, 32 chargements dépendants. Maintenant grille (bloc 16 jetons, bloc 32 experts, tranche 256 canaux) = 1 × 4 × 8 = **32 programmes**, partiels fp32 [16, 32] écrits dans `part[pk]`, le dernier programme du bloc de jetons (compteur atomique auto-remis à zéro, comme `_etroit_reduit`) somme les 8 partiels dans l'ordre 0..7, arrondit bf16 si le témoin le faisait, sélectionne. Même nom de noyau. Prédit ≤ 3 µs (W lu une fois sur 32 SM) → −0,9 ms/pas ; à mesurer par `ncu-routeur.sh` **avant** de rejouer la chaîne (micro-banc cudaEvent A/B `banc-routeur.py` + `ncu --set full -k regex:_route_logits_fusee --launch-count 3`).

**(3) `_partiel_reduit` 8,9 µs** — lu dans `attn_paginee.py` (`_partiel_reduit_kernel`) : **aucune attente active** — `tl.atomic_add` rend l'ancienne valeur, seul le dernier arrivé réduit, les autres sortent ; le coût est celui du partiel (A : `_partiel` 7,3 + `_reduce` 1,3 = 8,6 µs) + une queue de réduction dans un seul programme par groupe (~0,3-1,6 µs contre 4 × 48 programmes pour `_reduce`). Le « ≤ 0,2 ms/pas » (≤ 4,2 µs/appel) demande un **partiel plus rapide**, pas la fusion : à b=12, C = 8 tranches de 288 jetons sur N = 144 pages ; à ctx 300, **2 programmes actifs par groupe** (96 sur 384), chacun 4,5 itérations de 64 jetons. Piste (non faite, change aussi le témoin) : tranches plus courtes quand `voulu` est petit (C = 18 : 3 programmes actifs, 864 lancés dont 768 vides). Le micro-banc `banc-routeur.py attention` mesure A et B au cudaEvent et `ncu -k regex:_partiel_reduit` dit l'occupation ; laissé tel quel, dit tel quel.

**(4) PPL b=1 +0,36 %** — par lecture, ce qui change la numérique à b=1 sous COMPACT=1 : **pas le routeur** (jeté par la faute (1), et de toute façon même arrondi bf16 que `_router_logits`, seul l'ordre de la somme fp32), **pas `_etroit`** (`narrow_choix(1)` = cuda : `int8_gemv` à b=1, tête comprise), **pas kv/q/valid** (au bit) ; **reste l'attention fusionnée** : à b=1 C = 18 tranches, `lg = tl.sum(l·w)` sur CT = 32 voies dans un noyau dont la disposition (layout) n'est pas celle de `_reduce_kernel` → ordre de somme fp32 ≠ → sortie bf16 ± 1 ulp par endroits → sur un MoE, un expert bascule et les trajectoires divergent (premier jeton NLL ≠ au pas 8). Ce n'est pas une faute, c'est le même bruit que graphes ON/OFF (témoin A 29/756). **Le +0,36 % est dans le bruit de l'instrument** : `ppl-apparie.py` sur les JSON de Manon — Δ NLL apparié +0,0036, **2 SE apparié 0,91 %** (la SE de la NLL elle-même, 10 %, est la variance du texte, pas de l'instrument) → tenu. Bissection pour Manon : `ACVRAM_GLUE_COMPACT_ITEMS=routeur|attn|etroit|kv` (regime.py, `kernels.glue_compact(fusion)`), `equiv-b12.py` à bras libre + `GRAPHES=0` (témoin A-off), `BISSECTION=1 bash chaine.sh` ajoute les quatre bras.

Chaîne mise à jour (scellé C15-3b de Sage en tête, remplace l'ancien) : (a) B ≤ 600, prédit 545 ± 30 ; (b) juge « B pas plus loin de A que A-off » à b=12 (cases d'argmax ≠ et |Δlogit| max) + PPL ± 2 SE, et à b=1 (`ppl-apparie.py` A / A-off / B, 2 SE apparié) ; (c) 5/5 ; (d) ≥ 1 450 t/s ET J net ≤ 0,19 sous éco 2 700 (prédit Sage 1 480-1 560 · 0,185-0,195 ; issue gênante : routeur > 5 µs à ncu → B < A). Lancements prédits après 3b : **545/pas** (le B de Manon 785 − 240). Tests à sec : 103 passed, 3 skipped (carte). Commits : 876ffa6a (1), 3e62a9b5 (2 + ITEMS + banc/ncu), chaîne + fiche.

## C15-3c, 20/09 (Océane, à sec, après l'addendum 03 h 17 de Manon : routeur 13-19 µs, experts ≠ A, attention 178 registres)

**Cause de |Δtopw| 1,06 × 10⁻³ et des experts ≠ A** — `model.py` `_router_logits` : `dtype_voulu = x.dtype` (bf16) pour un softmax sans biais (Coder) → `F.linear(x, w)` **sort les logits en bf16** : chaque logit est arrondi à 2⁻⁸ relatif. W est le **même** tenseur bf16 dans les deux bras (`_router_w[bf16]`, pas une conversion fp32 → bf16) ; le Triton 3e62a9b5 calculait les mêmes produits exacts, sommés en fp32 dans un autre ordre, puis arrondissait en bf16 aussi : à chaque frontière d'arrondi (il y en a parmi 12 × 128 logits par appel : `test_c15_3c_le_temoin_rend_visible_tout_ecart_de_somme_a_2_moins_8` en trouve sur 65 536), un logit diffère d'un ulp bf16, et **un seul ulp bf16 sur un logit déplace un poids top-k de ~10⁻³** (softmax fp32 : Δp ≈ p · 2⁻⁷) ou bascule un expert à égalité proche. Conclusion : aucun noyau qui ne rejoue pas l'ordre de somme (inconnu, split-K) de cuBLAS ne peut rendre le routage du témoin — la seule façon d'avoir des experts identiques est le **même appel cuBLAS**.

**Fait** :
| item | commit | quoi | prédit | tests |
|---|---|---|---|---|
| (1)+(2) routeur | e94bf8b6 | `route_logits_fusee` = `F.linear(x.to(w.dtype), w)` avec le tenseur du cache `_router_w[dtype_voulu]` (`_routeur_compact` réécrit : logits **au bit** ceux du témoin, bf16 Coder / fp32 GLM) + `_route_fusee_kernel` à **num_warps=1** (12 programmes, les 128 experts dans un warp : les 3 × k réductions de la sélection en shuffles au lieu de mémoire partagée + barrières ; le témoin à 4 warps coûte 7 µs/appel) ; GEMM Triton retiré | sélection ≤ 2 µs (−5 µs × 48 = −0,24 ms/pas) ; lancements **B = 641** (13/couche : cuBLAS wmma + splitK gardés) — **scellé ≤ 600 prédit FAUX**, dit tel quel ; issue gênante : à 1 warp la sélection ne raccourcit pas (atomiques, stores) | `test_route_prep` : 512 jetons, W bf16 (fp16 à sec) et W fp32, softmax et sigmoid+biais → topi, eid, compteur **identiques**, |Δtopw| ≤ 4 ulp fp32 ; témoin cassant ; `test_glue_compact` : le routeur tourne une fois (F.linear 1, route_fusee 1 à 1 warp sous compact / 4 sous témoin, `_route` 0) |
| (3) attention | f194f7b1 | registres lus : k et v [64, 128] int8 → 16 bits (64 valeurs/fil à 4 warps), acc [16, 128] fp32, q, s/p [16, 64] — ceux du partiel, pas de la queue de réduction (plus petite, pas vivante en même temps) ; `WARPS_COMPACT = 8` pour le noyau fusionné seul, mêmes tuiles, même arithmétique | ≤ 110 registres/fil, occupation ×2 ; ≤ 1 ulp 16 bits sur carte (disposition des `tl.sum`), au bit à sec | `test_glue_compact` : `torch.equal` strict à sec contre les deux noyaux |
| (4) ncu, chaîne | — | `ncu-routeur.sh` : `sudo -n /usr/local/cuda-13.4/bin/ncu --set full --target-processes all -k regex:… --launch-count 3 /usr/bin/env HOME=<copie> …` (HOME de travail + copie des caches, comme `ncu-c1-tampon-19-09`) ; trois profils : sélection A (4 warps), sélection B (1 warp), attention B ; `banc-routeur.py routeur-A` ; `noeuds.py` : B attendu 641 (route_fusee 48, wmma 48, splitK 48) | — | — |

Commande pour Manon : `bash scratchpad/c15-niveau3-20-09/ncu-routeur.sh` (micro-banc A/B routeur et attention, puis les trois ncu) ; scellé de la fenêtre inchangé (Sage) : nœuds ≤ 600 (prédit faux : 641), ≥ 1 450 t/s ET J net ≤ 0,19, « B pas plus loin de A que A-off », capture 5/5 — `bash scratchpad/c15-niveau3-20-09/chaine.sh` si la sélection ≤ 3 µs et experts égaux. Tests à sec : 92 passed, 2 skipped (fichiers touchés + regime).

## Bilan de nuit (20/09 04 h 50 machine, `sage-c15-niveau3-coder-faux` § Ordre 04 h 45) : compte réel, « nœuds : pas le poste sur Coder b=12 », sélection demain
* **Compte réel** (Manon, nsys `--cuda-graph-trace=node`, 50 pas b=12) : témoin A **1 169 lancements/pas = 24/couche** (pas ~1 030 : la fiche initiale comptait sur le CSV de 30 noms) ; B à 209ab58f (Triton jeté, cuBLAS rejoué) **785** ; B à f3d27d17 (cuBLAS gardé pour le routage au bit, sélection 1 warp, attention fusionnée, glue KV/q/valid au bit, `_etroit` un nœud) **641 prédits** (13/couche) — le scellé initial ≤ 450 supposait `rope_kv` (F3a, réfuté le 17/09) et `moe_reduce` dans le GEMV down, et le ≤ 600 supposait les logits du routeur hors cuBLAS (β refusée : la sortie bf16 du témoin rend tout autre ordre de somme visible à 2⁻⁸ → experts qui basculent).
* **Ce que la nuit établit : les nœuds ne sont pas le poste sur Coder b=12.** Les ≈ 1,9 ms « d'espaces entre nœuds » du budget du 19/09 ne se sont pas rendus en retirant des lancements : le seul gain établi est le lancement d'attention épargné (**−7 µs par couche au micro-banc ≈ −0,35 ms/pas**, `verdict-c15-niveau3-coder-19-09` addendum 04 h 40) ; la sélection à 1 warp gagne 1,1 µs sur 4 warps (7,46 contre 8,58 µs sous ncu, 12 programmes, occupation 2 %) et le total B recule de 1,3 µs au micro-banc ; l'attention à 103 registres (178 avant) ne gagne rien (17,8 µs : bornée par le travail des 48 programmes non vides sur 384, pas par les registres). Le poste de Coder b=12 reste les experts (Marlin 3,73 ms, 54 %) et le pas de graphe lui-même, pas la glue.
* **Sélection ≤ 3 µs : à faire demain, à sec puis ncu** — la sélection actuelle fait K = 8 tours dépendants (max, argmin, somme, 3 stores, un atomique) ; forme prévue : **une passe par rang** — pour chaque expert, rang = nombre d'experts strictement plus grands + ceux égaux d'indice plus bas (matrice [BE, BE] en registres d'un warp : 128² = 512 comparaisons par fil), sélection = rang < K, stores dispersés `topi[t·K + rang]`, `usage` par `atomic_add` masqué, somme des K probabilités par une réduction (ordre ≠ du sériel : |Δtopw| ≤ 4 ulp fp32 admis, topi identiques) ; prédit ≤ 3 µs sous ncu (une charge, deux réductions de softmax, une de rang, une de somme). Chantier réduit **C15-3d** (Sage 04 h 45) : attention fusionnée + glue KV + sélection + `_etroit` un nœud, cuBLAS gardé ; scellé nœuds ≤ 660, servi b=12 sous 2 700 ≥ 1 390 t/s ET J ≤ 0,196, experts égaux True sur 256 pas, B ≤ témoin, capture 5/5 → défaut ; fenêtre Manon après le juge de position du niveau 2.

## C15-3d bis (20/09 05 h 30 machine) : « experts égaux » est une statistique contre le témoin, pas un contrôle au bit
Fenêtre ABAB de Manon (addendum 05 h 15, ace8970a, 8b647b3a) : **B à 8 warps 1 417,5 t/s · 0,2073 J contre A 1 253-1 289 · 0,2146 = +11,5 %, J 0,966 ×** (scellé relatif de Sage tenu : ≥ +3,5 % ET ≤ 1,005 ×) ; B à 4 warps 1 386 · 0,2116 (+9,1 %, 0,986 ×) et 369 W = 369 : les 20 W de la première fenêtre n'étaient pas les warps → **8 reste le défaut** (`ACVRAM_ATTN_WARPS_COMPACT`). **Experts égaux sous graphes : 0/255 pas dès le pas 0**, bissection par item : kv 255/255 ; attn 0/255 (couches 9+) ; `_etroit` 0/255 (17+) ; routeur 0/255 (22+) alors que ses logits sont le même cuBLAS — le même phénomène que le témoin A ON contre A OFF (29/756 argmax ≠, 01 h 19) : sur un MoE, toute différence d'ordre de somme (≤ 1 ulp) bascule un expert à égalité proche quelque part dans 12 jetons × 48 couches, et la première bascule change x pour toutes les couches suivantes. Un contrôle « True sur 256 pas » ne peut donc être tenu par aucun bras qui change un ordre de somme — il n'est tenu que par kv (au bit). **Règle (Sage 05 h 15)** : « experts égaux » = **statistique contre le témoin** : part des cases (pas × couche × jeton) dont l'ENSEMBLE d'experts diffère (top-k trié : une permutation à poids égaux n'est pas un routage différent), bras B contre A, comparée à A OFF contre A ; **tenu si ≤ 1,2 × le taux du témoin** → défaut 8 warps + .deb 0.6.24, sinon régime nommé `glue=compact`. `equiv-b12.py` imprime désormais ordre au bit / ensembles / cases ≠ et le verdict contre `experts-egaux-A-off.json` ; `banc-routeur.py` juge l'attention A et B contre la référence fp64 (2⁻⁸ du max de la ligne) aux formes réelles (`CTX=2048`, longueurs mêlées).
