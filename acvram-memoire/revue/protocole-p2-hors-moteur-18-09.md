# Protocole — P2 hors moteur (poste7) : PPL transformers sur poids déquantifiés, RATIO seulement (REGLES §4, verdict A7) — `-qkvo-i8c` contre le classé, même run, mêmes 3 tranches privées, juge géo

instrument : `scratchpad/p2-hors-moteur-18-09/dequant-vers-hf.py` (chargeur acvram réutilisé : `_ShardReader`/`_build_quant`/`_build_scaler` ; `dequantize_nvfp4` fp32 → bf16, `_dequantize_int8` ; échelle d'activation repliée dans les colonnes W[:, k] / scale[k] — sémantique `ChannelScaler.apply` x/scale ; refus si hadamard_block ≠ 0 ; config + tokenizer HF copiés) → deux dossiers HF bf16 sous `/mnt/AI_GENERATOR/hors-depot/p2-hf-18-09/{classe,i8c}` (≈ 61 Go chacun) ; puis `scratchpad/ppl-hf-17-09.py` (transformers bf16, device_map auto 26 GiB + CPU, fenêtres 1 024, MIN_CTX 1023) sur `corpus-prive/tranches-coder/tranche{0,1,2}` pour les deux ; ratio = géo(i8c) / géo(classé).
scellé (poste7) : ratio ≤ 1,005 → P2 ouvert (1,0148 × 1,005 = 1,020, la classe) ; > 1,005 → P2 fermé, converti gardé comme pièce ; prédiction 1,001-1,003. Les absolus transformers ne sont pas publiés comme absolus acvram (contrôle : géo(classé)_HF contre 1,0148 acvram, écart attendu ≤ 0,003 — informatif).
contrôle de l'instrument : le classé déquantifié doit rendre une PPL HF proche de la sienne sous acvram (1,0148 × bf16) — si l'écart dépasse 0,01, le repli (échelles, ordre, act_scale) est faux et le ratio ne vaut rien.
addendum (poste7) : le contrôle d’instrument est le RATIO classé_HF / bf16_HF avec la source bf16 mesurée dans le même run (suite-bf16.sh, 3 tranches, enchaînée après la chaîne) ; sans elle, contrôle affaibli = géo(classé)_HF − géo(i8c)_HF seul, dit tel quel.
addendum 17h55 (poste7) : venv effectif PROUVÉ, pas annoncé — scratchpad/p2-hors-moteur-18-09/venv-effectif.txt (cmdline, exe, maps : site-packages de /opt/ia/vLLM/.venv chargés, accelerate 1.15.0) ; l’en-tête du verdict le citera.
addendum 18h30 — GLM (poste2 8beb168, `GLM-4.7-Flash-srcbf16-nvfp4-k48-qkvo-i8c`) : même protocole, `chaine-glm.sh` — déquant classé (`k48-calibA`, AWQ : act_scale repliés) et i8c, PPL HF avec `PPL_PREFIXE="[gMASK]<sop>"` sur `corpus-encode-brut-glm/prive-tr*`, source bf16 `GLM-4.7-Flash-bf16` dans le même run ; venv PPL imprimé par `sys.prefix` en tête ; tenseurs fp32 conservés, K rembourré retaillé à la forme du manifeste ; scellé ≤ 1,005 ; contrôle classé_HF / bf16_HF ≈ 1,0150 (acvram). Ne démarre qu'après la fin de l'in situ P1 (jamais en même temps qu'une mesure de temps).
