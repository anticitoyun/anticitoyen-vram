# Verdict — budget nsys du prefill GLM-4.7-Flash L=2 047 au défaut : **327 ms de noyaux** = cœur d'attention fp32 (cuTLASS `simt_sgemm`, sans TF32) **220,5 ms (67 %)** + experts **42,2 (13 %)** + norm/élém **33,7 (10 %)** + projections déquant int8 **20,2** + glue 7,3 ; sous `ACVRAM_MLA_TF32=1` : **262,7 ms (−20 %)**, le cœur passe à **156,8** (−64 ms : les deux einsum basculent de `simt_sgemm` à `tensorop_s1688gemm` = TF32) — la variable AGIT ; le contrôle « nom contenant tf32 » était faux (les noyaux TF32 de cuTLASS s'appellent `s1688gemm`)

instrument : même `prefill-nsys.py` / `classer.py`, `chaine-glm.sh`, arbre `cce062b` (main `16528d1` + verdicts, C13-a b6249d0 inclus), prise `carte.sh` 19:07:47-19:08:33, bras A aucune variable, bras B `ACVRAM_MLA_TF32=1` seule (`[régime] ACVRAM_MLA_TF32=1 …`), modèle `GLM-4.7-Flash-srcbf16-nvfp4-k48-calibA`, carte 0 vide, charge 1,3 ; temps mural sous nsys 361,6 / 295,8 ms (5 661 / 6 921 j/s), gonflé ~10 % par le traçage ; sans nsys : 5 739 j/s (G1, 356,9 ms)
scellé : aucun (budget publié) ; prédiction poste7 : cœur ≥ 82 ms, « autres » > 100 ms ; poste2 : cœur 70-100, autres 60-120, experts 40-60. Contrôle annoncé par poste1 : noyaux TF32 visibles sous B, sinon inerte
mesuré (ms, × lancements) — **A défaut, 327,3** : `cutlass simt_sgemm_128x256 tn` **89,8** ×376 + `simt_sgemm_256x128 nn` **63,0** ×376 (les deux einsum scores / o_lat : 47 couches × 8 blocs) + `simt_sgemm_128x32 tn` **21,0** ×47 (un troisième produit fp32 par couche, hors portée de C13-a) + `softmax_warp_forward<float>` 10,4 ×376 + `masked_fill` 9,0 ×376 + copies/cat 9,4 = **cœur MLA ~203 + 17 = 220 ms** ; experts `Marlin` 33,7 ×99 (33 couches) + `nvfp4_gemm_grouped_mma2<64,4,128>` 8,5 ×39 (13 couches distinctes) = **42,2** ; `int8_dequant` 16,3 ×279 + GEMM bf16 3,8 = projections **20,2** (`CHEMINS_INT8 {'dequant': 837}` : les 5 projections MLA int8 g128 sont inéligibles au cublas) ; glue MoE 7,3 ; `nvfp4_quant_act` 1,4 ; **B TF32, 262,7** : `tensorop_s1688gemm_128x128 tn` **49,7** ×376 + `nn` **41,5** ×376 (−61,6 ms sur les deux einsum, ×1,68) ; `simt_sgemm_128x32` 20,1 ×47 **inchangé** ; softmax 10,0, masked_fill 8,7, experts 41,7, projections 20,0, norm/élém 33,6 — tout le reste identique à 1 % ; `grep -ic tf32` sur les noms : 0 et 0 (le contrôle par nom ne voit pas `s1688`)
verdict : **le cœur MLA en fp32 est 67 % du prefill GLM (220 ms sur 327), 2,7 × la borne basse de poste7 (≥ 82) ; « autres » = 43 ms, pas > 100 : la prédiction se trompait de poste, pas de diagnostic** — l'attention fp32 sans TF32 est bien LE poste (`mla.py`, einsum en float32) ; **C13-a agit** : −64 ms de noyaux (327 → 263, −20 %), 5 661 → 6 921 j/s sous nsys (le scellé C13-a ≥ 6 200 se juge sans nsys, fenêtre en cours) ; reste hors portée : le troisième `sgemm_128x32` (21 ms ×47), softmax + masked_fill fp32 (19 ms), copies/cat (9 ms) — ~50 ms de cœur encore en fp32/élémentaire ; les experts GLM (42 ms) sont un poste secondaire au prefill comme au décodage. Le contrôle « `*tf32*` dans le nom » aurait déclaré la variable inerte à tort : la preuve est le changement de famille de noyau (`simt_sgemm` → `tensorop_s1688gemm`, mma 16×8×8 TF32) et les −64 ms — un contrôle par nom doit connaître le nom réel du noyau attendu
suite : poste1 : (1) le `simt_sgemm_128x32` ×47 (21 ms) — quel einsum, pourquoi hors portée TF32 ; (2) softmax fp32 + masked_fill + cat (28 ms) : fusion ou bf16 après les scores ; (3) projections MLA int8 g128 → cublas (20 ms, même cause que le classé Coder) ; poste7 : budget GLM prefill = cœur 220 → 157 sous TF32, cible suivante ≈ 100 ms de cœur ; C13-a en cours (prefill ABAB + PPL 3 tranches) ; chef : indexer

## Rejouable
`ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/nsys-budget-19-09/chaine-glm.sh` (46 s) ; CSV `glm-defaut_…`, `glm-tf32_…cuda_gpu_kern_sum.csv`, journal `journal-glm.log`. Contrôle TF32 correct : `grep -c s1688gemm <csv>`.
