# Verdict — cellule témoin llama.cpp `gemma4-31b` + mmproj, 20 images 896×896 (Sage sage-go-multimodal-organisation-20-09, Manon (1) ; scellé du duel P3 (c) de sage-acvram-multimodal-20-09) : **TTFT llama.cpp = 0,691 s (médiane des 19 requêtes froides ; moy. 0,700, min 0,678, max 0,837)** — au-dessus du 0,4-0,6 prédit ; **J par requête 427 brut / 328 net** (1,36 s à 311 W pour ≈ 380 jetons d'invite + 47 de sortie), débit de sortie ≈ 70 t/s ; le chiffre du duel est posé : **acvram tenu si TTFT ≤ 0,69 s ET J ≤ 328 net ET top-1 identique ≥ 18/20**

instrument : `scratchpad/mm-temoin-20-09/temoin-llamacpp-vision.py` (llama-server 0.4.1-dev b4c9233c, `-m` Q4_K_M + `--mmproj` BF16, `-ngl 999 -c 4096 -np 1 -fa on --jinja`, HTTP `/v1/chat/completions` en flux, `image_url` data: base64 PNG + « Décris cette image en une phrase. », `max_tokens 48`, T=0 ; TTFT = premier delta de contenu ; `Energie` NVML par requête, repos 15 s après) sous UN `carte.sh` mesure `ACVRAM_CPUS=0-15` avec `-lgc 2700` (SM 2 445-2 692 relevés, `-rgc` rendu), `chaine.sh` ; **corpus : 20 images synthétiques déterministes** (`fabrique-images-20.py`, graine 20260920 : formes comptables, mots en majuscules, dégradés, damiers, barres, horloges ; `images-20.sha256`, `descriptions.tsv`) — les photos du poste sont privées ; Sage peut substituer 20 photos libres, le harnais est indifférent ; **premier essai 13:13 arrêté** (attribut `watts` inexistant sur `Energie` → `moyenne` ; le serveur orphelin a été tué par pid ; le script tue désormais son serveur sur exception) ; second essai 13:17:01-13:19:36, chargement 76 s (disque en cache), `poste=20-09-1030`, hôte calme, 3080 Ti pid 4286 (pair)
scellé (Sage, avant) : ce chiffre devient le scellé du duel ; prédit llama.cpp 0,4-0,6 s, acvram 0,25
mesuré : chauffe (img00) TTFT 0,546 · 1,22 s ; **img00 rejouée : 0,338 s = cache de prompt de llama.cpp (même image que la chauffe) — exclue** ; 19 froides : TTFT 0,678-0,837 (18 entre 0,678 et 0,715 ; img01 0,837), total 1,35-1,51 s, 47 jetons de sortie chacune, J 400-438 brut, W 286-324, repos 68,8 W ; serveur : `prompt eval 420-500 ms / 380-387 tokens` (+ encodage mmproj hors compteur) ; cohérence des réponses avec le contenu (mots-clés, info seulement, texte au JSON) : 12/20 — les damiers, dégradés, textes sont reconnus, les horloges/barres/formes le sont moins (à lire par Sage dans le JSON, pas ici)
verdict : la barre du duel est **0,69 s de TTFT**, plus haute que prévu : llama.cpp paie ≈ 0,45 s de prefill sur 380 jetons (≈ 850 j/s sur Q4_K_M 31B dense avec image) + ≈ 0,2 s de tour de vision ; acvram à 0,25 prédit doit donc gagner surtout sur le prefill (nvfp4 dense 31B, prédit 300 jetons ≈ 60 ms) ; la réponse llama.cpp est de 47 jetons à 70 t/s : à J égal, acvram devra décoder à ≥ 70 t/s sous 311 W — b=1 dense 31B nvfp4 ≈ 60-80 t/s : **J est le scellé le plus serré**
durée : 2,6 min de carte (13:17:01-13:19:36) + 3,4 perdues au premier essai
suite : Sage : accepter le corpus synthétique ou nommer 20 photos libres (sha256 à redater) ; références `transformers` bf16 (2) tout de suite après (gemma-4-31B-it-bf16 59 G : `device_map auto` 26 Gio carte + RAM, coût inconnu → mesuré sur la première image) ; Jérôme : indexer ; trou : aucun avant (2)

## Rejouable
`cd <worktree manon> && POSTE=20-09-1030 ACVRAM_ARBRE=$PWD PYA=<python venv> bash scratchpad/mm-temoin-20-09/chaine.sh` (3 min, disque chaud ; `temoin-gemma4-31b.json` avec les 20 réponses, `-serveur.log`). Images : `/opt/ia/vLLM/.venv/bin/python scratchpad/mm-temoin-20-09/fabrique-images-20.py scratchpad/corpus-prive/images-20` (PIL) ; légendes : `fabrique-legendes-50.py`.

## Addendum 13 h 35 — témoin rejoué au scellé réécrit de Sage (13 h 55 : `max_tokens=48` fixé pour les deux moteurs, J net publié aussi sur la fenêtre TTFT) et référence bf16 de la tour
instrument : même chaîne, `temoin-llamacpp-vision.py` corrigé (chauffe exclue par construction ; `joules_ttft` = compteur NVML lu au premier jeton, `joules_depuis()`), 13:30:14-13:33:5x, `-lgc 2700`, fils 0-15 ; référence tour : `references-tour.py` (transformers 5.17, tour SigLIP + `embed_vision` sur la carte, LM en RAM non appelé, 4 s), `references-tour-gemma4-31b-bf16.pt` 67 Mo + `.sha256`
scellé (Sage 13 h 55, avant) : (1) TTFT acvram ≤ médiane des 19 froides ; (2) J net ≤ celui de llama.cpp par requête à 48 jetons ET sur la fenêtre TTFT ; (3) 8 jetons greedy = transformers bf16 ≥ 16/20 ET log-prob des 20 descriptions géo ≤ +3 % (Sage 14 h 25 : chemin prouvé sur gemma-4-12B-it bf16, coût nvfp4 du 31B contre acvram bf16 étagé)
mesuré (19 froides, 48 jetons de sortie chacune) : **TTFT méd 0,665 s** (moy 0,672, min 0,649, max 0,807) ; total méd 1,33 s ; **J par requête 408 brut / 316 net** ; **J fenêtre TTFT 141 brut / 94 net** ; repos 70,0 W ; tour bf16 : 20 images, 279 jetons d'invite, embeds SigLIP [256, 1152] et projetés [256, 5376] par image
verdict : les deux chiffres du duel sont posés : **TTFT ≤ 0,665 s** et **J net ≤ 316 par requête (48 jetons) / ≤ 94 sur la fenêtre TTFT** ; la référence (a) de la tour est scellée ; **(b) attend une source bf16 de gemma-4-12B-it : absente du poste** (seulement exl3 4bpw et GGUF Q4) → téléchargement HF ≈ 24 Go sur mot de l'utilisateur ; **(c) attend P0 fusionné + acvram bf16 étagé** (prédit 3-5 s/passe)
durée : 3,6 min de carte (témoin 3,5 + tour 0,1)
suite : Jérôme : le mot pour le 12B ; Océane : P0 ; ma file : (c) dès le pointeur ; test_chaine_b_contre_fp64 (Sage 13 h 45) dans le trou d'attente
