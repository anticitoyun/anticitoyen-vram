# Verdict — P1 in situ, première prise : **ARRÊT à l'étape 2** — `tests/test_marlin_prefill_p1.py` 2 failed / 2 passed sur carte ; trois défauts du test et un écart réel du noyau en situ (3,7 % des sorties hors 2⁻⁷ à T = 1 024) ; aucune mesure de vitesse ni de PPL lancée (protocole : arrêt si le test échoue)

instrument : `scratchpad/marlin-situ-18-09/chaine.sh` (0a907b5 = main + laurine dc1774a + oceane-11, charge 0,98, .so en cache « chargée en 0 s ») ; sondes locales sur copies jetables du test (`_local.py`, supprimées), sous `ACVRAM_MOE_MMA=0`
commit : 0a907b5, 13:42-13:50
régime : test jouet E = 8, H = 256, I = 128, top-2 ; sondes à T = 96 (tel quel) et T = 1 024
scellé : test vert avant toute bascule (REGLES §7) ; in situ ≥ 15 700 j/s et PPL B0 ± 0,002 non atteints
mesuré : (1) tel quel (T = 96) : `assert hors == 0` **passe** puis `torch.roll(sc, 1, dims=2)` → `NotImplementedError: "roll_cuda" not implemented for 'Float8_e4m3fn'` (test:113) ; (2) roll sur la vue uint8 : **bras cassant VERT (`assert 0 > 0`)** — à T = 96, `par_expert = 96 × 2 / 8 = 24 ≤ _MOE_GEMM_MAX 48` : la branche `elif direct:` (model.py:1297, GEMM groupée CUDA 4 bits) passe AVANT `elif _PREFILL_GROUPED == "marlin"` (model.py:1329) — **la branche Marlin n'est jamais exécutée par le test**, l'égalité est triviale et le bras cassant ne touche pas ce qui est lu ; (3) sonde à **T = 1 024** (par_expert 256 > 48, branche Marlin atteinte) : **`hors` = 9 806 (sans AWQ) et 11 174 (AWQ) sur 262 144 sorties (3,7-4,3 %) hors 2⁻⁷ × (moy|y| + |y|)**, moyennes |y| égales (0,02682 / 0,02683), `torch.equal` faux — écart réel, borné, pas un effondrement
verdict : **test non vert → pas de mesure** ; défauts à porter par Laurine : (a) roll fp8 → `sc.view(torch.uint8)` ; (b) T du test au-dessus de `_MOE_GEMM_MAX × E / top_k` (ici ≥ 193 ; prendre 1 024) ou `ACVRAM_MOE_GEMM_MAX=0` dans le test, sinon la branche testée n'est pas Marlin ; (c) **l'écart en situ à 2⁻⁷** : le juge du banc (2⁻⁷ × Σ|x·w|, borne large) était à 0 hors bande, le juge du test (2⁻⁷ × (moy|y| + |y|), borne serrée) en trouve 3,7 % — à Laurine de dire quelle borne est la bonne (arrondi S0E5M3 de l'échelle ? ordre d'accumulation ?) ; la PPL prefill B0 ± 0,002 reste le juge final et peut être mesurée dès que Sage le veut, indépendamment du test

## Lecture
- REGLES §7 (« la garde qui couvre est celle qu'on a fait échouer ») s'applique au test lui-même : un bras cassant vert sur une branche non empruntée est la troisième occurrence du motif « sonde qui ne touche pas ce qui est lu » — la condition d'entrée (`direct`) est à écrire dans le test.
- La chaîne est prête (`prefill T/M`, profil, PPL 3 tranches) ; elle repart en une commande dès le test corrigé ou sur ordre explicite de mesurer sans lui.

## Addendum 14h15 — deuxième prise (Laurine 820f3f7, arbre ab4cdb6) : **ARRÊT à nouveau**, `tests/test_marlin_prefill_p1.py` 3 failed / 4 passed sur carte
1. `[96-sans_awq]` et `[96-awq_par_expert]` : `attendre_chemin(bloc, "groupe")` → **chemin pris `direct`** (compteurs `{'direct': 1}`) : à T = 96 le témoin « groupe » lui-même passe par la branche `direct` (par_expert 24 ≤ `_MOE_GEMM_MAX` 48) — le compteur de chemin fait son travail (c'est lui qui arrête), mais le cas T = 96 du test ne peut pas atteindre « groupe » sans `ACVRAM_MOE_GEMM_MAX=0` ou T ≥ 193 ; à retirer ou à forcer.
2. `[1024-awq_par_expert]` : **le témoin B0 (« groupe ») échoue son propre critère contre fp32** — `hors_groupe = 115 > 1e-4 × 262 144 = 26` (test, ligne `assert hors_groupe <= TOL_HORS * ref.numel()`), AVANT toute comparaison Marlin ; `[1024-sans_awq]` passe. Avec la table AWQ par expert, soit la référence fp32 du test n'applique pas l'échelle comme le moteur (xs / awq_g[e_sorted] avant les GEMM), soit la tolérance 1e-4 est trop serrée pour B0+AWQ ; dans les deux cas Marlin n'est pas jugé sur ce cas.
Aucune mesure lancée (protocole : test vert d'abord) ; la chaîne repart en une commande.

## Addendum 14h45 — troisième prise (Laurine 2a15272, arbre 1ee2da9, charge 9,7) : **tests 11/11 verts sur carte**, témoin T 9 775 j/s (σ 102) ; **bras Marlin : régime DÉGRADÉ, mesure refusée** — la double disposition ne tient pas en mémoire
`ACVRAM_PREFILL_GROUPED=marlin` : `[acvram] plan réajusté : 30 MLP de plus en RAM hôte sur cuda:0 (poids réels 10,5 Gio pour 30,1 Gio libres, activations de préfill réservées 17,55 Gio)` → `régime DÉGRADÉ — graphes=off couches_exilées=30/48 experts_exilés=3840/6144 … experts_layout=simple`, 48 lignes « repli lent (boucle par expert) : cause non relevée », `exiger_regime_nominal` refuse (`outils/regime.py:49`) → prefill-M rc=1 ; le profil et les 3 PPL ont tourné dans ce régime dégradé : **non publiés**.
Cause, lue dans le code : `loader.py:1706-1707` (`_reserve_prefill`) ajoute sous marlin `sum(mlp_bytes des couches MoE)` = la totalité des experts NVFP4 (≈ 14-15 Gio pour Coder), pas « +1,7 Go » — une seconde disposition repackée des mêmes codes 4 bits pèse exactement la pile : 17,55 Gio réservés (≈ 3 d'activations + 14,5 de disposition), il ne reste pas la place du modèle + KV sur 32 Gio → exil de 30 couches, `experts_layout` retombe à `simple`, et la branche Marlin n'est jamais empruntée. Le témoin T (même arbre) est nominal (9 775 j/s, experts_layout=simple, kv_budget 64768/8).
À décider (Sage / Laurine) : la double disposition n'est pas viable sur Coder à 32 Gio ; voies : (i) disposition Marlin SEULE pour les experts, avec le GEMV de décodage qui la consomme (repack unique, 0 octet de plus), (ii) repack à la volée par couche pendant le préfill (0,1 s/pile au banc × 48 = trop lent tel quel), (iii) exil assumé de la disposition Marlin en RAM hôte avec préchargement par couche (PCIe : 0,3 Gio/couche à 25 Gio/s = 12 ms/couche ≈ 0,6 s/préfill, hors scellé). Je ne mesure rien tant que le régime n'est pas nominal.

## Addendum 17h50 — in situ complet (248eaaf = main + laurine 73c89e7, forme (b) intégrée) : **ARRÊT étape 1** — capture godets 1/2/8/16 tous en échec sous `GEMV_LAYOUT=marlin` + `PREFILL_GROUPED=marlin`
Régime DÉGRADÉ dès le chargement : `plan réajusté : 22 MLP de plus en RAM hôte (poids réels 11,0 Gio pour 30,1 libres, activations de préfill réservées 17,04 Gio)`, 21/48 couches exilées, 2 727/6 144 experts exilés, `piles_ok False`, `PREFILL_GROUPED=marlin refusé : piles non NVFP4`, `experts_layout=double` ; 53 / 113 / 168 ms/pas aux godets 1/2/8, godet 16 refusé (`kv_planned_seqs 8`). Cause identique à la 3e prise : `loader.py:1706` réserve toujours la pile MoE entière pour une seconde disposition — 73c89e7 corrige l'aiguillage MoEBlock (gemv_marlin), pas la disposition unique. Rien de mesuré ; pytest gemv_marlin non exécuté (garde conftest : carte tenue par ma PPL P2, 2 h d'attente, rc 4). Condition pour la prochaine prise : disposition Marlin SEULE dans le chargeur (remplace la pile NVFP4 pour le GEMV et le préfill), réserve :1706 retirée, `experts_layout=marlin` dans la ligne régime.

## Addendum 20h25 — in situ complet sur la disposition unique (laurine a3e1aa6, arbre d5be8eb) : **chargement NOMINAL** (`experts_layout=marlin`, 0 couche exilée, piles_ok True, graphes on — la disposition unique tient en 32 Gio) puis **ARRÊT godet 1 : `AttributeError: 'NoneType' object has no attribute 'shape'`** à `model.py:1233` (`_forward_prefill_grouped` : `par_expert = topi.numel() / max(1, pg[1].shape[0])`) — la pile naturelle libérée après le repack Marlin (`pg[1]` = None) est encore lue pour compter les experts au premier préfill ; correction triviale (E depuis la pile Marlin ou `len`/forme conservée) mais aucun godet ne passe tant qu'elle n'est pas faite. pytest gemv_marlin : de nouveau rc 4 (garde conftest : carte tenue en mesure par un PID tiers 3628499 pendant 2 h après ma P2 — pas moi) ; non exécuté.
