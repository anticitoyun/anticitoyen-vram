# Sage — ordre du jour du 16/09 (décideuse technique ; Jérôme coordonne ; l'utilisateur tranche ce qui engage)

État de départ : main v0.6.5 (officiel b=12 : 11,77 ms / 930 t/s / 0,428 J, durée à corriger du biais `Energie`) ; `narrow_gemm` coupé, voyants b=1 et ties tenus, **PPL de Manon = dernière porte de 0.6.6** ; GLM : reconversion AWQ-experts en cours, pile AWQ livrée, duel prise A dès les trois voyants. Règles du jour inchangées (≥ 20 s au compteur, `CUDA_VISIBLE_DEVICES=0`, régime dans le nom, worktree figé pour toute mesure — c8992f8).

**Première commande après le redémarrage (Jérôme)** : relancer la série de téléchargements, `hf` reprend les fichiers partiels — `systemd-run --user --unit=hf-sage-15-09 /mnt/4TO_SATACMR_2022/Modeles/.journaux-sage-telechargements-15-09.sh` ; puis les deux de l'utilisateur (gpt-oss-120b, Coder-Next-FP8) vers le SSD `sources_hf/`. Aucune conversion avant la ligne OK du journal.

**File de carte, dans cet ordre** : 1. Manon, PPL teacher-forcing narrow (a10b987, 1,000 ± 0,002, 20 min) → **0.6.6** → 2. Laure, re-tampon officiel 0.6.6 B + témoin A (ABAB 20 s, 30 min) **après** avoir publié l'excès du biais (sans carte : constant → correction arithmétique, variable → re-tampon) → 3. Manon, PPL du GLM AWQ-experts (≤ ×1,01, top-1 ≥ 97 %) + Océane contrôle MTP → 4. Laure, **duel GLM prise A** (b=1/12, nous vs vLLM GadflyII NVFP4 et fp8 dyn), scellé `sage-objectif-14-09` : bras NVFP4 vLLM entre 0,7 et 1,3× notre débit → 5. Laurine, saut de la table d'unités (Coder **0,00 ± 0,03 ms**, réfuté > 0,05) puis 1aj-2 `rows=16` + profil eager (projections **2,38 → ≤ 1,8 ms**, réfuté > 2,2) → 6. Laure, cellule b=5 (MIN_T=5 ssi critère tenu) → 7. Manon, conversion **Qwen3.6-35B-A3B srcbf16** (experts NVFP4 snr_floor 25, projections/lm_head int8, MTP par index), PPL ≤ ×1,01 → duel apparié contre `nvidia/Qwen3.6-35B-A3B-NVFP4` (Laure, 17/09).

**Sans carte** : Océane — test d'équivalence auto-calibré par témoin (§ 7 : med/p90 ≤ 1,2×, max ≤ témoin, garde 150 ulp), helper `ACVRAM_MODELES`/`ACVRAM_SORTIES`, cliquet **433** ; Katy — inventaire des scripts `scratchpad/` (fichier, ligne, dernier lancement) et les deux README disques (SSD = convertis + sources bf16 ; HDD = GGUF/EXL3/AWQ/vLLM) ; Jérôme — REGLES §6 (ncu 2 × 46 min, `--launch-count` borné) et §9 (borne 1 050 = nos GEMV, carte 1,18-1,46 To/s), MECANISMES (tampon L2 chassé par 74 Mo de poids), INDEX des notes du 15/09.

**Ordre des conversions des nouveaux modèles** (après OK) : Qwen3.6-35B-A3B (MoE+GDN, duel apparié) → Qwen3.8-27B (dense GDN, duel apparié) → Nemotron-3.5-Lightning-30B-A3B (mamba2+MoE, duel apparié) → Kimi-Linear-48B-A3B (MLA+KDA) ; gpt-oss-20b n'est pas une conversion, c'est le chantier MX (`ue8m0` bloc 32), après le duel GLM. Chaque converti : PPL ≤ bf16 × 1,01, `piles_ok=True`, MTP hérité, sinon il ne se sert pas.

Ce qui engage l'utilisateur demain : rien de nouveau ; les deux téléchargements sont les siens.
