# Sage — le bogue est dans `nvfp4_quant_act` : pas d'échelle globale, un bloc dont amax/6 < 2⁻⁹ est mis à zéro ; le discriminateur ne pouvait pas le voir ; protocole pour Laurine, et le pas b=12 (16/09)

## § 1 — Cause probable, fichier et ligne, avec le test qui la réfute

1. **(1b) est écartée telle que je l'avais formulée** (étendue intra-bloc E2M1) : 1,0631 ≤ 1,1, sans zone grise. Mais le discriminateur compare à `quantize_nvfp4`, qui pose une **échelle globale fp32** (`quant/nvfp4.py:237-240`, `g = amax/(6 × 448)`) avant l'E4M3 par bloc. **Le noyau n'en a pas** : `acvram_kernels.cu:2021-2050` (`nvfp4_quant_act_kernel`) calcule `s = min(amax/6, 448)` sur les 16 valeurs bf16 **brutes**, le convertit en E4M3 (`satfinite`), et si `sdec == 0` écrit **`packed = 0` : tout le bloc devient zéro**. L'E4M3 s'arrête à 2⁻⁹ ≈ 0,00195 (sous-normal) : un bloc dont amax < 0,0117 disparaît, et entre amax 0,0117 et 0,094 l'échelle n'a plus que 1 à 3 bits. La fake-quant ne reproduit pas cela — sa docstring dit « bit près » du noyau, c'est faux pour les activations (REGLES §4 bis : l'instrument a répondu sur une autre arithmétique que celle qu'on juge).
2. **Pourquoi l'échelle AWQ déclenche le défaut** : `xs / s[e]` (`model.py:983`) avec `s ≥ 1` sur les canaux saillants **réduit l'amplitude** des blocs avant la quantification ; les blocs déjà petits passent sous 2⁻⁹ × 6. Sans table (`test_table_unite_sautee`, division sautée) seuls les blocs naturellement < 0,0117 sont touchés : +0,2 % sur GLM, et probablement une part des +0,92 % de Coder-30B (`verdict-moe-mma-reel-qwen3-coder`) — un bonus à vérifier, pas une promesse.
3. **Test à sec qui rend faux, Océane, 15 min, avant que Laurine écrive une ligne** : sur son `x` réel `[16, 2048]` et sa table réelle, pour les 8 experts : fraction des blocs de 16 dont `amax/6 < 2⁻⁹` (mis à zéro par le noyau) et `< 2⁻⁶` (sous-normal), pour `x` et pour `x/s`. Scellé : **fraction(x/s) ≥ 3 × fraction(x) et fraction(x/s) ≥ 1 %** → cause confirmée, on passe au 4 ; **fraction(x/s) < 0,3 %** → ce n'est pas ça, Laurine cherche l'ordre `route+pack` (le test `tests/test_moe_awq_pile.py:148` avec des échelles ≥ 10 et des activations ≤ 0,01, pas les synthétiques actuelles). Même script, bras (v) : fake-quant **avec l'arithmétique du noyau** (E4M3 de amax/6 brut, bloc à zéro si sous 2⁻⁹) sur `x/s` — attendu err(v) ≫ 0,139, c'est le chiffre que le noyau réel doit rendre.
4. **Correctif, Laurine, une ½ j, un seul commit** : une échelle globale d'activation. Deux façons, la première est gratuite :
   * **(A) puissance de deux fixe** : le noyau quantifie `amax × 2ᵏ/6` (constante de compilation) et la pile compense **exactement** dans `gscales` (`model.py:855`, aujourd'hui `ones` → `2⁻ᵏ`) ; zéro lancement, zéro nœud de graphe, bf16/fp32 exacts. Contrainte : `amax(x/s) × 2ᵏ/6 ≤ 448` sinon `satfinite` écrase à 6 — Océane publie `max` et `p99,9` de amax par bloc de `x` et `x/s` (même script) ; k = le plus grand tel que `max × 2ᵏ ≤ 2 688 / 4` (marge ×4). Attendu k = 6 à 8.
   * **(B) amax par appel** (réduction + scalaire dans l'épilogue, comme `quantize_nvfp4`) : exact pour toute amplitude, +1 lancement par site, +47 nœuds de graphe. Seulement si (A) laisse des blocs sous 2⁻⁹ ou sature.
   * **Dans le même commit, la division `xs / s[e]` entre dans le noyau** (pointeur de table + index d'expert par ligne) : elle coûte 22 µs/couche mesurés (`laurine-awq-gate-up`, § 2 ci-dessous), soit la conséquence scellée de `sage-glm-gateup` § 2.2 (« on fusionne la division dans `nvfp4_quant_act` avant le duel »). Une passe mémoire de moins sur `[G, K]`, plus de `.contiguous()`.
5. **Test d'équivalence du commit — pas pile contre boucle** (W4A4 contre W4A16 : ils diffèrent par construction, c'est le piège du 15/09). Ce qui rend faux :
   * (i) **noyau contre référence Python de la même arithmétique** (la référence de `acvram_kernels.cu:1986` existe déjà, elle s'étend à l'échelle globale et à la table) : bit-exact sur `x/s` réel, 16 jetons × 8 experts, et **zéro bloc mis à zéro** là où la référence fp32 n'en a pas ;
   * (ii) **erreur de la pile réelle contre `y_ref`** (script d'Océane, mêmes 8 experts, même norme) : avant correctif attendue ≫ 0,139 (c'est le chiffre à publier, il prouve le bogue) ; après, **≤ 1,1 × err(iv) = 0,153** — scellé, réfuté au-delà → il reste autre chose sur le chemin pile ;
   * (iii) **re-PPL du même converti alpha-commun, NOMINAL MMA=1** : ≤ 1,010 (`sage-glm-gateup` § 2.1, déjà scellé), attendu ≈ 1,009 (son chiffre W4A16) ; sans-AWQ : ≤ 1,022, attendu 1,015-1,020 ; **Coder-30B prefill MMA=1/0 : ≤ +0,5 %** (attendu, réfuté si +0,9 % inchangé → le coût Coder est ailleurs, on ne le cherche pas maintenant).
6. **Manon** : la métrique W4A4 (cb2784b) reste sur sa branche, mais elle n'est pas perdue : une fois le noyau corrigé, la question « l'AWQ aide-t-il les experts en W4A4 ? » se juge sur la reconversion indépendante (0,998 en W4A16 → scellé ≤ 1,005 en NOMINAL après correctif, réfuté > 1,010 → cb2784b entre). Reconversion **après** (iii), pas avant.

## § 2 — Pas b=12 : réfuté, et la conséquence scellée est précisément le correctif ci-dessus

1. **Réfuté** : 1,056 > 1,05. Le scellé comparait deux convertis, c'est une faute de rédaction (la mienne : « le pas du converti sans-AWQ en pile ») et elle ne se répare pas en refaisant le scellé — Laurine a déjà le chiffre à code égal : **B/B∅ = 1,018×, +1,04 ms, 22 µs/couche** pour une division bf16 sur `[G, K]` + `.contiguous()` + une seconde `nvfp4_quant_act`. C'est ×3-10 l'attendu, et la conséquence était écrite avant la mesure : fusion de la division dans le noyau (§ 1.4, même commit). Scellé pour le commit : **B/B∅ ≤ 1,008×** (≤ 10 µs/couche, une seconde quantification seule), réfuté > 1,015× → la seconde quantification elle-même est chère, on la partage quand `s_g = s_u` à ε près (comparaison de tables au chargement, pas à chaque pas). Leçon REGLES §3, une ligne : *un scellé sur un changement de code compare deux codes sur un converti, jamais deux convertis.*
2. **Les +2,06 ms du converti** (A `-sansawq` 55,56 → B∅ 57,62, code et tables d'experts identiques) : **oui, Manon l'explique avant le duel**, pas avant le correctif — le reconverti héritera de la même recette. À sec, 15 min : diff des deux manifestes (format par tenseur, tables AWQ présentes sur q/kv/o et les couches denses, MTP, `lm_head`). Attendu : les tables AWQ des projections int8 de l'attention (une division par entrée de projection, 3-4 par couche ≈ 40 µs) ; réfutable sur la carte en 10 min, Laurine ou Laure : **B∅∅ = toutes les tables ignorées** (attention comprise), ABAB 20 s ; scellé : B∅∅ = A ± 0,3 ms → cause trouvée, même fusion sur le chemin int8 (ou table unité sur l'attention si l'AWQ n'y vaut rien : A7 disait 0 sur 32 paires denses) ; sinon le diff des manifestes désigne le tenseur, et Manon reconvertit avec le format de A pour ceux-là.
3. **Ordre** : Océane § 1.3 (à sec, maintenant) → Laurine correctif + fusion (§ 1.4-5) → re-PPL (iii) → Manon reconversion indépendante → PPL NOMINAL ≤ 1,005 → B∅∅ et le pas du reconverti (scellé : ≤ 1,03 × A, à code égal cette fois) → duel. Le duel ne se joue pas avec +3 ms/pas non expliqués : c'est 5 % du débit.

## § 3 — Blocs à zéro : 0/16 384, hypothèse écartée ; le contrôle n'a regardé qu'un côté, et qu'une des trois entrées quantifiées (16/09, après daabe1b)

1. **Deux hypothèses à sec réfutées en une heure, les deux miennes** : je le note pour le compte. Ce qui reste discriminable à sec tient en 15 min avec le script d'Océane, sans rien écrire de neuf ; au-delà, c'est la sonde de Laurine.
2. **Le côté haut, pas regardé** : `s_up` descend à **0,053** (expert 3) — l'activation de ce canal est **multipliée par 19** avant `nvfp4_quant_act`. Le noyau borne `s = min(amax/6, 448)` (`acvram_kernels.cu:2039`) : un bloc dont amax > **2 688** garde une échelle fausse et ses 16 valeurs saturent à ±6 (`satfinite`). `quantize_nvfp4` (échelle globale) ne sature jamais : le discriminateur ne pouvait pas le voir non plus. À sec, Océane : sur `x/s_g` et `x/s_u` réels, **max et p99,9 de amax par bloc, et fraction de blocs > 2 688** ; scellé : fraction ≥ 0,1 % → cause (correctif (B) de § 1.4, échelle globale par appel — (A) fixe l'aggrave) ; 0 → écarté.
3. **La troisième entrée quantifiée, pas regardée** : le chemin MMA quantifie aussi l'entrée de `down_proj` — `act = silu(g)·u / s_d[e]` dans `moe_act` (`acvram_kernels.cu`, `awq[e_sorted[r] * Kd + c]`), puis `aq, asf = nvfp4_quant_act(act)` (`model.py:1028`). Un produit `silu(g)·u` a une distribution bien plus lourde que `x` post-norme (beaucoup de quasi-zéros, quelques grands) : les deux bornes (2⁻⁹ et 2 688) et l'étendue intra-bloc s'y jouent autrement. Même script, 4 bras sur **down_proj** (poids réels, `s_d` réelle, `act` réel des mêmes 16 jetons) : err(iv)/err(iii) et les deux fractions de blocs (bas, haut) sur `act/s_d`. Scellé : ratio ≥ 1,5 ou une fraction ≥ 1 % → la cause est sur l'entrée de down (précision, pas application ; correctif = (B) ou table unité sur `down_proj` seulement — `awq_d = None` est déjà un chemin du code, `:1108`), sinon écarté.
4. **Si 2 et 3 sont nuls : sonde de Laurine, et elle s'écrit dès maintenant sans carte.** Sur les 8 experts et 16 jetons d'Océane, pile réelle MMA=1, un dump par étape : (1) `xq, xsf` contre la référence Python de la même arithmétique (`cu:1986`) sur `xs` ; (2) `g`, `u` contre `(x/s)·(W·s)ᵀ` fp32 déquantifié ; (3) `act` ; (4) `aq, asf` ; (5) `d` ; (6) après `moe_reduce_trie`. Chaque étape en erreur relative contre la référence, **pour les deux convertis** (alpha-commun, sans-AWQ), mêmes jetons. Scellé : **exactement une étape** où err(alpha-commun)/err(sans-AWQ) ≥ 3 ; c'est elle. Aucune → le défaut n'est pas dans le prefill à 2 couches sur 16 jetons : il est dans ce que l'instrument PPL fait de plus (fenêtres de 2 048 jetons : `_tuiles` et le rembourrage `F.pad` à `pg[4]` avec `xs.shape[1]` tranché, `:983` contre `:1020` ; les tuiles `n=0`), et la sonde se rejoue sur une fenêtre réelle de PPL, pas sur 16 jetons synthétiques — c'est l'issue qui gênerait tout le monde, je la nomme.
5. **Ordre** : Océane 2 et 3 (15 min à sec, maintenant) ; Laurine écrit la sonde en parallèle (pas de carte) ; verdict d'Océane → soit correctif (B), soit la sonde tourne (10 min de carte). Manon inchangée.

## § 4 — Cause confirmée sur l'entrée de `down_proj` (6e17ad9) : pas d'objection au correctif, trois précisions qui évitent de le refaire

1. **k par site, pas un k unique.** L'entrée de gate/up plafonne à amax 2,70 ; l'entrée de down (`silu(g)·u / s_d`) a 23-31 % de ses blocs sous 0,0117 **et** une queue haute inconnue. La compensation vit dans `gscales` de chaque pile (`pg`/`pu` contre `pd`), donc deux constantes (`k_x`, `k_act`) ne coûtent rien. Océane, 5 min à sec, même script : **max et p99,9 de amax par bloc de `act/s_d`, et fraction restante sous 0,0117/2ᵏ pour k = 6, 8, 10, 12**. Choix : le plus grand k tel que max × 2ᵏ ≤ 672 (2 688/4) ; scellé **fraction restante ≤ 0,1 %** à ce k, sinon (B) amax par appel **sur ce site seulement** (les 47 nœuds de graphe ne se paient que sur le down).
2. **Les scellés du commit couvrent les trois entrées, pas seulement `x/s`** : (i) noyau contre référence Python **sur `act/s_d` réel aussi**, zéro bloc à zéro là où fp32 n'en a pas ; (ii) err de la pile **complète** (après down, avant `moe_reduce_trie`) contre `y_ref` ≤ 1,1 × err(iv) **de down** (le 0,153 était le chiffre de gate ; Océane a le nouveau) ; (iii) inchangé.
3. **Sonde par étape : annulée**, (i)-(iii) rendent « faux » sans elle. Elle ne revient que si (ii) est réfuté après correctif.
4. **Deux attendus à publier avec le commit** : Coder-30B prefill MMA=1/0 passe de +0,92 % à **≤ +0,5 %** (les blocs flushés sans échelle sont les mêmes ; réfuté si inchangé → le coût Coder est ailleurs) ; le décodage MMA (`:1123`, même noyau) en profite sans mesure séparée — le scellé ≤ 1,005 de `sage-glm-w4a4` § 2.2 tient tel quel.
5. **Pour le compte** : la cause était celle du § 1.1, sur la troisième entrée ; le discriminateur du § 1.3 l'a manquée parce qu'il ne regardait qu'une entrée sur trois — le bon contrôle énumère **toutes** les quantifications du chemin avant d'en tester une. Une ligne pour REGLES §4 bis.

## § 5 — Les +2,06 ms sont les 124 tables AWQ sur les projections int8 (610fe52) : une décision de recette avant la reconversion, et un contrôle à sec qui la protège

1. **Le fait** : `opts.awq=True` partout dans `-avant-noawq-experts` — 124 tables sur attention + down dense (`layers.py:479-481`, une division bf16 par jeton et par projection) = **+2,06 ms/pas, 3,7 % du pas**. Elles sont portées par des tenseurs **int8** : l'AWQ compense l'erreur d'une quantification 4 bits ; à 8 bits par canal l'erreur est ~16 × plus petite, et la division coûte autant. Attendu : ces tables ne valent rien en PPL, et le duel n'a pas à payer 3,7 % pour elles.
2. **Décision de recette pour la reconversion de Manon** : AWQ **sur les tenseurs NVFP4 seulement** (experts, et tout dense en nvfp4) ; **aucune table sur les projections int8**. Un seul converti, une seule PPL — mais ça met deux variables dans le scellé ≤ 1,005 (correctif W4A4 + retrait des tables int8). Pour que la PPL ne mélange pas les deux, un contrôle à sec **avant** la reconversion :
3. **Océane, 10 min à sec, même montage que le discriminateur** : sur une couche d'attention de GLM (poids bf16 réels de `srcbf16`, table réelle de `-avant-noawq-experts`, `x` réel des 16 jetons), erreur relative de `x·Wᵀ` en **int8 par canal** avec et sans échelle AWQ (`x/s`, `W·s`), pour q/kv/o. Scellé : **err(avec)/err(sans) ≥ 0,9** → l'AWQ int8 vaut < 10 % d'une erreur déjà ~0,3 % : retrait sans risque pour la PPL, la recette 2 s'applique, et le scellé ≤ 1,005 reste à une variable ; **< 0,7** → l'AWQ int8 aide vraiment sur GLM, on la garde, et la division se fusionne dans le chemin int8 (Laurine, après le correctif MoE) ; entre les deux : on garde pour la reconversion (une variable à la fois), retrait jugé plus tard par PPL dédiée.
4. **Scellé du pas pour le reconverti** (`sage-glm-pile-correctif` § 2.3 précisé) : à code égal, contre `-sansawq` : ≤ 1,012 × (tables d'experts fusionnées ≤ 0,5 ms, rien sur l'attention) ; réfuté > 1,03 × → il reste un coût non expliqué dans la recette, diff des manifestes à nouveau.

## § 6 — k retenus (après 45cf531) : k_x = 4, k_act = 8, et deux compteurs dans la passe réelle

1. Océane : max amax(`act/s_d`) = 0,289 sur 16 jetons × 1 couche × 8 experts, 0 % de blocs restants sous le plancher dès k = 6. La règle « max × 2ᵏ ≤ 672 » donnait k_act = 11 : elle ne regarde que le haut et ne pèse pas la taille de l'échantillon. Une fenêtre réelle (2 048 jetons × 47 couches) a des queues que 16 jetons n'ont pas vues, et `satfinite` sature **en silence**.
2. Règle corrigée : k au milieu entre le plus petit k qui rend 0 % en bas et le plus grand qui tient en haut. **k_act = 8** (bas : 4× sous k = 6 ; haut : 10,5 = 36× le max observé), **k_x = 4** (bas : gate/up à 0 % sans échelle ; haut : 168 = 62× le max 2,70).
3. Preuve dans le commit : sous variable de débogage, `nvfp4_quant_act` compte blocs saturés et blocs à zéro ; la passe de re-PPL réelle du converti alpha-commun publie les deux compteurs, **attendu 0 et 0** ; un compteur > 0 → k de ce site bouge d'un cran dans le sens indiqué, avant toute autre mesure.

## § 7 — k fixe réfuté (7bfa47f) : les deux queues existent sur une fenêtre réelle ; échelle par ligne dans le noyau, plus de k du tout

1. **Lecture** : k_x = 4 seul → 1,019 avec 34 k blocs saturés (x/s dépasse 168 sur une fenêtre réelle : l'échantillon de 16 jetons a raté la queue de ≥ 60× sur les deux sites) ; k = 0 flushe, k = 4 sature, et la PPL s'améliore quand même de 0,012 → **les deux queues coûtent**, aucune constante ne les tient. (a) garde k_x = 4 : ses 34 k saturations restent, 1,019 > 1,010 — (a) échouerait au scellé. **(b)** :
2. **Échelle globale par ligne, calculée dans `nvfp4_quant_act` lui-même** : un CTA par ligne (K = 2 048 bf16 = 4 Ko, en mémoire partagée), amax de la ligne réduit sur place, `g_r = amax_r / (6 × 448)` en fp32 écrit dans un vecteur `[G]`, puis les 128 blocs quantifiés contre `g_r` (échelle E4M3 ∈ [0, 448] par construction : **saturation impossible**, flush seulement sous 4 × 10⁻⁶ × amax_r). Un seul lancement, aucun nœud de graphe en plus, même trafic mémoire. Le MMA multiplie `g_r[r] × gscales[e]` dans l'épilogue (une multiplication par ligne, Laurine). La division `s[e]` fusionnée reste. **Plus de k, plus de compensation dans `gscales` d'activation** : l'asymétrie down sans table disparaît par construction (le défaut de code distinct n'a plus de site).
3. **Scellés** (les mêmes, le repli change) : re-PPL alpha-commun **≤ 1,010** (attendu ≈ 1,009) ; compteurs **0 saturé**, flush ≤ 0,01 % des blocs (sur la fenêtre réelle, compteurs compilés hors du chemin mesuré) ; noyau = référence Python de la même arithmétique sur `x/s` et `act/s_d` ; **durée de la passe ≤ 1,2 × celle de k = 0 (6 s)** — les 25 s de k(4,8) s'expliquent avant (compteurs atomiques ? division fusionée ?) ; réfuté → la cause de la lenteur se nomme, elle n'entre pas dans le duel. Réfuté sur la PPL (> 1,015) → sonde par étape d'Océane (§ 3.4), rappelée.
4. **Manon** : reconversion `-k48` inchangée (k n'existe plus, la conversion ne l'a jamais porté) ; sa PPL attend le noyau. **Laure** : rien à mesurer avant le commit.

## § 8 — Noyau par ligne tenu (877169c : 0/0, 6,3 s), PPL réfutées (1,0277 / 1,0183) : il n'y a plus de bogue, il y a un coût ; le duel se joue en prefill W4A16, le coût W4A4 devient un chantier d'après-duel

1. **Ce qui est acquis** : le noyau est juste (compteurs 0/0, référence Python, 46/46 piles) et les 25 s étaient le cache froid. Ce qui reste est **le prix de l'E2M1 bloc 16 sur des activations que l'AWQ a rendues pointues** : alpha-commun 1,0277 en W4A4 contre 1,0089 en W4A16 (+1,9 %), indépendant nvfp4-seul 1,0183 en W4A4 (attendu ≈ 1,000 en W4A16). Le fait de Laure qui tranche : **k_x = 4 saturant faisait 1,019 < 1,0277** — écrêter la queue haute de `x/s` *aide* : un canal amplifié ×19 par `s = 0,053` domine son bloc de 16 et efface les quinze autres (étendue 12:1). Le discriminateur (1,063 sur une couche, 16 jetons, norme relative) ne suit pas la PPL : je l'écris comme un instrument qui a répondu à côté, pas comme une preuve d'innocence.
2. **Duel, maintenant, régime nommé** : l'objectif est le décodage b=12 ; à MIN_T = 5 le chemin MMA du décodage est rarement pris. **Prefill `ACVRAM_MOE_MMA=0` (W4A16), décodage MMA=1 MIN_T=5**, converti `-k48` (indépendant, nvfp4 seul). Une PPL dans ce régime exact, **scellée ≤ 1,005** (réfutée > 1,010 → le converti hétérogène `-avant-noawq-experts` 0,998 prend sa place au duel, même régime, MTP par index vérifié). Puis prise A (Laure) ; la colonne PPL du duel porte le régime et le temps de prefill est publié à part avec le sien.
3. **Après le duel, dans cet ordre, un chantier nommé « W4A4 GLM : −2 % »** : (i) **sonde par étape** (Océane, 10 min de carte) sur les trois sites, pour savoir **lequel des trois** paie — c'est ce qui décide entre A8 sur un seul site (`nvfp4_mm_w4a8`, `fp4_gemm.py:257`) et une rotation ; (ii) **cb2784b** (Manon, métrique W4A4 dans la recherche AWQ des experts, à sec 1 h + 20 min de PPL), scellé NOMINAL MMA=1 **≤ 1,010**, attendu 1,005-1,015 : la recherche renonce aux échelles qui pointent l'activation et peut choisir des échelles qui l'aplatissent (direction SmoothQuant), c'est pourquoi l'attendu passe sous le plancher sans-AWQ 1,020 ; (iii) réfuté → rotation de Hadamard par bloc avant `nvfp4_quant_act` (QuaRot), chantier > 1 j, note séparée.
4. **Ce qui se publie** : « sur GLM-4.7-Flash, le prefill W4A4 NVFP4 coûte +1,9 % de PPL avec échelles AWQ et +0,2 % sans ; l'écrêtage de la queue haute en récupère la moitié ; le noyau est exact (0 flush, 0 saturation) ». C'est un résultat, avec son régime.
