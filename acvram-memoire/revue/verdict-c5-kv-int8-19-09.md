# Verdict — C5 (cache KV int8, le défaut effectif, contre `ACVRAM_KV_FORMAT=bf16`) : **FAUX** — PPL de décodage Coder (préfixe 8 192 en prefill + 512 jetons notés un par pas) **+0,74 % (tranche 1), +0,62 % (tr 0), −0,05 % (tr 2), géo +0,44 % > +0,4 %** ; deux tranches sur trois au-dessus du seuil, la troisième nulle

instrument : `scratchpad/c5-kv-19-09/chaine.sh` + `scratchpad/ppl-decode-kv-17-09.py` (teacher forcing : préfixe 8 192 par le prefill, puis 512 jetons notés au décodage, graphes on, b=1 ; **modifié ce soir** : plus de régime posé par l'instrument — les `setdefault` MOE_DECODE_MMA=0 / MOE_MMA=0 / NARROW_GEMM=1 du 17/09 masquaient le défaut mesuré), arbre C5 `821a18a` (poste1-c5-kv-int8, worktree `poste2-c5`, `arbre exécuté` imprimé), instrument commit `b8e0bab` ; prises `carte.sh` 19:23:30-19:27:11 (tr 1), puis tr 0 et tr 2 (19:28-19:31) ; corpus privé `tranches-coder/tranche{0,1,2}.txt`, mêmes `premiers_ids` dans les deux bras ; preuve du régime : `kv_dtype_effectif` `int8` / `bf16` et `[régime] … ACVRAM_KV_FORMAT=int8(capacités)` / `=bf16` (C5 nomme le format dans la ligne) ; moteur `prefill_int8=bf16` (arbre antérieur à P2-au-défaut, identique dans les deux bras)
scellé (poste7, avant) : ratio PPL(int8)/PPL(bf16) − 1 ≤ +0,004 → int8 reste le défaut ; > 0,004 → bf16 au défaut ; prédiction poste2 +0,001 à +0,003
mesuré : tranche 1 (scellée) **int8 12,6926 / bf16 12,5999 → +0,0074** ; tranche 0 **13,4974 / 13,4146 → +0,0062** ; tranche 2 **6,6425 / 6,6459 → −0,0005** ; géo des trois **+0,0044** ; 512 jetons notés par tranche, 512 pas, 8 192 de préfixe ; premier bras 3 min (autotune Triton du nouvel arbre), les suivants 42 s
verdict : **FAUX** — sur la tranche scellée +0,74 %, presque deux fois le seuil ; les deux autres encadrent (+0,62 %, −0,05 %) et la géo (+0,44 %) reste au-dessus de 0,4 % — de peu, ce que je dis : à 512 jetons notés la résolution de l'instrument est ~± 0,5 % (écart-type de la différence de NLL par jeton / √512), le seuil de 0,4 % est au niveau du bruit d'une tranche, pas de trois ; **ce qui est établi : le KV int8 coûte de l'ordre de +0,5 % de PPL de décodage à 8 k de contexte, pas 0**, et la prédiction +0,1-0,3 % était optimiste. Sur la lettre du scellé, bf16 devrait passer au défaut ; sur le fond, le coût de bf16 est la moitié de contexte en moins pour +0,5 % de PPL — un choix de poste7, pas un chiffre
suite : poste7 tranche (bf16 au défaut selon le scellé, ou int8 gardé avec la ligne « +0,5 % PPL décodage à 8 k » publiée, ou seuil réécrit à 3 tranches × 2 048 notés = 30 min) ; poste1 : « arrondi int8 borné » de 821a18a est-il dans le bras int8 mesuré (oui : arbre C5) — si un arrondi différent existe, le mesurer ici même ; chef : indexer ; ma file : experts distincts b=12 + C16-bis → C4 → porte A8+W8r → statics → NARROW_GEMM → P2+éco → C13-b (avec addendum 8ae21997)

## Rejouable
`TR=<0|1|2> ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/c5-kv-19-09/chaine.sh` ; `ppl-{int8-defaut,bf16}-tr*.json` (nll par jeton dedans).
