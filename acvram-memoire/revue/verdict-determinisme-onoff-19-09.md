# Verdict — déterminisme graphes ON/OFF, Coder b=12 (hygiène, Sage sage-reprise-20-09) : **la capture est innocente ; l'op est le lot arrondi au godet** — `GRAPHS_EAGER` (formes fixes sans capture) = ON **au bit** (0,00 ulp, 0/756 argmax), `GODETS_B=0` (lot exact sous graphes) = OFF **au bit** ; toute la différence ON/OFF (|Δ| 7,04 logits, argmax ≠ 29/756, dès le pas 0) est le passage de M = 12 à M = 16 lignes dans les noyaux du pas (GEMV/GEMM Marlin des experts, attention paginée, routeur cuBLAS) : un autre découpage de tuiles, un autre ordre de somme, des experts qui basculent ; **ma prédiction (écrite avant) tenue sur les deux lignes**

instrument : `scratchpad/determinisme-19-09/chaine.sh` (`equiv-b12.py` teacher forcing 256 + 64 pas, 12 séquences, bras A `GLUE_COMPACT=0`) : `on` (défaut), `eager` (`ACVRAM_GRAPHS_EAGER=1`), `godet0` (`ACVRAM_GODETS_B=0`), `off` (`GRAPHES=0`), comparaison des logits bf16 par (pas, séquence) en |Δ| et en ulp à l'échelle du max de la ligne ; arbre manon 77aabdae (⊇ main 3e062174), 4 prises `carte.sh` 08:54:34-08:55:59 (≈ 20 s chacune) ; journaux `scratchpad/determinisme-19-09/sortie/`
scellé (Sage, hygiène : « bissection par famille, nommer l'op » ; ma prédiction avant : (2) = (1) au bit et (3) ≈ OFF ≤ 1 ulp → l'op = le godet ; issue gênante : (2) ≠ (1) → la capture change quelque chose)
mesuré : `eager vs on` **|Δ| 0,000, 0/756** ; `godet0 vs off` **|Δ| 0,000, 0/756** ; `eager vs off` 7,041, 29/756 ; `godet0 vs on` 7,041, 29/756 ; `on vs off` 7,041, 29/756 ; PPL 12 fenêtres : on = eager 21,4494 ; godet0 = off 21,4505
verdict : **TENU — nommé** : la capture et le rejeu des graphes CUDA reproduisent l'exécution eager au bit sur Coder b=12 (REGLES § 6 n'a rien à reprocher aux graphes) ; ce qui sépare « ON » de « OFF » est **la clé de godet** (`GODETS_B=1`, lot arrondi à 16) : les mêmes noyaux, lancés sur 16 lignes dont 4 vides, ne rendent pas les mêmes bf16 sur les 12 vraies — ce n'est pas un bogue, c'est la même classe que β (ordre de somme) amplifiée par le routage top-k du MoE ; conséquence pour les juges : **le témoin de tout bras sous graphes est `GRAPHS_EAGER=1`, jamais `GRAPHES=0`**, et deux chemins ne sont comparables au bit qu'à godet égal ; le test « ON vs OFF 0,00 ulp » des fiches est à remplacer par « ON vs EAGER 0,00 ulp » (déjà fait pour le niveau 2 : =2 ON = =2 EAGER)
durée : 4 prises 1 min 25 s (08:54:34-08:55:59) ; rédaction 4 min
suite : Sage : REGLES § 6 : témoin = `GRAPHS_EAGER`, godet égal ; le « 29/756 » du témoin A (C15-3d) est le godet, pas les graphes ; Océane : rien ; Jérôme : indexer ; ma file : S2 `inactive_split_bytes` après piles (15 min) → pièce 2a (dede4074)

## Rejouable
`ACVRAM_ARBRE=<arbre> PYA=<python> bash scratchpad/determinisme-19-09/chaine.sh` (2 min).
