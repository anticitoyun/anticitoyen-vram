# Sage — qualité de (5) MoE MMA au décodage : le juge est la PPL, mon contrôle des ties est retiré (15/09)

1. **Je retire le contrôle des ties de `sage-laure-15-09`** : il comparait deux quantifications (A = GEMV W4A16, B = MMA W4A4, `model.py:1029-1033`), donc une divergence franche (0,75 ; s6 pas 17, écart 2,75) est le bruit A4 attendu, pas un défaut — ce contrôle rendait « réfuté » sur du code juste et n'aurait rien rendu sur un noyau faux. Le contrôle qui peut rendre « faux » existe déjà et est plus dur : **égalité au bit** décodage-MMA = prefill-MMA (`tests/test_moe_mma_decodage.py:61-62`, `tests/test_moe_decode_mma_graphe.py:56`), quantification par ligne indépendante de M (`test_moe_mma_decodage.py:51`).
2. **Le +2,58 % n'est pas ce chemin** : c'est Llama-2-7B, toutes projections, fake-quant (`verdict-a4-fakequant-llama2-7b`). Les experts Coder-30B en W4A4 réel valent **+0,919 %** (9,2056 vs 9,1218, `verdict-moe-mma-reel-qwen3-coder.md:22-27`), servis par défaut au prefill depuis le 13/09. Par construction (noyau au bit, quantification par ligne), le décodage B a la même PPL.
3. **Juge et seuil, scellés** : Manon, PPL par le chemin de décodage (teacher forcing, b=12, sous graphes), Coder-30B, `ACVRAM_MOE_DECODE_MMA=1`. Prédiction : **9,206 ± 0,02**. Ouverture par défaut si **≤ 9,213** (étalon a16 × 1,01, même règle que le 13/09). **Réfuté** si > 9,213 : ce n'est pas l'A4 (le prefill est à 9,2056), c'est un défaut propre au décodage (fantômes, godet, scatter) — retour à Laurine avec le pas et la couche, (5) reste coupé.
4. **Si réfuté, pas de W4A16 sur MMA** : `kind::mxf4nvf4` ne prend que du FP4 ; la variante est **W4A8 par `kind::mxf8f6f4`** (A e4m3, B e2m1), qui exige des échelles ue8m0 bloc 32 sur les poids — nouveau format, reconversion du parc, > 3 j : pas avant le duel GLM. En attendant (5) resterait un mode « eco » optionnel à qualité annoncée (+0,92 % PPL, −19 % J).
5. **Si conforme** : (5) par défaut le jour même ; le modèle servi devient W4A4 experts aux deux phases au lieu de l'hybride actuel (A4 prefill / A16 décodage) — la qualité annoncée est celle du 13/09, déjà acceptée. Les 5/12 séquences divergentes avant 64 jetons ne sont pas un critère ; l'accord greedy 32-128 jetons (duck-manon Q5) peut devenir une colonne d'étalon, pas un seuil.
6. Témoin bt=8 : sans objet (bt ∈ {16,32,64,128} imposé). Le témoin de faute qui vaut : le test au bit doit casser si l'on retire le saut `tile_n = 0` (déjà exigé pour (ii)).

## § 2 — Verdict (15/09, après le banc de Manon a10b987 et le certifié de Laure c6377d5)

1. **Ouvrir (5) par défaut** (`ACVRAM_MOE_DECODE_MMA=1`) : chemin de décodage prouvé (t ≤ 32 sur 8191/8191 pas), B/A = 0,9995 ≤ 1,010 tenu ; l'absolu 9,206 est retiré (montage KV continu, biais identique sur A et B, prédit dans l'en-tête avant mesure — c'est recevable).
2. **Ce que ce banc prouve, et pas plus** : sur 8 191 jetons la résolution est ~±1 %, il ne peut pas revoir le +0,919 % ; il exclut un défaut propre au décodage (qui aurait rendu plusieurs %). La qualité annoncée reste celle du prefill : **+0,919 % PPL** (343 896 jetons), le modèle servi devient W4A4 experts aux deux phases.
3. **Chiffre officiel b=12** = bras B de Laure c6377d5 (rondes ctx 2048, ≥ 20 s) : −13,2 % J, −6,4 % ms ; le −19 % / −13,6 % de Laurine (fenêtres 22 s) est un autre régime et ne se cite plus comme officiel. Toute mesure de décodage antérieure porte désormais « MMA=0 » dans son régime.
4. Dans le même commit que le défaut : le banc `outils/ppl-decode-mma-coder30b.py` et le test au bit restent ; `MMA=0` reste le témoin, pas un mode servi.
