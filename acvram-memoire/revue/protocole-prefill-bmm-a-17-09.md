# Protocole — commit A (prefill des experts par `bmm` au lieu de `torch._grouped_mm` déroulé) : vitesse à qualité constante, Coder-30B régime classé

instrument : `scratchpad/prefill-glm-acvram-15-09.py w4a16 2048` (7 rép., médiane, compteurs de chemin, processus neuf) ; `scratchpad/ppl-acvram-17-09.py` privé 3 tranches (fenêtres 2048 en prefill : c'est le chemin changé qui est jugé), géo × bf16 ; chaîne `scratchpad/prefill-bmm-17-09/chaine.sh`.
commit : arbre poste3 f28e61d (= main avec le commit A 99ec80f de poste4, 16/16 CPU verts) ; `Qwen3-Coder-30B-A3B-nvfp4` ; référence avant A : prefill 8 681 j/s (`verdict-profil-coder-pas-17-09`, même instrument), 8 633 (cellule), PPL privé géo 1,0144.
régime : `ACVRAM_PREFILL_GROUPED=bmm` (défaut, régime A) contre `grouped_mm` (témoin = ancien chemin, boucle de 128 `aten::mm` + `Memcpy DtoH` par pile) ; `ACVRAM_MOE_MMA=0 ACVRAM_MOE_DECODE_MMA=0 ACVRAM_NARROW_GEMM=1` ; `regime_ligne()` doit imprimer `ACVRAM_PREFILL_GROUPED=grouped_mm` sur le témoin et rien sur le défaut (`regime.py:60`).
scellé (poste7) : prefill Coder 2048 ≥ 11 500 j/s ; PPL = tout-torch ± 0,002 ; < 11 000 → réfuté (la boucle n'était pas le coût), B suit quand même.
mes prédictions : bmm 12 000-14 000 j/s (la boucle coûtait 136 ms de GEMM en tuiles 16×16 + 432 synchronisations DtoH sur 236 ms ; un `bmm` par pile ramène les experts à ≈ 90-110 ms : 236 → 170-190 ms) ; témoin grouped_mm = 8 400-8 900 (reproduit 8 681) ; PPL privé bmm = grouped_mm à ± 0,0005 (même arithmétique bf16, seul l'ordre des lancements change), toutes tranches.
falsification : bmm < 11 000 → la boucle n'était pas le coût, le poste est la déquant (50,8 ms) ou les tuiles cuBLAS elles-mêmes ; écart PPL > 0,002 sur une tranche → A change la qualité, non recevable ; témoin ≠ 8 681 ± 5 % → le témoin n'est pas l'ancien chemin.
