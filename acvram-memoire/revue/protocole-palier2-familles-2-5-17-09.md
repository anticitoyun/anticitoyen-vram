# Protocole — palier 2, familles 2-5 (ordre poste7 17/09 06h20) : Nemotron-3.5-Lightning-30B-A3B en palier plein ; Gemma-4-26B-A4B, Kimi-Linear-35B, Llama-3.3-70B en palier réduit (pas de bf16 sur disque : téléchargement = décision de l'utilisateur)

portée : plein = PPL privée × bf16 (classé ⇔ géo ≤ 1,02, 0 fenêtre explosée) + rondes b=1 ×1 / b=12 ×2 + J + prefill 2048, par moteur présent ; réduit = rondes + prefill (régime nommé) + PPL ABSOLUE par tranche étiquetée « non classable : pas de référence bf16 », AUCUN ratio à une référence quantifiée, colonne classé vide.
objets : (2) Nemotron-3.5-Lightning-30B-A3B (`nemotron_h`, hybride Mamba MoE) : bf16 `models/Nemotron-3.5-Lightning-30B-A3B-bf16` (62 Go, `ppl-hf-17-09.py` device_map=auto), acvram `models_acvram/Nemotron-3.5-Lightning-30B-A3B-srcexl3_6bpw-nvfp4` (source EXL3 6 bpw : double quantification, dit dans la table), vLLM `models_vllm/Nemotron-3.5-Lightning-30B-A3B-NVFP4` (officiel, KV FP8 → TRITON_ATTN) ; GGUF et EXL3 du parc = heretic : hors palier 2. (3) Gemma-4-26B-A4B : acvram `gemma-4-26B-A4B-heretic-APEX-I-Quality-nvfp4` + GGUF `gemma-4-26B-A4B-heretic-APEX-I-Quality` (même dérivé heretic ; les deux en réduit). (4) Kimi-Linear-35B : acvram `Kimi-Linear-35B-kda-nvfp4` seul (le GGUF du parc est un REAP abliteré : autre modèle). (5) Llama-3.3-70B : acvram `Llama-3.3-70B-Instruct-heretic-srci1_Q4_K_M-nvfp4` (40 Go, exil, main ≥ 1aa767b) + GGUF `Llama-3.3-70B-Instruct-heretic.i1-Q4_K_M` (42 Go, `llama.cpp ngl=53/80`, `BANC_NGL=53`).
instruments : ceux du palier 2 Qwen3.8 (`chaine.sh` de `scratchpad/palier2-qwen38-17-09`, généralisée en `scratchpad/palier2-17-09/chaine-famille.sh`) ; corpus `corpus-prive/tranches-glm/tranche{0,1,2}` ; régime acvram classé (`ACVRAM_MOE_MMA=0 ACVRAM_MOE_DECODE_MMA=0 ACVRAM_NARROW_GEMM=1`), `regime_ligne()` dans chaque JSON ; `PREFILL_PILES_INCONNUES=1` seulement si `piles_ok=None`.
scellé — Nemotron : acvram PPL 1,02-1,04 (non classé : NVFP4 depuis EXL3 + voie torch des couches Mamba), b=1 rondes ≤ 80 t/s, prefill 2048 ≤ 800 j/s ; vLLM PPL ≤ 1,02 (classé), b=1 ≥ 300, b=12 ≥ 1 200, prefill ≥ 10 000 ; falsification : vLLM > 1,02 dit que le checkpoint officiel porte la perte, pas le moteur. Gemma : acvram b=1 rondes ≤ 150, GGUF ≥ 200 ; PPL absolues publiées sans rapport. Kimi : acvram b=1 rondes 200-250, prefill ≤ 1 200 j/s. Llama-70B : acvram charge en exil (1aa767b) et rend un préfill (sinon le correctif est réfuté), b=1 ≤ 3 t/s (PCIe : 32/80 couches, ~570 ms/jeton annoncés), prefill ≤ 300 j/s ; llama.cpp ngl 53/80 b=1 4,3 ± 0,3 t/s ; les deux PPL absolues « non classable ».
budget : Nemotron ≈ 1 h 30 (bf16 30 min), Gemma 40 min, Kimi 25 min, Llama-70B 1 h ; un verdict par famille.

addendum 08h30 (chef/poste4, ee69a7b fusionné : pool dense partagé `ACVRAM_DENSE_SLOTS=4`, `_reserve_prefill` ajusté ; cause : `layers.py:188/233-244` allouait 2 copies GPU privées par poids exilé jamais rendues, hors `utilise()` de `loader.py:1213`) — scellé pour Llama-3.3-70B-nvfp4, ctx 2048, budget par défaut : exil 34-40/80, tampons dense ≤ 2 Gio, préfill passe, décodage b=1 ≈ 1,5 j/s (≤ 5) ; **faux si OOM encore ou exil > 52** → ligne « refus » avec ces fichier:ligne. `ppl-decode-kv-17-09.py` (25 s, Coder) à la fusion avant la mesure.

addendum 10h15 (chef/poste4 70082d4) — 70B décodage en exil : essai unique `ACVRAM_TRACE_COUCHES=1` (certifie b=1, timeout 600 s) + `nvidia-smi dmon -s tu -d 5` en parallèle ; scellé : lignes de couches qui avancent + rxpci ≫ 0 ⇒ (A) pas lent, coût PCIe mesuré, publier débit + cause ; dernière ligne figée sur la couche i + rxpci = 0 ⇒ (B) blocage CUDA à cette couche. Observation déjà faite au blocage précédent : GPU 0 %, 17-22 W pendant 30 min (contre (A)).

addendum 10h40 (poste4 410dd07, garde KV) — cause du « blocage » 70B : `_reajuster_plan` (VRAM lue avant le JIT) s'arrête à 37/80 ; `_borner_kv_par_la_vram` (VRAM lue après) prend sur le KV jusqu'à 0 ; `_kv_blocks_per_device` rend max(1, 0) ; `runner._admit` : need 17 > num_free → attente infinie. Correctif : `_kv_plancher`, `_borner_kv_avec_exil` (4 tours puis RuntimeError « refus : budget KV insuffisant »), `_admit` refuse ce qui ne tiendra jamais. Scellé pour UN essai (après fusion par chef) : exil 40-44/80, KV ≥ 0,33 Gio, première ligne CERT < 5 min, b=1 ≈ 1-1,5 j/s ; faux si RuntimeError refus ou moteur à vide (alors `ACVRAM_TRACE_STEPS=1`).
