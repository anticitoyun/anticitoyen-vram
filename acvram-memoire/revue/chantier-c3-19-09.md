# Chantier C3 — MTP GLM-4.7-Flash, à sec (19/09, branche `oceane-c3-mtp`)

Source : `sage-poursuite-chantiers-19-09` § 2 ligne C3. Aucune mesure carte ce soir.

## Objectif
Brancher la tête MTP du checkpoint GLM-4.7-Flash (`…-srcbf16-nvfp4-k48-qkvo-i8c`) comme brouillon spéculatif : **1 jeton proposé par pas** (`--spec-k 1`), vérification exacte par le chemin existant (`GardeSpeculation` → `_speculative_decode` → `verify_proposal`).

## Prédiction scellée et seuil (recopiés de la commande, AVANT toute mesure)
* acceptation ≥ 60 % ; b=1 t/s **≥ 1,30 ×** le défaut tenu (sinon **faux**) ; greedy identique jeton pour jeton au décodage sans MTP (invariant REPRISE § 6).
* « Défaut » = la même ligne de commande sans MTP, c'est-à-dire `--speculative ngram --spec-k 4` (`cli.py:1014-1029`), bras A de la même fenêtre — pas un chiffre d'une autre note.
* Arithmétique (à k=1, jetons/pas = 1 + a) : gain = (1 + a) · pas / (pas + tête). À a = 0,60 et pas = 2,67 ms (374,9 t/s libre, E1-bis), tenir 1,30 × exige **tête ≤ 0,61 ms** : 366,8 Mio de poids de tête (0,25 ms à 1,5 To/s) + une seconde passe `lm_head` int8 309,6 Mio (0,2 ms) + les lancements eager d'une couche MLA + MoE hors graphe (0,15-0,3 ms). **Issue qui me gêne, nommée d'avance : acceptation tenue (≥ 60 %) ET débit faux (1,15-1,30 ×) parce que la tête tourne en eager** — ce serait un verdict sur l'implémentation, pas sur le MTP ; on ne resserre pas le seuil après.
* Valeurs d'alarme : `voie` ≠ graphe sur le bras MTP (`runner.py:1379-1381` exige les graphes à q_len = 2) → c'est l'instrument qui a rendu, pas la tête ; acceptation < 30 % → contexte MLA de la tête non amorcé (l'analogue du 14 % de `_amorcer`), à traiter comme un défaut de chargement.

## Ce qui existe déjà (fichier:ligne, HEAD 86e7097)
* `speculative.py:439-583` `MTPProposer` (propose/commit/release, `_amorcer` = prefill de la tête sur l'invite, `_hidden_cible`) ; `:357-418` `verify_proposal` ; `:49-93` `GardeSpeculation` (lot_max 2, `runner.py:428`). `runner.py:1365-1436` `_speculative_decode` (hybride = sous graphes, b=1, forme fixe k+1, `rollback_hybrid`) ; `:1438-1476` lot = dernier vrai jeton puis propositions.
* `model.py:2715-2718, 2762-2770, 2900-2908, 2916-2954` : `self.mtp`, `_mtp_prefill` (états AVANT la norme finale, tout le prefill), `_garder_hidden` dans un tampon réservé (`MTP_HIDDEN_LIGNES` 16) ; `graphs.py:716-718` réserve avant capture. `mtp.py:34-63` `MTPHead` (enorm/hnorm/eh_proj/bloc/norme finale) ; `:66-75` `cles_mtp` ne lit que `model.mtp.<n>`.
* `loader.py:947-956, 959-1015` `_charger_mtp` : attention DENSE (`q/k/v/o_proj`) + `MLP` dense + `PagedKVCache` ; `ACVRAM_MTP=non` (HORS_REGIME `regime.py:160`, garde `cli.py:186`). `cli.py:669-692` : `auto` → mtp si `model.mtp`, sinon ngram ; `mtp` refuse sans tête. `convert.py:389-403` route le bloc `model.layers.47.*` au format de la dernière couche. `outils/gpu/mesure/etape-a-taux-acceptation.py` : taux d'acceptation none/ngram/mtp, 3 familles, sans temps.

## Ce que le checkpoint porte (manifeste, lecture JSON + en-têtes safetensors)
`num_hidden_layers` 47, `num_nextn_predict_layers` 1 → tête sous `model.layers.47.*`, **213 tenseurs** : `enorm/hnorm` [2048], `eh_proj` [2048, 4096] int8, `input/post_attention_layernorm`, MLA (`q_a_proj` [768, 2048], `q_a_layernorm` [768], `q_b_proj` [5120, 768], `kv_a_proj_with_mqa` [576, 2048], `kv_a_layernorm` [512], `k_b_proj` [20, 512, 192] bf16, `v_b_proj` [20, 256, 512] bf16, `o_proj` [2048, 5120]), MoE (`gate` [64, 2048] bf16 + `e_score_correction_bias` fp32, 64 experts nvfp4 [1536, 2048]/[2048, 1536], `shared_expert` int8), `shared_head.head` [154880, 2048] int8, `shared_head.norm` [2048], `embed_tokens` [154880, 2048] bf16. Poids : 366,8 Mio hors copies (couche 46 : 358,6 Mio ; modèle 16,10 Gio → 1/48), + 605 Mio embed + 309,6 Mio head si on les rechargeait.
* `shared_head.head` ≡ `lm_head` (qweight ET scales) et `layers.47.embed_tokens` ≡ `model.embed_tokens` sur 3 tranches de 256 lignes (0, milieu, fin) → **réutiliser** `model.lm_head`/`embed_tokens` ; identité sur les 154 880 lignes : **non vérifiée**. `shared_head.norm` ≠ `model.norm` (3 tranches) → norme finale propre, obligatoire.

## Preuve à sec faite ce soir (`tests/test_mtp_glm_c3.py`, 8 tests, 1,5 s)
Rejouer : `CUDA_VISIBLE_DEVICES= ACVRAM_TESTS_PENDANT_MESURE=1 timeout 600 <python> -m pytest tests/test_mtp_glm_c3.py -q -p no:cacheprovider` → `8 passed`.
* (a) `prefixes_mtp` (`mtp.py`) reconnaît `model.mtp.<n>.` ET `model.layers.<num_layers>.` ; `attendus_mtp_glm(config.json)` → 213 formes ; `verifier_mtp_glm` lève `KeyError` (clé absente), `ValueError` (forme ≠ config, tenseur plein quantifié, tenseur inconnu sous le préfixe) — chaque cas cassé exprès sur un manifeste synthétique. Sur le converti réel : 213/213, 64 experts nvfp4, toutes les clés safetensors promises présentes dans leur fragment avec la forme annoncée (en-têtes), puis **un seul tenseur lu** (`enorm`, [2048] bf16, fini).
* (b) modèle jouet CPU (fixture `converted`), `MTPProposer` réel + tête factice bf16 (`TeteOracle` : connaît la suite gloutonne sans MTP, se trompe exprès 1 appel sur 3), `spec_k=1` : 24 jetons **identiques** à la suite sans MTP, 13 proposés / 9 acceptés / **4 rejetés** (sans rejet l'égalité ne prouverait rien) ; témoin : `verify_proposal` remplacé par « tout accepter » → la suite **diffère**.
* Trouvé par la sonde, corrigé dans le même commit : `MTPProposer.commit` posait `_ligne = len(jetons émis)` (`speculative.py:573`) alors que le moteur passe accepté + 1 (`runner.py:1434`, même contrat que `NGramProposer.commit:208`) ; après CHAQUE rejet la tête lisait l'état de la proposition rejetée (écart relatif 2,1-3,0e-2 contre 2,1-2,7e-3 pour l'état juste), masqué par la borne `h.shape[0]-1` quand tout passait. À k=1 c'est une proposition sur deux après rejet qui partait d'un contexte imaginaire. Test : 0 ligne fausse corrigé, exactement une par rejet avec l'ancienne ligne réintroduite.
* `_charger_mtp` (`loader.py:970-984`) : la tête GLM est maintenant nommée au chargement (« non chargée, bloc MLA + MoE ») au lieu d'être ignorée en silence. Aucune variable `ACVRAM_*` ajoutée.

## État
* FAIT : détection, contrat de formes, sonde et correctif `_ligne`, exactitude k=1 prouvée sur le jouet (chemin non hybride, eager).
* RESTE (code GLM, aucune ligne écrite) : (1) `loader.py:959-1015` construire la tête avec `MLAttention` + `faire_mlp()` MoE comme `loader.py:770-812`, `shared_head.norm` en norme finale, `lm_head`/`embed` partagés, budget VRAM +366,8 Mio dans `_borner_kv_par_la_vram` (`:1088`) ; (2) `mtp.py:62` `self.layer(x, batch, self.cache)` → `DecoderLayerGDN.forward` (`model.py:2276-2317`) garde l'état MLA dans `batch.gdn_store[index][seq_id]` : le proposeur doit porter son `gdn_store`/`seq_ids`, et rembobiner le latent (`mla.py:234`, concaténation) à `len(ids)-1` lignes au rejet — `st.length` ne suffit plus ; (3) `speculative.py:463` `tete.cache.cfg.num_blocks` n'existe pas pour MLA ; (4) le chemin hybride (`runner.py:1374-1381`, GLM a `layer_types` × 47 `full_attention`) : spéculation seulement sous graphes, b=1, `max_ql = 2` — à capturer avant la fenêtre (`warm_graphs`) ; (5) `regime_ligne()` doit porter « spéculation = mtp, k = 1 » (`ACVRAM_MTP` est HORS_REGIME) ; (6) test d'équivalence sur GLM réel = PPL en forçage enseignant (`ppl-decode-kv`) + greedy 512 jetons bit à bit A/B.
* NON VÉRIFIÉ : identité `shared_head.head`/`lm_head` hors des 768 lignes lues ; tout chiffre de temps ; le taux d'acceptation de la tête GLM (jamais exécutée ici) ; le coût eager de la tête.

## Première fenêtre carte (après (1)-(4), ≤ 30 min, verrou `outils/carte.sh`)
b=1, ABAB 4 × 5 min, même invite code (`etape-a-taux-acceptation.py` famille `code`, 512 jetons, temperature 0), A = `--speculative ngram --spec-k 4`, B = `--speculative mtp --spec-k 1`, `-lgc` identique aux deux bras, `regime_ligne()` en tête de chaque bras, `voie` = graphe attesté sur 100 % des pas B, `nvidia-smi --query-compute-apps` avant/après. Verdict six lignes : t/s A, t/s B, ratio (tenu ≥ 1,30, sinon faux), acceptation B (tenu ≥ 60 %), greedy A = B jeton pour jeton sur 512 (sinon **bogue**, pas de verdict de vitesse), PPL décodage B = A ± 0,002.
