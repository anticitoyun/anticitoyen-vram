# Verdict — budget nsys du pas de décodage GLM-4.7-Flash (défaut, graphes on) : b=1 **7,73 ms de noyaux** (pas mesuré 8,47) = MLA **4,2 ms (54 %)** + petits noyaux élémentaires **2,0 ms (26 %, ~1 700 lancements/pas)** + experts **1,1 ms (15 %)** ; b=12 **13,2 ms** = experts 5,0 (37 %) + MLA 6,2 (47 %) + élémentaires 1,3 (10 %) — la prédiction « MoE v1 4-5 ms » est réfutée : le poste est l'attention MLA et la glue, pas les experts

instrument : `scratchpad/nsys-budget-19-09/decode-nsys.py` (Engine direct, graphes ON, `--max-batch` = b, 256 jetons d'invite, 20 pas d'échauffement hors capture, 50 pas entre `torch.cuda.profiler.start/stop`) sous `nsys profile -t cuda --cuda-graph-trace=node --capture-range=cudaProfilerApi`, puis `nsys stats --report cuda_gpu_kern_sum` et `classer.py` (regex par nom de noyau ; la table ci-dessous est reclassée à la main depuis les noms, CSV `glm-decode-b{1,12}_cuda_gpu_kern_sum.csv`) ; chaîne `chaine-decode-glm.sh`, arbre `9b2e67d` (main `7aebbfa` + oceane-11 `03769a7`), prise `carte.sh` 18:26:59-18:27:32, aucune variable `ACVRAM_*`, moteur `graphes=on(hybrides≤4|12)`, 0 repli eager ; sans root : durées de noyaux seulement. **Le temps mural sous nsys est faux** (51,8 / 58,3 ms/pas : le traçage par nœud de graphe coûte ×6) ; ne compte que la somme des noyaux : 7,73 / 13,21 ms/pas, soit 91 % / 89 % du pas mesuré sans nsys (8,47 / 14,81, G1-bis) — le reste est l'espace entre noyaux
scellé : aucun (budget publié, non scellé) ; prédiction Sage b=1 : MoE v1 4-5 ms, MLA 1,5-2,5, reste 1-1,5 ; prédiction Manon b=1 : MoE 3,5-4,5, MLA 2-3, glue 1-2
mesuré (ms/pas, × lancements/pas) — **b=1, 7,73 ms** : `mla_1p_kernel<20,32,4>` **2,09** ×47 · `int8_gemv_kernel<4,1,bf16,bf16>` **1,30** ×233 (projections MLA int8 q_a/q_b/kv_a/o) · `int8_gemv<…,float>` 0,20 ×1 + `gemvx` cuBLAS 0,15 ×47 · `mla_1p_combine` 0,15 ×47 · `_route_fusee` 0,17 ×46 · **experts** : `nvfp4_gemv_marlin<bf16,2>` (gate·up) 0,48 ×33 + `nvfp4_gemv_marlin<float,1>` (down) 0,33 ×33 + `nvfp4_gemv_grouped_xreg<float,4,2>` (v1) 0,33 ×39 · **élémentaires** : `direct_copy` 0,60 ×212 + 0,11 ×164 · `rmsnorm_bf16` 0,36 ×189 · binaire bf16 0,22 ×188 · `add` 0,14 ×234 · `gather` 0,15 ×164 · tête `gemv2T` cuBLAS 0,18 ×47 ; **b=12, 13,21 ms** : marlin gate·up **2,26** ×33 · `mla_1p_kernel` **2,11** ×47 (inchangé de b=1 : borné par la latence à b=1) · `_etroit_kernel` (Triton, projections int8 à M=12) **1,59** ×234 · `nvfp4_gemm_grouped_mma2<16,4,128>` **1,47** ×39 · marlin down 1,22 ×33 · `gemmSN_TN` cuBLAS fp32 (scores/o_lat) 1,00 ×93 + `reduce` fp32 0,49 ×233 · `mla_prep_batch` 0,97 ×47 · rmsnorm 0,29 ×142 · `nvfp4_quant_act` 0,17 ×39 · route/combine 0,32 · copies/fill/elementwise ~0,9
verdict : **b=1 : MLA 4,2 ms (cœur 2,09 + projections int8 1,65 + combine/route/prep 0,45) = 54 %, élémentaires 2,0 ms = 26 % (~1 700 lancements par pas, 36 par couche : copies, rmsnorm, add, gather), experts 1,14 ms = 15 %, tête 0,3** — la prédiction « MoE v1 4-5 ms » est réfutée par un facteur 4 : sous P1 les experts GLM passent par le GEMV Marlin (gate·up ×33, down ×33) plus le v1 `xreg` ×39, et coûtent 1,1 ms ; ce qui fait 8,4 ms au lieu de ~3 pour Coder, c'est le cœur MLA (`mla_1p_kernel` 2,1 ms à b=1 — 45 µs par couche, latence de grille : il coûte le même 2,1 ms à b=12) et les 1 700 petits noyaux. **b=12 : experts 5,0 ms (37 %) + MLA 6,2 ms (47 % : cœur 2,1, `_etroit` int8 1,6, sgemm/reduce fp32 1,5, prep 1,0) + élémentaires 1,3**. Question ouverte pour Océane : 33 couches par le GEMV Marlin et 39 par `xreg`/`mma2` pour 46 couches MoE + 1 dense — deux chemins d'experts coexistent dans le même pas (compteur de lancements ×/50), à expliquer avant tout chantier
suite : Océane (moteur) : (1) `mla_1p_kernel` à b=1 = 2,1 ms pour 47 couches et ne bouge pas jusqu'à b=12 → grille sous-occupée à b=1 (split-K/split-heads : ×2-4 possible sur 27 % du pas), (2) la glue élémentaire (2,0 ms, 26 % à b=1) est le second poste : fusion rmsnorm+add+copie par couche, (3) `_etroit` int8 à M=12 = 1,6 ms ×234 : les projections MLA int8 (4 par couche + 1) sont 12 % du pas b=12 ; Sage : le budget dit où ne PAS aller (experts GLM à b=1) ; Jérôme : verdict à indexer ; ma file : MTP-exact → nsys prefill Coder → nsys prefill GLM (+ contrôle tf32) → C13-a → C2/C5/C4 → porte A8+W8r → statics → NARROW_GEMM → P2+éco

## Rejouable
`ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/nsys-budget-19-09/chaine-decode-glm.sh` (33 s) ; rapports `glm-decode-b{1,12}.nsys-rep`, CSV `*_cuda_gpu_kern_sum.csv`, journal `journal-decode-glm.log`. Lire les ms/pas = « Total Time (ns) » / 50 / 1e6 ; lancements/pas = Instances / 50.

## Note 18 h 40 — réponse d'Océane à la question ouverte
Les deux chemins d'experts coexistent PAR COUCHE, ce n'est pas un bogue : `up_distinct` (tables AWQ gate ≠ up, `torch.equal(g, u)`, model.py ~963) se décide dans chaque `MoEBlock` — 33 couches non distinctes prennent la disposition unique Marlin (gate·up fusionné ×33 + down ×33), 13 couches distinctes sont refusées par `_construire_marlin` (d3bf9d2) et gardent la pile naturelle → `_grouped` = 3 GEMV génériques (xreg ×39 = 13 × 3 à b=1 ; mma2 ×39 à b=12). 33 + 13 = 46 couches MoE. C10 (`ACVRAM_MARLIN_DISTINCT`) unifie ; le budget tient : experts 1,14 ms sur 8,4 à b=1, le poste est `mla_1p_kernel` puis la glue.
