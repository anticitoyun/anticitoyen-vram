# Sage — b12x : ni monter la pile, ni fusionner à l'aveugle ; extraire le noyau (15/09)

Acquis (P1, aa91705) : mes seuils réfutés par le bas (Coder 62,7 µs/couche froid, GLM 150 ; W au plafond mais 2 750-2 910 MHz). Je retire « 1 050 Go/s = borne carte » : c'est la borne de nos GEMV ; la carte lit 1,18-1,46 To/s (b12x froid, 28 et 96 experts). REGLES §9 à corriger par Jérôme en ce sens.

**Décision : (c) AOT, pas (a) ni (b).** b12x est statique : le compiler une fois sous un venv torch 2.14 + cutlass-dsl **isolé** (pas notre pile, qui reste 2.13), extraire cubin + signature de lancement, le charger depuis notre extension (`cuModuleLoad`/`cuLaunchKernel`), convertir hors ligne les experts au layout b12x si le nôtre diffère, capturer sous graphes. C'est le seul chemin qui sert **aussi GLM** (150 µs à 34 experts = le MoE du livrable), ce que (b) ne donne pas. **Laurine, 1 j.** Seuils : sur nos tenseurs avec fantômes, **62,7 µs ± 10 %** froid ; pas complet Coder b=12 (réf. certifiée c6377d5, bras B) **−1,8 à −2,3 ms**, accepté si ≤ −1,5, **réfuté si ≥ −0,8** ; J −12 % au moins ; qualité : banc Manon a10b987, **b12x/A ≤ 1,010** (b12x quantifie lui-même ses activations : c'est une autre A4, la PPL rejuge). **Réfuté structurellement** si le cubin dépend du runtime cutlass-dsl (descripteurs TMA construits en Python à chaque appel) : alors (a) reste une expérience isolée, on ne monte pas la pile pour un noyau, et on passe à (b).

**(b) seulement après ce verdict, et mesure avant noyau** : ncu sur notre chemin B pour partager les 45 µs — 3 GEMM (75 µs, 0,99 To/s) contre glue (30 µs : `nvfp4_quant_act` ×2, `moe_act`, `moe_reduce_trie`, scatter). Première marche = fusionner la quantification dans l'épilogue de `moe_act` et la réduction dans celui de `down` (lancements et passages L2 en moins, aucun GEMM nouveau, 1 j) : scellé glue **30 → ≤ 12 µs** ; réfuté si ncu donne déjà la glue < 15 µs — l'écart est alors dans le débit DRAM des GEMM (profondeur cp.async, TMA, bead 0si), et la fusion gate·up+act+down de Laurine (−25/−35 µs prédits) se heurte à 28 CTA pour 170 SM : elle ne se lance qu'avec un plan de découpe écrit et chiffré avant.

1aj : q/k NVFP4 tiennent (0,990), v/o/lm_head cassent (1,047) — j'attends D et E de Manon avant de sceller ; lm_head int8 seul est mon pari (réfuté si D > 1,01).

## § 2 — Licence (15/09 soir, Laurine 71aaad5 ; avis préparé pour la reprise, décision à l'utilisateur)

Fait : le `.o` de b12x n'a aucune dépendance au runtime cutlass-dsl ni à torch 2.14 (11 enveloppes cudart + 3 stubs TVM-FFI) ; `nvidia_cutlass_dsl` 4.7.1 est sous NVIDIA Software License, `libcute_dsl_runtime.so` non distribuable ; le statut du `.o` (sortie du compilateur sur une source Apache-2.0) est une lecture, pas un droit.

Avis : **(c) devient l'instrument, pas le produit, quelle que soit la réponse.** 1. Usage interne sans condition : le `.o` sert au duel GLM et aux bancs comme **plafond de référence** (62,7 / 150 µs) — c'est déjà ce qu'on lui demande. 2. Pas d'embarquement dans le `.deb` tant que la licence n'a pas été **lue** par l'utilisateur (clause « output of the Software ») ; un doute de licence dans un paquet publié coûte plus qu'un noyau. 3. **(b) devient un port, pas une fusion à l'aveugle** : la source b12x est Apache-2.0, on lit son plan de découpe (comment il occupe 170 SM avec 28 experts : répartition des CTA, split, épilogue fusionné) et on l'écrit en CUDA C++ dans notre extension — ce qui lève l'objection « 28 CTA » de mon § 1 et ne dépend d'aucun compilateur NVIDIA hors nvcc. Seuil du port : **≤ 75 µs/couche froid** sur Coder (b12x 62,7 ; nous 105-110), soit ≥ 70 % de l'écart repris ; réfuté si > 90. Durée : 2-3 j Laurine, après le duel. 4. Si l'utilisateur autorise l'embarquement, (b) reste utile pour GLM MLA-side (rien ne change au plan) mais passe derrière 1aj.
