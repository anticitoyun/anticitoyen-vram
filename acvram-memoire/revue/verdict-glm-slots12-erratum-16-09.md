# Verdict — ERRATUM duel GLM : le régime `ACVRAM_HYBRID_SLOTS=12` (celui de `certifie-b12`, donc du duel et des nsys) calcule une attention fausse après un prompt réel, sur main ce71723 comme sur dcb09ba ; à 4 créneaux tout est juste

- **instrument** : `arbitre-prefill-mla-16-09.py` (prefill W4A16 rejoue prompt 128 + k jetons greedy du bras, logit comparé au logit du décodage à k ; 84 points) ; PPL décodage teacher-forcé tranche 0 ; sorties `scratchpad/arbitre-slots12-16-09/` (+ `ce71723/`)
- **commit** : **main ce71723** (arbre du duel, worktree poste3-qa2) et **main dcb09ba** (travail/poste3 f7d8afb) ; même régime que le duel : W4A16 prefill, décodage W4A16 (`MOE_DECODE_MMA=0`), `-k48`, b=12, **`ACVRAM_HYBRID_SLOTS=12`** — la valeur que `certifie-b12-15-09.py:55` et `nsys-rejeu-b12-15-09.py:23` posent (`= B`)
- **régime** : graphes actifs, prompt 128 (les ties), ou prompt 1 jeton (PPL) ; témoin `HYBRID_SLOTS=4` (défaut : 4 séquences en créneaux + 8 en `forward_batch` eager)
- **scellé** (poste7 § 9) : main touché → erratum du duel (« régime invalide, chiffre retiré, à refaire ») ; sinon défaut limité à mla1-3
- **mesuré** : ce71723 : **12 créneaux : défaut 58/84, boucle 58/84 (identiques), |Δ| médian 5,85, k=2 : 3-5/12, |Δ| jusqu'à 48** ; **4 créneaux : 81/84, |Δ| 1,13**. dcb09ba, 12 créneaux : C 54/84, D fp8 53/84 (C ≠ D enfin : max |Δ| 245, 0/12 bit-identiques), boucle 57/84 ; PPL décodage (prompt 1 jeton) C 7,9138 (4 créneaux : 7,8886, +0,32 %), D/C 0,9960
- **verdict** : **main touché** — pas le chemin batché (boucle et batché rendent le même faux à 12 créneaux), **le régime à 12 créneaux lui-même** : le décodage est juste à k=1 (7-11/12), faux à k=2-4 (3-6/12, |Δ| 40-48), et revient vers 10-12/12 à k ≥ 32 — l'état chargé au passage prefill → créneau (`static_load`, 128 lignes) n'est pas celui que le décodage lit ensuite, et l'erreur se dilue à mesure que les lignes écrites par le décodage dominent. **Le ×4,08 du duel (56,1 ms) mesurait ce régime : chiffre retiré, à refaire.** La PPL de prefill du duel (1,0018) n'est pas touchée (aucun créneau). fp8 : enfin exercé (C ≠ D), non jugeable sur ce régime

## 1. Ce qui reste solide et ce qui tombe
- Solide : équivalences à 4 créneaux (boucle 83, défaut 81, poste4 C 81) ; la PPL de prefill ; W4A4 décodage +1,42 % (mesuré à prompt 1 jeton, 4 créneaux) ; **le fait** que 12 créneaux ≠ 4 créneaux sur deux mains différentes.
- Tombe : le pas b=12 GLM du duel (56,1 ms, rondes à 12 créneaux) comme chiffre d'un moteur juste ; les nsys 43,9 → 17,2 ms (rejeu à 12 créneaux) comme chiffres d'un chemin juste — les temps restent des temps, ils ne sont plus rattachés à une attention correcte tant que le défaut n'est pas nommé ; la PPL décodage 8,0003/8,0175 (1 jeton de prompt, 4 créneaux) ne voyait rien de tout cela.
- À nommer par poste4 (le test qui tranche, 5 min chacun) : le seuil (`HYBRID_SLOTS` = 5, 8, 12 : où ça casse), et si c'est le godet (12 → 16, 4 créneaux de rembourrage `_SID_REMBOURRAGE`, `graphs.py:520-537`) ou `static_load` au-delà de 4 ; la règle de commit : arbitre prefill ≥ 80/84 **à `HYBRID_SLOTS = b`**, plus jamais au défaut 4.
