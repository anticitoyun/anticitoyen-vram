# Protocole — poste D (b=12 : 3,2 ms par pas entre le pas nu 13,24 ms et la cellule rondes 16,4 ms) + § 3 (experts distincts GLM calibA / -k48)

instrument : `scratchpad/trace-pas-b12-17-09.py` (une ronde comme `certifie-b12` : 12 invites de 256, ctx 2048, Coder classé, graphes ; chaque pas horodaté avec son lot et son type prefill / plein / traîne ; horloge SM, puissance, température tous les 50 pas ; au pas 600, 40 pas pleins sous torch.profiler : temps GPU des noyaux contre temps hôte du pas) ; `certifie-b12-15-09.py` `CERT_PUR=1` (fenêtre pure de 22 s à lot constant, sous plafond) ; § 3 : `scratchpad/experts-distincts-glm-17-09.py` (eager, crochet sur `MoEBlock._route`, 200 pas à b=12, experts distincts par couche et par pas, mêmes invites pour les deux convertis).
commit : arbre laure 3d99ee9 (= main) ; régime classé `ACVRAM_MOE_MMA=0 ACVRAM_MOE_DECODE_MMA=0 ACVRAM_NARROW_GEMM=1`, SLOTS=12 ; une carte, 400 W.
scellé (Sage, `sage-profil-verdict-17-09` D) : ≥ 60 % des 3,2 ms dans un seul poste nommé ; dispersé (< 40 % partout) → chantier « moteur ». § 3 : experts distincts par pas `calibA` ≤ 0,96 × `-k48`, sinon « non localisé ».
mes prédictions : le poste n'est pas hôte mais **horloge sous plafond** — le pas nu de 13,24 ms vient d'une fenêtre d'une seconde à horloge libre (2,9 GHz, carte froide), la cellule de 20 s tourne bridée à 400 W (`invalidations: bridage pendant la fenêtre : puissance` dans chaque JSON de rondes) : les pas pleins à chaud mesureront 15,0-16,0 ms (≥ 60 % de l'écart) avec une horloge SM ≤ 2,6 GHz ; la fenêtre `CERT_PUR` de 22 s rendra 760-820 t/s (contre 906 sur une seconde) ; hôte (pas − noyaux GPU sous profileur) ≤ 0,6 ms ; prefill + traîne ≤ 0,6 ms par pas équivalent (12 × 256 jetons par ronde, fin simultanée des 12 séquences). § 3 : le routage n'explique pas + 3,5 % — experts distincts `calibA` / `-k48` dans [0,97 ; 1,03] → « non localisé ».
falsification : pas pleins à chaud ≤ 14,0 ms → l'horloge n'est pas le poste ; hôte ≥ 1,9 ms → Sage a raison (hôte) ; prefill + traîne ≥ 1,9 ms → structure de ronde ; § 3 : rapport ≤ 0,96 → hypothèse de routage tenue.
