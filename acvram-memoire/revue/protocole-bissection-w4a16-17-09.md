# Protocole — bissection du 1 % W4A16 (poste7 geste B) : où notre chemin perd sur Marlin à poids identiques

Instrument : `ppl-acvram-17-09.py` (privé, 3 tranches, préfixe, géo) sur `GLM-4.7-Flash-vllm-direct` en W4A16 (`ACVRAM_MOE_MMA=0`) ; référence : le même converti en CUDA = 1,0280 × bf16 ; cible : vLLM Marlin W4A16 = 1,0164 · commit : arbre poste3 08cd4f9 (= main 5853c6e).
Bras : (i) `ACVRAM_DISABLE_KERNELS=1` — tout en torch (rmsnorm, déquant MoE + `grouped_mm` bf16, projections, tête) ; (ii) `ACVRAM_MLA_EAGER_TORCH=1` — **inerte en prefill** (le drapeau ne gate que la branche `t == 1` de `mla.py:250` ; le prefill MLA est déjà en torch fp32 einsum, `mla.py:265-300`) : gardé comme bras qui ne doit PAS bouger (attendu 1,028 ; s'il bouge, je me trompe sur le chemin) ; (ii') `ACVRAM_PREFILL_DEQUANT=1` — MoE seul en torch (`model.py:980-984`), le reste en CUDA — remplace (ii) pour la question « noyau MoE ou amont ».
Lecture scellée (poste7, adaptée) : (i) ≤ 1,020 → nos noyaux CUDA perdent le 1 % ; (i) ≈ 1,028 → la perte est en amont (lecteur compressed-tensors, routage, normes, tête) ; si (i) ≤ 1,020 : (ii') ≤ 1,020 → déquant/GEMM MoE W4A16 ; (ii') ≈ 1,028 → ailleurs dans CUDA (rmsnorm, projections int8 de la tête/attention… mais l'attention est bf16 ici).
Vitesses vLLM W4A16 (-a16) : en-tête du duel (`decode-glm-vllm-16-09.py`, BANC_KV=fp8, slots 1/4/12, fenêtre 20 s ; `prefill-glm-vllm-15-09.py` 2048) — prédiction : décodage b=12 ≤ vLLM W4A4 (796 t/s) de 10-25 % (Marlin W4A16 déquantifie), prefill ≤ 26 732 j/s.
