# Verdict — C14-bis (ncu d'occupation de `mla_1p_kernel<20,16,2>` à b=1, GLM, `poste7-niveau2-faux-c14-defaut-20-09` § 1) : **le noyau est sous-occupé par sa GRILLE — 32 blocs de 256 fils pour 170 SM (warps actifs 20 %, émission 24 %, SM 2,4 %)**, ni par les registres (120, 2 blocs/SM possibles) ni par la shared ; « ≤ 15 µs » demande ≥ 170 blocs (tranches de 4 lignes à L=512, ou les 20 têtes réparties sur 2-4 blocs), pas une tuile plus fine

instrument : `donnees-c14-19-09/c14bis/{chaine.sh,cible.py}` — GLM calibA, arbre `scratchpad/c14-build-19-09` (b765f1b2 compilé, cache kernels-9afa451e02c4), invite 256 jetons, 40 pas de décodage b=1 sans graphes sous `cudaProfilerStart/Stop`, ncu root (`--cache-control none --replay-mode kernel -k regex:mla_1p --launch-count 40`), métriques de lancement, d'occupation, de décrochage ; CSV `ncu-c14bis.csv` (les pourcentages ncu sont ×100 dans le CSV)
commit : poste1-11 (ce commit), carte 21:57-22:05 sous verrou, PID 4284 llama-server étranger présent
régime : GLM calibA, éco off (l'arbre c14-build précède l'éco), `experts_layout=marlin(33/46)`, L = 512 (godet MLA de 256 + 40)
scellé (poste7) : ncu d'occupation AVANT d'insister sur ≤ 15 µs (16,8 mesuré par poste2, étape 4) ; prédiction poste1 (en tête de chaîne) : occupation limitée par les registres ou grille < 170 SM ; si grille < 170 → la règle de grille est le levier, pas la tuile
mesuré : `mla_1p_kernel<20,16,2,0>` — **grille 32 blocs** × 256 fils, 120 registres/fil, shared statique 0 (dynamique non rendue), limite d'occupation par les registres 2 blocs/SM, par la shared 1, par les blocs 24 ; **sm__warps_active 20,4 %**, **smsp__issue_active 24,0 %**, sm__throughput 2,4 % ; `mla_1p_combine_kernel` — grille **20 blocs** × 128, 64 regs, warps actifs 16,3 %, émission 9,0 %, décrochage dominant **short scoreboard** (76 866) puis barrier (11 703) ; durées : `gpu__time_duration` rendue « n/a » par ce ncu sous `--cache-control none` (poste2, nsys : 16,8 µs et 8,4 µs)
verdict : **prédiction « grille < 170 » tenue, « registres » réfutée** — à L=512 la règle « ≥ ctx/64 blocs » (`_grille_par_lot`) donne L/16 = 32 tranches, un bloc chacune : 19 % des SM, et chaque bloc est à latence (1 warp actif sur 5) ; la demi-tuile n'y change rien. Levier C14-c (à chiffrer, pas cette nuit) : **≥ 170 blocs à b=1** — tranches de 4 lignes (128 blocs à L=512, 512 à L=2048) et/ou les 20 têtes en 2-4 groupes par tranche (têtes indépendantes dans le noyau : grille × 2-4 sans recombinaison supplémentaire) ; coût : le combine grandit avec les tranches (déjà 3,1 → 8,4 µs : short scoreboard = lectures des partiels) — le combine doit devenir un seul bloc par tête avec réduction en shared, ou être fusionné dans le dernier bloc (compteur atomique). Prédiction pour C14-c : cœur b=1 25,2 → 12-15 µs/couche si la grille passe ≥ 170 ET le combine ≤ 3 µs ; sinon la latence du combine mange le gain.
