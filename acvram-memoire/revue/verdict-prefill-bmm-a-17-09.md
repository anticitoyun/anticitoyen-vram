# Verdict — commit A (prefill des experts par `bmm`) : **réfuté et pire** — 5 486 j/s contre 8 614 (témoin `grouped_mm`) ; PPL constante (1,0004) ; la boucle n'était pas le coût, les petites tuiles cuBLAS le sont, et `w[experts]` ajoute 100 ms de copies

instrument : `scratchpad/prefill-glm-acvram-15-09.py w4a16 2048` (7 rép., médiane, processus neuf par régime) ; `ppl-acvram-17-09.py` privé 3 tranches (fenêtres 2048 en prefill) ; `profil-pas-coder-17-09.py prefill` sous `bmm` (noyaux nommés, comparé au profil `grouped_mm` de `verdict-profil-coder-pas-17-09`) — `scratchpad/prefill-bmm-17-09/`
commit : arbre laure 247aced (= main f28e61d avec le commit A 99ec80f) ; protocole `protocole-prefill-bmm-a-17-09.md` scellé avant ; `Qwen3-Coder-30B-A3B-nvfp4`
régime : `[régime] ACVRAM_NARROW_GEMM=1 ACVRAM_MOE_MMA=0 ACVRAM_MOE_DECODE_MMA=0 extension=oui` (bmm, défaut, absent de la ligne) contre `… ACVRAM_PREFILL_GROUPED=grouped_mm …` (témoin, nommé dans les 4 JSON/logs) ; compteurs `fpg=336 pile=1008 gemm=0 mma=0` identiques ; une carte, 400 W
scellé : Sage ≥ 11 500 j/s, PPL = tout-torch ± 0,002, < 11 000 → réfuté ; moi 12 000-14 000, témoin 8 400-8 900, PPL ± 0,0005
mesuré : prefill 2048 **bmm 5 486 j/s (373,3 ms, σ 34)** · **grouped_mm 8 614 j/s (237,7 ms, σ 47)** — témoin tenu (8 681 hier, − 0,8 %) · PPL privé géo **bmm 11,9086 / grouped_mm 11,9036 = 1,0004** (tranches 1,0009 / 0,9994 / 1,0010 ; ± 0,0005 réfuté de peu, ± 0,002 tenu) · profil bmm : GPU 330,7 ms contre 225,8 — GEMM cuBLAS **119 ms contre 120** (8 895 lancements contre 13 902 : le `bmm` n'a pas remplacé les tuiles 32×32 / 16×16, il en a retiré un tiers) ; **`index_elementwise_kernel` 44,1 + 23,1 ms (7 596 lancements) + `vectorized_gather_kernel` 33,5 ms (1 659)** = ≈ 100 ms de copies neuves (`w[experts]`, `src`/`dst` par seau) ; déquant inchangée 52,1 ms
verdict : **A est réfuté (< 11 000) et régresse de 36 % : la boucle de 128 `mm` n'était pas le coût — ce sont les GEMM eux-mêmes, à M ≈ 128 lignes par expert, que cuBLAS sert en tuiles wmma 16×16 / 32×32 à ~10 % de crête, et le `bmm` par seau les remplace par des `bmm` sur des blocs rembourrés qui coûtent le même prix (119 contre 120 ms) tout en payant 100 ms de rassemblement des poids bf16 (`w[experts]` copie 3 Mo par expert et par pile : ≈ 58 Go par prefill). La qualité est constante (1,0004). Ne pas fusionner A comme défaut : `grouped_mm` reste le régime classé du prefill ; B (noyau CUDA `nvfp4_gemm_grouped_w4a16` lisant les piles NVFP4 sur place, sans déquant ni gather) attaque les deux postes : 52 ms de déquant + 120 ms de GEMM à faible occupation.**

| régime | prefill 2048 j/s | ms | PPL privé géo | GEMM cuBLAS ms (lancements) | copies / gather ms | déquant ms |
|---|---|---|---|---|---|---|
| grouped_mm (témoin, ancien) | 8 614 | 237,7 | 11,9036 | 120 (13 902) | 3 | 52 |
| bmm (A, défaut du commit) | **5 486** | 373,3 | 11,9086 | 119 (8 895) | ≈ 100 | 52 |

Réserves : le `bmm` cuBLAS sur (n experts, cap, K) × (n, K, M) avec cap 32-256 tombe sur les mêmes petites tuiles : la borne à battre n'est pas le nombre de lancements mais l'occupation à petit M — un seul GEMM (16 384 lignes triées, offsets) ou le noyau B ; le rassemblement `w[experts]` peut se remplacer par une vue (`w` déjà en pile E × M × K, `bmm` sur `w[e0:e1]` contigu) si l'on ordonne les seaux par indice d'expert — cela retire 100 ms mais ne ramène qu'à 8 600 ; A ne vaut d'être repris que si B tarde. Le défaut `ACVRAM_PREFILL_GROUPED=bmm` de `model.py:1581` est à inverser avant toute mesure de cellule.
