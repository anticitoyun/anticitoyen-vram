# Protocole — contrôle de l'ajout GUI `energie.j_par_jeton` de `/metrics` (Jérôme 4b83a2f, NVML côté serveur, delta entre deux appels) contre `energie.py` sur la même fenêtre (Sage, sage-rpw-in-situ-gui-18-09)

instrument : `scratchpad/controle-gui-18-09/{chaine.sh,controle.py}` — serveur `acvram.cli serve Qwen3-Coder-30B-A3B-nvfp4 --port 8094 --max-batch 12` sous carte.sh, régime classé (rpw=4 défaut à 062079e) ; 1 salve de chauffe (12 requêtes × 256 jetons), puis : `GET /metrics` (arme la fenêtre serveur) → `with Energie()` → 6 salves de 12 requêtes parallèles × 256 jetons (~60-90 s) → `GET /metrics` DANS le `with` (ferme la fenêtre serveur avant l'arrêt du compteur) ; dénominateur commun = delta `engine.decode_tokens` entre les deux appels ; `usage.completion_tokens` sommé comme témoin du dénominateur ; aucune sortie de modèle imprimée ni conservée.
scellé (Sage) : |GUI / energie.py − 1| ≤ 0,10 tenu, sinon faux. Prédiction : écart < 3 % (même compteur NVML `TotalEnergyConsumption`, même fenêtre à ~1 s près sur ~70 s, dénominateur identique) ; un écart > 10 % signerait une fenêtre décalée (delta armé au mauvais appel) ou un dénominateur différent (prefill compté d'un côté).
