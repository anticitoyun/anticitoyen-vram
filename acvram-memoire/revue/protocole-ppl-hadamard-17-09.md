# Protocole — PPL du converti Hadamard (Manon, main 40ec541) : prefill + décodage, préfixe, 12/12

Instrument : `ppl-acvram-17-09.py` (fenêtres `encode_brut`, cibles 1024..2047, médiane, 4 ids) sur `corpus-encode-brut-glm/{prive,public}-tr{0,1,2}.txt` ; `ppl-decode-prefixe-17-09.py` (invite `encode_brut`, 12 séquences × 2 047, allocateur redimensionné, assert 12/12) sur les 3 tranches privées ; bf16 de référence : `ppl-refonte-17-09` (prefill, min-ctx 1023) et `bloc-sage11` (décodage, min-ctx 0) · commit : arbre laure 40ec541 (= main) · régime : `GLM-4.7-Flash-srcbf16-nvfp4-hadamard512` (AWQ + rotation 512), W4A16 prefill (`ACVRAM_MOE_MMA=0`), décodage W4A16 MIN_T=5 ; décodage en **eager** (capture de graphes 0/10 godets sur ce converti — Laurine) : la PPL ne dépend pas du chemin (arbitre 80/84 graphes = 79/84 eager), le temps n'est pas mesuré.
Scellé (Sage) : médiane ×bf16 ≤ 1,010 sur le privé ET le public. Mes prédictions : privé [1,010 ; 1,022] (la rotation gagne sur le `-k48` 1,029 sans revenir sous 1,010 : 60 %) ; public [0,995 ; 1,005] ; |privé − public| < 0,032 (le `-k48`) — si < 0,01, la piste « RTN experts sur registre français » est confirmée comme cause principale de l'inversion. Décodage ≈ prefill ± 0,01 au cadrage min-ctx 0.
