# Protocole — bissection du biais ppl-decode-kv +0,008 (poste4 b118782/acd1331, poste7-p1-situ-verdict-18-09) : noyau GEMV (b) ou KV écrit par le préfill Marlin ?

instrument : (1) `outils/biais-gemv-marlin-18-09.py <Coder> --couches 0,12,24,47 --jetons 12` sous carte.sh — par couche, v1 / (b) / témoin (échelles tronquées d'un bit, doit rendre faux) contre fp32 sur piles réelles : `hors`, `biais_signe` (≤ 1e-4), `gain − 1` ; (2) `ACVRAM_DOUBLE_DISPOSITION_DIAG=1` : 4 cellules `ppl-decode-kv` Coder 256+1024 = {`PREFILL_GROUPED` groupe | marlin} × {`GEMV_LAYOUT` naturel | marlin}, régime classé ; témoin connu : groupe × naturel = 5,5426 ; marlin × marlin = 5,5506 (in situ) ; ligne [régime] `experts_layout=double(diag)` attendue, régime NOMINAL exigé (si la double disposition exile, la cellule est invalide et le dit).
prédiction (poste4) : (b) ≡ v1 à 1e-6 par couche ; le +0,008 suit le PRÉFIXE Marlin (KV écrit par le préfill), pas le décodage — donc groupe × marlin ≈ 5,5426 et marlin × naturel ≈ 5,5506. Prédiction contraire (moi, écrite avant) : si le biais est dans (b), c'est marlin × naturel ≈ 5,5426 et groupe × marlin ≈ 5,5506. Le témoin tronqué doit rendre `gain − 1 < 0` net ; s'il rend ≈ 0, l'instrument ne voit rien et rien n'est jugé.
addendum (poste7, ordre précis) : (1) toutes couches, routages RÉELS (trace `ACVRAM_TRACE_ROUTAGE` d'un certifie b=12 eager de 20 s), v1/(b)/témoin ; (2) bissection 2×2 sur **8 192 + 1 024** jetons, cache Triton chaud, **chaque cellule en deux exemplaires** (résolution de l'instrument ± 0,003 : la paire accuse, pas la valeur). Prédictions croisées : poste4 → le +0,008 suit le préfixe Marlin ; poste7 → il suit le décodage (b) ; si |gain − 1| < 1e-4 partout et la cellule {groupe, (b)} porte l'écart → biais d'un autre type (ordre des experts dans la réduction, fantômes). Décisions : suit le décodage → (b) corrigé avant tout défaut ; suit le préfixe → silu·u et down en fp32 dans Marlin (prefill doit rester ≥ 15 700) ; paires dans ± 0,003 → dispersion, in situ à rejouer en double.
