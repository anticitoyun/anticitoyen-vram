# Protocole — poste E, mesure de référence : `paged_attn_partial` / `paged_attn_reduce` à b=12 ctx 2048, b=1 ctx 2048, b=12 ctx 300 (Coder, KV int8)

instrument : `scratchpad/attn-reference-17-09.py <b> <ctx_invite>` (graphes, 20 pas profilés, noyaux seuls ; grille/bloc/registres/shared lus dans la trace chrome ; octets KV calculés depuis la disposition int8 `[NB,16,HKV,D]` + échelles fp16 ; BW atteinte = octets KV / durée du noyau partiel, crête 1 792 Go/s). ncu refusé sur ce poste (`ERR_NVGPUCTRPERM`, compteurs réservés à l'administrateur — pas de réglage système de ma part) : occupation et octets DRAM réels restent hors de portée, dits comme tels.
commit : arbre poste3 6d2c5fb (= main) ; régime classé ; `PA_CHUNK` défaut → chunk 64 (`acvram_kernels.cu:3866`), grille `(B·QL, HQ, C)`, 4 warps par bloc, un jeton par warp et par itération, 4 octets int8 par lane (`acvram_kernels.cu:891-1010`).
scellé (poste7) : le seuil de E est ≤ 1,5 ms à ctx 2048 / b=12 et ne bouge pas.
mes prédictions : b=12 ctx 2048 : partial 4,8-5,4 ms (+ reduce ≈ 0,1), octets KV ≈ 1,2 Go → BW 220-260 Go/s (12-15 % de crête) ; b=1 ctx 2048 : partial 0,5-0,8 ms pour 0,1 Go (≈ 15-20 %) — le noyau souffre du **contexte** (latence par jeton : lectures d'un octet, réduction warp par jeton) et non du lot : ms/jeton-de-contexte identiques à ± 20 % entre b=1 et b=12 ; b=12 ctx 300 : ≈ 1,1 ms (le profil d'hier). Forme de E attendue : lectures vectorisées 16 octets, plusieurs jetons par warp, tuile de clés en shared, softmax en ligne par tuile.
falsification : b=1 à ≥ 30 % de crête et b=12 à ≤ 15 % → le noyau souffre du lot (grille B·HQ·C trop grande, partiels) ; BW b=12 ≥ 40 % → mon modèle d'octets est faux (L2 8× redondant compté une fois).
