# Verdict — forme (b), GEMV de décodage lisant la disposition Marlin (Laurine 5f9c4cc) : **b=12 6,13 ms/pas = 0,886 × GEMV (6,91)** ; **b=1 0,82 ms = 0,909 × GEMV (0,90)** → **scellé ≤ 0,97 TENU aux deux lots**, exact contre fp32 (0 hors 2⁻⁷) ; noyaux mesurés attestés sous profileur : `nvfp4_gemv_marlin_kernel<bf16,2>` + `<float,1>` (bras b) contre `nvfp4_gemv_grouped_gateup_kernel<bf16,4>` + `nvfp4_gemv_grouped_xreg_kernel<float,4,1>` (GEMV) — **disposition unique Marlin viable, P1 continue à 0 octet de plus**

instrument : `outils/banc-marlin-decode-18-09.py` (5f9c4cc) via copie locale `scratchpad/forme-b-18-09/banc-sonde.py` = même banc + sonde torch.profiler (noms des noyaux CUDA du bras (b) et du GEMV au routage 0, condition Sage) ; le bras (b) appelle `ext.nvfp4_gemv_marlin_gateup` / `ext.nvfp4_gemv_marlin` DIRECTEMENT (banc :136-137), pas MoEBlock ; sous graphe, formes Coder, 20 routages tirés, juge fp32 par ligne ; extension compilée seule sous carte.sh avant (131 s), `.so` Marlin en cache
commit : 9371414 (laure = main + laurine 5f9c4cc), 15:32-15:34, charge 2,6-2,7, carte libre entre deux tranches de P2
régime : classé (rpw=4, xreg=down) ; `pytest tests/test_gemv_marlin.py` : **14 passed, 1 failed** (`test_le_bloc_moe_decode_prend_gemv_marlin` : chemin `gemv_v1` pris au lieu de `gemv_marlin` — intégration MoEBlock, 4e sonde attrapée par `attendre_chemin` ; les 14 tests noyau passent)
scellé (Sage) : (b) ≤ 0,97 × GEMV à b=12 ET b=1, chaque chemin exact vs fp32 → disposition unique ; sinon P1 fermé VRAM ; prédiction Laurine (SASS, à sec) b=12 5,0-6,5, b=1 2,2-2,9
mesuré : b=12 par couche (b) 0,133 ms, GEMV 0,146, Marlin GEMM 0,149 → **(b) 6,13 / GEMV 6,91 / Marlin 6,99 ms/pas**, 0 hors sur 196 608 pour les trois ; b=1 (b) 0,0171 ms, GEMV 0,0190, Marlin 0,0324 → **0,82 / 0,90 / 1,55 ms/pas**, 0 hors sur 16 384 ; rapports **0,886** et **0,909**
verdict : **TENU** — la disposition Marlin lue par un GEMV est plus rapide que le GEMV actuel aux deux lots (−11 % à b=12, −9 % à b=1) : la prédiction b=12 est tenue (6,13 ∈ 5,0-6,5), la prédiction b=1 (2,2-2,9) est réfutée en mieux de 1,4 ms (l'instr/octet compté à sec ne se paie pas : la lecture par fragments 64 o est aussi bien coalescée) ; P1 continue en disposition unique — reste à Laurine : le chemin MoEBlock qui doit prendre `gemv_marlin` (test rouge), puis l'in situ (prefill ≥ 15 700, PPL B0 ± 0,002, décodage b=12/b=1 ≥ cellules actuelles)

## Lecture
- Témoins de ce run : GEMV 6,91 (b=12) reproduit le 6,92 du verdict précédent ; à b=1 0,90 contre 0,82 plus tôt (même noyau xreg : dispersion de 10 % sur 17-19 µs par couche, sous graphe) — le rapport 0,909 est pris dans le même run, c'est lui qui compte.
- La sonde répond à la condition de Sage : sans elle, un bras (b) passant par MoEBlock aurait mesuré v1 contre v1 (c'est ce que le test d'intégration a attrapé) ; à ajouter au banc de Laurine (10 lignes).
