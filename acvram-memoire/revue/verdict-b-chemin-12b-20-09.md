# Verdict — (b) chemin multimodal (dispersion, masque, positions, cache) : acvram `gemma-4-12B-it-bf16-vision` contre transformers bf16 12B, 20 images, ordre texte→image (poste7 14 h 25 / 17 h 15) : **8 jetons greedy identiques 20/20 (TENU)** — **log-prob des 20 descriptions : géo −6,9 % de PPL relative (acvram plus confiant que HF), sd 8,1 %, +2 SE = −3,5 % : « non pire » tenu, mais la clause ± 0,5 % est FAUSSE — l'écart est systématique (17 images sur 20 à Δ > 0, moyenne +0,072 nats/jeton, 0,266 au plus)** ; comptes de cibles égaux 20/20 ; premier jeton = argmax HF 20/20

instrument : références `references-transformers.py` (12B bf16 entier sur la carte, `.to("cuda")` sans accelerate, 76 s, `references-gemma4-12b-bf16.pt` + sha256, cibles = description seule du premier jeton divergent à la fin de tour exclue) ; mesure `mesure-c.py` sur main 647d8b38 (⊇ 3f6cb0ec ; `graphes=on`, cache de préfixe OFF, décodage forcé, 43 s), alias `gemma-4-12B-it-bf16-vision` (`acvram convert --format bf16`, 23 G, `vision: oui` ; le 12B « unified » a sa vision sous `model.vision_embedder.*` — gardée depuis 4b595e32) ; `compare-ref.py` seuil 0,5 (rc 65 si comptes ≠ : 0) ; 16:23:30-16:25:45 sous `carte.sh` mesure, fils 0-15, `poste=20-09-1030` ; 3080 Ti pid 4286 (pair)
scellé (poste7, avant) : 8 jetons greedy identiques ≥ 16/20 (17 h 15 : « 20/20 ») après le préfixe commun ; log-prob des 20 descriptions géo ± 0,5 % ; cibles comptées égales
mesuré : greedy 8/8 sur les 20 images ; lp/jeton HF moy −6,844, acvram −6,772 ; Δ par image (acvram − HF) : +0,010 +0,086 +0,030 +0,266 +0,090 +0,018 +0,010 +0,196 +0,007 +0,198 +0,020 +0,063 +0,083 +0,126 +0,108 +0,110 −0,001 −0,012 −0,059 +0,082 ; PPL relative géo −6,9 %, sd 8,1 % ; `final_logit_softcapping: 30.0` dans la config (le moteur le porte : engine/config.py:119, :623)
verdict : le chemin image est juste au jeton (20/20, comme diff-couches d'poste1 : argmax 8/8, KL ≤ 0,012 sur img00) ; la log-prob des cibles improbables (−5 à −13 nats) est systématiquement un peu plus haute chez acvram : un biais d'échelle des logits, pas un bruit (17/20 du même signe) — candidats à nommer par le code, pas par moi : cache KV int8 (défaut acvram) contre bf16 chez HF, softcap ou `output_multiplier` appliqués différemment, accumulation bf16/tf32 de la tête ; KL 0,012 sur les pas d'argmax ne voit pas 0,07 nat sur des cibles à 10⁻⁴ ; **(b) = chemin tenu, échelle des logits à nommer avant de sceller « ± 0,5 % »**
durée : 2,2 min de carte (références 1,3 + mesure 0,7 + comparaison)
suite : poste1 : un pas de teacher forcing sur une cible improbable avec cache KV bf16 (`ACVRAM_KV_…`) contre int8 → si le Δ tombe, c'est le cache ; sinon la tête (softcap / multiplier) ; poste7 : (b) tenu sur le jeton, clause lp à requalifier (bruit d'échelle, seuil ± 0,5 % non résolu par cet instrument) ; ma file : decode-pas 31B (30 min) → témoin vLLM → 0.6.33 + S2

## Rejouable
références : `MAXMEM=cuda LEGENDES=0 … references-transformers.py /mnt/AI_GENERATOR/models_acvram/gemma-4-12B-it-bf16 <images> <sortie.pt>` (80 s) ; mesure : `ACVRAM_MODELE_MESURE=/mnt/AI_GENERATOR/models_acvram/gemma-4-12B-it-bf16-vision … mesure-c.py <images> <sortie.json>` (45 s) ; `compare-ref.py <mesure> <références.pt> 0.5`.
