# Verdict — nsys du pas de décodage Coder (classé, défaut, graphes on) : b=12 **6,87 ms de noyaux sur 7,36 mesurés (93 % ; hors graphe 0,49 ms = 7 %)** = Marlin gate·up **2,38** + down **1,35** (t_marlin **3,73 = 54 %**) + `_etroit` int8 q/k/v/o **1,06 (15 %)** + norm/élém 0,64 + attention paginée 0,41 + route 0,33 + tête/KV 0,26 + réductions fp32 0,23 + rope 0,14 ; b=1 **3,09 sur 3,33 (93 %)** = `int8_gemv` q/k/v/o **0,92 (30 %)** + Marlin **1,05 (34 %)** + route 0,32 + attention 0,24 + rmsnorm 0,21 + tête/KV 0,16 + rope 0,09

instrument : `scratchpad/nsys-budget-19-09/decode-nsys.py` (Engine direct, graphes ON, `--max-batch` = b, 256 jetons d'invite, 20 pas d'échauffement, 50 pas capturés) sous `nsys profile -t cuda,osrt --cuda-graph-trace=node` + `nsys stats cuda_gpu_kern_sum` + `classer.py` (table reclassée à la main depuis les CSV `coder-decode-b{1,12}_cuda_gpu_kern_sum.csv`) ; `chaine-decode-coder.sh`, arbre `700181c` (main `a7397e2`), prise `carte.sh` 19:16:34-19:18:09, aucune variable, `graphes=on(hybrides≤4|12)`, 0 repli eager ; **temps mural sous `osrt` inutilisable** (881 / 378 ms/pas : le traçage des appels système coûte ×100) → le pas de référence vient du **même script sans nsys, 200 pas** (19:18, 2 × 30 s) : **3,330 ms (b=1), 7,357 ms (b=12)** ; « hors graphe » = mesuré − Σ noyaux. Réserve d'instrument : le harnais `serve` donne 2,67 ms/jeton à b=1 (375 t/s) et 8,96 ms/pas à b=12 (1 339 t/s, C11 A1) — l'Engine direct est plus lent à b=1 et plus rapide à b=12 que le serveur ; deux instruments, deux chiffres, le budget par noyau vaut pour les deux (mêmes noyaux)
scellé : aucun (budget publié) ; prédiction Manon : b=1 ≈ 2,2 ms (Marlin 1,2, attention 0,3, glue 0,5, tête 0,2), b=12 ≈ 7,5 (Marlin/mma2 4,5, attention 1,0, glue 1,2, reste 0,8), hors graphe ≤ 10 %
mesuré (ms/pas = Σ/50, × lancements/pas) — **b=1, 3,09** : `int8_gemv_kernel<4,1,bf16,bf16>` **0,73** ×96 + `<…,float>` 0,19 ×1 (q/k/v/o int8 g128 + tête) · `nvfp4_gemv_marlin<bf16,2>` (gate·up) **0,71** ×48 · `nvfp4_gemv_marlin<float,1>` (down) **0,34** ×48 · `_route_fusee` 0,32 ×48 · `_partiel` 0,15 + `_reduce` 0,09 ×49 (attention paginée split-K) · rmsnorm 0,21 ×96 · cuBLAS `gemvx` 0,09 ×48 + `kv_write_int8` 0,07 ×48 · rope 0,09 ×48 · `moe_reduce` 0,04 · élémentaires 0,04 ; 25 noms, ~700 lancements/pas ; **b=12, 6,87** : Marlin gate·up **2,38** ×48 · Marlin down **1,35** ×48 · `_etroit_kernel` (Triton int8 M=12, q/k/v/o) **1,06** ×97 · rmsnorm 0,23 + élémentaires 0,41 (×490) · `_partiel` 0,35 + `_reduce` 0,06 · `_route_fusee` 0,33 · `reduce_kernel<128,4>` fp32 0,23 ×98 · cutlass wmma bf16 (tête M=12) 0,13 + splitK 0,05 + `kv_write` 0,08 · rope 0,14 · `moe_reduce` 0,05 ; 30 noms
verdict : **b=12 : t_marlin = 3,73 ms = 54 % du pas ; la glue (norm + élémentaires + route + réductions + rope + moe_reduce) = 1,58 ms = 23 % ; les projections int8 étroites 1,06 = 15 % ; hors graphe 7 %** — ma prédiction tenait la somme (7,5 vs 7,36) mais surestimait l'attention (1,0 prédit, 0,41 mesuré) et sous-estimait la glue. **b=1 : le premier poste n'est pas Marlin (1,05) mais `int8_gemv` q/k/v/o (0,92 ms, 30 %)** : à M=1 le classé lit ses projections int8 g128 par un GEMV int8 aussi cher que les 8 experts — c'est le poste que P2 (i8c) déplaçait (396 vs 375 t/s) et que C11 sert par la vue g128 à M ≥ 2 ; la prédiction 2,2 ms ignorait ce poste. Marlin à b=12 fait 3,73 ms pour 12 × 8 experts lus contre 1,05 à b=1 : ×3,55 pour ×12 de jetons — le GEMV Marlin se partage bien mais n'est pas gratuit (Océane M1 : 398 W = plafond, rapport cyclique ≈ 1) ; Mesure 2 (W_mma2) reste conditionnée (Sage : seulement en pile avec 2700 et W_mma2 ≤ 300)
suite : Océane : (1) b=1, `int8_gemv<4,1>` ×96 = 0,73 ms : un GEMV int8 g128 sur 4 projections × 48 couches — fusion q/k/v en un lancement ou vue i8c (C11 ne couvre que M ≥ 2) ; (2) `_route_fusee` 0,32-0,33 ms ×48 quel que soit b : latence pure, 7 µs par couche ; (3) glue 1,58 ms à b=12 (490 élémentaires) ; Sage : Coder b=1 = 30 % projections int8 + 34 % experts + 36 % glue/attention/tête — pas un poste unique ; Jérôme : indexer ; ma file : C2 témoin → C5 → C4 → porte A8+W8r → statics → NARROW_GEMM → P2+éco → addendum C13-a

## Rejouable
`ACVRAM_TYPE=mesure outils/carte.sh bash scratchpad/nsys-budget-19-09/chaine-decode-coder.sh` (95 s) ; pas sans nsys : `decode-nsys.py <modele> <b> 200` sous `carte.sh` (`coder-decode-b{1,12}-sans-nsys.log`).
