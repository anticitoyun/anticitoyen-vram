# Verdict — raisons de bridage sous « éco 2 700 » à b=12 (Sage, sage-c15-niveau3-coder-faux § 3) : **la carte est au plafond de puissance, pas à 2 700** — pendant un `certifie` b=12 Coder (moteur `-lgc 2700`), **95 % des échantillons sous charge sont sous 2 650 MHz (min 2 527, p5 2 535, médiane 2 550)** et **tous portent la seule raison `sw_power_cap` (masque 0x4)** : W moy 387, max 401 sur un plafond de 400 W ; T max 47 °C, aucune raison thermique ni matérielle

instrument : `scratchpad/bridage-19-09/sonde.sh` — `nvidia-smi --query-gpu=clocks.sm,power.draw,temperature.gpu,clocks_throttle_reasons.{active,sw_power_cap,hw_slowdown,sw_thermal_slowdown,hw_thermal_slowdown,hw_power_brake_slowdown,sync_boost,applications_clocks_setting} -lms 100` en parallèle d'un `certifie-b12.py` A (Coder, 12 séquences, sous `carte.sh` 01:39:42-01:41:00, éco 2 700 posé par le moteur), 780 échantillons dont 309 sous charge (> 200 W) ; plafond lu `power.limit 400 W / max 600 W`, `clocks.max.sm 3 135`
scellé (Sage, avant) : nommer la raison des 2 450-2 510 MHz vus sous verrou à 330-360 W (niveau 3 (d)) ; hypothèses : puissance, thermique, `-lgc` non tenu
mesuré : certifie A **8,508 ms · 1 410 t/s · 0,279 J/j · 392,8 W** ; échantillons sous charge : sm min **2 527**, p5 2 535, méd **2 550**, max 2 992 (rampe) ; W moy **387**, max **401** ; T max **47 °C** ; sous 2 650 MHz : **294/309 (95,1 %)**, raison active **`sw_power_cap` 294/294**, masque `0x0000000000000004` seul ; les 15 échantillons ≥ 2 650 sont la rampe (masque 0x0)
verdict : **sous `-lgc 2700`, à b=12, la carte tourne à ≈ 2 550 MHz bornée par les 400 W** — `-lgc` fixe un plafond, pas un plancher ; le « 2 700 » du régime est un maximum atteint seulement quand la charge laisse de la marge (b=1 : 2 670-2 692 dans les fiches). Conséquences : (1) la ligne de régime `eco=2700(2685)` lit une horloge légère et **ne décrit pas le régime servi à b=12** (2 550) ; (2) les cellules b=12 « éco 2 700 » sont en fait des cellules **« 400 W »**, ce qui explique que 2 700 et « libre » se valent à 2 % près (E1 : les deux sont au plafond de puissance) ; (3) les 330-360 W du niveau 3 (d) avec 2 450-2 510 sont le même régime avec un serveur moins dense (banc HTTP) — pas un bridage nouveau
suite : Sage : nommer le régime b=12 « plafond 400 W » dans les fiches, et décider si l'éco doit poser aussi `-pl` (une cellule à −pl 350 dirait si J/j baisse encore à vitesse égale) ; Océane : `eco.lire_sous_charge` devrait publier `sw_power_cap` quand elle est active (la ligne de régime le dirait) ; Jérôme : indexer ; ma file : niveau 2 (707bfbdf, 1 min) → test_paquet_charge_utile 0.6.23 → capture du parc → diagnostic poste E

## Rejouable
`bash scratchpad/bridage-19-09/sonde.sh` (1,5 min ; `echantillons.csv`, `journal.log`).
