# Scellé — décode par lots b=12, Coder : où part l'écart acvram 1 378,6 → vLLM 1 596,1 t/s (Laurine, 21/09 09 h 03, à sec, avant tout code)

**Cadre.** Cellule T1 poste 1030 (`verdict-t1-poste-1030-20-09`, 0.6.30, `-lgc 2700`, harnais `banc-llamacpp-16-09.py` : 12 flux × (invite 256 + 1 024 jetons), fenêtre 20 s) : acvram **8,70 ms/pas** (12/1 378,6), vLLM Marlin W4A16 **7,52 ms/pas** (12/1 596,1) — **écart 1,18 ms/pas (13,6 %)**. Le harnais compte l'invite dans la fenêtre : 12 × 256 groupés = 3 072 j / 22 707 j/s = 0,135 s par lot de 8,9 s (1,5 %, identique chez vLLM) → **pas de décodage servi ≈ 8,55 ms à longueur moyenne 768**. Instrument et arbre : 0 carte pour cette note ; sources = `verdict-nsys-coder-b12-19-09` (CSV `scratchpad/nsys-budget-19-09/coder-decode-b12_cuda_gpu_kern_sum.csv`, arbre 700181c, horloge **libre**, 50 pas en rafale, longueur ~300, sans tokenizer ni HTTP), `prediction-4-3ms-hote-decodage-14-09`, `p1-flashinfer-b12x-15-09`, `runner.py:1325-1686`, `server/app.py:91-135`, manifeste du modèle (`mlp_bytes` 339 886 080/couche = **2,655 Mo par expert**, `kv_bytes_per_token` 49 920).

## (a) Budget du pas b=12 (nsys 19/09, Σ 6,873 ms, 1 169 lancements/pas = 24 nœuds/couche, un seul rejeu de graphe par pas)
Marlin gate·up 2,381 (×48, 49,6 µs) + down 1,346 (×48, 28,0 µs) = **3,73 (54 %)** · `_etroit` int8 q/k/v/o 1,065 (×97, 11,0 µs) · attention `_partiel`+`_reduce` 0,414 · `_route_fusee` 0,334 (×48, 7 µs : latence pure) · glue : `reduce_kernel` fp32 0,234 (×96) + rmsnorm 0,231 (×97) + élémentaires 0,214 (×144) + rope 0,136 + Fill 0,081 (×97) + casts 0,062 (×96) + moe_reduce 0,048 + splitK 0,046 + divers 0,036 = **1,09** · tête wmma 0,125 + kv_write 0,082. Mesuré Engine direct 7,357 → **hors noyaux 0,49 ms (6,6 %)** = trou hôte + non-graphe.

**Trois confondus entre 7,36 (Engine direct, 19/09) et 8,55 (servi)** — 1,2 ms qu'aucune mesure existante ne sépare : (i) horloge libre en rafale contre 2 700 tenue (T1 lit 2 625-2 692 MHz) : prédit **+4…+7 % sur les noyaux** (+0,3…+0,5 ms) ; (ii) longueur 300 → 768 moyenne : l'attention paginée (0,41 à ~300) croît ~linéairement → prédit **+0,4…+0,6 ms** ; (iii) service = tokenizer (`_decode_delta` : 2 `decode` par flux et par pas, 24 appels sous GIL) + 12 événements SSE/pas sur le fil asyncio en concurrence de GIL avec le fil moteur + `_deliver` : prédit **+0,2…+0,5 ms**, jamais mesuré (le 14/09 a mesuré 0,25-0,32 ms de trou hôte en Engine direct, sans tokenizer ni HTTP).

## Hypothèses, classées (gain prédit × certitude / coût), avec la mesure qui réfute chacune
| # | hypothèse | à sec, aujourd'hui | mesure décisive (trou Manon) | prédiction / réfuté si |
|---|---|---|---|---|
| H0 | GEMV NVFP4 appelé b fois au lieu d'un GEMM par lot | **RÉFUTÉE** par le CSV : Marlin ×48/pas à b=12 (un lancement par couche, pas ×576) ; 49,6 µs contre 14,8 à b=1 = ×3,35 pour ×12 jetons | — | — |
| H0' | lancement de noyaux / graphes par requête | **RÉFUTÉE** : `graphs.run(batch)` une fois par pas (`_plain_decode_sync`), 1 169 nœuds dans un rejeu, hors graphe 0,49 ms | — | — |
| H1 | ordonnanceur synchrone : `.tolist()` attend le GPU, puis l'hôte (emit, tokenizer, SSE, `_admit`, `_build_batch`) tourne GPU vide ; `ACVRAM_PIPELINE=1` (recouvrement à un pas, `runner.py:1637`, corrigé le 14/09 : trou GPU 0,25 ms) existe, **coupé par défaut, jamais mesuré en service** | borne haute Engine direct : 0,49 ms/pas (6,6 %) ; en service (iii) s'ajoute | **M2, ≤ 5 min** : serve b=12 + harnais T1, événements CUDA autour de `graphs.run` (busy GPU / mur par pas, 30 s, 2 700, ~len 768) ; puis A/B `PIPELINE=0/1` même arbre, même fenêtre | trou hôte servi **0,5…0,9 ms** ; pipeline **−0,4…−0,7 ms/pas → 1 378,6 → ≥ 1 440 t/s** ; réfuté si trou < 0,3 ms ou gain < +2 % (1 405) |
| H2 | glue = 433 nœuds/pas (reduce fp32 ×96, élémentaires ×144, Fill ×97, casts ×96) au plancher de lancement 0,6-2,4 µs = 0,59 ms, hors `glue_compact` déjà pris (C15-3d) | compté dans le CSV | à sec : nommer chaque nœud par couche (`torch.profiler` sur jouet CPU + relecture `moe.py`/`attention.py`) ; carte ≤ 1 min : nsys après fusion | fusion → **−0,35…−0,45 ms (4-5 %)** ; réfuté si < −0,2 |
| H3 | `_etroit` int8 M=12 : 0,906 Go/pas (18,9 Mo/couche) en 1,065 ms = **0,85 To/s**, plancher 0,58 à 1,55 To/s ; `torch._int_mm` (cuBLASLt, `kernels/__init__.py:804`) refusé sous M ≤ 16 sans mesure à M=12 | arithmétique | micro-banc ≤ 1 min : `_int_mm` M=12 contre `_etroit`, formes 2048→5120 et 4096→2048, 1 000 lancements sous graphe | **−0,3…−0,45 ms** ; réfuté si `_int_mm` M=12 < 1,2 To/s |
| H4 | Marlin b=12 : 77,6 µs/couche contre FlashInfer 62,7 à L2 froid (P1) ; mais le plancher dépend de **D = experts distincts par couche et par pas** (octets = D × 2,655 Mo × 48), **jamais mesuré sur texte servi** (uniforme : 69 → 8,8 Go → 4,9 ms > 3,73 mesuré, donc D réel < 69 ; D = 45 → 5,7 Go → 3,7 ms à 1,55 To/s : Marlin serait déjà au plancher) | D inconnu | ≤ 2 min : `ACVRAM_DUMP_MOE` (a2711bcd, ma branche) sur 200 pas b=12 texte réel → D par couche → plancher exact ; Marlin/plancher | si Marlin ≥ 0,85 × plancher : H4 close ; sinon **−0,3…−0,5 ms** |
| H5 | lot < 12 sur une fraction des pas (admission, trains de fin) | harnais : 12 arrivées en un pas, prefill groupé (`_step` : `ACVRAM_PREFILL_BATCH`), fin simultanée à 1 024 (`ignore_eos`) → 100 % des pas pleins hors rampe | `certifie-b12` atteste déjà ; en service : compter `b_reel` par pas (journal) | **< 1 %** ; réfuté si > 2 % de pas partiels |

Somme des gains prédits H1+H2+H3(+H4) : **1,05…1,6 ms** contre 1,18 à combler — l'ordre du chantier est M2 (H1, 0 code, un trou) → H3 (1 j) → H2 (2 j) → H4 (D d'abord, puis 3 j si ouvert). Écarté d'avance : toute prédiction sur D sans le dump ; toute cellule Engine direct comparée à une cellule servie (deux instruments, REGLES § 3).

## (b) Première cellule b=12 après le premier correctif (H1 : pipeline en service, si M2 le confirme)
Instrument : harnais T1 inchangé (`banc-llamacpp-16-09.py`, 12 × 1 024, invite 256, 20 s), même poste 1030, `-lgc 2700`, `ACVRAM_CPUS=0-15`, deux passes ; témoin A = même arbre `PIPELINE=0` dans la même fenêtre (dispersion T1 : 1 366,7-1 390,5, ± 0,9 %). **Scellé : B ≥ 1 440 t/s (+4,5 %, > 2 × dispersion) ET J net B ≤ 1,005 × J A ; réfuté si B < 1 405 (+2 %) ; indécidable entre les deux → un troisième bras.** Alarme : si A sort de 1 355-1 400, le poste a bougé, la fenêtre ne juge rien. Prédiction si M2 réfute H1 (trou < 0,3 ms) : la première cellule vient de H3 (`_int_mm` M=12), scellé **≥ 1 420** sur les mêmes bras.

## (c) Équivalence : la sortie ne change pas
Test suivi `tests/test_lot_equivalence_b12.py` (marqueur carte, ≤ 60 s) : 12 invites fixes (graine, 256 j) + 1 invite seule, greedy 200 jetons, **A = arbre témoin, B = correctif** : `token_ids` identiques au bit **à b=1 (1 flux) et à b=12 (12 flux), même lot des deux côtés** (REGLES § 3 : forme égale) ; le premier jeton divergent est nommé par (flux, pas). Pour H1 la preuve existe sur 12 × 200 (`graphes-preparer-divergence-14-09`) et se rejoue ; pour H2/H3 (arithmétique changée) le critère est ± 1 ulp bf16 sur les logits par ligne + argmax identique, et `ppl-decode-kv` 3 × 512 publié avec son SE, jamais seul. À sec, avant tout : `py_compile` + jouet Llama CPU (`tests/` T4) où le pipeline et le sync donnent les mêmes ids sur 4 séquences × 50.

durée : 0 min de carte ; M2 + D + micro-banc H3 = **≤ 8 min** de trou en tout, en trois prises séparées.
