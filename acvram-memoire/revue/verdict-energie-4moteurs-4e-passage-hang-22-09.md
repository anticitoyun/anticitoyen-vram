# Banc énergie 4 moteurs, 4ᵉ passage — HANG, capture de graphe CUDA jamais terminée — 22/09 (poste2)

* instrument : `chaine-energie-4moteurs.sh` sha `a130816e` (après réalignement poste4, main `2b1660bb`/`d47b20f6`) — précédé d'une preuve isolée ≤10 min (vLLM+trtllm b=12 seuls, script temporaire `/tmp/preuve-energie.sh` tronqué du même fichier avant l'orchestration finale, non committé) : **preuve RÉUSSIE**, vLLM 1753,5 t/s/349,2 W, trtllm 2061,5 t/s (joules_net 7176,3/21,02 s ≈ 342 W) — mode service déclaré (~35 min), chaîne complète enchaînée dans la même prise.
* commit : `02b674a3` (preuve), `a130816e` (4ᵉ passage)
* mesuré : **HANG dès la 1ʳᵉ fenêtre (acvram A1)** — `serveur-acvram-1.log` : chargement normal (régime imprimé, chauffe contexte 0,6 s, 2304/2304 jetons tenus), puis dernière ligne `[graphe] mémoire avant capture : libre 3,34 Gio, réservé 27,38, alloué 19,78 (cache non rendu 7,59)` — **la capture de graphe CUDA ne s'est jamais terminée**, le processus est resté vivant sans jamais répondre à `/v1/models` (`attendre_port` a expiré correctement, « serveur jamais prêt »). **La chaîne n'a jamais nettoyé ce serveur orphelin** : aucune fenêtre suivante (llamacpp, vLLM, trtllm) n'a démarré pendant 19 minutes d'observation. J'ai tué l'arbre entier (chaîne + serveur acvram orphelin + veilleur `ACVRAM_DUREE_MAX=2400` de carte.sh) après diagnostic ; GPU et verrou vérifiés propres après.
* verdict : **HANG, non mesuré au-delà de la preuve isolée**. Deux causes distinctes à corriger séparément : (1) le hang de capture lui-même, dans le moteur acvram (garde-fou de capture manquant — à nommer par poste1) ; (2) l'absence de nettoyage de la chaîne quand `lancer()` échoue après un `attendre_port` expiré alors que le process réel continue de tourner (le `trap ... RETURN` de `fenetre()` suppose que `PID_SRV` est correctement suivi, mais un serveur qui n'ouvre jamais son port peut ne pas être retrouvé par `arreter_moteur` basé sur `ss -tlnp` — à poste4).
* durée : ~21 min avant intervention (14:31-14:52), diagnostic + nettoyage ~3 min

## Suite
Pas de 5ᵉ tentative avant les deux correctifs (poste1 : garde-fou capture ; poste4 : nettoyage robuste indépendant du port ouvert). La preuve isolée vLLM+trtllm reste acquise et valide, non affectée par ce hang (survenu après, sur un moteur différent). Carte rendue à poste3 ; j'enchaîne le rejeu d'invite4 (KL Coder, correctif repetition_penalty déjà poussé) dès sa restitution.
