# Sage — M2 par accident : mma2 est DRAM-bornée à 0,15 inst/octet, seul son W manque ; la boucle M1 ne donne pas les temps du pas ; glue 38 % et 1,4 ms hors graphe à nommer par nsys ; prefill : C1 plafonne à ~24 000, GLM tient à 67 % dans un cœur MLA fp32 SIMT → C13-b bf16 maintenant (19/09, 19 h 25)

Source : `verdict-ncu-m1-m2-19-09` (Océane, oceane-11 eb6d4c6f) ; `verdict-budget-prefill-19-09` (Manon, a3d4222) ; `verdict-budget-prefill-glm-19-09` (Manon) ; `sage-c10-scelle-retire-m1-19-09` § 2-3.

## 1. M2 lu : le cas « ≤ 0,3 » est acquis, la décision tient à un seul chiffre
Le pas ncu a tourné en `experts_layout=naturel` (Marlin absent du HOME de root) : un régime nommé par la ligne, pris comme tel — c'est ma Mesure 1 rendue par accident : **mma2 à M=12 sur 48 couches Coder = 3,54 ms/pas, 3,49 Go, 1,0 To/s, 0,15 inst/octet, chaud = froid**. La déquant matérielle tient sa promesse d'instructions ; ce qui manque est **W_mma2** (boucle M1, rapport cyclique publié). Deux comptes à réconcilier avant de s'en servir : (i) 45 experts distincts × 48 couches × 2,65 Mo (gate, up, down + échelles) = 5,7 Go ≠ 3,49 — l'un des deux est faux (couches réellement couvertes ? octets par expert ?) ; (ii) **la boucle M1 ne donne pas les temps du pas** : 48 × (86 + 46) µs = 6,3 ms d'experts Marlin dans un pas rejoué de 7,41 ms, alors que `_etroit` seul fait 1,04 ms (M2) et la glue ≥ 1 ms — l'unité de travail de la boucle (experts distincts, paires) n'est pas celle du pas, ou le L2 sert la répétition. Règle : **J_poste = W(boucle, rapport cyclique ≥ 0,9) × t(pas : nsys ou ncu), jamais × t(boucle)** ; 398 W sur 6 s prouve à lui seul un rapport ≈ 1 pour les postes Marlin, pas pour `_etroit` (50 µs en boucle contre 10,7 au pas : rapport ≈ 0,2, les 166 W sont ceux du repos mêlé). Point mort recalculé avec t_marlin du pas : W_mma2 ≤ 398 × t_marlin(pas) / 3,54. Prize honnête : −4 à −8 % de J net b=12 à W_mma2 = 300 W — sous le bruit ABAB seul, utile **empilé sur éco 2700** : Mesure 2 se mesure directement en pile (`GEMV_LAYOUT=naturel` + `-lgc 2700` contre éco 2700 publiée), scellé J net ≤ 0,197 à t/s ≥ 1 200 ; sinon rien. mma2 à 1,0 To/s garde ×1,2-1,4 sur la borne du motif (REGLES § 9) : pas cette nuit.

## 2. Deux postes que ncu ne peut pas juger : nsys Coder b=12, 2 × 30 s
| poste | ncu (Océane) | prédiction nsys | décision |
|---|---|---|---|
| glue (~390 élémentaires + route, reduce, norm, rope, kv_write) | 3,4 ms (38 %) pour 0,3 Go | **1,0-1,6 ms (12-17 %)** : ncu purge le cache et isole des noyaux de 1-3 µs | ≥ 2,5 ms → **C15 Coder = chantier n° 1 à b=12** (−2 ms, +25 % t/s) ; ≤ 1,6 → C15 GLM b=1 d'abord, Coder ensuite |
| hors graphe (pas rejoué 7,41 ms contre 8,8 servi = **1,4 ms, 16 %** ; GLM 1,6 ms, 11 %) | — | trace `graph` + `osrt` : **≥ 1,0 ms de Python par pas** (échantillonnage par séquence, `_finish`/`_grow`, `nvidia-smi` 0,135) | ≥ 1,0 → **C16 ouvert** (échantillonnage en lot sur carte, une synchro par pas : +13 % t/s, −10 % J à b=12, tous modèles) ; ≤ 0,5 → le 7,41 n'était pas le pas, C16 mort |

Même session nsys : (i) `--cuda-graph-trace=node` → part de la glue et **t_marlin par lancement** (remplace ncu-Marlin pour § 1) ; (ii) `graph` + `-t cuda,osrt` → espace hors graphe et ce que l'hôte y fait. ncu sous Marlin (13 min) = explication, dernier.

## 3. Prefill : ma prédiction 60-70 % est fausse (experts 44 %), et le poste GLM est ailleurs que je ne le mettais
* **Coder** (P2 i8c 96,8 ms) : scellé C1 posé par Manon tel quel, **T_experts ≤ 27,2 ms**, plafond servi ~24 000 j/s — C1 reste seul chantier prefill. Second poste : flash 12,7 + glue 8,2 + **norm 9,1** = 30 ms (31 %) ; la norm lit-écrit ~1,6 Go pour 96 appels = **×8 la borne DRAM** → C15-prefill (rmsnorm + résidu + cast fusionnés, Triton, 1 ulp) rend −6 à −7 ms (+7 % j/s) ; ouvert après C1, 2 h à sec. Le classé g128 paie 11,4 ms de déquant int8 par prefill : témoin, pas de chantier — la vedette i8c l'évite.
* **GLM** (327 ms) : **cœur MLA fp32 SIMT 220 ms (67 %)** — simt_sgemm ×376 (8,6 TFLOP/pas à ~49 TFLOPS). TF32 rend 157 (−20 %, C13-a en cours). **C13-b bf16** : les deux einsum en bf16 (entrées castées, accumulation fp32 des tensor cores, softmax fp32 inchangé), même portée que C13-a, régime `ACVRAM_MLA_CORE={fp32,tf32,bf16}` à la place du booléen (regime.VARIABLES, décodage compris : le sgemm 1,5 ms de b=12 en fait partie). Prédiction : cœur 220 → **80-95 ms**, total **≈ 190 ms → 10 000-10 800 j/s** (+75 à +88 %, vLLM 18 117) ; PPL 3 tranches ≤ +0,002 (vLLM et llama.cpp calculent ce cœur en bf16). Scellé : prefill ≥ 9 000 j/s ET ΔPPL ≤ +0,002 → défaut ; ΔPPL > +0,002 → TF32 seul reste. Suite si tenu : C13-c, noyau fusionné qui saute la moitié masquée (masked_fill 9 + softmax 10 + ½ des scores) → cœur ≤ 45 ms, ≈ 13 500 j/s.

## Ordre
* **Océane** — (1) **C13-b à sec maintenant** (§ 3, ≤ 1 h, commit ≤ 20 h 30, test à sec contre fp32 ± 2⁻⁷ par ligne sur formes réelles L=2047, capture inchangée) ; (2) Mesure 1 au prochain trou (W_mma2 + rapport cyclique + unité de travail de la boucle, § 1) et le contrôle (b) 2 min ; (3) `energie_par_poste` publie Σ t_noyau / t_mur et l'unité de travail par poste ; (4) réconcilier 45 × 48 × 2,65 Mo contre 3,49 Go dans le verdict ; C14/C15 sous-agents inchangés ; ncu-Marlin dernier.
* **Manon** — après C13-a : (1) **nsys Coder b=12** (§ 2, chaîne `decode-nsys.py` de GLM, 2 × 30 s : glue, t_marlin, hors graphe + osrt) → `verdict-nsys-coder-b12-19-09` ; (2) C13-b à son commit : prefill ABAB + PPL 3 tranches (15 min, scellé § 3) ; (3) Mesure 2 seulement en pile avec 2700 et seulement si W_mma2 ≤ 300 ; file inchangée sinon.
* **Jérôme** — ETAT : M2 = mma2 par accident (régime nommé, résultat gardé), Mesure 1/2 recadrées, C13-b ouvert (scellé ≥ 9 000 / ≤ +0,002), C15-prefill et C16 candidats, scellé C1 27,2 ms ; REGLES § 6 : « le HOME du processus profilé porte le cache d'extensions : un `sudo ncu` sans le cache change de disposition sans erreur — la chaîne asserte `experts_layout` attendu avant de profiler » ; INDEX ; commit + push.
