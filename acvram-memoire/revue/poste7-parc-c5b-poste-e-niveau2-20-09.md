# poste7 — capture du parc : deux modèles servis mal servis (gemma b=1 en eager sans le dire, Ornith ne décode pas) → deux bogues P1 et une règle « un alias se prouve par un jeton décodé » ; C5-b fusionne opt-in ; poste E : le test comparait deux bras entre eux, pas au vrai ; niveau 2 : la dégradation croît avec la position — les tables rope à 8 192 sont le suspect (20/09, 03 h 33, horloge machine)

Source : `verdict-capture-parc-19-09` (poste2 dc66fa3b) ; `verdict-c5b` addendum 530e1593 ; `verdict-poste-e-test-triton-19-09` (3ff93723) ; `verdict-c15` addendum 03 h 36 (baaac9de) ; REGLES § 7 (critère vérifié contre la référence), § 4 (repli eager = cellule invalide), `verdict-config-modeles-19-09` (S2 « 0 cassé » = chargement).

## 1. Parc : 9/11 MoE tiennent 4/4 ; deux ne tiennent pas, et le service ne le disait pas
* **gemma-4-26B-A4B, godet 1 en eager** : « capture impossible : CUDA OOM » à la première capture seulement → b=1 servi à **16,25 ms/pas contre 5,65 à b=2 : 2,9 × plus lent, silencieusement**. Bogue P1 (modèle du catalogue de l'utilisateur) : (a) la première capture manque de mémoire parce que le plan ne réserve pas le bassin des graphes avant d'allouer le KV — capture des godets **avant** la réservation KV finale, ou réservation explicite du bassin au plan (`tiering`) ; (b) **un repli eager en service s'annonce** : ligne de régime `graphes=on(repli eager: godet 1)`, `/metrics`, journal de démarrage — la règle de mesure (REGLES § 4) devient une règle de service. poste1, fiche + correctif à sec demain ; d'ici là le catalogue porte « b=1 en eager, 2,9 × plus lent ».
* **Ornith-35B : OOM Triton au godet 1, ne décode pas.** Un alias qui charge et ne décode pas est un alias cassé ; S2 « 0 cassé » ne prouvait que le chargement. **Règle (REGLES § 3)** : *un alias servi se prouve par un jeton décodé au godet 1 sous le régime servi, pas par un chargement — le test du parc (S2) décode un jeton par alias et publie ok/repli/échec.* Catalogue : Ornith « cassé au décodage » jusqu'au correctif (OOM Triton = tuile/cache Triton au godet 1 : poste1 à sec, après C15-3b).

## 2. Tranché
* **C5-b d3a07614 : fusion en opt-in** (tests carte 9 passed dont `prefill un par un`, budget 3 passed) ; vitesse à la reprise, comme dit.
* **Poste E** : `test_triton_egale_le_noyau_cuda_actuel` est faux par construction — à [37, 0, 300] Triton 0,94 et CUDA 0,89 × la borne de la référence fp64, **de côtés opposés** → 1,83 entre eux ; les deux noyaux sont justes (0/32 contre fp64). REGLES § 7 mot pour mot : un critère qui compare deux bras entre eux au lieu de chacun au vrai peut rendre faux deux bras justes. Correctif (poste1, 5 min à sec) : juger **chacun contre la référence fp64** ; « 2 × TOL » est un pis-aller, pas le juge. Aucun bogue de noyau ; INDEX : « poste E clos, défaut de test ».
* **Niveau 2** : `prep` = ordre de somme seul au pas 1, mais PPL `=2` contre `=0` **+1,71 % (SE 0,78)** à 3 tranches, **+0,39 % dès 256, +3,27 % à 8 192** : la dégradation **croît avec la position** — le suspect est ce qui dépend de la position : **les tables rope (`tables32`) lues par le noyau à grande position** (précision des angles à p ≈ 8 192, ou table par défaut plus courte que la position). Juge d'poste1 à pos 8 192 : q_eff noyau contre torch **par élément et par position** (p = 256, 2 048, 8 192) ; prédiction : ≤ 8 ulp à 256, ≫ à 8 192 → correctif dans la table (même construction que torch, fp64 → fp32), puis `=2` se rejuge (`=2` EAGER contre `=1` EAGER ≤ 1 ulp bf16 par couche à p = 8 192 ET PPL ± 2 SE aux deux longueurs). C14 (≤ 8 ulp, −0,18 %) ne dégradait pas avec la position : la comparaison dit que ce n'est pas l'ordre de somme.

## Ordre
* **poste1** — C15-3c (routeur grille ≥ 128, ncu ≤ 3 µs) en tête ; puis juge q_eff par position (§ 2) ; poste E (5 min) ; fiches gemma (capture OOM godet 1 + repli annoncé) et Ornith (OOM Triton) pour demain.
* **poste2** — C15-3c fenêtre à son pointeur ; rien d'autre : la carte reste disponible.
* **chef** — fusion C5-b opt-in ; catalogue : gemma « b=1 en eager, 2,9 × » et Ornith « cassé au décodage » ; REGLES § 3 (règle § 1) ; ETAT : parc 9/11, deux bogues P1, poste E clos (test), niveau 2 suspect tables rope ; INDEX ; commit + push.
