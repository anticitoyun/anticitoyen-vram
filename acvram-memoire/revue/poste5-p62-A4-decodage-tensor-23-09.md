# Pièce 62, jalon A4 — décodage MoE sur tensor cores (`ACVRAM_MOE_TENSOR=1`) : −0,47 ms/pas à b = 12 (6 856 → 6 389 µs, −6,8 %), KL Coder 5/5 identique au GEMV, capture de graphes tenue — après remplacement de l'aligneur Triton (0,573 ms/pas) par l'aligneur CUDA porté de vLLM (0,163)

* instrument : `outils/gpu/mesure/frontiere-pas.py 12 200` sous graphes (`ACVRAM_MOE_TENSOR=0` puis `=1`, `scratchpad/poste5-p62-23-09/chaine-A4.sh`) ; `scratchpad/poste5-p62-23-09/kl-tensor-b12.py` (KL(HF ‖ acvram) au décodage b = 12, 12 séquences identiques, teacher forcing 8 pas, 5 dumps HF de `kl-coder-texte-22-09`) ; nsys `--cuda-graph-trace=node` + `familles-noyaux.py --detail autres` (`familles-A4-2.sh`) ; tests `tests/test_moe_tensor_decodage.py` (4) et `tests/test_aligneur_cuda_marlin_port.py` (4)
* commit : arbre de f2501d30 + ce commit (chemin `gemm_experts_tensor`, `aligner_blocs_cuda`, port de `moe_align_sum_kernels.cu` vLLM v0.29.0 commit 98dff2a) ; alias `Qwen3-Coder-30B-A3B-nvfp4-qkvo-i8c`
* régime : b = 12 pleins, graphes on (5 godets capturés, repli_eager=0), ligne de régime `chemin_moe=mma-a4(atteint=gemv_marlin+marlin_tensor)+tensor` ; carte tenue 9 + 16 + 16 + 16 + 16 + 26 s, llama-server étranger (PID 4627, 5,6 Go) présent mais inactif avant et après
* scellé : « A4 opt-in `ACVRAM_MOE_TENSOR=1`, ligne de régime, capture de graphe » (chef) ; prédiction A4/1 (carnet 02 h 3x) : pas 8,5 → 7,3-7,7 ms — **RÉFUTÉE** (−0,11 ms/pas, l'aligneur Triton mangeait le gain) ; prédiction A4/2 (carnet 02 h 4x, avant la mesure) : aligneur 0,573 → 0,15-0,20 ms/pas, frontière −0,45 à −0,60 ms/pas, réfutée si gain < 0,3 ; KL 5/5 inchangée
* mesuré : frontière méd. **6 856,4 → 6 388,6 µs/pas (−467,8, −6,8 %)**, p90 6 984 → 6 503 ; familles (noyaux, ms/pas) : experts_marlin 3,178 (GEMV du 22/09 : 3,92), aligneur (« autres ») 0,573 → **0,163** (moe_align_block_size 2,6 µs + count_and_sort 0,8 µs × 48), glue_torch 0,206 → 0,166, experts_glue 0,092 ; total noyaux 6,626 → **6,189** (GEMV, pièce 59 : 6,74) ; KL Coder décodage : max global 0,4105 (invite 2, pas 0) sous TENSOR=0 comme =1, 5/5 ≤ 0,74, écart tensor−GEMV par invite ≤ 0,016 sur kl_max ; argmax 39/40 égaux dans les deux modes ; tests 8/8 verts (fantômes, expert à tous les jetons, égalité Triton/CUDA à godets 2/8/16, capture sur tampons fixes)
* verdict : **A4 tenu, prédiction A4/2 tenue** (−0,47 dans −0,45/−0,60 ; aligneur 0,163 dans 0,15-0,20) ; le chemin tensor-core est opt-in, capturable, exact au 2⁻⁷ contre le GEMV servi, et rend 6,8 % du pas b = 12 ; reste sur la table : glue 0,166 + experts_glue 0,092 + aligneur 0,163 = 0,42 ms/pas de non-GEMM (fusion possible, hors A4) ; le GEMM lui-même est à 3,178 contre 3,23 mesuré sur l'op vLLM (pièce 61/0) — plancher tensor-core atteint
* durée : 1 h 20 (dont 1 min 40 de carte) ; gênant nommé : la glue restante, elle ne mange pas la moitié du gain (0,42 sur 0,75 de GEMM gagné — mais 0,47 net seulement, il manque 0,28 entre familles et frontière : le trou nsys 0,331 est à retrancher des deux)

## Pour A5 (poste2, cellule b = 12 ABBA)
Bascule : `ACVRAM_MOE_TENSOR=1` dans l'environnement du service ; contrôle : ligne de régime `+tensor(atteint=…marlin_tensor)` et `repli_eager=0` ; prédiction cellule : 3,3-4,0 % de jetons/s en plus (0,47 ms sur un pas de ~7 ms côté service, dilué par l'hôte), réfuté si < 2 % ; énergie par jeton en baisse du même ordre.
