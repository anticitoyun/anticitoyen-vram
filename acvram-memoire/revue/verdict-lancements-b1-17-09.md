# Verdict — Coder-30B b=1 régime classé 0.6.8 : **1 275 lancements par pas** (26,6 par couche), petits noyaux **1,16 ms = 33 %** du pas GPU de 3,50 ms ; les deux prédictions de Sage tenues (≥ 600 ; ≥ 28 %), la mienne réfutée par le haut (900-1 100)

instrument : `scratchpad/profil-pas-coder-17-09.py b1` (fenêtre pure à ctx ≈ 300, graphes, 40 pas profilés, noyaux seuls ; lancements par pas et par poste ajoutés) — `scratchpad/lancements-b1-17-09/profil-b1.{json,log}` ; protocole `protocole-lancements-b1-17-09.md` scellé avant
commit : arbre laure (= main, 0.6.8 : E `triton`, C `mixte` = cuda à b=1, B0 `groupe`) ; `[régime] ACVRAM_NARROW_GEMM=1 ACVRAM_MOE_MMA=0 ACVRAM_MOE_DECODE_MMA=0 extension=oui`, moteur NOMINAL graphes=on
régime : b=1, ctx ≈ 300, pas nu 3,53 ms (283,3 t/s pur ; cellule rondes 257,8), GPU 3,498 ms — le rejeu de graphe cache l'hôte (0,03 ms)
scellé : Sage ≥ 600 lancements, petits noyaux ≥ 28 % (1,17 ms / 30 %) ; moi 900-1 100, petits 30-34 %, pas GPU 3,5-3,8
mesuré : **1 275 lancements par pas, 38 noyaux distincts** ; par poste (reclassé : `_partiel_kernel` et `_reduce_kernel` de E sont de l'attention) — élémentaire **443** (0,397 ms), glue MoE **241** (0,427), norme/rope/act **145** (0,335), dense GEMM 145 (1,039 : `int8_gemv` ×96 0,750 + tête 0,190 + gemvx 0,099), experts 96 (0,924 : gateup 0,591 + warp 0,333), attention 144 (0,336 : partiel 0,157 + reduce 0,099 + `kv_write` 0,080 — E a ramené l'attention de 0,85 à 0,34), autre 61 (≈ 0,04) · **petits noyaux (élémentaire + glue + norme) = 1,159 ms = 33,1 %** · les 10 plus gros petits : `moe_route` 0,265 (×48), `rmsnorm` 0,235 (×97), `rope` 0,100 (×48), copies `direct_copy` 0,088 + 0,049 (×96 + 48), `compare_scalar` 0,082 (×96), `scatter_gather` 0,054, `moe_reduce` 0,054, `arange` 0,052 (×96), `bitwise_not` 0,041 (×48)
verdict : **la cible de F est réelle et plus grosse que prévue : 1 275 lancements par pas dont 829 petits (élémentaire 443, glue 241, norme 145), pour 1,16 ms sur 3,50 (33 %) ; à b=1 le pas ne contient que 0,92 ms d'experts et 1,04 ms de dense — le tiers restant est de la colle torch (copies, comparaisons, `arange`, `bitwise_not`, scatter) et des noyaux maison lancés un par un (route, rmsnorm ×2 par couche, rope, reduce). ≤ 6 noyaux par couche (Sage) = ≤ 289 lancements : ×4,4 de moins ; si la colle disparaît et que route + norme + rope fusionnent avec leurs voisins, le pas tombe vers 2,5-2,7 ms → 370-400 t/s pur, ≈ 330-360 en rondes — le scellé b=1 ≥ 300 est atteignable, llama.cpp (341,4) est à portée. Ordre suggéré par les ms : (1) colle torch de `_forward_grouped` (copies/compare/arange/scatter : 0,33 ms, 380 lancements), (2) `moe_route` + `moe_reduce` dans le gemv groupé (0,32 ms, 96), (3) `rmsnorm` + `rope` + `kv_write` fusionnés à la projection (0,42 ms, 193).**
