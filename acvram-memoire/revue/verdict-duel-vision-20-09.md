# Verdict — duel (c') multimodal Gemma 4 31B, 1 image 896×896 + « Décris cette image en une phrase. », 20 images scellées, 48 jetons de sortie forcés des deux côtés, sans canal de pensée, `-lgc 2700`, fils 0-15 (Sage 13 h 55 / 15 h 45 / 16 h 25) : **TENU sur les trois chiffres, avec la réserve « expansion d'image propre à chaque moteur »** — **TTFT acvram nvfp4 0,058 s (méd. des 19 froides ; 0,055-0,069) contre llama.cpp 0,651 s (0,638-0,761) : × 11** ; **J net par requête 204 contre 314** (à 48 jetons chacun) ; **J net sur la fenêtre TTFT 3,2 contre 88,9** ; ms par jeton d'invite **0,21 contre 1,70** ; invite : **279 jetons (256 image + 23 texte) contre 383 (≈ 336 image + 47 texte)** ; sha256 du texte rendu **différent** (`cf2488f38441539b` acvram / `cd27906e8319e0d7` llama.cpp) — les deux moteurs rendent le même échange utilisateur, pas la même requête de jetons ; à côté, non scellé : **débit de sortie acvram 57 t/s contre llama.cpp 70** (acvram décode plus lentement ce 31B dense ; il gagne le J par le prefill et la tour)

instrument : même harnais HTTP en flux `temoin-llamacpp-vision.py` des deux côtés (image data: base64, `max_tokens 48`, `ignore_eos` — llama.cpp sans pensée n'émettait sa fin de tour qu'après 19-41 jetons, acvram après 15 : la sortie égale exige `ignore_eos` des deux côtés ; T=0 ; TTFT = premier delta de contenu ; `Energie` NVML par requête, compteur relu au premier jeton pour la fenêtre TTFT ; repos 15 s après) ; acvram : `duel-c-prime.sh` (serve `--max-batch 1 --max-model-len 4096` sous `carte.sh` cpus 0-15, alias `gemma-4-31B-it-nvfp4-vision`, main 48a32a3b ⊇ 066f2e89, régime `eco=2700(2692) hote=thp,omp8,cpus0-15`, 15:38-15:40) ; llama.cpp : `chaine.sh` (`-lgc 2700` posé, llama-server 0.4.1-dev b4c9233c, `--jinja --reasoning off --reasoning-format none --chat-template-kwargs {"enable_thinking": false}`, mmproj BF16 `clip.vision.image_size 224, patch 16, projector gemma4v`, `--image-min/max-tokens` au défaut « read from model », aucun réglage du lanceur → les ≈ 336 jetons image sont son défaut, 15:45-15:47) ; contrôle Sage : `/apply-template` + `/tokenize` côté llama.cpp, texte du processeur côté acvram ; hôte : gpg 125 % au lancement (Femoceane), 3080 Ti pid 4286 (pair) ; `poste=20-09-1030`
scellé (Sage, avant) : TTFT acvram ≤ 0,69 (le témoin du jour) ; J net ≤ 328 à 48 jetons ET sur la fenêtre TTFT ; sha256 du texte rendu identique (réserve nommée sinon) ; jetons d'invite et ms/jeton publiés ; acvram prédit 0,25 ± 0,05 s
mesuré : acvram (essai 48 jetons, `duel-acvram-nvfp4-48.json`) TTFT 0,0580 · total 0,790 s · 48 jetons (42 deltas à contenu + 6 vides) · J brut 260 / net 204 · fenêtre TTFT 7,2 / 3,2 · 329 W · 2 587-2 685 MHz · repos 71,0 W ; premier essai à fin de tour libre : TTFT 0,0592, 15 jetons, J net 61 ; llama.cpp (`duel-llamacpp-48.json`) TTFT 0,6508 · total 1,339 · 48 jetons · J brut 412 / net 314 · fenêtre 136,5 / 88,9 · 306 W · 2 437-2 692 MHz · repos 72,0 ; essai sans pensée à fin de tour libre : 0,644 s, 19-41 jetons, J net 218 ; essais du matin (canal ouvert, 47 jetons) : 0,691 / 0,665 s
verdict : acvram sert l'image en 58 ms là où llama.cpp en met 651 (tour bf16 45 ms + prefill nvfp4 de 279 jetons ≈ 13 ms contre mmproj + prefill Q4_K_M de 383 jetons ≈ 390 ms + encodage) — la prédiction de Sage (0,25) était quatre fois trop prudente ; à sortie égale le J par requête est de 35 % plus bas, mais **le décodage à b=1 est 19 % plus lent que llama.cpp** (57 contre 70 t/s : le 31B dense nvfp4 n'a pas le levier MoE de Coder/GLM), à mettre dans la file après le multimodal ; réserve : requêtes de jetons différentes (256 contre ≈ 336 jetons image, textes rendus ≠) — comparable « à requête utilisateur égale » (Sage 15 h 45), pas au jeton
durée : 7,1 min de carte (acvram 2 essais 2 × 1,6 min ; llama.cpp 2 essais 3,9 min) ; carte rendue 15:46:58
suite : Sage : revendication multimodal « TTFT × 11, J −35 % à sortie égale, décodage −19 % » ; Océane : bras bf16 étagé + images (défaut nommé par diff-couches), puis décodage dense b=1 ; ma file : (c) référence transformers 31B offload 5 images (5 × 13 min) à son mot

## Rejouable
acvram : `POSTE=20-09-1030 IGNORE_EOS=1 SUFFIXE=-48 ACVRAM_ARBRE=<main> MANON=<worktree manon> PYA=<python venv> bash scratchpad/mm-c-20-09/duel-c-prime.sh` (2 min) ; llama.cpp : `IGNORE_EOS=1 ACVRAM_ARBRE=<manon> PYA=<python venv> bash scratchpad/mm-temoin-20-09/chaine.sh` (2 min, disque chaud).

## Addendum 15 h 52 — diff des deux textes rendus (Sage 17 h 00 : marqueur seul → la cellule tient avec la réserve)
instrument : `difflib` sur `invite_rendue.texte_rendu` des deux JSON (à sec) — acvram : texte du `Gemma4Processor` (`apply_chat_template`) ; llama.cpp : `/apply-template` du serveur
mesuré : **trois différences, aucune sur le texte** : (1) `<bos>` présent dans le rendu du processeur HF, absent du rendu jinja de llama.cpp (llama-server l'ajoute à la tokenisation, `add_special`) ; (2) le marqueur d'image : `<|image|>` (HF, expansé en 256 jetons par le processeur) contre `<__media_…__>` (mtmd, expansé en ≈ 336) ; (3) la **place** du marqueur — avant le texte côté acvram, après côté llama.cpp — qui vient de MES harnais (mesure-c.py met `[image, texte]`, le harnais HTTP du duel `[texte, image_url]` pour les deux moteurs : dans le duel les deux serveurs ont reçu le même ordre, le rendu acvram de cet ordre n'est pas capturé, `/apply-template` n'existe pas chez acvram) ; l'invite (« Décris cette image en une phrase. »), les jetons de tour et le canal de pensée vide sont identiques caractère pour caractère
verdict : différence = marqueur d'image + `<bos>` de tokenisation → **la cellule tient avec la réserve « expansion d'image propre à chaque moteur »** (prédiction de Sage tenue) ; à faire au prochain rejeu : même ordre `[texte, image]` dans mesure-c.py pour que le sha256 côté acvram soit celui de la requête du duel
