# Verdict — P2 hors moteur (ratio transformers, REGLES §4 / A7) : **i8c / classé = 0,9947 ≤ 1,005 → P2 OUVERT** ; contrôle d'instrument **classé_HF / bf16_HF = 1,0149** (acvram : 1,0148) — l'instrument est juste à 10⁻⁴ ; le converti par canal est **meilleur** que le classé (prédiction 1,001-1,003 réfutée dans le bon sens)

instrument : `scratchpad/p2-hors-moteur-18-09/` — `dequant-vers-hf.py` (chargeur acvram : `dequantize_nvfp4` fp32→bf16, `_dequantize_int8`, act_scale repliés = 1,0 partout pour i8c, 0 Hadamard) → HF bf16 57 Go × 2 sous `/mnt/AI_GENERATOR/hors-depot/p2-hf-18-09/{classe,i8c}` ; `ppl-hf-17-09.py` (transformers 5.17, accelerate 1.15, `device_map=auto` 26 GiB + CPU, fenêtres 1 024, MIN_CTX 1023, 12 fenêtres × 3 tranches privées Coder) sur classé, i8c et la source bf16 (`models/Qwen3-Coder-30B-A3B-Instruct`) dans le MÊME run ; **venv effectif prouvé** sur le processus vivant (`venv-effectif.txt` : cmdline `/opt/ia/vLLM/.venv/bin/python`, maps = site-packages de ce venv)
commit : 85296d8 (chaîne `chaine-ppl.sh`), 17:46-18:15, charge 2,8-7,4, carte tenue par la PPL seule (garde conftest active)
régime : hors moteur — aucun noyau acvram ; les absolus transformers ne sont pas des absolus acvram (A7) : seuls les ratios sont publiés
scellé (Sage) : i8c / classé ≤ 1,005 → P2 ouvert (1,0148 × 1,005 = 1,020 = la classe) ; > 1,005 → fermé, converti gardé ; prédiction 1,001-1,003 ; contrôle d'instrument = classé_HF / bf16_HF ≈ 1,0148 (écart > 0,01 = repli faux)
mesuré (PPL HF par tranche) : bf16 12,462 / 11,269 / 11,508 · classé 12,603 / 11,397 / 11,764 · i8c 12,530 / 11,382 / 11,660 → géo bf16 11,735 · classé 11,910 · i8c 11,848 ; **classé / bf16 = 1,0149** (acvram 17/09 : 1,0148, écart 0,0001) ; **i8c / classé = 0,9947** (par tranche 0,9942 / 0,9988 / 0,9912, les trois < 1) ; i8c / bf16 = **1,0096**
verdict : **P2 OUVERT** — 0,9947 ≤ 1,005 avec 0,0103 de marge ; le contrôle d'instrument tient à 10⁻⁴ (le repli chargeur → HF reproduit exactement la PPL acvram du classé : la déquantification est fidèle) ; q/k/v/o INT8 symétrique par canal depuis bf16 (Manon) rend une PPL plus basse que l'INT8 affine par groupes de 128 promu depuis NVFP4 du classé — en absolu acvram attendu ≈ 1,0148 × 0,9947 ≈ **1,0094** (à confirmer dans le moteur avec le chemin `torch._int_mm`) ; l'in situ moteur (PPL acvram + prefill ≥ 11 000 j/s) vient avec le chemin `_int_mm` de Laurine après P1

## Lecture
- Le converti i8c est meilleur sur les trois tranches : la symétrie par canal (échelle par ligne de sortie, sans zéro-point) sur des projections denses bien conditionnées vaut mieux que l'affine par groupe de 128 ; l'arithmétique `_int_mm` (int8 × int8 → int32, échelle appliquée après) en profite directement.
- Instrument hors moteur validé (contrôle 1,0149 / 1,0148) : réutilisable pour juger un converti dont le moteur n'a pas encore le chemin — en ratio, jamais en absolu.
- Réserve : les PPL i8c ont été calculées sur les poids déquantifiés en bf16 par transformers (GEMM bf16) ; le moteur fera int8 × int8 avec quantification des activations par jeton — la PPL acvram peut différer de quelques 10⁻³ ; c'est l'in situ qui tranchera la classe.
